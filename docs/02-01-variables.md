# 02-1: Variables (ตัวแปร — คลังเก็บข้อมูล) 📦

> **"A variable is a named container for a particular set of bits or type of data."**
> — *MDN Web Docs*

ในโลกจริง เราเก็บของในกล่อง ตู้เซฟ หรือถุง ในโลก JavaScript เราเก็บข้อมูลใน **"ตัวแปร" (Variables)** ครับ

> **💡 Analogy (เปรียบเทียบ):**
> ตัวแปรเหมือน **"กล่องติดป้ายชื่อ"** ครับ:
> - **กล่อง** = พื้นที่ในหน่วยความจำ (Memory)
> - **ป้ายชื่อ** = ชื่อตัวแปร (Identifier) เช่น `userName`, `totalPrice`
> - **ของในกล่อง** = ค่าข้อมูล (Value) เช่น `"Dolar"`, `42`
> - **`const`** = กล่องที่ **ผนึกฝาด้วยกาว** — เปิดดูได้ แต่เปลี่ยนของข้างในไม่ได้
> - **`let`** = กล่องที่ **ฝาเปิดปิดได้** — เปิดออกมาเปลี่ยนของข้างในได้ตลอด
> - **`var`** = กล่องเก่าที่ **ไม่มีป้ายบอกห้อง** — ของหลุดออกมาแค่ไหนก็ไม่รู้ (อันตราย!)

## 1. Declaring Variables (การประกาศตัวแปร) 🛠️

ตาม [MDN](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Grammar_and_types#declaring_variables) JavaScript มี 3 คำสั่งสำหรับประกาศตัวแปร:

```javascript
// ✅ const — ค่าคงที่ (เปลี่ยนค่าไม่ได้)
const PI = 3.14159;
const APP_NAME = "MyApp";

// ✅ let — ตัวแปรที่เปลี่ยนค่าได้
let score = 0;
score = 100; // ✅ เปลี่ยนค่าได้

// ❌ var — แบบเก่า (ห้ามใช้ในโค้ดสมัยใหม่!)
var oldStyle = "don't use me";
```

### กฎทอง: **`const` first, `let` when needed, `var` never!**

```javascript
// ✅ Best Practice: ใช้ const เป็นค่าเริ่มต้น
const userName = "Dolar";     // ชื่อไม่เปลี่ยน → const
const birthYear = 1999;       // ปีเกิดไม่เปลี่ยน → const

// ✅ ใช้ let เฉพาะเมื่อจำเป็นต้องเปลี่ยนค่า
let currentScore = 0;         // คะแนนเปลี่ยนได้ → let
let isLoggedIn = false;       // สถานะเปลี่ยนได้ → let

currentScore = 50;            // ✅ เปลี่ยนค่าได้
isLoggedIn = true;            // ✅ เปลี่ยนค่าได้

// ❌ const เปลี่ยนค่าไม่ได้!
// userName = "Somchai";      // TypeError: Assignment to constant variable
```

### 🧠 Challenge: const or let?
จงเลือกว่าตัวแปรไหนควรใช้ `const` หรือ `let`:
1. จำนวนวันในสัปดาห์ (7)
2. อุณหภูมิปัจจุบัน
3. URL ของ API server
4. จำนวนสินค้าในตะกร้า
5. อัตราแลกเปลี่ยนเงิน (เปลี่ยนทุกวัน)

::: details ✨ ดูเฉลย
1. `const` — 7 วันไม่เปลี่ยน
2. `let` — อุณหภูมิเปลี่ยนตลอดเวลา
3. `const` — URL มักจะคงที่ตลอดโปรแกรม
4. `let` — จำนวนสินค้าเพิ่มลดได้
5. `let` — อัตราแลกเปลี่ยนเปลี่ยนได้
:::

---

## 2. Naming Rules & Conventions (กฎการตั้งชื่อ) 📝

ตาม [MDN](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Grammar_and_types#variables) ชื่อตัวแปร (Identifier) มีกฎดังนี้:

### กฎบังคับ (Syntax Rules — ฝ่าฝืน = Error):

```javascript
// ✅ ชื่อที่ถูกต้อง
const userName = "Dolar";    // ขึ้นต้นด้วยตัวอักษร
const _private = true;       // ขึ้นต้นด้วย _ ได้
const $dollar = 100;         // ขึ้นต้นด้วย $ ได้
const camelCase123 = "ok";   // ตัวเลขอยู่กลางหรือท้ายได้
const ชื่อ = "Dolar";        // Unicode ได้ (แต่ไม่แนะนำ)

// ❌ ชื่อที่ผิดกฎ (SyntaxError!)
// const 123abc = "nope";    // ❌ ขึ้นต้นด้วยตัวเลขไม่ได้
// const my-name = "nope";   // ❌ มี - (hyphen) ไม่ได้
// const my name = "nope";   // ❌ มีช่องว่างไม่ได้
// const class = "nope";     // ❌ ใช้ Reserved Word ไม่ได้
```

### Reserved Words (คำสงวน) — ห้ามใช้เป็นชื่อตัวแปร:

```javascript
// ❌ ตัวอย่างคำสงวนที่ใช้ไม่ได้:
// break, case, catch, class, const, continue, debugger,
// default, delete, do, else, export, extends, finally,
// for, function, if, import, in, instanceof, let, new,
// return, super, switch, this, throw, try, typeof, var,
// void, while, with, yield
```

### Conventions (ข้อตกลงในวงการ — ไม่ฝ่าฝืน ≠ Error แต่เป็นมารยาท):

| Convention | ใช้กับ | ตัวอย่าง |
|:-----------|:------|:---------|
| **camelCase** | ตัวแปร, ฟังก์ชัน | `firstName`, `getUserName()` |
| **PascalCase** | Class, Constructor | `UserProfile`, `HttpClient` |
| **SCREAMING_SNAKE_CASE** | ค่าคงที่สำคัญ | `MAX_RETRIES`, `API_URL` |
| **_underscore prefix** | ตัวแปร Private (convention) | `_internalData` |

```javascript
// ✅ Good naming — อ่านแล้วรู้เรื่อง
const userAge = 25;
const isLoggedIn = true;
const maxRetries = 3;
const API_BASE_URL = "https://api.example.com";

// ❌ Bad naming — อ่านแล้วไม่รู้เรื่อง
const x = 25;         // x คืออะไร?
const flag = true;     // flag อะไร?
const data = 3;        // data อะไร?
const a = "https://api.example.com"; // a?!
```

### 🧠 Challenge: Fix the Names
จงแก้ไขชื่อตัวแปรให้ถูกต้องตาม Convention:
```javascript
const MyAge = 25;              // (1) ปัญหาอะไร?
const USEREMAIL = "a@b.com";   // (2) ปัญหาอะไร?
let Number_of_items = 10;      // (3) ปัญหาอะไร?
```

::: details ✨ ดูเฉลย
```javascript
const myAge = 25;              // ← camelCase (ไม่ใช่ PascalCase)
const userEmail = "a@b.com";   // ← camelCase (SCREAMING_CASE ใช้กับค่าคงที่สำคัญเท่านั้น)
let numberOfItems = 10;        // ← camelCase (ไม่ใช้ underscore แบบ snake_case)
```
:::

---

## 3. Declaration vs Initialization (ประกาศ vs กำหนดค่า) 📋

ตาม [MDN](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Grammar_and_types#declaration_and_initialization):

```javascript
// Declaration (ประกาศ) = สร้างกล่องว่าง
let score;
console.log(score); // undefined ← ยังไม่มีค่า

// Initialization (กำหนดค่าเริ่มต้น) = ใส่ของลงไปในกล่อง
score = 100;
console.log(score); // 100

// Declaration + Initialization พร้อมกัน (แนะนำ!)
let userName = "Dolar";

// const ต้อง Initialize ทันทีตอนประกาศ!
// const PI; // ❌ SyntaxError: Missing initializer in const declaration
const PI = 3.14159; // ✅
```

### Multiple Declarations (ประกาศหลายตัวพร้อมกัน):

```javascript
// แบบที่ 1: แยกบรรทัด (แนะนำ — อ่านง่าย)
const firstName = "Dolar";
const lastName = "D";
const age = 25;

// แบบที่ 2: รวมบรรทัด (ไม่แนะนำ — อ่านยาก)
const firstName2 = "Dolar", lastName2 = "D", age2 = 25;

// แบบที่ 3: Destructuring (Modern — เหมาะกับ Object/Array)
const { name, email } = { name: "Dolar", email: "d@mail.com" };
const [first, second, third] = [10, 20, 30];
```

---

## 4. Hoisting & Temporal Dead Zone (TDZ) ⚡

ตาม [MDN - Variable Hoisting](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Grammar_and_types#variable_hoisting) **Hoisting** คือพฤติกรรมที่ JS Engine "ยก" การประกาศตัวแปรขึ้นไปบนสุดของ Scope ก่อนรันโค้ด — แต่พฤติกรรมต่างกันตามคำสั่ง:

### `var` Hoisting — ยกขึ้น แต่ค่าเป็น `undefined`:

```javascript
// โค้ดที่เราเขียน:
console.log(x); // undefined (ไม่ Error!)
var x = 5;

// สิ่งที่ JS Engine เห็นจริงๆ:
// var x;              ← ยกขึ้น (เป็น undefined)
// console.log(x);     // undefined
// x = 5;              ← กำหนดค่าทีหลัง
```

### `let` / `const` — อยู่ใน TDZ จนกว่าจะถูกประกาศ:

```javascript
// โค้ดที่เราเขียน:
console.log(y); // ❌ ReferenceError: Cannot access 'y' before initialization
let y = 10;

// สิ่งที่เกิดขึ้น:
// let y;       ← ถูก Hoist แต่อยู่ใน "Temporal Dead Zone" (TDZ)
// console.log(y); ← อยู่ใน TDZ → ReferenceError!
// y = 10;      ← ออกจาก TDZ ตรงนี้
```

### 📊 Hoisting Behavior Table

```
   ┌─────────────────────────── TDZ ────────────────────────┐
   │                                                        │
   ▼            var x = 5;         let y = 10;              ▼
========================================================================
Timeline:   [Code Start] ──────────────────────► [Declaration Line]
                │                                        │
   var:   ✅ undefined (ใช้ได้แต่ค่าเป็น undefined)        ├── ค่าถูกกำหนด
   let:   ❌ TDZ (ReferenceError!)                        ├── ออกจาก TDZ
   const: ❌ TDZ (ReferenceError!)                        └── ออกจาก TDZ
```

| ส่วน | `var` | `let` | `const` |
|:-----|:------|:------|:--------|
| **Hoist** (ยกขึ้น) | ✅ ยก Declaration | ✅ ยก Declaration | ✅ ยก Declaration |
| **ใช้ก่อนบรรทัดประกาศ** | ✅ ได้ (`undefined`) | ❌ ReferenceError (TDZ) | ❌ ReferenceError (TDZ) |
| **ค่าก่อนบรรทัดประกาศ** | `undefined` | ❌ เข้าถึงไม่ได้ | ❌ เข้าถึงไม่ได้ |

### 🧠 Challenge: TDZ Detective 🔍
จงทำนาย Output (หรือ Error) ของแต่ละบรรทัด:
```javascript
console.log(a); // (1)?
console.log(b); // (2)?
console.log(c); // (3)?

var a = 1;
let b = 2;
const c = 3;

console.log(a); // (4)?
console.log(b); // (5)?
console.log(c); // (6)?
```

::: details ✨ ดูเฉลย
1. `undefined` — `var a` ถูก Hoist เป็น `undefined`
2. ❌ **ReferenceError** — `let b` อยู่ใน TDZ
3. ❌ **ReferenceError** — `const c` อยู่ใน TDZ

*(หมายเหตุ: ถ้าบรรทัด 2 Error โปรแกรมจะหยุดทันที ไม่ถึงบรรทัด 3-6)*

4. `1` — หลังจากบรรทัดประกาศ ใช้ได้ปกติ
5. `2`
6. `3`
:::

---

## 5. const กับ Object/Array (Gotcha!) 🤯

นี่คือจุดที่ผู้เริ่มต้น **สับสนมากที่สุด**: `const` ป้องกันการ **เปลี่ยนตัวแปร** (Reassignment) แต่ **ไม่ได้ป้องกันการแก้ไขข้อมูลข้างใน**:

```javascript
// Primitive: const = เปลี่ยนค่าไม่ได้เลย
const name = "Dolar";
// name = "Somchai"; // ❌ TypeError!

// Object: const = เปลี่ยน Reference ไม่ได้ แต่แก้ไข Properties ได้!
const user = { name: "Dolar", age: 25 };

user.age = 26;              // ✅ แก้ไข Property ได้!
user.score = 100;           // ✅ เพิ่ม Property ได้!
delete user.score;          // ✅ ลบ Property ได้!
console.log(user);          // { name: "Dolar", age: 26 }

// user = { name: "New" };  // ❌ TypeError! เปลี่ยนตัว Object ทั้งก้อนไม่ได้!

// Array: เหมือน Object
const numbers = [1, 2, 3];
numbers.push(4);            // ✅ เพิ่มสมาชิกได้!
numbers[0] = 99;            // ✅ แก้ไขสมาชิกได้!
console.log(numbers);       // [99, 2, 3, 4]

// numbers = [5, 6, 7];     // ❌ TypeError! เปลี่ยน Array ทั้งตัวไม่ได้!
```

> **💡 Analogy:** `const` สำหรับ Object/Array เหมือน **"ที่อยู่บ้านที่เปลี่ยนไม่ได้"** (Reference) แต่คุณยัง **ตกแต่งภายในบ้านได้** (Mutation) ตามสบาย!
>
> เรื่องนี้จะเจาะลึกในบท [05-3: Reference vs Value](/05-03-reference-vs-value) ครับ

### ถ้าต้องการ Object ที่ป้องกันการแก้ไขจริงๆ:

```javascript
const frozenUser = Object.freeze({ name: "Dolar", age: 25 });
frozenUser.age = 100;        // ไม่ Error แต่ไม่มีผล! (Silent fail)
console.log(frozenUser.age); // 25 ← ยังเป็นค่าเดิม

// ⚠️ Object.freeze() ทำได้แค่ "Shallow Freeze"
// Object ซ้อนข้างในยังแก้ได้!
```

### 🧠 Challenge: Will it Error?
```javascript
const car = { brand: "Honda", model: "Civic" };

car.brand = "Toyota";   // (1) Error?
car.year = 2024;         // (2) Error?
car = {};                // (3) Error?
```

::: details ✨ ดูเฉลย
1. ✅ **ไม่ Error** — แก้ไข Property ของ Object ได้ (`car.brand` = `"Toyota"`)
2. ✅ **ไม่ Error** — เพิ่ม Property ใหม่ได้ (`car.year` = `2024`)
3. ❌ **TypeError!** — เปลี่ยน Reference (ตัว Object ทั้งก้อน) ไม่ได้ เพราะ `const`

**กฎง่ายๆ:** `const` ห้ามเปลี่ยน **"กล่อง"** (Reference) แต่ **แก้ไส้ในของกล่อง** (Properties) ได้!
:::

---

## 6. var vs let vs const — สรุปครบ 📊

| คุณสมบัติ | `var` ❌ | `let` ✅ | `const` ✅ |
|:----------|:--------|:--------|:----------|
| **ปีที่เกิด** | ES1 (1997) | ES6 (2015) | ES6 (2015) |
| **Scope** | Function | **Block** | **Block** |
| **Hoisting** | ✅ → `undefined` | ✅ → TDZ | ✅ → TDZ |
| **เปลี่ยนค่า (Reassign)** | ✅ ได้ | ✅ ได้ | ❌ ไม่ได้ |
| **ประกาศซ้ำ (Redeclare)** | ✅ ได้ (Bug!) | ❌ Error | ❌ Error |
| **ต้อง Initialize ทันที** | ❌ ไม่ต้อง | ❌ ไม่ต้อง | ✅ **ต้อง!** |
| **แก้ไข Object Properties** | ✅ ได้ | ✅ ได้ | ✅ **ได้!** |
| **แนะนำให้ใช้ไหม?** | ❌ **ไม่!** | ✅ เมื่อต้องเปลี่ยนค่า | ✅ **ค่าเริ่มต้น** |

---

## 7. Final Challenge: The Variable Lab 🧪

### 🎯 Challenge 1: Predict All Outputs
```javascript
var a = 1;
let b = 2;
const c = 3;

if (true) {
    var a = 10;   // (1) a ตอนนี้เป็นอะไร?
    let b = 20;   // (2) b ตอนนี้เป็นอะไร?
    // const c = 30; // ถ้าเอา comment ออก จะ Error ไหม?
    console.log(a, b, c); // (3)?
}

console.log(a); // (4)?
console.log(b); // (5)?
console.log(c); // (6)?
```

::: details ✨ ดูเฉลย
3. `10, 20, 3` — ภายใน Block: `var a` = 10 (ทับค่าเดิม!), `let b` = 20 (Block scope ใหม่), `c` = 3 (จาก Global)
4. **`10`** — `var a` ไม่ respect Block Scope → ค่า `10` รั่วออกมาทับค่าเดิม!
5. **`2`** — `let b` ใน Block เป็น "ตัวใหม่" → ไม่กระทบ `b` ข้างนอก
6. **`3`** — `const c` ไม่เปลี่ยนแปลง

**บทเรียน:** `var` อันตราย เพราะมัน Shadow ตัวแปรข้างนอกโดยไม่ตั้งใจ!
:::

### 🎯 Challenge 2: Refactor to Modern JS
แปลงโค้ดเก่านี้ให้ใช้ `const`/`let` อย่างถูกต้อง:
```javascript
var TAX_RATE = 0.07;
var productName = "iPhone";
var price = 35000;
var total = price + (price * TAX_RATE);
var i;
for (i = 0; i < 3; i++) {
    var message = "Item " + i;
    console.log(message);
}
```

::: details ✨ ดูเฉลย
```javascript
const TAX_RATE = 0.07;            // ค่าคงที่ → const
const productName = "iPhone";     // ไม่เปลี่ยน → const
const price = 35000;              // ไม่เปลี่ยน → const
const total = price + (price * TAX_RATE); // ไม่เปลี่ยน → const

for (let i = 0; i < 3; i++) {    // เปลี่ยนค่า + Block scope → let
    const message = "Item " + i;  // ไม่เปลี่ยนใน iteration → const
    console.log(message);
}
```

**หลักคิด:**
1. เริ่มจาก `const` ทุกตัว
2. ถ้า Error (เพราะต้องเปลี่ยนค่า) → เปลี่ยนเป็น `let`
3. ไม่ใช้ `var` เลย!
:::

---

> **📖 คำศัพท์เทคนิค (Glossary):**
> *   **Variable (ตัวแปร):** พื้นที่ในหน่วยความจำที่มีชื่อกำกับ ใช้เก็บข้อมูล
> *   **Identifier (ชื่อตัวแปร):** ชื่อที่ใช้อ้างอิงตัวแปร/ฟังก์ชัน ต้องเป็นไปตามกฎ Naming Rules
> *   **Declaration (การประกาศ):** การสร้างตัวแปรขึ้นมา (`const x;` / `let y;`)
> *   **Initialization (การกำหนดค่าเริ่มต้น):** การใส่ค่าแรกให้ตัวแปร (`x = 10`)
> *   **Assignment (การกำหนดค่า):** การเปลี่ยนค่าตัวแปร (`x = 20`)
> *   **Reassignment:** การกำหนดค่าใหม่ให้ตัวแปร — `const` ห้าม, `let` ได้
> *   **Hoisting:** พฤติกรรมที่ JS ยกคำประกาศตัวแปรขึ้นไปบนสุดของ Scope ก่อนรันโค้ด
> *   **TDZ (Temporal Dead Zone):** ช่วงเวลาตั้งแต่เริ่ม Scope จนถึงบรรทัดประกาศ `let`/`const` ที่ตัวแปรเข้าถึงไม่ได้
> *   **Block Scope:** ขอบเขตภายใน `{}` ที่ `let`/`const` เคารพ
> *   **Function Scope:** ขอบเขตภายในฟังก์ชัน ที่ `var` ใช้
> *   **Reserved Word (คำสงวน):** คำที่ JS ใช้แล้ว ห้ามเอามาตั้งชื่อตัวแปร เช่น `if`, `class`, `return`
> *   **camelCase:** Convention ตั้งชื่อตัวแปร: เริ่มตัวเล็ก แล้วขึ้นตัวใหญ่คำถัดไป เช่น `myUserName`
> *   **Object.freeze():** Method ที่ป้องกันการแก้ไข Properties ของ Object (Shallow)
> *   **Shadowing:** เมื่อตัวแปรใน Block ชื่อเดียวกับข้างนอก ตัวข้างในจะ "บัง" ตัวข้างนอก

---
👉 **[ไปต่อ: 02-2 - Data Types (ชนิดข้อมูล)](/02-02-data-types)**
