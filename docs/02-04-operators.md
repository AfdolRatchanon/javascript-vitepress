# 02-4: Operators (ตัวดำเนินการ) 🧮

> **"Mathematics is the music of reason."**
> — *James Joseph Sylvester*

เพื่อให้โปรแกรมทำงานอะไรบางอย่างได้ เราต้องมีเครื่องมือในการ **คำนวณ**, **เปรียบเทียบ**, และ **ตัดสินใจ** เครื่องมือเหล่านั้นเราเรียกว่า **Operators** ครับ

> **💡 Analogy (เปรียบเทียบ):**
> Operators เหมือน **"เครื่องมือช่าง"** 🧰 แต่ละชิ้นมีหน้าที่ต่างกัน:
> - `+` `-` `*` `/` = ไขควง ค้อน เลื่อย (เครื่องมือทำงาน)
> - `>` `<` `===` = ไม้บรรทัด (เครื่องมือวัด/เปรียบเทียบ)
> - `&&` `||` `!` = สวิตช์ไฟ (เปิด/ปิด — เลือกทาง)

## 1. Arithmetic Operators (การคำนวณทางคณิตศาสตร์) ➕

ใช้จัดการกับข้อมูลประเภท `Number` เป็นหลัก ตาม [MDN](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Expressions_and_operators#arithmetic_operators):

```javascript
console.log(10 + 5);    // 15   (บวก)
console.log(10 - 5);    // 5    (ลบ)
console.log(10 * 5);    // 50   (คูณ)
console.log(10 / 3);    // 3.33333... (หาร — ได้ทศนิยม!)
console.log(10 % 3);    // 1    (หารเอาเศษ — Modulo)
console.log(2 ** 10);   // 1024 (ยกกำลัง — ES2016)
```

### 📊 Arithmetic Operators Table

| Operator | ชื่อ | ตัวอย่าง | ผลลัพธ์ | ใช้บ่อยแค่ไหน |
|:---------|:-----|:---------|:--------|:-------------|
| `+` | บวก (Addition) | `10 + 5` | `15` | ⭐⭐⭐ ทุกวัน |
| `-` | ลบ (Subtraction) | `10 - 5` | `5` | ⭐⭐⭐ |
| `*` | คูณ (Multiplication) | `10 * 5` | `50` | ⭐⭐⭐ |
| `/` | หาร (Division) | `10 / 3` | `3.333...` | ⭐⭐ |
| `%` | เศษจากการหาร (Modulo) | `10 % 3` | `1` | ⭐⭐ เช็คเลขคู่/คี่ |
| `**` | ยกกำลัง (Exponentiation) | `2 ** 3` | `8` | ⭐ |

### The `+` Operator — Two Faces (คนสองหน้า!) 🎭

`+` เป็น Operator ที่ **ทำได้ 2 อย่าง** — ขึ้นอยู่กับ Data Type:

```javascript
// Face 1: Addition (ถ้าเป็น Number)
console.log(5 + 3);        // 8 (คณิตศาสตร์)

// Face 2: Concatenation (ถ้ามี String)
console.log("Hello" + " " + "World");  // "Hello World" (ต่อข้อความ)

// ⚠️ Gotcha: Number + String = String!
console.log(5 + "3");      // "53" (ไม่ใช่ 8!)
console.log("5" + 3);      // "53"
console.log(5 + 3 + "px"); // "8px" (5+3=8 ก่อน แล้วค่อย +"px")
console.log("$" + 5 + 3);  // "$53" ("$"+5="$5" ก่อน แล้วค่อย +3)
```

> **กฎ:** ถ้ามี **String ข้างใดข้างหนึ่ง** → `+` จะกลายเป็น **Concatenation** (ต่อข้อความ) แทนการบวก!

### Modulo Deep Dive (`%` — เอาไปใช้จริง!):

```javascript
// ✅ ใช้เช็คเลขคู่/คี่ (ใช้บ่อยมาก!)
console.log(10 % 2);  // 0 → เลขคู่ ✅
console.log(7 % 2);   // 1 → เลขคี่

// ✅ ใช้วนรอบ (Wrap Around) — เช่น วันในสัปดาห์
const dayNumber = 10;
const dayOfWeek = dayNumber % 7;
console.log(dayOfWeek); // 3 → วันพุธ (ถ้า 0=อาทิตย์)

// ✅ ใช้เช็คหารลงตัว (FizzBuzz!)
for (let i = 1; i <= 15; i++) {
    if (i % 15 === 0) console.log("FizzBuzz");
    else if (i % 3 === 0) console.log("Fizz");
    else if (i % 5 === 0) console.log("Buzz");
    else console.log(i);
}
```

### 🧠 Challenge: The + Trap
ทายผลลัพธ์:
```javascript
console.log(1 + 2 + "3");   // (1)?
console.log("1" + 2 + 3);   // (2)?
```

::: details ✨ ดูเฉลย
1. `"33"` → `1+2=3` ก่อน (Number+Number=3) แล้ว `3+"3"="33"` (Number+String=String)
2. `"123"` → `"1"+2="12"` ก่อน (String+Number=String) แล้ว `"12"+3="123"`
:::

---

## 2. Operator Precedence (ลำดับความสำคัญ — ใครทำก่อน?) 🥇

ตาม [MDN](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Operator_precedence):

```javascript
// ถ้าเขียน 1 + 2 * 3:
console.log(1 + 2 * 3);   // 7 (ไม่ใช่ 9!)
// เพราะ * ทำก่อน +

// ใช้วงเล็บบังคับลำดับ:
console.log((1 + 2) * 3); // 9 ✅
```

### 📊 Precedence Ranking (สูง → ต่ำ)

| ลำดับ | Operator | ชื่อ | ตัวอย่าง |
|:-----:|:---------|:-----|:---------|
| 1 | `()` | วงเล็บ (Grouping) | `(1 + 2) * 3` |
| 2 | `**` | ยกกำลัง | `2 ** 3` |
| 3 | `++` `--` | Increment/Decrement | `x++` |
| 4 | `!` | NOT | `!true` |
| 5 | `*` `/` `%` | คูณ หาร เศษ | `6 * 2` |
| 6 | `+` `-` | บวก ลบ | `5 + 3` |
| 7 | `<` `>` `<=` `>=` | Comparison | `x > 5` |
| 8 | `===` `!==` | Equality | `x === y` |
| 9 | `&&` | AND | `a && b` |
| 10 | `\|\|` | OR | `a \|\| b` |
| 11 | `=` `+=` `-=` | Assignment | `x = 5` |

> **⭐ กฎทอง:** ถ้าจำไม่ได้ → **ใส่วงเล็บ `()` เสมอ!** นอกจากป้องกัน Bug แล้ว ยังอ่านง่ายด้วย

### 🧠 Challenge: Predict the Result
```javascript
let result = 2 + 3 * 4 ** 2 - 10;
console.log(result);  // ?
```

::: details ✨ ดูเฉลย
ลำดับ:
1. `4 ** 2` = `16` (ยกกำลังก่อน)
2. `3 * 16` = `48` (คูณ)
3. `2 + 48` = `50` (บวก)
4. `50 - 10` = **`40`** (ลบ)

**ผลลัพธ์: `40`**
:::

---

## 3. Assignment Operators (กำหนดค่า) 📥

ใช้เพื่อเก็บค่าลงในตัวแปร หรืออัปเดตค่าเดิม:

```javascript
let x = 10;      // กำหนดค่าเริ่มต้น

x += 5;          // x = x + 5 → 15
x -= 3;          // x = x - 3 → 12
x *= 2;          // x = x * 2 → 24
x /= 4;          // x = x / 4 → 6
x %= 4;          // x = x % 4 → 2
x **= 3;         // x = x ** 3 → 8

console.log(x);  // 8
```

### 📊 Assignment Operators Table

| Operator | ตัวอย่าง | เทียบเท่ากับ | ชื่อ |
|:---------|:---------|:------------|:-----|
| `=` | `x = 10` | — | Assignment |
| `+=` | `x += 5` | `x = x + 5` | Addition Assignment |
| `-=` | `x -= 5` | `x = x - 5` | Subtraction Assignment |
| `*=` | `x *= 2` | `x = x * 2` | Multiplication Assignment |
| `/=` | `x /= 2` | `x = x / 2` | Division Assignment |
| `%=` | `x %= 2` | `x = x % 2` | Remainder Assignment |
| `**=` | `x **= 2` | `x = x ** 2` | Exponentiation Assignment |

### Increment & Decrement (การเพิ่ม/ลดทีละ 1):

```javascript
let count = 0;

// Postfix: ใช้ค่า "ก่อน" แล้วค่อยเพิ่ม/ลด
console.log(count++);  // 0 (แสดง 0 ก่อน แล้วค่อยเพิ่มเป็น 1)
console.log(count);    // 1

// Prefix: เพิ่ม/ลด "ก่อน" แล้วค่อยใช้ค่า
console.log(++count);  // 2 (เพิ่มเป็น 2 ก่อน แล้วค่อยแสดง)
console.log(count);    // 2
```

### 📊 Prefix vs Postfix

| แบบ | Syntax | ลำดับ | ตัวอย่าง (`x = 5`) |
|:-----|:-------|:-----|:-------------------|
| **Postfix** | `x++` | ใช้ค่าก่อน → เพิ่มทีหลัง | `console.log(x++)` → `5` (x กลายเป็น 6) |
| **Prefix** | `++x` | เพิ่มก่อน → ใช้ค่าทีหลัง | `console.log(++x)` → `6` (x กลายเป็น 6) |

### 🧠 Challenge: Postfix vs Prefix
```javascript
let a = 5;
let b = a++;
let c = ++a;
console.log(a, b, c);  // ?, ?, ?
```

::: details ✨ ดูเฉลย
```
a = 7, b = 5, c = 7
```
1. `b = a++` → b ได้ค่าเดิม (5) แล้ว a เพิ่มเป็น 6
2. `c = ++a` → a เพิ่มเป็น 7 ก่อน แล้ว c ได้ค่าใหม่ (7)
:::

---

## 4. Comparison Operators (เปรียบเทียบ) ⚖️

ใช้เปรียบเทียบค่าสองค่า ผลลัพธ์จะเป็น **Boolean (`true` หรือ `false`)** เสมอ ตาม [MDN](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators#relational_operators):

```javascript
console.log(10 > 5);     // true   (มากกว่า)
console.log(10 < 5);     // false  (น้อยกว่า)
console.log(10 >= 10);   // true   (มากกว่าหรือเท่ากับ)
console.log(5 <= 2);     // false  (น้อยกว่าหรือเท่ากับ)
```

### 📊 Comparison Operators Table

| Operator | ชื่อ | ตัวอย่าง | ผลลัพธ์ |
|:---------|:-----|:---------|:--------|
| `>` | มากกว่า | `10 > 5` | `true` |
| `<` | น้อยกว่า | `10 < 5` | `false` |
| `>=` | มากกว่าหรือเท่ากับ | `10 >= 10` | `true` |
| `<=` | น้อยกว่าหรือเท่ากับ | `5 <= 2` | `false` |
| `===` | เท่ากัน (Strict) | `5 === "5"` | `false` |
| `!==` | ไม่เท่ากัน (Strict) | `5 !== "5"` | `true` |
| `==` | เท่ากัน (Loose) ⚠️ | `5 == "5"` | `true` |
| `!=` | ไม่เท่ากัน (Loose) ⚠️ | `5 != "5"` | `false` |

### ⚠️ `==` vs `===` (The Deadly Trap!)

```javascript
// == (Loose): แปลง Type ก่อนเทียบ → อันตราย!
console.log(5 == "5");      // true 😱 (แปลง "5" เป็น 5 ก่อน)
console.log(0 == false);    // true 😱
console.log("" == false);   // true 😱
console.log(null == undefined); // true 😱

// === (Strict): เทียบทั้งค่าและ Type → ปลอดภัย!
console.log(5 === "5");     // false ✅ (ต่าง Type)
console.log(0 === false);   // false ✅
console.log("" === false);  // false ✅
console.log(null === undefined); // false ✅
```

> **🚨 กฎเหล็ก: ให้ใช้ `===` และ `!==` เสมอ! ลืม `==` ไปได้เลย!**

---

## 5. Logical Operators (ตรรกศาสตร์) 🚥

ใช้สำหรับเชื่อมเงื่อนไขหลายๆ อย่างเข้าด้วยกัน ตาม [MDN](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Logical_AND):

```javascript
const age = 25;
const hasLicense = true;

// && (AND): ต้องจริงทั้งคู่
console.log(age >= 18 && hasLicense);  // true (อายุผ่าน AND มีใบขับขี่)

// || (OR): จริงอย่างน้อยหนึ่งอัน
console.log(age < 18 || hasLicense);   // true (อายุไม่ผ่าน แต่มีใบขับขี่)

// ! (NOT): กลับค่า
console.log(!true);   // false
console.log(!false);  // true
```

### 📊 Truth Table (ตารางความจริง)

| A | B | `A && B` | `A \|\| B` | `!A` |
|:--|:--|:---------|:-----------|:-----|
| true | true | **true** | **true** | false |
| true | false | **false** | **true** | false |
| false | true | **false** | **true** | true |
| false | false | **false** | **false** | true |

### Short-Circuit Evaluation (ลัดวงจร):

```javascript
// && หยุดทันทีที่เจอ false
false && console.log("Won't print");   // ไม่พิมพ์ (false แล้ว หยุดเลย)
true && console.log("Will print");     // "Will print"

// || หยุดทันทีที่เจอ true
true || console.log("Won't print");    // ไม่พิมพ์ (true แล้ว หยุดเลย)

// ⭐ ใช้ || สำหรับค่า Default:
const username = "" || "Guest";
console.log(username);  // "Guest" (เพราะ "" เป็น falsy)

// ⭐ ใช้ ?? (Nullish Coalescing) สำหรับ null/undefined เท่านั้น:
const score = 0 ?? 100;
console.log(score);     // 0 (เพราะ 0 ไม่ใช่ null/undefined)
```

### 🧠 Challenge: Logical Thinking
จงทายว่าผลลัพธ์ของเงื่อนไขนี้คืออะไร?
```javascript
const result = (10 > 5) && (2 === "2") || !(false);
console.log(result);  // ?
```

::: details ✨ ดูเฉลย
1. `(10 > 5)` → `true`
2. `(2 === "2")` → `false` (Strict check — ต่าง Type)
3. `!(false)` → `true`
4. `true && false` → `false` (AND)
5. `false || true` → **`true`** ✅ (OR)
:::

---

## 6. Special & Modern Operators ✨

### Optional Chaining `?.` (ES2020):
```javascript
const user = { name: "Dolar", address: null };

// ❌ เดิม: ต้อง check ก่อน
// const city = user.address && user.address.city;

// ✅ ใช้ ?. : ถ้าเป็น null/undefined → หยุดแล้ว return undefined (ไม่ Error!)
const city = user.address?.city;
console.log(city); // undefined (แทนที่จะ Error!)
```

### Nullish Coalescing `??` (ES2020):
```javascript
// ?? จะเลือก "ค่าอื่น" เฉพาะเมื่อค่าซ้ายเป็น null หรือ undefined
console.log(null ?? "default");     // "default"
console.log(undefined ?? "default");// "default"
console.log(0 ?? "default");        // 0 (0 ไม่ใช่ null!)
console.log("" ?? "default");       // "" ("" ไม่ใช่ null!)

// เทียบกับ ||:
console.log(0 || "default");        // "default" (0 เป็น falsy!)
console.log("" || "default");       // "default" ("" เป็น falsy!)
```

### Ternary Operator `? :` (Conditional):
```javascript
const age = 20;
const status = age >= 18 ? "Adult" : "Minor";
console.log(status); // "Adult"

// เทียบเท่ากับ:
// if (age >= 18) { status = "Adult"; } else { status = "Minor"; }
```

---

## 7. Challenges: The Math Lab 🧪

### 🎯 Challenge 1: Modular Arithmetic
เราอยากรู้ว่าตัวเลขในตัวแปร `n` เป็น **เลขคู่** หรือ **เลขคี่**
จงเขียน Expression โดยใช้ `%`

::: details ✨ ดูเฉลย
```javascript
let n = 10;
let isEven = (n % 2 === 0);
console.log(isEven); // true (เลขคู่)
```
:::

### 🎯 Challenge 2: The Logic Gate
ระบบสมัครงาน:
- ผู้สมัครต้องมี `age` มากกว่า 18
- **และ** ต้องมี `hasDegree` เป็น true **หรือ** มี `experience` มากกว่า 3 ปี

```javascript
const isQualified = (age > 18) ___ (hasDegree ___ experience > 3);
```

::: details ✨ ดูเฉลย
```javascript
const isQualified = (age > 18) && (hasDegree || experience > 3);
// ต้องมีวงเล็บครอบ เพื่อให้เลือกอย่างใดอย่างหนึ่งก่อน
// แล้วค่อยเอาไป "และ" กับเรื่องอายุครับ
```
:::

### 🎯 Challenge 3: Comparison Maze
ทายผลลัพธ์ทุกบรรทัด:
```javascript
console.log("" == false);     // (1)?
console.log("" === false);    // (2)?
console.log(null == undefined);// (3)?
console.log(NaN === NaN);     // (4)?
```

::: details ✨ ดูเฉลย
1. `true` — `""` และ `false` ถูกแปลงเป็น `0` ทั้งคู่ (Loose)
2. `false` — ต่าง Type (String vs Boolean) → Strict จะไม่แปลง
3. `true` — `null == undefined` เป็นกรณีพิเศษ (เท่ากัน!)
4. `false` — ⚠️ **NaN ไม่เท่ากับอะไรเลย แม้แต่ตัวมันเอง!**
:::

---

> **📖 คำศัพท์เทคนิค (Glossary):**
> *   **Operator (ตัวดำเนินการ):** สัญลักษณ์ที่ใช้สั่งให้โปรแกรมประมวลผล (เช่น `+`, `-`, `===`)
> *   **Operand (ตัวถูกดำเนินการ):** ข้อมูลที่ถูกกระทำ (เช่น ใน `5 + 2` → `5` และ `2` คือ Operand)
> *   **Precedence (ลำดับความสำคัญ):** กฎที่บอกว่า Operator ไหนทำก่อน/หลัง
> *   **Modulo (`%`):** Operator หารเอาเศษ — ใช้เช็คเลขคู่/คี่
> *   **Concatenation:** การต่อ String เข้าด้วยกัน ด้วย `+`
> *   **Strict Equality (`===`):** เทียบทั้ง "ค่า" และ "Type" ต้องตรงกันทั้งคู่
> *   **Loose Equality (`==`):** เทียบแค่ "ค่า" โดยแปลง Type ก่อน (⚠️ อันตราย)
> *   **Short-Circuit:** พฤติกรรมที่ `&&` / `||` หยุดประมวลผลทันทีที่รู้ผลลัพธ์
> *   **Ternary Operator (`? :`):** Operator 3 ส่วนสำหรับเลือกค่าตามเงื่อนไข
> *   **Optional Chaining (`?.`):** เข้าถึง Property โดยไม่ Error ถ้าเป็น null/undefined
> *   **Nullish Coalescing (`??`):** เลือกค่า Default เฉพาะเมื่อค่าเป็น null/undefined

---
👉 **[ไปต่อ: Project — BMI Calculator](/02-project-bmi)**
