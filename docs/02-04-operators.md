# 02-4: Operators (ตัวดำเนินการ) 🧮

> **"Mathematics is the music of reason."**
> — *James Joseph Sylvester*

เพื่อให้โปรแกรมทำงานอะไรบางอย่างได้ เราต้องมีเครื่องมือในการ **คำนวณ**, **เปรียบเทียบ**, และ **ตัดสินใจ** เครื่องมือเหล่านั้นเราเรียกว่า **Operators** ครับ

## 1. Arithmetic Operators (การคำนวณทางคณิตศาสตร์) ➕
ใช้จัดการกับข้อมูลประเภท `Number` เป็นหลัก

| Operator | Action | Example | Result |
| :--- | :--- | :--- | :--- |
| **`+`** | บวก (Addition) | `10 + 5` | `15` |
| **`-`** | ลบ (Subtraction) | `10 - 5` | `5` |
| **`*`** | คูณ (Multiplication) | `10 * 5` | `50` |
| **`/`** | หาร (Division) | `10 / 2` | `5` |
| **`%`** | หารเอาเศษ (Remainder/Modulus) | `10 % 3` | `1` |
| **`**`** | ยกกำลัง (Exponentiation) | `2 ** 3` | `8` |

### 🧠 Deep Dive: Operator Precedence (ใครมาก่อน?)
ถ้าเขียน `1 + 2 * 3` จะได้อะไร? `9` หรือ `7`?
คำตอบคือ **`7`** ครับ เพราะ JavaScript มีลำดับความสำคัญ (เหมือนวิชาเลข):
1.  **วงเล็บ `()`** — สำคัญที่สุด! ทำก่อนเพื่อน
2.  **ยกกำลัง `**`**
3.  **คูณ/หาร/เศษ `* / %`**
4.  **บวก/ลบ `+ -`**

> **Tip:** ถ้าไม่แน่ใจ ให้ใส่ **วงเล็บ `()`** ไว้เสมอ นอกจากจะป้องกันบั๊กแล้ว ยังทำให้คนอื่นอ่านโค้ดเราง่ายขึ้นด้วย!

## 2. Assignment Operators (การกำหนดค่า) 📥
ใช้เพื่อเก็บค่าลงในตัวแปร หรืออัปเดตค่าเดิม

| Operator | Example | Equivalent to (มีความหมายเท่ากับ) |
| :--- | :--- | :--- |
| **`=`** | `x = 10` | `x = 10` |
| **`+=`** | `x += 5` | `x = x + 5` |
| **`-=`** | `x -= 5` | `x = x - 5` |
| **`*=`** | `x *= 2` | `x = x * 2` |
| **`/=`** | `x /= 2` | `x = x / 2` |

### Increment & Decrement (การเพิ่ม/ลดทีละ 1)
เราใช้บ่อยมากในเรื่อง Loops:
- `x++` (เพิ่มทีละ 1)
- `x--` (ลดทีละ 1)

## 3. Comparison Operators (การเปรียบเทียบ) ⚖️
ใช้เปรียบเทียบค่าสองค่า ผลลัพธ์ที่ได้จะเป็น **Boolean (`true` หรือ `false`)** เสมอ

| Operator | Action | Example | Result |
| :--- | :--- | :--- | :--- |
| **`>`** | มากกว่า | `10 > 5` | `true` |
| **`<`** | น้อยกว่า | `10 < 5` | `false` |
| **`>=`** | มากกว่าหรือเท่ากับ | `10 >= 10` | `true` |
| **`<=`** | น้อยกว่าหรือเท่ากับ | `5 <= 2` | `false` |
| **`===`** | เท่ากัน (Strict) | `5 === "5"` | `false` |
| **`!==`** | ไม่เท่ากัน (Strict) | `5 !== "5"` | `true` |

> **คำเตือน:** เราเรียนเรื่อง `==` vs `===` ไปแล้วในบท 02-03 จำได้ไหม? **ให้ใช้ 3 ขีด (`===`) เสมอ!**

## 4. Logical Operators (การตัดสินใจทางตรรกศาสตร์) 🚥
ใช้สำหรับเชื่อมเงื่อนไขหลายๆ อย่างเข้าด้วยกัน

| Operator | Name | Logic | Example |
| :--- | :--- | :--- | :--- |
| **`&&`** | **AND** (และ) | ต้อง **จริงทั้งคู่** ถึงจะเป็นจริง | `(5 > 1) && (10 > 5)` → `true` |
| **`||`** | **OR** (หรือ) | ขอแค่ **จริงอย่างน้อยหนึ่ง** | `(5 < 1) || (10 > 5)` → `true` |
| **`!`** | **NOT** (ไม่) | กลับจากจริงเป็นเท็จ (และตรงข้าม) | `!(5 > 1)` → `false` |

### 🧠 Challenge: Logical Thinking
จงทายว่าผลลัพธ์ของเงื่อนไขนี้คืออะไร?
`const result = (10 > 5) && (2 === "2") || !(false);`

::: details ✨ ดูเฉลย
1. `(10 > 5)` → `true`
2. `(2 === "2")` → `false` (Strict check)
3. `!(false)` → `true`
4. `(true && false)` → `false`
5. `false || true` → **`true`** ✅
:::

> **คำศัพท์เทคนิค (Glossary):**
> *   **Operator** (ตัวดำเนินการ): สัญลักษณ์ที่ใช้สั่งให้โปรแกรมประมวลผล (เช่น `+`, `-`)
> *   **Operand** (ตัวถูกดำเนินการ): ข้อมูลที่ถูกกระทำ (เช่น ใน `5 + 2` นั้น `5` และ `2` คือ Operand)
> *   **Precedence** (ลำดับความสำคัญ): กฎที่บอกว่าควรทำเครื่องหมายไหนก่อนหลัง
> *   **Boolean** (ค่าความจริง): ผลลัพธ์จากการเปรียบเทียบที่มีแค่ `true` หรือ `false`

---

## 5. Challenges: The Math Lab 🧪

### 🎯 Challenge 1: Modular Arithmetic
เราอยากรู้ว่าตัวเลขในตัวแปร `n` เป็น **เลขคู่** หรือ **เลขคี่**
จงเขียน Expression โดยใช้ Operator `%` เพื่อตรวจสอบ (ใบ้: เลขคู่หาร 2 แล้วเหลือเศษเท่าไหร่?)

::: details ✨ ดูเฉลย
```javascript
let n = 10;
let isEven = (n % 2 === 0);
console.log(isEven); // true
```
:::

### 🎯 Challenge 2: The Logic Gate
คุณกำลังเขียนระบบสมัครงาน:
*   ผู้สมัครต้องมี `age` มากกว่า 18
*   **และ** ต้องมี `hasDegree` เป็น true **หรือ** มี `experience` มากกว่า 3 ปี

จงเติมเครื่องหมายในช่องว่าง:
`const isQualified = (age > 18) ___ (hasDegree ___ experience > 3);`

::: details ✨ ดูเฉลย
```javascript
const isQualified = (age > 18) && (hasDegree || experience > 3);
```
*(ต้องมีวงเล็บครอบเงื่อนไขหลัง เพื่อให้เลือกอย่างใดอย่างหนึ่งก่อน แล้วค่อยเอาไป "และ" กับเรื่องอายุครับ)*
:::

---
👉 **[ไปต่อ: Project 3 - BMI Calculator](/02-project-bmi)**
