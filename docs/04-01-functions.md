# 04-1: Functions & Arrow Functions (ฟังก์ชัน) 🧬

> **"First-class functions are the bread and butter of JavaScript programming."**
> — *Kyle Simpson (You Don't Know JS)*

การเขียนโค้ดโดยไม่มีฟังก์ชัน ก็เหมือนการทำอาหารโดยต้องเริ่มปลูกข้าวกินเองทุกมื้อครับ! ฟังก์ชันช่วยให้เรา **"ห่อหุ้ม" (Encapsulate)** ชุดคำสั่งเก็บไว้ แล้วเรียกใช้เมื่อไหร่ก็ได้ที่ต้องการ

> **💡 Analogy (เปรียบเทียบ):**
> ฟังก์ชันเหมือน **"สูตรอาหาร"** ครับ คุณเขียนวิธีทำ (ชุดคำสั่ง) ไว้ครั้งเดียวในกระดาษ เมื่อไหร่ที่คุณหิว (ต้องการรันโค้ด) คุณแค่หยิบสูตรนั้นมาทำตาม คุณก็จะได้อาหาร (ผลลัพธ์) ออกมา โดยไม่ต้องมานั่งคิดใหม่ว่าจะเริ่มหั่นผักยังไง!

## 1. ทำไมต้องใช้ฟังก์ชัน? (Why Functions?) 🤔

ก่อนจะเขียนฟังก์ชันเป็น เรามาดูก่อนว่าถ้า **ไม่มีฟังก์ชัน** โค้ดจะแย่ขนาดไหน:

```javascript
// ❌ BAD: ไม่ใช้ฟังก์ชัน (ต้องเขียนซ้ำทุกครั้ง)
const price1 = 100;
const tax1 = price1 * 0.07;
const total1 = price1 + tax1;
console.log(`สินค้า 1: ${total1} บาท`); // 107

const price2 = 250;
const tax2 = price2 * 0.07;
const total2 = price2 + tax2;
console.log(`สินค้า 2: ${total2} บาท`); // 267.5

// ถ้ามีสินค้า 100 ชิ้น ต้องเขียนโค้ดซ้ำ 100 ครั้ง!
// ถ้ารัฐบาลเปลี่ยนภาษีเป็น 10% ต้องไล่แก้ทีละจุด!
```

```javascript
// ✅ GOOD: ใช้ฟังก์ชัน (เขียนสูตรครั้งเดียว ใช้ได้ทุกที่)
function calculateTotal(price) {
    const tax = price * 0.07;
    return price + tax;
}

console.log(`สินค้า 1: ${calculateTotal(100)} บาท`);  // 107
console.log(`สินค้า 2: ${calculateTotal(250)} บาท`);  // 267.5
console.log(`สินค้า 3: ${calculateTotal(500)} บาท`);  // 535
// ถ้าเปลี่ยนภาษี แก้แค่จุดเดียวในฟังก์ชัน!
```

### ข้อดี 3 ประการหลัก:
| หลักการ | ความหมาย | ตัวอย่าง |
|:--------|:---------|:---------|
| **DRY** (Don't Repeat Yourself) | ไม่เขียนโค้ดซ้ำๆ | เขียนสูตรคำนวณภาษีครั้งเดียว ใช้ได้ตลอด |
| **Organize** (จัดระเบียบ) | แบ่งโค้ดเป็นส่วนๆ อ่านง่าย | `calculateTax()`, `formatPrice()`, `showResult()` |
| **Modular** (แยกส่วน) | แต่ละฟังก์ชันทำงานเฉพาะทาง | สามารถทดสอบ (Test) และแก้ไขแยกส่วนได้ |

### 🧠 Challenge: Functional Thinking
ถ้าคุณต้องเขียนโค้ดเพื่อ "คำนวณภาษี" ให้กับสินค้า 100 ชิ้น การมีฟังก์ชันจะช่วยคุณได้อย่างไรเมื่อรัฐบาลประกาศ "เปลี่ยนอัตราภาษี" จาก 7% เป็น 10%?
::: details ✨ ดูเฉลย
คุณแค่แก้สูตรคำนวณ **ในฟังก์ชันที่เดียว** ครับ! `const tax = price * 0.07` → `const tax = price * 0.10` แค่นี้สินค้าทั้ง 100 ชิ้นจะอัปเดตตามทันที ไม่ต้องตามไปแก้ 100 จุดให้เหนื่อย
:::

---

## 2. Anatomy of a Function (กายวิภาคของฟังก์ชัน) 🔬

ตาม [MDN Web Docs](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Functions) ฟังก์ชันประกอบด้วยส่วนสำคัญดังนี้:

```javascript
//  ① keyword  ② name   ③ parameters
function greet   (name,   greeting) {
    // ④ Function Body (ตัวคำสั่ง)
    const message = `${greeting}, ${name}!`;

    // ⑤ Return Statement (ส่งค่ากลับ)
    return message;
}

// ⑥ Function Call + Arguments
const result = greet("Dolar", "สวัสดี");
console.log(result); // "สวัสดี, Dolar!"
```

### ผ่าตัดทีละส่วน:

| ส่วนประกอบ | คำอธิบาย | ตัวอย่าง |
|:-----------|:---------|:---------|
| ① `function` keyword | คำสั่งบอก JS ว่าเราจะสร้างฟังก์ชัน | `function` |
| ② Function Name | ชื่อที่ใช้เรียกฟังก์ชัน (ควรเป็น verb) | `greet`, `calculateTax` |
| ③ Parameters | ตัวแปร "placeholder" สำหรับรับข้อมูลเข้า | `(name, greeting)` |
| ④ Function Body | ชุดคำสั่งที่จะถูกรันเมื่อเรียกใช้ฟังก์ชัน | `{ ... }` |
| ⑤ `return` | ส่งค่ากลับออกมาจากฟังก์ชัน | `return message;` |
| ⑥ Arguments | ค่าจริงๆ ที่ส่งเข้าไปตอนเรียกฟังก์ชัน | `"Dolar"`, `"สวัสดี"` |

> **📌 กฎการตั้งชื่อตาม Convention:**
> ชื่อฟังก์ชันควรขึ้นต้นด้วย **กริยา (Verb)** เสมอ เพราะฟังก์ชัน "ทำ" อะไรบางอย่าง:
> - ✅ `getUser()`, `calculateTotal()`, `showMessage()`, `isValid()`
> - ❌ `data()`, `user()`, `total()` ← เหมือนชื่อ "ตัวแปร" มากกว่า

### 🧠 Challenge: Name the Function
จงตั้งชื่อฟังก์ชันที่ดีสำหรับงานเหล่านี้:
1. ฟังก์ชันที่ตรวจสอบรหัสผ่านว่าถูกต้องไหม
2. ฟังก์ชันที่แปลงอุณหภูมิจากเซลเซียสเป็นฟาเรนไฮต์
3. ฟังก์ชันที่ส่งอีเมล

::: details ✨ ดูเฉลย
1. `validatePassword()` หรือ `isPasswordValid()`
2. `convertToFahrenheit()` หรือ `celsiusToFahrenheit()`
3. `sendEmail()`
:::

---

## 3. Declaration vs Expression (สองแบบในการสร้างฟังก์ชัน) 🎭

ตาม [MDN](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Functions#defining_functions) JavaScript มี 2 วิธีหลักในการสร้างฟังก์ชัน:

### A. Function Declaration (แบบดั้งเดิม)

```javascript
// ✅ เรียกใช้ก่อนประกาศได้ (เพราะ Hoisting)
console.log(square(5)); // 25

function square(number) {
    return number * number;
}
```

### B. Function Expression (เก็บในตัวแปร)

```javascript
// ❌ ต้องประกาศก่อน แล้วค่อยเรียกใช้ (TDZ)
// console.log(square(5)); // ReferenceError!

const square = function(number) {
    return number * number;
};

console.log(square(5)); // 25

// Named Function Expression (ใส่ชื่อให้ Function Expression)
const factorial = function fac(n) {
    return n < 2 ? 1 : n * fac(n - 1); // เรียกตัวเองได้!
};
console.log(factorial(5)); // 120
```

### 📊 Comparison Table: Declaration vs Expression

| คุณสมบัติ | Declaration | Expression |
|:----------|:-----------|:-----------|
| **Syntax** | `function name() {}` | `const name = function() {}` |
| **Hoisting** | ✅ ถูกยกขึ้นทั้งตัว (ใช้ก่อนประกาศได้) | ❌ อยู่ใน TDZ (ต้องประกาศก่อน) |
| **ชื่อ** | ต้องมีชื่อเสมอ | ไม่มีก็ได้ (Anonymous) |
| **ใช้เป็น Callback** | ได้ แต่ไม่นิยม | ✅ เหมาะมาก |
| **เมื่อไหร่ใช้** | Logic หลักของโปรแกรม | ส่งเป็น Argument, เก็บในตัวแปร |

### 🚨 Deep Dive: Function Hoisting (MDN)

ตาม [MDN - Function Hoisting](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Functions#function_hoisting):
JS interpreter จะ **"ยก" (Hoist)** ทั้ง Function Declaration ขึ้นไปบนสุดของ Scope ก่อนเริ่มรัน:

```javascript
// โค้ดที่เราเขียน:
console.log(add(2, 3)); // 5 ✅ ทำงานได้!

function add(a, b) {
    return a + b;
}

// สิ่งที่ JS Engine เห็นจริงๆ (หลัง Hoist):
// function add(a, b) { return a + b; }  ← ถูกยกขึ้นมา
// console.log(add(2, 3)); // 5
```

**⚠️ แต่ Expression ไม่ถูก Hoist แบบพร้อมใช้!**

```javascript
console.log(subtract(5, 2)); // ❌ ReferenceError!
const subtract = function(a, b) { return a - b; };

// ❌ เหตุผล: ตัวแปร subtract ถูก Hoist แต่อยู่ใน TDZ
// (Temporal Dead Zone) จนกว่าจะถึงบรรทัดที่ประกาศ
```

### 🧠 Challenge: Hoisting Detective
โค้ดนี้จะทำงานได้หรือไม่? ทำไม?
```javascript
sayHello(); // (1)?

function sayHello() {
    console.log("Hello!");
}

sayBye(); // (2)?

const sayBye = function() {
    console.log("Bye!");
};
```

::: details ✨ ดูเฉลย
1. ✅ **ได้!** — `sayHello` เป็น Function Declaration → ถูก Hoist ทั้งตัว
2. ❌ **ReferenceError!** — `sayBye` เป็น Function Expression (const) → อยู่ใน TDZ

**กฎง่ายๆ:** Declaration = เรียกก่อนได้, Expression = ต้องประกาศก่อนใช้
:::

---

## 4. Arrow Functions (ฟังก์ชันลูกศร ES6) 💘

ตาม [MDN - Arrow Functions](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Functions/Arrow_functions) Arrow Function คือ **Syntax แบบย่อ** สำหรับเขียน Function Expression

### A. Syntax การเขียน (จากยาวไปสั้น)

```javascript
// 1. Traditional Function
const add1 = function(a, b) {
    return a + b;
};

// 2. Arrow Function (ปกติ)
const add2 = (a, b) => {
    return a + b;
};

// 3. Arrow Function (Implicit Return — บรรทัดเดียว)
const add3 = (a, b) => a + b;

// 4. Arrow Function (Parameter ตัวเดียว — ไม่ต้องมีวงเล็บ)
const double = n => n * 2;

// 5. Arrow Function (ไม่มี Parameter — ต้องมีวงเล็บว่าง)
const getRandom = () => Math.random();
```

### 📊 Arrow Syntax Cheatsheet

| สถานการณ์ | Syntax | ตัวอย่าง |
|:----------|:-------|:---------|
| **หลาย Parameters + หลายบรรทัด** | `(a, b) => { ... }` | `(a, b) => { const sum = a + b; return sum; }` |
| **หลาย Parameters + บรรทัดเดียว** | `(a, b) => expr` | `(a, b) => a + b` |
| **Parameter เดียว** | `x => expr` | `n => n * 2` |
| **ไม่มี Parameter** | `() => expr` | `() => Math.random()` |
| **Return Object** | `() => ({...})` | `() => ({ name: "Dolar" })` |

> **⚠️ Gotcha: Return Object ต้องครอบวงเล็บ!**
> ```javascript
> // ❌ Error — JS จะงงว่า {} คือ Code Block
> const getUser = () => { name: "Dolar" };
> console.log(getUser()); // undefined!
>
> // ✅ ครอบ () เพื่อบอกว่านี่คือ Object
> const getUser = () => ({ name: "Dolar" });
> console.log(getUser()); // { name: "Dolar" }
> ```

### B. ข้อแตกต่างสำคัญระหว่าง Arrow Function กับ Regular Function

ตาม MDN, Arrow Function **ไม่ใช่แค่ย่อ Syntax** มันมีพฤติกรรมที่ต่างออกไปหลายอย่าง:

| คุณสมบัติ | Regular Function | Arrow Function |
|:----------|:----------------|:---------------|
| **`this` binding** | มี `this` ของตัวเอง (Dynamic) | ❌ **ไม่มี** `this` ของตัวเอง (Lexical) |
| **`arguments` object** | ✅ มี | ❌ ไม่มี |
| **ใช้เป็น Method** | ✅ เหมาะ | ❌ **ไม่ควร** |
| **ใช้เป็น Constructor (`new`)** | ✅ ได้ | ❌ ไม่ได้ |
| **Hoisting** | ✅ (Declaration) | ❌ (เพราะเป็น Expression) |

```javascript
// ⚠️ ปัญหาที่พบบ่อย: Arrow Function กับ Object Method
const player = {
    name: "Dolar",

    // ❌ Arrow Function — this ไม่ชี้ไปที่ player!
    greetArrow: () => {
        console.log(`Hi, I'm ${this.name}`); // this = window/undefined
    },

    // ✅ Regular Function — this ชี้ไปที่ player
    greetRegular() {
        console.log(`Hi, I'm ${this.name}`); // this = player
    },
};

player.greetArrow();   // "Hi, I'm undefined" 😱
player.greetRegular(); // "Hi, I'm Dolar" ✅
```

> **กฎเหล็ก:**
> - ✅ ใช้ Arrow Function กับ: **Callbacks**, **Array Methods** (`.map`, `.filter`), **Short expressions**
> - ❌ ห้ามใช้ Arrow Function กับ: **Object Methods**, **Event Handlers ที่ต้องใช้ `this`**

### 🧠 Challenge: The Transformer 🤖
จงแปลงฟังก์ชันเหล่านี้ให้เป็น **Arrow Function** ที่สั้นที่สุด:

```javascript
// 1.
function double(n) { return n * 2; }

// 2.
function greet(name) { return "Hello, " + name; }

// 3.
function isEven(n) { return n % 2 === 0; }

// 4.
function getPI() { return 3.14159; }
```

::: details ✨ ดูเฉลย
```javascript
const double = n => n * 2;
const greet = name => "Hello, " + name;
const isEven = n => n % 2 === 0;
const getPI = () => 3.14159;
```
**เคล็ดลับ:**
- Parameter ตัวเดียว → ตัดวงเล็บ `()` ออกได้
- Body บรรทัดเดียว → ตัดปีกกา `{}` และ `return` ออกได้
- ไม่มี Parameter → ต้องใส่ `()` ว่างๆ
:::

---

## 5. Functions as First-Class Citizens (ฟังก์ชันเป็นสิ่งมีชีวิต!) 🌟

ใน JavaScript ฟังก์ชันคือ **"First-Class Object"** (ตาม [MDN](https://developer.mozilla.org/en-US/docs/Glossary/First-class_Function)) หมายความว่าเราสามารถ:

### A. เก็บฟังก์ชันในตัวแปร
```javascript
const greet = function(name) { return `Hi, ${name}!`; };
console.log(greet("Dolar")); // "Hi, Dolar!"
```

### B. ส่งฟังก์ชันเป็น Argument (Callback)
```javascript
function doMath(a, b, operation) {
    return operation(a, b); // เรียกใช้ฟังก์ชันที่ถูกส่งเข้ามา
}

const result1 = doMath(10, 5, (a, b) => a + b); // 15
const result2 = doMath(10, 5, (a, b) => a * b); // 50
console.log(result1, result2);
```

### C. Return ฟังก์ชันออกจากฟังก์ชัน (Higher-Order)
```javascript
function createMultiplier(factor) {
    return (number) => number * factor; // Return ฟังก์ชันใหม่!
}

const double = createMultiplier(2);
const triple = createMultiplier(3);

console.log(double(5));  // 10
console.log(triple(5));  // 15
```

### D. เก็บฟังก์ชันใน Array หรือ Object
```javascript
const operations = [
    (a, b) => a + b,  // index 0: บวก
    (a, b) => a - b,  // index 1: ลบ
    (a, b) => a * b,  // index 2: คูณ
];

console.log(operations[0](10, 5)); // 15
console.log(operations[2](10, 5)); // 50
```

### 🧠 Challenge: Higher-Order Function
จงสร้างฟังก์ชัน `createGreeter(greeting)` ที่:
- รับ `greeting` เช่น `"สวัสดี"`
- Return ฟังก์ชันใหม่ที่รับ `name` แล้ว return `"สวัสดี, Dolar!"`

```javascript
const hello = createGreeter("Hello");
const sawaddee = createGreeter("สวัสดี");

console.log(hello("Dolar"));    // "Hello, Dolar!"
console.log(sawaddee("Somchai")); // "สวัสดี, Somchai!"
```

::: details ✨ ดูเฉลย
```javascript
function createGreeter(greeting) {
    return (name) => `${greeting}, ${name}!`;
}

const hello = createGreeter("Hello");
const sawaddee = createGreeter("สวัสดี");

console.log(hello("Dolar"));      // "Hello, Dolar!"
console.log(sawaddee("Somchai")); // "สวัสดี, Somchai!"
```
**หลักการ:** ฟังก์ชัน `createGreeter` เป็น **Higher-Order Function** เพราะมัน return ฟังก์ชันอีกตัวหนึ่งออกมา
:::

---

## 6. Recursion (ฟังก์ชันเรียกตัวเอง) 🔄

ตาม [MDN](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Functions#recursion) ฟังก์ชันสามารถ **เรียกตัวเอง** ได้ เรียกว่า **Recursive Function** มันคล้ายกับ Loop แต่ใช้วิธีคิดที่ต่างออกไป:

```javascript
// Factorial: 5! = 5 × 4 × 3 × 2 × 1 = 120
function factorial(n) {
    // Base Case (เงื่อนไขหยุด) — สำคัญมาก!
    if (n <= 1) return 1;

    // Recursive Case (เรียกตัวเอง)
    return n * factorial(n - 1);
}

console.log(factorial(5)); // 120
// ลำดับการทำงาน:
// factorial(5) → 5 * factorial(4)
//                    4 * factorial(3)
//                        3 * factorial(2)
//                            2 * factorial(1)
//                                return 1    ← Base Case!
//                            return 2 * 1 = 2
//                        return 3 * 2 = 6
//                    return 4 * 6 = 24
// return 5 * 24 = 120
```

> **⚠️ คำเตือน:** Recursive Function **ต้องมี Base Case** (เงื่อนไขหยุด) เสมอ! ไม่งั้นจะเกิด **Stack Overflow** (หน่วยความจำล้น) เหมือน Infinite Loop

### 🧠 Challenge: Countdown Recursion
เขียนฟังก์ชัน `countdown(n)` ที่นับถอยหลังจาก `n` ถึง 0 โดยใช้ Recursion (ห้ามใช้ Loop!)

```javascript
countdown(5);
// Output: 5, 4, 3, 2, 1, 0, "🚀 Launch!"
```

::: details ✨ ดูเฉลย
```javascript
function countdown(n) {
    if (n < 0) {
        console.log("🚀 Launch!");
        return; // Base Case
    }
    console.log(n);
    countdown(n - 1); // Recursive Call
}

countdown(5);
// 5
// 4
// 3
// 2
// 1
// 0
// 🚀 Launch!
```
:::

---

## 7. IIFE — Immediately Invoked Function Expression 🏃‍♂️

ตาม [MDN](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Functions#immediately_invoked_function_expressions_iife) IIFE คือฟังก์ชันที่ **สร้างแล้วรันทันทีเลย** โดยไม่ต้องเรียกชื่อ:

```javascript
// IIFE Pattern: ครอบวงเล็บแล้วเรียก ()
(function() {
    const secret = "Hidden from outside!";
    console.log(secret); // "Hidden from outside!"
})();

// console.log(secret); // ❌ ReferenceError! (ถูกซ่อนไว้ข้างใน)

// IIFE แบบ Arrow Function
(() => {
    console.log("Arrow IIFE!");
})();
```

> **Use Case:** ใช้ IIFE เมื่อต้องการสร้าง **Private Scope** ป้องกันตัวแปรรั่วไหลออกไปซ้อนกับโค้ดอื่น  
> (ในยุค Modern JS เรามี `let`/`const` + Block Scope แล้ว IIFE จึงไม่ค่อยจำเป็นแล้ว แต่ยังเจอในโค้ดเก่าๆ)

---

## 8. Pure Functions (ฟังก์ชันบริสุทธิ์) 🧊

**Pure Function** คือฟังก์ชันที่:
1. **ผลลัพธ์ขึ้นอยู่กับ Input เท่านั้น** (ไม่อิงจากสิ่งภายนอก)
2. **ไม่มี Side Effect** (ไม่เปลี่ยนแปลงอะไรภายนอกฟังก์ชัน)

```javascript
// ✅ Pure: ผลลัพธ์ขึ้นกับ Input เท่านั้น ไม่แก้ไขอะไรข้างนอก
function add(a, b) {
    return a + b;
}
console.log(add(2, 3)); // 5 (เรียกกี่ทีก็ได้ 5 เสมอ)

// ❌ Impure: อ่านค่า/แก้ไขตัวแปรภายนอก
let total = 0;
function addToTotal(amount) {
    total += amount; // Side Effect! แก้ไข total ภายนอก
    return total;
}
console.log(addToTotal(10)); // 10
console.log(addToTotal(10)); // 20 (ผลลัพธ์ต่างกัน!)
```

> **Best Practice:** พยายามเขียน **Pure Functions** ให้มากที่สุด เพราะ Debug ง่าย ทดสอบง่าย และ Predict ผลลัพธ์ได้แน่นอน

---

## 9. Final Challenge: The Function Lab 🧪

### 🎯 Challenge 1: Predict the Output
```javascript
const multiply = function mul(a, b) {
    return a * b;
};

console.log(multiply(3, 4));  // (1)?
console.log(typeof multiply); // (2)?
// console.log(mul(3, 4));    // (3)?
```

::: details ✨ ดูเฉลย
1. **12** — ทำงานปกติ
2. **"function"** — ใน JS ฟังก์ชันก็เป็น "ชนิดข้อมูล" ชนิดหนึ่ง!
3. **ReferenceError** — ชื่อ `mul` เข้าถึงได้แค่จากข้างในฟังก์ชันเท่านั้น (Named Expression)
:::

### 🎯 Challenge 2: Refactor to Arrow
แปลงโค้ดนี้ทั้งหมดเป็น Arrow Function ที่สั้นที่สุด:
```javascript
function isPositive(n) { return n > 0; }
function square(n) { return n * n; }
function getFullName(first, last) { return first + " " + last; }
```

::: details ✨ ดูเฉลย
```javascript
const isPositive = n => n > 0;
const square = n => n * n;
const getFullName = (first, last) => `${first} ${last}`;
```
:::

---

> **📖 คำศัพท์เทคนิค (Glossary):**
> *   **Function Declaration:** วิธีสร้างฟังก์ชันด้วย keyword `function` ที่ถูก Hoist ทั้งตัว
> *   **Function Expression:** วิธีสร้างฟังก์ชันโดยเก็บใส่ตัวแปร ไม่ถูก Hoist แบบพร้อมใช้
> *   **Arrow Function:** Syntax แบบย่อสำหรับ Function Expression ที่ไม่มี `this` ของตัวเอง
> *   **First-Class Function:** คุณสมบัติที่ฟังก์ชันถูกปฏิบัติเหมือน "ค่า" — เก็บในตัวแปร, ส่งเป็น Argument ได้
> *   **Higher-Order Function:** ฟังก์ชันที่รับฟังก์ชันเป็น Argument หรือ Return ฟังก์ชัน
> *   **Callback:** ฟังก์ชันที่ถูกส่งเป็น Argument ให้ฟังก์ชันอื่นเพื่อเรียกใช้ทีหลัง
> *   **Recursion:** เทคนิคที่ฟังก์ชันเรียกตัวเอง ต้องมี Base Case ป้องกัน Infinite Loop
> *   **IIFE:** (Immediately Invoked Function Expression) ฟังก์ชันที่สร้างแล้วรันทันทีเลย
> *   **Pure Function:** ฟังก์ชันที่ผลลัพธ์ขึ้นกับ Input เท่านั้น ไม่มี Side Effect
> *   **Side Effect:** การเปลี่ยนแปลงค่าภายนอกฟังก์ชัน (เช่น แก้ตัวแปร Global, เขียน Console)
> *   **Encapsulation:** การห่อหุ้มคำสั่งไว้ในที่เดียวเพื่อความเป็นระเบียบ
> *   **Hoisting:** การที่ JS ยกคำประกาศฟังก์ชันไปไว้บนสุด ทำให้เรียกใช้ได้ก่อนบรรทัดที่เขียน

---
👉 **[ไปต่อ: 04-2 - Data Flow & Parameters](/04-02-data-flow)**
