# Project 4: The Modular Calculator (เครื่องคิดเลขแยกส่วน) 🧮

> **"The way to get started is to quit talking and begin doing."**
> — *Walt Disney*

โปรเจกต์นี้เราจะนำความรู้เรื่อง **Functions** และ **Switch Statement** มาประกอบร่างกันเพื่อสร้างเครื่องคิดเลขที่โค้ดสะอาดและดูแลรักษาง่ายครับ!

> **💡 Analogy (เปรียบเทียบ):** 
> การเขียนโปรแกรมแบบแยกส่วน เหมือนการเป็น **"วิศวกรโรงงาน"** ครับ แทนที่จะสร้างเครื่องจักรยักษ์ตัวเดียวที่ทำได้ทุกอย่าง เราเลือกที่จะสร้าง "หุ่นยนต์แขนกล" ตัวเล็กๆ หลายตัว (Functions) ที่ทำหน้าที่เฉพาะทาง (บวก, ลบ, คูณ, หาร) แล้วใช้ "ศูนย์ควบคุม" (Controller) เป็นตัวสั่งการว่าแขนกลตัวไหนต้องทำงานเมื่อไหร่!

## 1. The Concept (แนวคิด) 💡
แทนที่จะเขียน Logic ทุกอย่างยัดลงไปในที่เดียว เราจะแยกมันออกเป็นส่วนย่อยๆ ตามหน้าที่:
1.  **Helper Functions:** ฟังก์ชันเล็กๆ ที่ทำหน้าที่เดียว (บวก, ลบ, คูณ, หาร)
2.  **Controller Function:** ฟังก์ชันหลักที่คอยตัดสินใจว่าจะเรียกใช้ Helper ตัวไหน

## 2. Step 1: Create Helper Functions 🛠️
สร้างฟังก์ชันสำหรับรับค่า 2 ตัวและส่งผลลัพธ์กลับมา

```javascript
const add = (a, b) => a + b;
const subtract = (a, b) => a - b;
const multiply = (a, b) => a * b;
const divide = (a, b) => a / b;
```
> **สังเกต:** เราใช้ Arrow Functions แบบย่อ (Implicit Return) เพราะ Logic มันสั้นมาก

## 3. Step 2: The Main Controller 🎮
สร้างฟังก์ชัน `calculate` ที่รับค่า `a`, `b`, และ `operation`

```javascript
function calculate(a, b, operation) {
    switch (operation) {
        case "add":
            return add(a, b);
        case "subtract":
            return subtract(a, b);
        case "multiply":
            return multiply(a, b);
        case "divide":
            return divide(a, b);
        default:
            return "Error: Invalid operation";
    }
}
```

## 4. Step 3: Test It Out! 🧪
ลองเรียกใช้งานดูครับ

```javascript
console.log(calculate(10, 5, "add"));      // 15
console.log(calculate(10, 5, "divide"));   // 2
console.log(calculate(10, 0, "divide"));   // Infinity (ใน JS หาร 0 ได้ Infinity)
console.log(calculate(10, 5, "power"));    // Error: Invalid operation
```

## 5. Challenge: Level Up! 🚀
โค้ดข้างบนยัง "ดิบ" อยู่ครับ มาช่วยกันเติมฟีเจอร์หน่อย:

1.  **Handle Division by Zero:** ถ้าบวกหารด้วย 0 ให้ Return ว่า "Cannot divide by zero!"
2.  **Power Operation:** เพิ่มฟังก์ชัน `power(a, b)` (ยกกำลัง) และรองรับใน Switch

::: details ✨ ดูเฉลย (Solution)
```javascript
// 1. Update divide helper
const divide = (a, b) => {
    if (b === 0) return "Cannot divide by zero!";
    return a / b;
};

// 2. Add power helper
const power = (a, b) => a ** b;

// 3. Update switch
function calculate(a, b, operation) {
    switch (operation) {
        // ... (existing cases)
        case "divide": 
            return divide(a, b); // เรียกตัวใหม่ที่มี check 0
        case "power":
            return power(a, b);
        // ...
    }
}
```
:::

---

> **🏆 ความสำเร็จในบทนี้ (Success Criteria):**
> 1. [ ] สามารถสร้าง **Pure Functions** ที่รับค่าและส่งผลลัพธ์กลับมาได้ถูกต้อง
> 2. [ ] เข้าใจการใช้ `switch` เพื่อจัดการทางเลือกที่หลากหลาย
> 3. [ ] รู้วิธีการแยก Logic ใหญ่ๆ ออกเป็นฟังก์ชันย่อย (Modularization)
> 4. [ ] สามารถดักจับ Error เบื้องต้นได้ (เช่น การหารด้วยศูนย์)

---
👉 **[ไปต่อ: Module 5 - 05-1 Arrays (การจัดเก็บข้อมูล)](/05-01-arrays)**
**ยินดีด้วย!** คุณเขียนโปรแกรมแบบแยกส่วน (Modular) เป็นแล้ว 👏
บทต่อไปเราจะมาดูเรื่อง **Arrays & Objects** (โครงสร้างข้อมูล) กันต่อครับ!
