# 00-1: Environment Setup (การเตรียมเครื่องมือ) 🛠️

> **"First, solve the problem. Then, write the code."**
> — *John Johnson*

ก่อนที่จะเริ่มสร้างสรรค์เวทมนตร์ด้วย JavaScript เราต้อง **"เตรียมห้องปรุงยา"** ให้พร้อมก่อนครับ ในคอร์สนี้เราจะใช้เครื่องมือมาตรฐานระดับโลกที่ Developer ทั่วโลกใช้กันจริง!

> **💡 Analogy (เปรียบเทียบ):**
> การเขียนโค้ดก็เหมือน **"การทำอาหาร"** 🍳:
> - **Computer** = ห้องครัว
> - **VS Code** = เขียงและมีด (ที่ช่วยหั่นและจัดเตรียมวัตถุดิบ)
> - **Node.js** = เตาแก๊ส (ที่ทำให้วัตถุดิบสุกและกลายเป็นอาหารจริงๆ)
> - **JavaScript** = สูตรอาหาร (Recipe)
>
> ถ้าขาดอย่างใดอย่างหนึ่ง เราก็ทำอาหาร (Run Code) ไม่ได้ครับ!

## 1. Install Node.js (เตาแก๊สพลังสูง) 🔥

Node.js คือสิ่งที่ทำให้คอมพิวเตอร์ของเรา **"เข้าใจ"** ภาษา JavaScript (ปกติ JS ทำงานได้แค่ใน Browser แต่ Node.js เอามันมารันในเครื่องเราได้)

1.  ไปที่เว็บไซต์ [nodejs.org](https://nodejs.org/)
2.  ดาวน์โหลดเวอร์ชัน **LTS (Long Term Support)**
    *   *LTS = เวอร์ชันเสถียร เหมาะสำหรับใช้งานจริง (แนะนำ!)*
    *   *Current = เวอร์ชันล่าสุด มีฟีเจอร์ใหม่ แต่อาจมีบัค*
3.  ติดตั้งตามปกติ (กด Next รัวๆ > Install > Finish)

### ✅ ตรวจสอบการติดตั้ง (Verify)
เปิดโปรแกรม **Terminal** (Mac/Linux) หรือ **Command Prompt (cmd) / PowerShell** (Windows) แล้วพิมพ์:

```bash
node -v
```

ถ้าสำเร็จ จะขึ้นเลขเวอร์ชัน เช่น `v20.10.0` (ขอแค่มีตัวเลขขึ้น ถือว่าผ่าน!)



## 2. Install VS Code (มีดเชฟระดับเทพ) 🔪

VS Code (Visual Studio Code) คือ Code Editor ที่ดีที่สุดในโลกตอนนี้ ฟรี เร็ว และฉลาดมาก!

1.  ไปที่ [code.visualstudio.com](https://code.visualstudio.com/)
2.  ดาวน์โหลดและติดตั้งให้เรียบร้อย (รองรับทั้ง Windows, Mac, Linux)

### 🧩 Recommended Extensions (ส่วนเสริมที่ต้องมี)
เพื่อให้ชีวิตดีขึ้น 10 เท่า ให้ลง Extension พวกนี้ครับ:

1.  **Prettier - Code formatter**
    *   *หน้าที่:* จัดระเบียบโค้ดให้อัตโนมัติ (ไม่ต้องมาเคาะ Space เอง)
    *   *วิธีใช้:* ตั้งค่า `Editor: Format On Save` เป็น `true` → กด Save ปุ๊บ โค้ดสวยปั๊บ!
2.  **ESLint**
    *   *หน้าที่:* คุณครูฝ่ายปกครอง คอยเตือนเวลาเราเขียนผิดหลักไวยากรณ์ (Syntax Error)
3.  **Code Runner** (Optional)
    *   *หน้าที่:* ปุ่ม Play ▶️ สำหรับรันโค้ดง่ายๆ (ไม่ต้องพิมพ์คำสั่งใน Terminal)



## 3. สร้างโปรเจกต์แรก (First Project) 🚀

มาลองเทสระบบกันหน่อยว่าห้องครัวเราพร้อมไหม:

1.  สร้างโฟลเดอร์ใหม่ชื่อ `learn-js` บนหน้า Desktop
2.  เปิด **VS Code**
3.  ไปที่ **File > Open Folder...** แล้วเลือกโฟลเดอร์ `learn-js`
4.  สร้างไฟล์ใหม่ (New File) ชื่อ `hello.js`
5.  พิมพ์โค้ดลงไป:

```javascript
console.log("Hello, JavaScript World!");
```

6.  เปิด Terminal ใน VS Code (**Terminal > New Terminal**)
7.  สั่งรันโค้ดด้วย Node.js:

```bash
node hello.js
```

🎉 **ผลลัพธ์:** คุณควรเห็นคำว่า `Hello, JavaScript World!` บนหน้าจอ



## 4. (Optional) การรันคอร์สนี้บนเครื่องตัวเอง 📚

ถ้าคุณโหลด Source Code ของคอร์สนี้มาเพื่อแก้ไขหรือดูตัวอย่าง:

### 🛠️ Editing Mode (โหมดแก้ไข)
```bash
npm run docs:dev
```
*   ใช้สำหรับ **"เรียนและแก้ไข"**
*   มีระบบ **Hot Reload** (แก้โค้ดปุ๊บ เว็บรีเฟรชปั๊บ)

### 🏗️ Production Preview (โหมดจำลองของจริง)
```bash
npm run docs:build
npm run docs:preview
```
*   ใช้ดูหน้าเว็บบับสมบูรณ์ (เหมือนที่อยู่บน Server)
*   *Note:* ต้อง Build ก่อนเสมอ ไม่งั้นจะหาไฟล์ไม่เจอ



## 5. Checklist: Ready for Takeoff? ✅

เช็คความพร้อมครั้งสุดท้าย ก่อนไปลุยของจริง!

- [ ] 🟢 **Node.js**: ติดตั้งเรียบร้อย (เช็ค `node -v` ผ่าน)
- [ ] 🔵 **VS Code**: ติดตั้งเรียบร้อย
- [ ] 🟣 **Prettier**: ติดตั้งและตั้งค่า Format On Save แล้ว
- [ ] 🟡 **First File**: รัน `hello.js` ผ่านแล้ว

ถ้าครบทุกข้อ... ยินดีด้วยครับ! คุณพร้อมที่จะเป็น JavaScript Developer แล้ว! 🚀



> **📖 คำศัพท์เทคนิค (Glossary):**
> *   **Runtime Environment:** สภาพแวดล้อมที่ทำให้โปรแกรมทำงานได้ (Node.js เป็น Runtime ของ JS)
> *   **Code Editor:** โปรแกรมสำหรับเขียนโค้ด (เหมือน Word แต่สำหรับโปรแกรมเมอร์)
> *   **Terminal / Command Line:** หน้าจอดำๆ ที่ใช้พิมพ์คำสั่งสั่งงานคอมพิวเตอร์
> *   **Extension:** ส่วนเสริมที่ช่วยเพิ่มความสามารถให้โปรแกรม (เช่น เพิ่ม Helper ใน VS Code)
> *   **LTS:** Long Term Support (เวอร์ชันเสถียรที่จะได้รับการดูแลยาวนาน)


👉 **[ไปต่อ: 01-1 - History of JavaScript (กำเนิด JS)](/javascript/01-01-history)**
