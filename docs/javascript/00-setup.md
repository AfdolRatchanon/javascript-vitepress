# 00 - Setup Environment (การเตรียมเครื่องมือ)

ก่อนที่เราจะเริ่มเขียนโค้ด (Writing Code) เราต้องเตรียมเครื่องมือให้พร้อมก่อนครับ ในคอร์สนี้เราจะใช้ **Node.js** เป็นหลัก เพื่อให้โฟกัสที่ Logic ของภาษา JavaScript ได้เต็มที่

## 1. Install Node.js
Node.js คือ Runtime Environment ที่ทำให้เราสามารถรัน JavaScript นอก Browser ได้ (เช่น บนเครื่องคอมพิวเตอร์ของเราเอง)

1.  ไปที่เว็บไซต์ [nodejs.org](https://nodejs.org/)
2.  ดาวน์โหลดเวอร์ชัน **LTS (Long Term Support)** (แนะนำสำหรับผู้เริ่มต้น เพราะเสถียรที่สุด)
3.  ติดตั้งตามปกติ (Next > Next > Install > Finish)

### ตรวจสอบการติดตั้ง (Verify Installation)
เปิดโปรแกรม **Terminal** (Mac/Linux) หรือ **Command Prompt (cmd)** / **PowerShell** (Windows) แล้วพิมพ์คำสั่ง:

```bash
node -v
```

ถ้าติดตั้งสำเร็จ จะขึ้นเลขเวอร์ชันครับ เช่น `v20.10.0` (ตัวเลขอาจต่างกันไม่เป็นไร ขอแค่ขึ้นก็พอ)

## 2. Install Visual Studio Code (VS Code)
VS Code คือ Code Editor ที่นิยมที่สุดในโลก ปรับแต่งได้เยอะ และฟรี!

1.  ไปที่ [code.visualstudio.com](https://code.visualstudio.com/)
2.  ดาวน์โหลดและติดตั้งให้เรียบร้อย

### แนะนำ Extensions (ส่วนเสริม)
เพื่อให้เขียน JS สนุกขึ้น แนะนำให้ติดตั้ง Extension เหล่านี้:
- **Prettier - Code formatter**: ช่วยจัดรูปแบบโค้ดให้อัตโนมัติ สวยงามเสมอ
- **ESLint**: ช่วยเตือนเมื่อเราเขียนโค้ดผิดหลักไวยากรณ์

## 3. สร้างโปรเจกต์แรก (Create First Project)
1.  สร้างโฟลเดอร์ใหม่ชื่อ `learn-js` ที่ไหนก็ได้ในเครื่อง
2.  เปิดโฟลเดอร์นั้นด้วย VS Code (คลิกขวา > Open with Code)
3.  สร้างไฟล์ใหม่ชื่อ `hello.js`

## 4. (Optional) การเปิดดูบทเรียนนี้ (Running this Course)
ถ้าคุณต้องการเปิดเว็บรวมบทเรียนนี้ขึ้นมาดูบนเครื่องตัวเอง ให้ทำดังนี้:

**สำหรับแก้ไข/เขียนบทเรียนเพิ่ม (Editing Mode):**
```bash
npm run docs:dev
```
*   ใช้คำสั่งนี้เป็นหลัก เวลาเราจะแก้ไขหรืออ่านเนื้อหา
*   มันจะ Hot Reload (แก้ปุ๊บ เปลี่ยนปั๊บ) ไม่ต้องรอนาน

**สำหรับดูตัวอย่างเว็บจริง (Preview Production Build):**
```bash
npm run docs:build
npm run docs:preview
```
*   ต้องรัน `docs:build` ก่อนเสมอ เพื่อสร้างไฟล์เว็บจริง (โฟลเดอร์ `.vitepress/dist`)
*   ถ้าไม่ build ก่อน จะเจอ Error `ENOENT: no such file or directory`

---

## 5. Checklist: Ready for takeoff? (รายการตรวจสอบ) ✅
ก่อนไปต่อ เช็คให้ชัวร์ว่าพร้อมแล้ว:
- [ ] ติดตั้ง **Node.js** แล้ว (พิมพ์ `node -v` ใน Terminal แล้วขึ้นเลข)
- [ ] ติดตั้ง **VS Code** แล้ว
- [ ] ติดตั้ง Extension **Prettier** แล้ว
- [ ] สร้างโฟลเดอร์ `learn-js` และไฟล์ `hello.js` แล้ว

พร้อมแล้วไปบทต่อไปกันเลย! 👉 [01 - Introduction to JavaScript](/01-01-history)
