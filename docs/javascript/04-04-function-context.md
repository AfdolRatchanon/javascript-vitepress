# 04-04 Function Context (`this`) 🎭

> **"In JavaScript, context is everything. `this` doesn't refer to the function itself, but to *how* the function was called."**
> — *Kyle Simpson (Author of You Don't Know JS)*

เรื่อง `this` ใน JavaScript คือหนึ่งใน "บอสใหญ่" ที่ทำให้ Developer (แม้แต่ระดับ Senior) ปวดหัวมากที่สุด! เพราะคำว่า `this` ไม่ได้ยึดติดกับตัวฟังก์ชัน แต่มัน **เปลี่ยนไปตามวิธีการเรียกใช้ (Call Site)** ครับ

> **💡 Analogy (เปรียบเทียบ):**
> ลองจินตนาการว่า `this` คือคำว่า **"แม่"** ครับ:
> - ถ้าผมพูดว่า **"แม่"** → หมายถึง **แม่ของผม**
> - ถ้าคุณพูดว่า **"แม่"** → หมายถึง **แม่ของคุณ**
> - ถ้าลูกพี่ลูกน้องพูดว่า **"แม่"** ในงานรวมญาติ → อาจจะงงว่าหมายถึงป้าคนไหน?
>
> คำว่า "แม่" (`this`) คือคำเดิมเขียนเหมือนเดิม แต่ **"ใครเป็นคนพูด" (Context)** จะเป็นตัวกำหนดความหมายที่แท้จริงครับ!

---

## 📚 MDN Reference
- [this](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/this)
- [Function.prototype.bind()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Function/bind)

---

## 1. Implicit Binding (เรียกแบบปกติ) 👈

กฎข้อแรกและสำคัญที่สุด: **"ดูที่จุด (dot) ข้างหน้า"**

ถ้าฟังก์ชันถูกเรียกในรูปแบบ `obj.method()` → `this` คือ **Object หน้าจุด (`obj`)**

```javascript
const user = {
    name: "Alice",
    sayHi() {
        // มี 'user' เป็นคนเรียก (อยู่หน้าจุด)
        console.log(`Hi, I am ${this.name}`);
    }
};

user.sayHi(); // "Hi, I am Alice"
```

### ⚠️ กับดัก: Losing Context (Context หาย)

ถ้าเราดึงฟังก์ชันออกมาใส่ตัวแปร แล้วเรียกตัวแปรนั้นเฉยๆ (ไม่มีจุดนำหน้า) → `this` จะเปลี่ยนไป!

```javascript
const say = user.sayHi; // ก๊อปปี้แค่ตัวฟังก์ชันออกมา

// ❌ เรียกเฉยๆ ไม่มี 'user.' นำหน้า
say(); 
// ผลลัพธ์: "Hi, I am undefined" 
// (ใน Strikt Mode จะ Error, ในปกติจะเป็น Window/Global)
```

**คำอธิบาย:** เหมือนเราจดเบอร์โทรศัพท์ร้าน Pizza ไว้ในกระดาษเปล่า พอเพื่อนมาเจอแล้วโทรออก เขาไม่รู้ว่าเป็นเบอร์ร้าน Pizza (Context หายไปแล้ว)

---

## 2. Explicit Binding (บังคับกำหนดเอง) 🛠️

ถ้าเราอยาก "ยัดเยียด" ว่า `this` ต้องเป็นใคร เราใช้ 3 เมธอดเทพ: `.call()`, `.apply()`, และ `.bind()`

### 2.1 `.call()` และ `.apply()` (ใช้เดี๋ยวนั้น)
สั่งรันฟังก์ชันทันที โดยระบุ `this` เป็นพารามิเตอร์ตัวแรก

```javascript
const chef = { name: "Gordon" };
const menu = {
    cook(dish, price) {
        console.log(`${this.name} cooking ${dish} for $${price}`);
    }
};

// ยืมฟังก์ชัน cook ของ menu มาใช้กับ chef
menu.cook.call(chef, "Beef Wellington", 100); 
// "Gordon cooking Beef Wellington for $100"

// apply เหมือน call แต่รับ arguments เป็น Array
menu.cook.apply(chef, ["Pasta", 50]); 
```

### 2.2 `.bind()` (ใช้วันหลัง)
ไม่ได้รันทันที แต่จะ **"สร้างฟังก์ชันใหม่"** ที่ถูกล็อกคอ `this` ไว้ถาวร (นิยมใช้มากใน React ยุคเก่า)

```javascript
const cookForGordon = menu.cook.bind(chef); // สร้างฟังก์ชันใหม่ ล็อก this เป็น chef

// ไปเรียกใช้งานที่อื่น หรือส่งเป็น Callback ก็ไม่หลุด
setTimeout(() => cookForGordon("Salad", 20), 1000);
// 1 วิผ่านไป: "Gordon cooking Salad for $20"
```

---

## 3. Arrow Functions (`this` แบบทะลุกำแพง) 💘

Arrow Function (`() => {}`) **ไม่มี `this` เป็นของตัวเอง!**
มันจะใช้ `this` ของ **Scope ภายนอก** ที่ห่อหุ้มมันอยู่ (Lexical Scoping) ทันที เหมือนมันล่องหนทะลุออกไปหาตัวแม่

### ตัวอย่าง: ปัญหา Callback ใน Object

```javascript
const team = {
    members: ["Jane", "Bill"],
    teamName: "Avengers",
    
    // ❌ แบบปกติ: this ใน forEach จะหลุด (ชี้ไปที่ Window)
    showMembersError() {
        this.members.forEach(function(member) {
            console.log(`${member} is in ${this.teamName}`); // Error or undefined
        });
    },

    // ✅ แบบ Arrow: this ทะลุออกไปหา showMembersCorrect ซึ่งคือ team
    showMembersCorrect() {
        this.members.forEach((member) => {
            console.log(`${member} is in ${this.teamName}`); 
        });
    }
};

team.showMembersCorrect();
// "Jane is in Avengers"
// "Bill is in Avengers"
```

---

## 4. `this` in Class vs Function 🏛️

### 📊 Comparison Table: Arrow vs Regular Function

| Feature | Regular Function `function() {}` | Arrow Function `() => {}` |
|:--------|:---------------------------------|:--------------------------|
| **`this`** | Dynamic (ขึ้นอยู่กับว่าใครเรียก) | Lexical (ยึดตาม Scope ที่เขียน) |
| **`arguments`** | มีตัวแปร `arguments` ให้ใช้ | ไม่มี (ต้องใช้ `...args` แทน) |
| **Constructor** | ใช้ `new` ได้ | ใช้ `new` **ไม่ได้** (Crash ทันที) |
| **เหมาะสำหรับ** | Object Methods, Event Handlers | Callbacks, Functional Logic |

### Real-World Use Case: Event Listeners 🖱️

ในงาน DOM หรือ React, การใช้ `this` ใน Event Listener เป็นจุดตายของมือใหม่:

```javascript
class Button {
    constructor() {
        this.count = 0;
        // ต้อง Bind เพื่อกัน Conext หายเมื่อถูกปุ่มกดเรียก
        this.handleClick = this.handleClick.bind(this);
    }

    handleClick() {
        this.count++;
        console.log(this.count);
    }
}

// แต่ถ้าเขียนแบบ Modern JS (Class Fields + Arrow) ไม่ต้อง Bind!
class ModernButton {
    count = 0;
    
    // Arrow Function ล็อก this อัตโนมัติไปที่ Instance ของ Class
    handleClick = () => {
        this.count++;
        console.log(this.count);
    }
}
```

---

## 5. Challenges 🏆

### 🎯 Challenge 1: Who is `this`?
**โจทย์:** ผลลัพธ์ของโค้ดด้านล่างคืออะไร?

```javascript
const robot = {
    name: "RoboCop",
    greet: function() {
        console.log(`I am ${this.name}`);
    }
};

const speak = robot.greet;
speak(); 
```
::: details ✨ ดูเฉลย
**ตอบ:** `I am undefined` (หรือ Error ใน Strict Mode)
**เหตุผล:** เพราะเราเรียก `speak()` แบบ function call ปกติ ไม่ได้เรียกผ่าน `robot.greet()` ทำให้ `this` หลุดออกจาก `robot` ไปเป็น Global/Undefined
:::

### 🎯 Challenge 2: Saving the Context
**โจทย์:** แก้โค้ดใน Challenge 1 ให้ `speak()` สามารถทำงานได้ถูกต้องและพิมพ์ "I am RoboCop" (มีอย่างน้อย 2 วิธี)
::: details ✨ ดูเฉลย
```javascript
// วิธี 1: .bind()
const speak = robot.greet.bind(robot);
speak();

// วิธี 2: .call()
speak.call(robot);

// วิธี 3: Wrapper Function
const speak = () => robot.greet();
```
:::

### 🎯 Challenge 3: Arrow Trap
**โจทย์:** ทำไมโค้ดนี้ถึงไม่ทำงานตามที่คิด (พิมพ์ undefined)? และจะแก้ยังไง?

```javascript
const cat = {
    lives: 9,
    jump: () => {
        this.lives--;
        console.log(this.lives);
    }
};
cat.jump();
```
::: details ✨ ดูเฉลย
**สาเหตุ:** Arrow Function **ไม่มี `this` ของตัวเอง** มันจะไปเอา `this` ของ Scope ข้างนอก ซึ่งในที่นี้คือ Global Scope (Window) ซึ่งไม่มีตัวแปร `lives`
**วิธีแก้:** เปลี่ยน `jump` ให้เป็น **Regular Function**
```javascript
jump: function() { ... } 
// หรือ Shorthand
jump() { ... }
```
:::

### 🎯 Challenge 4: Borrowing Method
**โจทย์:** มี Object `calculator` ที่มีฟังก์ชัน `sum` จงใช้ `.apply()` เพื่อเอาฟังก์ชันนั้นมาบวกเลขจาก Array `numbers`

```javascript
const calculator = {
    sum(a, b) { return a + b; }
};
const numbers = [5, 10];

// เขียนโค้ดตรงนี้ (ห้ามแก้ข้างบน)
```
::: details ✨ ดูเฉลย
```javascript
const result = calculator.sum.apply(null, numbers);
console.log(result); // 15

// หมายเหตุ: เราใส่ null ในช่อง this เพราะฟังก์ชัน sum ไม่ได้ใช้ this ข้างใน
// ใน ES6 เราอาจใช้ Spread แทนได้: calculator.sum(...numbers)
```
:::

---

## 📖 Glossary (คำศัพท์เทคนิค)

| คำศัพท์ | ความหมาย |
|:-------|:---------|
| **Context (`this`)** | บริบทหรือ "ตัวตน" ของ Object ที่กำลังเรียกใช้งานฟังก์ชันนั้นๆ |
| **Implicit Binding** | การกำหนด `this` อัตโนมัติ โดยดูจาก Object ที่อยู่หน้าจุด (.) เวลาเรียก |
| **Explicit Binding** | การกำหนด `this` ด้วยตัวเองผ่าน `.call()`, `.apply()`, `.bind()` |
| **Lexical Scope** | ขอบเขตของตัวแปรที่ถูกกำหนดโดย "ตำแหน่งที่เราเขียนโค้ด" (Arrow Function ใช้หลักการนี้กับ `this`) |
| **Invocation** | การ "เรียก" ฟังก์ชันให้ทำงาน (เช่น `func()`) |
| **Constructor Call** | การเรียกฟังก์ชันด้วยคำสั่ง `new` ซึ่งจะสร้าง Object ใหม่และกำหนด `this` ให้ Object นั้น |
| **Global Object** | Object ใหญ่สุดของระบบ (Window ใน Browser, Global ใน Node.js) |
| **Method** | ฟังก์ชันที่เป็น Property ของ Object |

---

👉 **[ไปต่อ: Module 5 - Arrays](/javascript/05-01-arrays)**
