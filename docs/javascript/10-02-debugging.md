# 10-2: Debugging (เทคนิคหาบัค — เครื่องมือนักสืบ) 🔍

> **"Debugging is twice as hard as writing the code in the first place."**
> — *Brian Kernighan*

การเขียนโค้ดให้ถูกเป็นเรื่องหนึ่ง แต่เมื่อมีบัค → ต้อง **หาให้เจอ** และ **แก้ให้ตรงจุด**! บทนี้จะสอนเครื่องมือและเทคนิคที่มืออาชีพใช้จริง

> **💡 Analogy (เปรียบเทียบ):**
> Debugging เหมือนเป็น **นักสืบ** 🕵️ — คุณเห็นผลลัพธ์ที่ผิด (ศพ) แต่ต้องย้อนกลับไปหา **ต้นเหตุ** (ฆาตกร) โดยใช้เครื่องมือ (หลักฐาน) และกลยุทธ์ (ตรรกะ)

---

## 1. console Methods (ไม่ใช่แค่ .log!) 📝

ตาม [MDN](https://developer.mozilla.org/en-US/docs/Web/API/console): `console` มี Method **มากกว่า 10 ตัว** ไม่ใช่แค่ `.log()`! การใช้ Method ที่เหมาะสมช่วยให้ Debug **เร็วและมีประสิทธิภาพ** กว่าแค่ log ทุกอย่าง:

```javascript
// 1. console.log() — พิมพ์ข้อมูลทั่วไป
console.log("Hello");
console.log("User:", { name: "Dolar", age: 25 });

// 2. console.error() — ข้อความ Error (สีแดง!)
console.error("❌ Something went wrong!");

// 3. console.warn() — ข้อความเตือน (สีเหลือง!)
console.warn("⚠️ This function is deprecated!");

// 4. console.table() — แสดงเป็นตาราง! 📊
const users = [
    { name: "Dolar", age: 25 },
    { name: "Somchai", age: 30 },
];
console.table(users);

// 5. console.group() / groupEnd() — จัดกลุ่ม
console.group("User Details");
    console.log("Name: Dolar");
    console.log("Age: 25");
console.groupEnd();

// 6. console.time() / timeEnd() — จับเวลา!
console.time("Loop");
for (let i = 0; i < 1000000; i++) { /* ... */ }
console.timeEnd("Loop"); // "Loop: 12.5ms"

// 7. console.count() — นับจำนวนครั้ง
function handleClick() {
    console.count("clicked");
}
handleClick(); // "clicked: 1"
handleClick(); // "clicked: 2"

// 8. console.assert() — พิมพ์เฉพาะเมื่อ false
console.assert(1 === 1, "This won't show");
console.assert(1 === 2, "❌ This will show!");

// 9. console.dir() — แสดง Object แบบ Interactive
console.dir(document.body);

// 10. console.clear() — ล้างหน้าจอ Console
console.clear();
```

### 📊 Console Methods Quick Reference

| Method | ใช้ทำอะไร | เมื่อไหร่ |
|:-------|:---------|:---------|
| `.log()` | พิมพ์ทั่วไป | ตรวจค่าตัวแปร |
| `.error()` | แสดง Error (สีแดง) | เมื่อเกิด Error |
| `.warn()` | เตือน (สีเหลือง) | Deprecated, risky code |
| `.table()` | ตาราง | Array/Object ที่มีโครงสร้าง |
| `.time()` | จับเวลา | วัด Performance |
| `.count()` | นับครั้ง | นับว่าฟังก์ชันถูกเรียกกี่ครั้ง |
| `.assert()` | เช็คเงื่อนไข | ตรวจสอบว่าค่าถูกต้อง |
| `.group()` | จัดกลุ่ม | จัด Log ให้อ่านง่าย |

---

## 2. Chrome DevTools — Debugger 🛠️

`console.log()` เป็นวิธี Debug ที่ง่ายที่สุด แต่ **ไม่ใช่วิธีที่ดีที่สุด!** DevTools Debugger ให้เรา **หยุดโค้ดกลางทาง** ดูค่าตัวแปรทุกตัว แล้วเดินทีละบรรทัดได้ — เหมือน**ดูสโลว์โมชัน**ว่าโค้ดทำอะไร!

### เปิด DevTools:
- **Windows:** `F12` หรือ `Ctrl + Shift + I`
- **Mac:** `Cmd + Option + I`

### Breakpoints (จุดหยุด):

**Breakpoint** คือ "จุดหยุด" — เมื่อโค้ดวิ่งมาถึงจุดนี้ จะ **หยุดชั่วคราว** ให้เราตรวจสอบค่าตัวแปร, Call Stack, และ Scope ทั้งหมด! มี 2 วิธีตั้ง:

```javascript
function calculateTotal(items) {
    let total = 0;
    
    debugger; // ⭐ Breakpoint ในโค้ด! — DevTools จะหยุดตรงนี้
    
    for (const item of items) {
        total += item.price * item.quantity;
    }
    
    return total;
}
```

> 💡 **Tip:** ตั้ง Breakpoint ใน DevTools (คลิกเลขบรรทัดใน Sources Tab) สะดวกกว่า `debugger` เพราะไม่ต้องแก้โค้ด!

### DevTools Panels:

| Panel | ใช้ทำอะไร |
|:------|:---------|
| **Elements** | ดู/แก้ HTML + CSS |
| **Console** | รันโค้ด JavaScript |
| **Sources** | ดูโค้ด + ตั้ง Breakpoints |
| **Network** | ดู HTTP Requests (Fetch, API) |
| **Application** | ดู localStorage, cookies |
| **Performance** | วัดความเร็ว |

### Breakpoint Controls:

| ปุ่ม | ชื่อ | ทำอะไร |
|:-----|:-----|:------|
| ▶️ | Resume | ทำงานต่อจนเจอ Breakpoint ถัดไป |
| ⏭️ | Step Over | ข้ามไปบรรทัดถัดไป |
| ⬇️ | Step Into | เข้าไปใน Function |
| ⬆️ | Step Out | ออกจาก Function ปัจจุบัน |

---

## 3. Debugging Strategies 🧠

เครื่องมือดีแค่ไหนก็ไม่ช่วยถ้าไม่มี **วิธีคิด** ที่ถูกต้อง! นักพัฒนามืออาชีพมีกลยุทธ์เหล่านี้ติดตัว:

### Strategy 1: Binary Search Debugging (ค้นหาแบบแบ่งครึ่ง)

**หลักการ:** ใส่ `console.log()` ตรง**กลาง** Function → ดูว่าข้อมูลยังถูกหรือผิด → ตัดครึ่งที่ถูกออก → ทำซ้ำจนเจอบัค! (เหมือนเกมทายตัวเลข)

```javascript
// บัค: Function return ค่าผิด
function processData(data) {
    const step1 = filterData(data);
    console.log("Step 1:", step1); // ✅ ถูก? → บัคอยู่ข้างล่าง

    const step2 = transformData(step1);
    console.log("Step 2:", step2); // ❌ ผิด! → บัคอยู่ใน transformData!

    const step3 = formatData(step2);
    return step3;
}
// เทคนิค: ใส่ log ครึ่งทาง → หาว่าบัคอยู่ครึ่งบนหรือครึ่งล่าง
```

### Strategy 2: Rubber Duck Debugging 🦆

**หลักการ:** อธิบายโค้ดทีละบรรทัดให้ **"เป็ดยาง"** ฟัง (หรือเพื่อน, แมว, ใครก็ได้!) — เป็นเทคนิคที่ดูตลกแต่ **ได้ผลจริง!** เพราะตอนที่เราต้องอธิบายเป็นคำพูด สมองจะ **คิดช้าลง** และมักจะ**เจอบัค**ระหว่างอธิบาย!

> 💡 ที่มาของชื่อ: จากหนังสือ *The Pragmatic Programmer* ที่แนะนำให้วางเป็ดยางบนโต๊ะแล้วคุยกับมัน!

### Strategy 3: Comment Out (ปิดทีละส่วน)

**หลักการ:** Comment โค้ดออกทีละส่วน จนเจอว่า **ส่วนไหนทำให้พัง** → ถ้า Comment แล้วไม่พัง = บัคอยู่ในส่วนนั้น!

```javascript
function buggyFunction() {
    // ปิดทีละส่วน จนเจอว่าส่วนไหนทำให้พัง
    doA();
    doB();
    // doC(); // ← ลอง Comment → ถ้าไม่พัง = บัคอยู่ใน doC!
    doD();
}
```

---

## 4. Common Bugs & Fixes 🐛

นี่คือ **5 บัคที่พบบ่อยที่สุด** ในการเขียน JavaScript — ถ้ารู้จักก่อน จะเจอน้อยลง!

```javascript
// 🐛 Bug 1: Off-by-one Error
for (let i = 0; i <= arr.length; i++) { } // ❌ <= ทำให้เกินอีก 1
for (let i = 0; i < arr.length; i++) { }  // ✅ <

// 🐛 Bug 2: == vs ===
if (input == 0)  { } // ❌ "" == 0 is true!
if (input === 0) { } // ✅ Strict comparison

// 🐛 Bug 3: Missing return
function add(a, b) {
    a + b; // ❌ ลืม return!
}
function add(a, b) {
    return a + b; // ✅
}

// 🐛 Bug 4: Async/Await
async function getData() {
    const data = fetch("/api"); // ❌ ลืม await!
    const data2 = await fetch("/api"); // ✅
}

// 🐛 Bug 5: Mutation
const original = [1, 2, 3];
const copy = original; // ❌ ไม่ได้ Copy! (Reference เดียวกัน)
const copy2 = [...original]; // ✅ Spread = Copy จริง!
```

---

## 5. Real-World Use Case: Debug ปัญหา API ไม่แสดงข้อมูล 🌐

สถานการณ์จริง: เรียก API แล้วหน้าเว็บไม่แสดงข้อมูล — ต้อง Debug ยังไง?

```javascript
// ❌ โค้ดที่มีปัญหา — หน้าเว็บว่างเปล่า!
async function loadUsers() {
    const response = await fetch("/api/users");
    const data = response.json();  // 🐛 บัคอยู่ตรงนี้!
    renderUsers(data);
}

// ✅ Debug ทีละขั้น:
async function loadUsers() {
    console.log("1. เริ่ม fetch...");
    
    const response = await fetch("/api/users");
    console.log("2. Response status:", response.status);  // ตรวจว่า 200 หรือไม่
    console.log("3. Response OK?", response.ok);
    
    const data = await response.json();  // ✅ ลืม await! ต้องใส่
    console.log("4. Data:", data);        // ตรวจว่าข้อมูลถูกต้อง
    console.table(data);                  // ดูเป็นตารางชัดกว่า
    
    renderUsers(data);
}
```

**ขั้นตอน Debug แบบมืออาชีพ:**
1. เปิด **Network Tab** → ดูว่า Request ส่งไปหรือไม่ + Status Code
2. ใส่ `console.log()` ทีละขั้น → หาว่าข้อมูลหายตรงไหน
3. ใช้ `console.table()` → ดูโครงสร้างข้อมูลที่ได้จาก API
4. ตรวจ Error ใน **Console Tab** → มี TypeError หรือ SyntaxError หรือไม่

::: tip 💡 Pro Tip
บัคที่พบบ่อยที่สุดกับ `fetch()` คือ **ลืม `await`** ก่อน `.json()` — เพราะ `.json()` return Promise ไม่ใช่ข้อมูลจริง!
:::

---

## 6. Challenges 🏆

ทดสอบความเข้าใจกับโจทย์ 4 ข้อ (1 ข้อต่อ 1 หัวข้อ):

### 🎯 Challenge 1: Log Master
**หัวข้อ:** 1. console Methods

**โจทย์:** ถ้าต้องการพิมพ์ Object `users` (ที่เป็น Array of Objects) ให้อ่านง่ายเป็นตาราง ควรใช้คำสั่งอะไร?
::: details ✨ ดูเฉลย
`console.table(users)`
:::

### 🎯 Challenge 2: Stop! in the Name of Code
**หัวข้อ:** 2. Chrome DevTools

**โจทย์:** คำสั่งอะไรที่เขียนในโค้ดแล้วทำให้ DevTools **หยุด** การทำงานในบรรทัดนั้นทันที?
::: details ✨ ดูเฉลย
`debugger;`
:::

### 🎯 Challenge 3: Half-Split Search
**หัวข้อ:** 3. Debugging Strategies

**โจทย์:** เทคนิค "Binary Search Debugging" คือการทำอะไร?
::: details ✨ ดูเฉลย
คือการตัดโค้ด หรือ ใส่ log ตรง **กึ่งกลาง** การทำงาน เพื่อดูว่าบัคอยู่ครึ่งบนหรือครึ่งล่าง
:::

### 🎯 Challenge 4: Bug Hunter
**หัวข้อ:** 4. Common Bugs

**โจทย์:** โค้ด `if (input == 0)` มีโอกาสเกิดบัคอะไร? และควรแก้อย่างไร?
::: details ✨ ดูเฉลย
บัคคือ `"" == 0` เป็น true (Type Coercion)
แก้โดยใช้ `===` (Strict Equality)
:::

---

> **📖 คำศัพท์เทคนิค (Glossary):**
> *   **Bug:** ข้อผิดพลาดในโค้ดที่ทำให้ทำงานไม่ถูกต้อง
> *   **Debugging:** กระบวนการค้นหาและแก้ไข Bug
> *   **Breakpoint:** จุดหยุดที่ทำให้โค้ดหยุดทำงานเพื่อตรวจสอบ
> *   **`debugger`:** คำสั่งที่ทำให้ DevTools หยุดที่บรรทัดนั้น
> *   **Stack Trace:** ข้อมูลลำดับการเรียก Function ที่นำไปสู่ Error
> *   **DevTools:** เครื่องมือสำหรับ Developer ที่มาพร้อม Browser
> *   **Console Panel:** แท็บสำหรับรันและดู Output ของ JavaScript
> *   **Network Panel:** แท็บสำหรับดู HTTP Requests ทั้งหมด
> *   **Off-by-one:** Bug ที่เกิดจากการนับผิดไป 1

---
👉 **[ไปทำโปรเจกต์: Project — Form Validator](/10-project-form-validator)**
