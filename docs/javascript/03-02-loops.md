# 03-2: Loops (ลูป — การทำซ้ำ) 🔄

> **"The art of programming is the art of organizing complexity."**
> — *Edsger W. Dijkstra*

คอมพิวเตอร์เก่งเรื่อง **ทำซ้ำ** มากที่สุดครับ มนุษย์เขียน 1+2+3+...+1000 คงใช้เวลาหลายชั่วโมง แต่ Loop ทำได้ในเสี้ยววินาที

> **💡 Analogy (เปรียบเทียบ):**
> Loop เหมือน **"เครื่องซักผ้า"** ครับ:
> - **`for` loop** = เครื่องที่ตั้ง "จำนวนรอบ" ไว้ล่วงหน้า (เช่น ซัก 10 รอบ)
> - **`while` loop** = เครื่องที่ "ซักไปเรื่อยๆ จนกว่าน้ำจะใส" (ไม่รู้ล่วงหน้าว่ากี่รอบ)
> - **`do...while` loop** = เครื่องที่ "ซักอย่างน้อย 1 รอบ แล้วค่อยเช็คว่าต้องซักต่อไหม"
> - **`break`** = ปุ่มหยุดฉุกเฉิน
> - **`continue`** = ปุ่มข้ามรอบนี้ ไปรอบถัดไปเลย

## 1. The `for` Loop (วน Loop แบบนับ) 🎯

ตาม [MDN](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Loops_and_iteration#for_statement) `for` loop เหมาะเมื่อ **รู้จำนวนรอบล่วงหน้า**:

### Anatomy (กายวิภาค):

```javascript
// for (①เริ่มต้น; ②เงื่อนไข; ③อัปเดต)
for (let i = 0; i < 5; i++) {
    console.log(i);
}
// Output: 0, 1, 2, 3, 4

// ลำดับการทำงาน:
// ① let i = 0       (ครั้งแรกครั้งเดียว)
// ② i < 5 → true    (เช็คก่อนทุกรอบ)
//   → รัน body: console.log(0)
// ③ i++ → i = 1     (หลังจบ body)
// ② i < 5 → true
//   → รัน body: console.log(1)
// ③ i++ → i = 2
// ... ซ้ำไปจนกว่า...
// ② i < 5 → false (i = 5) → หยุด!
```

### ตัวอย่างที่ใช้บ่อย:

```javascript
// นับ 1 ถึง 10
for (let i = 1; i <= 10; i++) {
    console.log(i);
}

// นับถอยหลัง
for (let i = 10; i >= 0; i--) {
    console.log(i);
}
// 10, 9, 8, ..., 1, 0

// นับทีละ 2 (เลขคู่)
for (let i = 0; i <= 20; i += 2) {
    console.log(i);
}
// 0, 2, 4, 6, ..., 20

// วนรอบ Array
const fruits = ["🍎", "🍌", "🍊"];
for (let i = 0; i < fruits.length; i++) {
    console.log(`${i}: ${fruits[i]}`);
}
// 0: 🍎, 1: 🍌, 2: 🍊
```

---

## 2. The `while` Loop (วน Loop แบบเงื่อนไข) 🔁

ตาม [MDN](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Loops_and_iteration#while_statement) `while` เหมาะเมื่อ **ไม่รู้จำนวนรอบล่วงหน้า**:

```javascript
// ตัวอย่าง: เพิ่มเงินจนถึง 1000
let money = 100;
let months = 0;

while (money < 1000) {
    money = money * 1.05; // ดอกเบี้ย 5% ต่อเดือน
    months++;
}

console.log(`ใช้เวลา ${months} เดือน เงินเป็น ${money.toFixed(2)} บาท`);
// "ใช้เวลา 48 เดือน เงินเป็น 1040.13 บาท"
```

### while vs for — เมื่อไหร่ใช้อะไร?

```javascript
// ✅ for: เมื่อรู้จำนวนรอบ
for (let i = 0; i < 10; i++) { /* ... */ }

// ✅ while: เมื่อไม่รู้จำนวนรอบ
let input = "";
while (input !== "quit") {
    // รอ user พิมพ์ "quit" — ไม่รู้ว่าจะกี่รอบ
    // input = prompt("Enter command:");
    break; // (ตัวอย่าง — ในจริงจะไม่มี break)
}
```

---

## 3. The `do...while` Loop (ทำก่อน เช็คทีหลัง) ❗

ตาม [MDN](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Loops_and_iteration#do...while_statement) `do...while` **รัน body อย่างน้อย 1 ครั้ง** ก่อนเช็คเงื่อนไข:

```javascript
// do...while: รัน Body ก่อน แล้วค่อยเช็คเงื่อนไข
let count = 10;

do {
    console.log(`Count: ${count}`);
    count++;
} while (count < 5);
// Output: "Count: 10" ← ถูกรัน 1 รอบ แม้ว่า 10 > 5!

// เปรียบเทียบ: while จะไม่รันเลย
count = 10;
while (count < 5) {
    console.log(`Count: ${count}`); // ← ไม่เคยรัน! เพราะเช็คก่อน
    count++;
}
```

### 📊 Loop Comparison Table

| | `for` | `while` | `do...while` |
|:--|:-----|:--------|:-------------|
| **เช็คเงื่อนไข** | ก่อนรัน Body | ก่อนรัน Body | **หลังรัน Body** |
| **รันขั้นต่ำ** | 0 ครั้ง | 0 ครั้ง | **อย่างน้อย 1 ครั้ง** |
| **เหมาะกับ** | รู้จำนวนรอบ | ไม่รู้จำนวนรอบ | ต้องทำก่อน 1 ครั้งเสมอ |
| **นิยม** | ⭐⭐⭐ (มากสุด) | ⭐⭐ | ⭐ (น้อยสุด) |

---

## 4. for...of and for...in (ES6 Loops) 🌟

### `for...of` — วน "ค่า" ของ Iterable (Array, String, Map, Set):

```javascript
const colors = ["Red", "Green", "Blue"];

// ✅ for...of: วนค่า (Value)
for (const color of colors) {
    console.log(color);
}
// Red, Green, Blue

// ใช้กับ String
for (const char of "Hello") {
    console.log(char);
}
// H, e, l, l, o
```

### `for...in` — วน "Key" ของ Object:

```javascript
const user = { name: "Dolar", age: 25, role: "dev" };

// for...in: วน Key ของ Object
for (const key in user) {
    console.log(`${key}: ${user[key]}`);
}
// name: Dolar
// age: 25
// role: dev

// ⚠️ อย่าใช้ for...in กับ Array! (เพราะ key เป็น String, อาจรวม Property อื่น)
// ❌ for (const i in colors) { ... } // i เป็น "0", "1", "2" (String)
// ✅ for (const color of colors) { ... }
```

### 📊 for...of vs for...in

| | `for...of` | `for...in` |
|:--|:----------|:----------|
| **วนอะไร** | **ค่า** (Value) | **Key** (Property Name) |
| **ใช้กับ** | Array, String, Map, Set | **Object** |
| **Key Type** | — | String |
| **ใช้กับ Array?** | ✅ แนะนำ | ❌ ไม่แนะนำ |

---

## 5. Loop Control: break & continue 🛑

### `break` — หยุดทั้ง Loop ทันที:

```javascript
// หาเลข 7 ตัวแรกใน Array
const numbers = [3, 1, 4, 1, 5, 9, 7, 6, 5, 3, 5];

for (const num of numbers) {
    if (num === 7) {
        console.log("🎯 เจอเลข 7 แล้ว!");
        break; // หยุด Loop ทันที ไม่ต้องวนต่อ
    }
    console.log(`ตรวจ: ${num}`);
}
// ตรวจ: 3
// ตรวจ: 1
// ตรวจ: 4
// ตรวจ: 1
// ตรวจ: 5
// ตรวจ: 9
// 🎯 เจอเลข 7 แล้ว!
```

### `continue` — ข้ามรอบนี้ ไปรอบถัดไป:

```javascript
// พิมพ์เฉพาะเลขคี่
for (let i = 1; i <= 10; i++) {
    if (i % 2 === 0) continue; // ข้ามเลขคู่
    console.log(i);
}
// 1, 3, 5, 7, 9
```

### Visual: break vs continue

```
Loop: [1] → [2] → [3] → [4] → [5] → [6]

break ที่ 4:
[1] → [2] → [3] → [4]  ✋ STOP! (หยุดทั้งหมด)

continue ที่ 4:
[1] → [2] → [3] → [⤳4⤳] → [5] → [6] (ข้ามแค่รอบนั้น)
```

---

## 6. ⚠️ Infinite Loops (วน Loop ไม่รู้จบ — อันตราย!) 💀

Loop ที่ไม่มีทางหยุดจะทำให้ **โปรแกรมค้าง** (Freeze/Crash):

```javascript
// ❌ Infinite Loop: ลืมอัปเดตตัวนับ
// let i = 0;
// while (i < 5) {
//     console.log(i);  // i ยังเป็น 0 ตลอด → ไม่มีวันถึง 5!
//     // ลืม i++!
// }

// ❌ Infinite Loop: เงื่อนไขเป็น true เสมอ
// while (true) {
//     console.log("Forever...");
// }

// ✅ วิธีป้องกัน:
// 1. ตรวจสอบว่า Loop มีทางออกเสมอ
// 2. ตรวจสอบว่าตัวนับถูกอัปเดต
// 3. ใช้ break เป็นทางออกฉุกเฉิน
// 4. ทดสอบกับจำนวนรอบน้อยๆ ก่อน
```

### Safety Check: 3 สิ่งที่ Loop ต้องมี

```
✅ 1. Initialization — ค่าเริ่มต้น (let i = 0)
✅ 2. Condition — เงื่อนไขหยุด (i < 10)  
✅ 3. Update — อัปเดตค่า (i++)
❌ ถ้าขาดข้อใดข้อหนึ่ง → อาจเกิด Infinite Loop!
```

---

## 7. Nested Loops (Loop ซ้อน Loop) 🎁

```javascript
// สูตรคูณ
for (let i = 1; i <= 3; i++) {
    for (let j = 1; j <= 3; j++) {
        console.log(`${i} × ${j} = ${i * j}`);
    }
    console.log("---");
}
// 1 × 1 = 1
// 1 × 2 = 2
// 1 × 3 = 3
// ---
// 2 × 1 = 2
// ...
```



---

## 8. Common Loop Patterns (แพทเทิร์นที่ใช้บ่อย) 📚

### A. Accumulator Pattern (สะสมค่า):
```javascript
const prices = [100, 250, 50, 300, 75];
let total = 0;

for (const price of prices) {
    total += price;
}
console.log(`Total: ${total} บาท`); // Total: 775 บาท

// Modern: ใช้ reduce() แทน
const total2 = prices.reduce((sum, p) => sum + p, 0);
```

### B. Search Pattern (ค้นหา):
```javascript
const students = ["Dolar", "Somchai", "Somsak", "Noi"];
let found = false;

for (const name of students) {
    if (name === "Somchai") {
        found = true;
        break;
    }
}
console.log(found ? "เจอ!" : "ไม่เจอ");

// Modern: ใช้ includes() หรือ find()
console.log(students.includes("Somchai") ? "เจอ!" : "ไม่เจอ");
```

### C. Transform Pattern (แปลงข้อมูล):
```javascript
const numbers = [1, 2, 3, 4, 5];
const doubled = [];

for (const n of numbers) {
    doubled.push(n * 2);
}
console.log(doubled); // [2, 4, 6, 8, 10]

// Modern: ใช้ map()
const doubled2 = numbers.map(n => n * 2);
```

## Real-World Use Case: แสดงรายการสินค้าจาก API 🌐

```javascript
const products = [
    { name: "iPhone", price: 42900, inStock: true },
    { name: "iPad", price: 15900, inStock: false },
    { name: "AirPods", price: 6790, inStock: true },
];

// แสดงเฉพาะสินค้าที่มีในสต็อค
for (const product of products) {
    if (!product.inStock) continue;  // ข้ามสินค้าหมด
    console.log(`${product.name} - ฿${product.price.toLocaleString()}`);
}
// Output:
// iPhone - ฿42,900
// AirPods - ฿6,790
```

---

## 9. Challenges 🏆

ทดสอบความเข้าใจกับโจทย์ 8 ข้อ (1 ข้อต่อ 1 หัวข้อ):

### 🎯 Challenge 1: Counting Machine
**หัวข้อ:** 1. The for Loop

**โจทย์:** ใช้ `for` loop พิมพ์เลขคู่ (Even Numbers) ตั้งแต่ 2 ถึง 10 (2, 4, 6, 8, 10)
::: details ✨ ดูเฉลย
```javascript
for (let i = 2; i <= 10; i += 2) {
    console.log(i);
}
```
:::

### 🎯 Challenge 2: Growth Hacking
**หัวข้อ:** 2. The while Loop

**โจทย์:** มีเงิน 1 บาท คูณ 2 ไปเรื่อยๆ (1, 2, 4, 8, ...) **หยุดเมื่อเกิน 100** จงใช้ `while` loop
::: details ✨ ดูเฉลย
```javascript
let money = 1;
while (money <= 100) {
    console.log(money);
    money *= 2;
}
```
:::

### 🎯 Challenge 3: Just Once
**หัวข้อ:** 3. The do...while Loop

**โจทย์:** พิสูจน์ว่า `do...while` ทำงาน 1 ครั้งแม้เงื่อนไขเป็นเท็จ โดยตั้งเงื่อนไข `while(false)`
::: details ✨ ดูเฉลย
```javascript
do {
    console.log("I run at least once!");
} while (false);
```
:::

### 🎯 Challenge 4: Iterator
**หัวข้อ:** 4. for...of vs for...in

**โจทย์:** มี Array `users = ["A", "B", "C"]` จงใช้ `for...of` เพื่อพิมพ์ชื่อทุกคน
::: details ✨ ดูเฉลย
```javascript
const users = ["A", "B", "C"];
for (const user of users) {
    console.log(user);
}
```
:::

### 🎯 Challenge 5: Find & Stop
**หัวข้อ:** 5. break & continue

**โจทย์:** วนลูป 1-10 ถ้าเจอเลข 5 ให้ **หยุดทันที (`break`)**
::: details ✨ ดูเฉลย
```javascript
for (let i = 1; i <= 10; i++) {
    if (i === 5) break;
    console.log(i);
}
```
:::

### 🎯 Challenge 6: The Fixer
**หัวข้อ:** 6. Infinite Loops

**โจทย์:** แก้โค้ดนี้ไม่ให้ Loop ค้าง:
```javascript
let i = 0;
while (i < 5) {
    console.log(i);
    // ... ลืมอะไรไป?
}
```
::: details ✨ ดูเฉลย
```javascript
let i = 0;
while (i < 5) {
    console.log(i);
    i++; // ✅ ต้องเพิ่มค่า i ด้วย!
}
```
:::

### 🎯 Challenge 7: Coordinate System
**หัวข้อ:** 7. Nested Loops

**โจทย์:** ใช้ Nested Loop พิมพ์คู่ลำดับ (Row, Col) ขนาด 2x2:
`(0,0) (0,1) (1,0) (1,1)`
::: details ✨ ดูเฉลย
```javascript
for (let row = 0; row < 2; row++) {
    for (let col = 0; col < 2; col++) {
        console.log(`(${row},${col})`);
    }
}
```
:::

### 🎯 Challenge 8: The Sum
**หัวข้อ:** 8. Patterns

**โจทย์:** หาผลรวมของ Array `[10, 20, 30]` โดยใช้ `for...of`
::: details ✨ ดูเฉลย
```javascript
const nums = [10, 20, 30];
let sum = 0;
for (const n of nums) {
    sum += n;
}
console.log(sum); // 60
```
:::

---

> **📖 คำศัพท์เทคนิค (Glossary):**
> *   **Loop (ลูป):** โครงสร้างที่ทำให้โค้ดทำงานซ้ำๆ จนกว่าจะตรงเงื่อนไขหยุด
> *   **Iteration:** การทำงานหนึ่งรอบของ Loop
> *   **`for` loop:** Loop ที่มี Initialization, Condition, Update ในที่เดียว
> *   **`while` loop:** Loop ที่เช็คเงื่อนไขก่อนรัน Body ทุกรอบ
> *   **`do...while` loop:** Loop ที่รัน Body ก่อนอย่างน้อย 1 ครั้ง แล้วค่อยเช็คเงื่อนไข
> *   **`for...of`:** Loop ที่วน "ค่า" ของ Iterable (Array, String)
> *   **`for...in`:** Loop ที่วน "Key" ของ Object
> *   **`break`:** คำสั่งหยุด Loop ทั้งหมดทันที
> *   **`continue`:** คำสั่งข้ามรอบปัจจุบัน ไปรอบถัดไป
> *   **Infinite Loop:** Loop ที่ไม่มีทางหยุด ทำให้โปรแกรมค้าง
> *   **Nested Loop:** Loop ซ้อนใน Loop สำหรับงานที่ต้อง 2 มิติ (เช่น ตาราง)
> *   **Accumulator Pattern:** รูปแบบที่ใช้ตัวแปรสะสมค่าในแต่ละรอบ Loop

---
👉 **[ไปต่อ: Project 3 - FizzBuzz Challenge](/03-project-fizzbuzz)**
