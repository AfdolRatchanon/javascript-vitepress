# 01-1: History & Standards (เจาะลึกประวัติและมาตรฐาน) 📜

> **"Any application that can be written in JavaScript, will eventually be written in JavaScript."**
> — *Jeff Atwood (Atwood's Law)*

ก่อนจะเขียน JavaScript ได้ดี คุณต้อง **เข้าใจที่มาของมัน** ครับ ภาษานี้มีประวัติที่บ้า เต็มไปด้วยเรื่องดราม่า และยังคงพัฒนาอย่างไม่หยุดยั้งจนทุกวันนี้

> **💡 Analogy (เปรียบเทียบ):**
> ประวัติ JavaScript เหมือน **"หนังฮีโร่ที่เริ่มต้นจากตัวตลก"** ครับ:
> - ถูกสร้างใน **10 วัน** (เหมือนสร้างหุ่นยนต์จากของเหลือใช้ในโรงรถ)
> - ถูกดูถูกว่า **"ของเล่น"** มานานหลายปี
> - แต่สุดท้ายกลายเป็น **ภาษาโปรแกรมที่ใช้มากที่สุดในโลก!**

## 1. The Birth of JavaScript (กำเนิด — 1995) 🎂

### 10 วันที่เปลี่ยนโลก

*   **ผู้สร้าง:** คุณ **Brendan Eich** (พนักงาน Netscape)
*   **เวลาที่ใช้:** **10 วัน!** (ใช่ครับ ภาษาระดับโลกนี้ถูกร่างต้นแบบในเวลาไม่ถึง 2 สัปดาห์)
*   **ชื่อแรก:** Mocha → LiveScript → **JavaScript** (เปลี่ยนเพื่อเกาะกระแส Java ที่กำลังฮิต)
*   **เป้าหมายเดิม:** ทำให้เว็บ "ขยับได้" — ตรวจสอบ Form, เปลี่ยนสีปุ่ม, แสดง Alert ง่ายๆ

```
Timeline (ไทม์ไลน์):
1995:  ☕ Java กำลังฮิต → Netscape อยากได้ภาษาง่ายๆ สำหรับ Browser
       │
       ▼
1995:  🔧 Brendan Eich สร้าง "Mocha" ใน 10 วัน
       │
       ▼
1995:  📛 เปลี่ยนชื่อ Mocha → LiveScript → JavaScript
       │  (กลยุทธ์การตลาดเพื่อเกาะกระแส Java)
       │
       ▼
1996:  📄 Netscape ส่ง JavaScript ให้ ECMA International เพื่อทำ "มาตรฐาน"
```


---

## 2. The Browser Wars (สงคราม Browser — 1995-2001) ⚔️

หลังจาก JavaScript เกิดขึ้น ก็เกิด **สงคราม Browser** ครั้งแรก:

```
                    ⚔️ Browser War I ⚔️
                    
🦊 Netscape Navigator        🌐 Internet Explorer (IE)
   └── JavaScript                └── JScript
   
ผลลัพธ์: แต่ละค่ายทำ "JavaScript" ของตัวเอง → โค้ดที่ใช้ได้ใน Netscape
         อาจใช้ไม่ได้ใน IE! Developer ทุกข์ทรมานมาก 😤
```

### ปัญหา: "ต่างคนต่างทำ"

| | Netscape | Internet Explorer |
|:--|:---------|:-----------------|
| **ชื่อภาษา** | JavaScript | JScript |
| **ใช้ร่วมกันได้** | ❌ ไม่เสมอ | ❌ ไม่เสมอ |
| **ผล** | Developer ต้องเขียนโค้ด 2 ชุด! | 💀 |

> **เปรียบเทียบ:** เหมือนสมัยที่ชาร์จมือถือ แต่ละยี่ห้อใช้ **สายชาร์จคนละแบบ** — Apple ใช้ Lightning, Samsung ใช้ Micro-USB, Nokia ใช้แบบอื่นอีก จนมาถึงยุค **USB-C** (= ECMAScript) ที่ทำให้ทุกอย่างใช้สายเดียวกันได้!


---

## 3. ECMA International to the Rescue 🤝

เพื่อแก้ปัญหา "ต่างคนต่างทำ" Netscape จึงส่ง JavaScript ให้ **ECMA International** (European Computer Manufacturers Association) เป็นผู้ดูแลมาตรฐาน:

*   **ECMAScript (ES):** คือ **"สเปค" (Specification)** — กฎเหล็กที่บอกว่า JavaScript ต้องมีอะไรบ้าง
*   **JavaScript:** คือ **"ภาษาจริงๆ"** ที่ทำตามกฎนั้น (= Implementation)

> **เปรียบเทียบ:**
> - **ECMAScript** = สูตรอาหาร (Recipe) 📖
> - **JavaScript** = อาหารที่ปรุงจริง (Dish) 🍜
> - **TC39** = คณะกรรมการตัดสิน MasterChef (คนเขียนสูตร) 👨‍🍳

### The TC39 Proposal Process (กระบวนการยื่นข้อเสนอ):

Feature ใหม่ไม่ได้ถูกเพิ่มมาลอยๆ — ต้องผ่าน **5 ด่าน**:

```
Stage 0: Strawman       → "ไอเดียลอยๆ" (ใครยื่นก็ได้)
    │
    ▼
Stage 1: Proposal       → "น่าสนใจ!" (TC39 รับพิจารณา)
    │
    ▼
Stage 2: Draft          → "เขียนสเปคคร่าวๆ" (Syntax เกือบสมบูรณ์)
    │
    ▼
Stage 3: Candidate      → "เกือบได้แล้ว!" (Browser เริ่ม Implement)
    │
    ▼
Stage 4: Finished ✅    → "เสร็จสมบูรณ์!" (บรรจุลงมาตรฐานฉบับถัดไป)
```

### 📊 ECMAScript Versions Timeline

| Version | ปี | Feature เด่น | สถานะ |
|:--------|:----|:------------|:------|
| **ES1** | 1997 | เวอร์ชันแรก! | โบราณ |
| **ES3** | 1999 | try/catch, RegExp | Legacy |
| **ES5** | 2009 | `forEach`, `map`, JSON, Strict Mode | Legacy Standard |
| **ES6 (ES2015)** | 2015 | ⭐ `let`, `const`, Arrow Functions, Classes, Modules, Promises | **Major Milestone!** |
| **ES2016** | 2016 | `includes()`, `**` (exponentiation) | ✅ Modern |
| **ES2017** | 2017 | `async/await`, `Object.entries()` | ✅ Modern |
| **ES2020** | 2020 | `?.` (Optional Chaining), `??` (Nullish Coalescing), BigInt | ✅ Modern |
| **ES2022** | 2022 | `.at()`, Top-level `await`, `#private` fields | ✅ Modern |
| **ES2024** | 2024 | `groupBy()`, `Promise.withResolvers()` | ✅ Latest |

> **⭐ ES6 (ES2015) คือจุดเปลี่ยนที่สำคัญที่สุด** — มันเปลี่ยน JavaScript จาก "ภาษาของเล่น" เป็น "ภาษาระดับ Enterprise" ครับ!


---

## 4. Java vs JavaScript (คนละเรื่อง!) ☕ ≠ 🟨

> ⚠️ **Java กับ JavaScript เหมือนกันแค่ 4 ตัวอักษรแรก!**
> เหมือน **"Car" กับ "Carpet"** — ชื่อคล้ายแต่ไม่เกี่ยวกันเลย!

| Feature | Java ☕ | JavaScript 🟨 |
|:--------|:------|:-------------|
| **Type** | Compiled Language (แปลงทั้งไฟล์ก่อนรัน) | Interpreted / JIT Compiled (แปลงขณะรัน) |
| **Typing** | Statically Typed (`int x = 5;`) | Dynamically Typed (`let x = 5;`) |
| **OOP** | Class-based (บังคับใช้ Class) | Prototype-based (ยืดหยุ่น) |
| **Platform** | JVM (Java Virtual Machine) | Browser + Node.js |
| **ใช้ทำอะไร** | Backend, Android App, Enterprise | Web (Frontend + Backend), Mobile, Desktop, IoT |
| **สร้างปี** | 1995 (Sun Microsystems) | 1995 (Netscape) |
| **ชื่อเต็ม** | Java | JavaScript (เปลี่ยนชื่อเพื่อการตลาด!) |

---

## 5. The JavaScript Engine (เครื่องยนต์ JS) ⚙️

ตาม [MDN](https://developer.mozilla.org/en-US/docs/Web/JavaScript) JavaScript ไม่ได้รันตรงๆ บนเครื่อง — มันต้องผ่าน **JavaScript Engine**:

### Popular Engines:

| Engine | ใช้ใน | ผู้สร้าง |
|:-------|:------|:--------|
| **V8** | Chrome, Node.js, Deno | Google |
| **SpiderMonkey** | Firefox | Mozilla |
| **JavaScriptCore** | Safari | Apple |
| **Chakra** | Edge (เก่า) | Microsoft |

### JIT Compilation (Just-In-Time):

เดิมที JS เป็นภาษา **Interpreted** (แปลทีละบรรทัด) แต่ปัจจุบัน Engine ใช้เทคนิค **JIT Compilation**:

```
กระบวนการ JIT Compilation:

Source Code ("let x = 5;")
    │
    ▼
① Parse → สร้าง AST (Abstract Syntax Tree)
    │         คือ "แผนผังโครงสร้าง" ของโค้ด
    ▼
② Compile → แปลงเป็น Bytecode (รันได้เลย — แต่ยังช้า)
    │
    ▼
③ Run → รัน Bytecode ผ่าน Interpreter (Ignition ใน V8)
    │
    ▼
④ Profile → ส่องหาโค้ดที่รัน "บ่อยมาก" (Hot Code)
    │
    ▼
⑤ Optimize → แปลง Hot Code เป็น Machine Code สุดแรง! ⚡
              (TurboFan ใน V8)
```

> **เปรียบเทียบ:** JIT เหมือน **"ล่ามแปลภาษา"** ที่แรกๆ แปลทีละคำ (ช้า) แต่พอเจอประโยคที่พูดบ่อยๆ ก็ **จดจำไว้เลย** (เร็ว!) เช่น "Where is the bathroom?" ไม่ต้องแปลทีละคำอีกต่อไป


---

## 6. JavaScript Today — The Swiss Army Knife 🔧

JavaScript ไม่ได้แค่ "ทำเว็บ" อีกต่อไป! มันกลายเป็น **มีดสวิส** ที่ทำได้ทุกอย่าง:

```
               ┌─────────────────────────────────┐
               │       JavaScript Ecosystem      │
               └─────────────────┬───────────────┘
                                 │
        ┌────────┬───────┬───────┼────────┬──────────┬──────────────┐
        ▼        ▼       ▼       ▼        ▼          ▼              ▼
    Frontend  Backend   Mobile  Desktop   Game      AI/ML           IoT
    React     Node.js   React   Electron  Three.js  TensorFlow.js
    Vue       Express   Native  Tauri               Brain.js
    Angular   Fastify  
    Svelte    Nest.js  
```

| ด้าน | เทคโนโลยี | ตัวอย่าง |
|:-----|:----------|:---------|
| **Frontend** | React, Vue, Angular, Svelte | Facebook, Netflix, Gmail |
| **Backend** | Node.js, Express, Nest.js | PayPal, NASA, Uber |
| **Mobile** | React Native, Expo | Instagram, Discord |
| **Desktop** | Electron, Tauri | VS Code, Slack, Figma |
| **Game** | Three.js, PixiJS | เกม 3D ใน Browser |
| **AI/ML** | TensorFlow.js, Brain.js | Image Recognition ใน Browser |

> **Stack Overflow Survey 2024:** JavaScript เป็นภาษา **#1** ที่ Developer ใช้มากที่สุดในโลก ติดต่อกัน **12 ปีซ้อน!**

---

## 7. Challenges 🏆

ตรวจสอบความเข้าใจกันหน่อยครับ กับโจทย์ 6 ข้อที่ตรงกับ 6 หัวข้อที่เราเรียนไป:

### 🎯 Challenge 1: The 10-Day Legend
**หัวข้อ:** 1. The Birth of JavaScript

**โจทย์:** JavaScript ถูกสร้างขึ้นครั้งแรกในระยะเวลากี่วัน? และชื่อแรกสุดของมันคืออะไร? (ก่อนจะชื่อ JavaScript)
::: details ✨ ดูเฉลย
**10 วัน** ครับ! และชื่อแรกคือ **Mocha** (ก่อนเปลี่ยนเป็น LiveScript และ JavaScript)
:::

### 🎯 Challenge 2: Why Standards?
**หัวข้อ:** 2. The Browser Wars

**โจทย์:** ปัญหาหลักที่เกิดขึ้นในยุค "Browser Wars" คืออะไร? และทำไมเราถึงต้องการมาตรฐาน (Standardization)?
::: details ✨ ดูเฉลย
ปัญหาคือ **"Code เดียวกันรันได้ไม่เหมือนกันในแต่ละ Browser"** ทำให้ Developer ทำงานยากมาก มาตรฐานจึงช่วยให้ทุก Browser ทำงานตรงกันครับ
:::

### 🎯 Challenge 3: Recipe vs Dish
**หัวข้อ:** 3. ECMA International

**โจทย์:** จงเติมคำในช่องว่าง: "ECMAScript เปรียบเสมือน _____ ส่วน JavaScript เปรียบเสมือน _____"
::: details ✨ ดูเฉลย
ECMAScript เปรียบเสมือน **สูตรอาหาร (Recipe/Specification)**
JavaScript เปรียบเสมือน **อาหารที่ปรุงจริง (Dish/Implementation)**
:::

### 🎯 Challenge 4: Not The Same
**หัวข้อ:** 4. Java vs JavaScript

**โจทย์:** บอกความแตกต่างระหว่าง Java และ JavaScript มาอย่างน้อย 2 ข้อ
::: details ✨ ดูเฉลย
1. **Typing:** Java เป็น Statically Typed (ต้องประกาศ Type), JS เป็น Dynamically Typed
2. **การรัน:** Java ต้อง Compile ก่อน, JS แปลงโค้ดขณะรัน (Interpreted/JIT)
3. **Platform:** Java รันบน JVM, JS รันบน Browser/Node.js
:::

### 🎯 Challenge 5: Engine Matching
**หัวข้อ:** 5. The JavaScript Engine

**โจทย์:** จับคู่ Browser กับ Engine ให้ถูกต้อง:
- Chrome ใช้ _____
- Firefox ใช้ _____
- Safari ใช้ _____
::: details ✨ ดูเฉลย
- Chrome ใช้ **V8**
- Firefox ใช้ **SpiderMonkey**
- Safari ใช้ **JavaScriptCore**
:::

### 🎯 Challenge 6: The Swiss Army Knife
**หัวข้อ:** 6. JavaScript Today

**โจทย์:** ยกตัวอย่างการนำ JavaScript ไปใช้ในงานที่ **ไม่ใช่** Web Frontend มา 2 ประเภท
::: details ✨ ดูเฉลย
เลือกตอบได้เลยครับ:
- **Backend:** Node.js
- **Mobile App:** React Native
- **Desktop App:** Electron
- **AI/ML:** TensorFlow.js
- **IoT:** Johnny-Five
:::

---

> **📖 คำศัพท์เทคนิค (Glossary):**
> *   **ECMAScript (ES):** มาตรฐานกลาง (Specification) ของภาษา JavaScript
> *   **TC39:** คณะกรรมการที่ดูแลพัฒนามาตรฐาน ECMAScript
> *   **JavaScript Engine:** โปรแกรมที่แปลง JS Code เป็น Machine Code (เช่น V8)
> *   **V8:** JS Engine ของ Google ที่ใช้ใน Chrome และ Node.js
> *   **JIT Compilation:** เทคนิคแปลงโค้ดแบบ "ทันที" ระหว่างรัน ไม่ต้อง Compile ล่วงหน้า
> *   **AST (Abstract Syntax Tree):** โครงสร้างต้นไม้ที่ Engine ใช้แทนโค้ดเพื่อวิเคราะห์
> *   **Interpreted:** ภาษาที่แปลโค้ดทีละบรรทัดขณะรัน (ช้ากว่า Compiled)
> *   **Compiled:** ภาษาที่แปลงโค้ดทั้งไฟล์เป็น Machine Code ก่อนรัน (เร็วกว่า)
> *   **Hot Code:** โค้ดที่ถูกรันบ่อยๆ และจะถูก JIT Compiler ปรับให้เร็วขึ้น
> *   **Proposal Stage (0-4):** ขั้นตอนที่ Feature ใหม่ต้องผ่านก่อนเข้ามาตรฐาน ECMAScript

---
👉 **[ไปต่อ: 01-2 - Hello World & Console Deep Dive](/01-02-hello-world)**
