# 07-1: Asynchronous Concepts (แนวคิดการทำงานแบบ Async) ⏳

> **"Asynchronous programming is about what happens when part of your program runs now and part runs later."**
> — *Kyle Simpson (You Don't Know JS)*

ถ้าทุก Module ที่ผ่านมา โค้ดของเรา **ทำทีละบรรทัด รอให้เสร็จก่อนถึงค่อยทำบรรทัดต่อไป** — นั่นเรียกว่า **Synchronous** แต่ในโลกจริง มีงานที่ **ต้องรอ** (โหลดข้อมูล, อ่านไฟล์, ตั้งเวลา) ถ้ารอทุกอย่าง โปรแกรมจะ **แข็ง** ไปเลย!

> **💡 Analogy (เปรียบเทียบ):**
> - **Synchronous** = **สั่งอาหารแล้วยืนรอที่เคาน์เตอร์** 🧍 จนได้อาหารถึงค่อยไปทำอย่างอื่น
> - **Asynchronous** = **สั่งอาหารแล้วนั่งรอที่โต๊ะ** 💺 ระหว่างรอก็เล่นมือถือ ตอบไลน์ได้ พอเสร็จเขาเรียกเราไปรับ

---

## 1. Synchronous vs Asynchronous 🔄

### Synchronous (ทำทีละอย่าง — Block!):

```javascript
console.log("1. สั่งอาหาร 🍜");
console.log("2. รออาหาร... ⏳ (Block! ทำอย่างอื่นไม่ได้)");
console.log("3. ได้อาหาร! กิน 🍽️");
console.log("4. จ่ายเงิน 💰");
// Output: 1 → 2 → 3 → 4 (ตามลำดับ)
```

### Asynchronous (เริ่มงาน แล้วไปทำอย่างอื่นก่อน):

```javascript
console.log("1. สั่งอาหาร 🍜");

setTimeout(() => {
    console.log("3. อาหารมาแล้ว! 🍽️");
}, 3000); // รอ 3 วินาที (แต่ไม่ Block!)

console.log("2. ระหว่างรอ... เล่นมือถือ 📱");

// Output: 1 → 2 → 3 (ข้อ 2 มาก่อน 3!)
```

### 📊 Sync vs Async

| | **Synchronous** | **Asynchronous** |
|:--|:---------------|:-----------------|
| **ทำงาน** | ทีละบรรทัด | เริ่มแล้วไปทำอย่างอื่น |
| **Blocking** | ✅ Block (ต้องรอ) | ❌ ไม่ Block |
| **เหมาะกับ** | งานเร็วๆ (คำนวณ) | งานที่ต้องรอ (Network, Timer, File) |
| **ตัวอย่าง** | `Math.sqrt(4)` | `fetch()`, `setTimeout()`, `readFile()` |

---

## 2. The Event Loop — หัวใจของ Async! 🔄

ตาม [MDN](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Event_loop):

JavaScript เป็น **Single-Threaded** (มีแค่ 1 คนทำงาน) แต่ยังทำ Async ได้ เพราะมี **Event Loop**!

### ส่วนประกอบ:

```
┌───────────────────────────────┐
│        Call Stack 📚          │ ← กองงาน (ทำทีละอัน LIFO)
│   (ฟังก์ชันที่กำลังทำงาน)          │
└──────────┬────────────────────┘
           │
           │ ถ้าเจองาน Async → ส่งไปให้ Web APIs
           ▼
┌───────────────────────────────┐
│        Web APIs 🌐            │ ← Browser จัดการให้ (Timer, Fetch, DOM Events)
│   setTimeout, fetch, etc.     │
└──────────┬────────────────────┘
           │
           │ เมื่อเสร็จ → ส่ง Callback ไปที่ Queue
           ▼
┌───────────────────────────────┐
│     Callback Queue 📬        │ ← คิวรอ (เข้าคิวรอเรียก)
│   (Macro Tasks: setTimeout)   │
└──────────┬────────────────────┘
           │
           │ Event Loop: ถ้า Call Stack ว่าง → หยิบจาก Queue มาทำ
           ▼
       🔄 Event Loop
    "Call Stack ว่างยัง? ว่างแล้ว? เอางานจาก Queue มา!"
```

### ตัวอย่างทีละขั้น:

```javascript
console.log("A");           // 1️⃣ → Call Stack → พิมพ์ "A" ทันที

setTimeout(() => {
    console.log("B");       // 3️⃣ → Callback Queue → รอ Call Stack ว่าง
}, 0);                      // แม้ 0ms ก็ต้องรอ!

console.log("C");           // 2️⃣ → Call Stack → พิมพ์ "C" ทันที

// Output: A → C → B
// ⚠️ ไม่ใช่ A → B → C! เพราะ setTimeout ต้องผ่าน Queue ก่อน
```



---

## 3. Callbacks (ฟังก์ชัน "โทรกลับ") 📞

**Callback** = ฟังก์ชันที่ส่งเป็น Argument ให้ฟังก์ชันอื่น แล้วจะถูก **"เรียกกลับ"** เมื่อเสร็จ:

```javascript
// setTimeout — Callback พื้นฐาน
function greet() {
    console.log("สวัสดี! (หลังจาก 2 วินาที)");
}
setTimeout(greet, 2000); // เรียก greet() หลังจาก 2 วิ

// addEventListener — Callback ที่เราเคยใช้มาแล้ว!
button.addEventListener("click", function() {
    console.log("คลิกแล้ว!"); // เรียกเมื่อ "คลิก" เกิดขึ้น
});

// Array methods — Callback ทุกตัว!
[1, 2, 3].forEach(function(num) {
    console.log(num * 2); // เรียกทุก element
});
```

### ⚠️ Callback Hell (นรกของ Callback!) 🔥

เมื่อมีงาน Async หลายชั้นซ้อนกัน → โค้ดจะเป็น **"พีระมิดมรณะ"**:

```javascript
// ❌ Callback Hell!
getUser(userId, function(user) {
    getOrders(user.id, function(orders) {
        getOrderDetails(orders[0].id, function(details) {
            getShipping(details.shippingId, function(shipping) {
                console.log(shipping.status);
                // 😱 ซ้อนลึกมาก! อ่านยาก ดูแลยาก!
            });
        });
    });
});
```

```
Callback Hell (พีระมิดมรณะ):

getUser(id, function(user) {
    ╰── getOrders(user.id, function(orders) {
            ╰── getDetails(orders[0].id, function(details) {
                    ╰── getShipping(details.id, function(shipping) {
                            ╰── 😱 ซ้อนลึกไม่รู้จบ!
                        });
                    });
                });
            });
```

> **วิธีแก้ Callback Hell → ใช้ Promise! (เรียนในบทถัดไป)**

---

## 4. setTimeout & setInterval ⏱️

### `setTimeout()` — ทำครั้งเดียวหลังจากเวลาที่กำหนด:

```javascript
// setTimeout(callback, milliseconds)
// 1000ms = 1 วินาที

const timerId = setTimeout(() => {
    console.log("⏰ หมดเวลา!");
}, 3000); // ทำงานหลัง 3 วินาที

// ยกเลิก Timer ก่อนถึงเวลา:
clearTimeout(timerId);
console.log("❌ Timer ถูกยกเลิก!");
```

### `setInterval()` — ทำซ้ำเรื่อยๆ ตามช่วงเวลา:

```javascript
let count = 0;
const intervalId = setInterval(() => {
    count++;
    console.log(`Tick ${count} ⏱️`);

    if (count >= 5) {
        clearInterval(intervalId); // หยุดเมื่อครบ 5 ครั้ง
        console.log("⏹️ หยุดแล้ว!");
    }
}, 1000);

// Output (ทุก 1 วินาที):
// Tick 1 ⏱️
// Tick 2 ⏱️
// Tick 3 ⏱️
// Tick 4 ⏱️
// Tick 5 ⏱️
// ⏹️ หยุดแล้ว!
```

### 📊 setTimeout vs setInterval

| | `setTimeout` | `setInterval` |
|:--|:------------|:-------------|
| **ทำกี่ครั้ง** | **1 ครั้ง** | **ซ้ำเรื่อยๆ** |
| **ยกเลิก** | `clearTimeout(id)` | `clearInterval(id)` |
| **ใช้ทำ** | Delay, Debounce | Countdown, Polling, Animation |

---

## 5. Practical Patterns 🛠️

### Debounce — รอให้หยุดพิมพ์ก่อนค่อยค้นหา:

```javascript
let timer;

searchInput.addEventListener("input", (e) => {
    clearTimeout(timer); // ยกเลิก Timer เก่า

    timer = setTimeout(() => {
        console.log("🔍 ค้นหา:", e.target.value);
        // เรียก API ที่นี่
    }, 500); // รอ 500ms หลังจากหยุดพิมพ์
});

// ✅ ผลลัพธ์: ไม่ค้นหาทุกตัวอักษร แต่รอให้หยุดพิมพ์ 0.5 วิก่อน!
```

### Countdown Timer:

```javascript
function startCountdown(seconds) {
    let remaining = seconds;

    const display = document.querySelector("#timer");
    display.textContent = remaining;

    const intervalId = setInterval(() => {
        remaining--;
        display.textContent = remaining;

        if (remaining <= 0) {
            clearInterval(intervalId);
            display.textContent = "⏰ หมดเวลา!";
        }
    }, 1000);
}

startCountdown(10); // นับถอยหลัง 10 วินาที
```

---

## 6. Microtasks vs Macrotasks 🔬

มี Queue 2 แบบ:

```javascript
console.log("1. Sync");

setTimeout(() => console.log("4. Macrotask (setTimeout)"), 0);

Promise.resolve().then(() => console.log("3. Microtask (Promise)"));

console.log("2. Sync");

// Output: 1 → 2 → 3 → 4
// ⚠️ Microtask (Promise) ทำก่อน Macrotask (setTimeout) เสมอ!
```

### 📊 Queue Priority

| Priority | ชื่อ | ตัวอย่าง | ทำเมื่อ |
|:--------:|:-----|:---------|:-------|
| 🥇 1 | **Call Stack** | Sync code | ทำทันที |
| 🥈 2 | **Microtask Queue** | `Promise.then`, `queueMicrotask` | หลัง Sync แต่ก่อน Macrotask |
| 🥉 3 | **Macrotask Queue** | `setTimeout`, `setInterval` | หลัง Microtask |

## Real-World Use Case: Debounce Search Input 🌐

```javascript
// ใช้ setTimeout ทำ Debounce — รอให้หยุดพิมพ์ก่อนค่อยค้นหา
function debounce(fn, delay) {
    let timer;
    return function(...args) {
        clearTimeout(timer);
        timer = setTimeout(() => fn(...args), delay);
    };
}

const searchAPI = debounce(async (query) => {
    console.log("Searching:", query);
    const res = await fetch("/api/search?q=" + query);
    const data = await res.json();
    renderResults(data);
}, 300);

searchInput.addEventListener("input", (e) => searchAPI(e.target.value));
```

> 💡 ถ้าไม่มี Debounce ทุก keystroke จะเรียก API — พิมพ์ "hello" = 5 requests! Debounce ลดเหลือ 1 request

---

## 7. Challenges 🏆


ทดสอบความเข้าใจกับโจทย์ 6 ข้อ (1 ข้อต่อ 1 หัวข้อ):

### 🎯 Challenge 1: Sync vs Async
**หัวข้อ:** 1. Synchronous vs Asynchronous

**โจทย์:** โค้ดนี้ใช้เวลารันรวมเท่าไหร่? (โดยประมาณ)
```javascript
console.log("Start");
setTimeout(() => console.log("A"), 1000);
setTimeout(() => console.log("B"), 1000);
console.log("End");
```
::: details ✨ ดูเฉลย
**1 วินาที** ครับ (เพราะ A และ B เริ่มจับเวลาพร้อมกันแบบ Async ไม่ได้รอต่อคิวกันแบบ Sync)
:::

### 🎯 Challenge 2: The Loop Master
**หัวข้อ:** 2. Event Loop

**โจทย์:** ผลลัพธ์คืออะไร? และทำไม?
```javascript
console.log("A");
setTimeout(() => console.log("B"), 0);
console.log("C");
```
::: details ✨ ดูเฉลย
**A → C → B** ครับ
เพราะ `setTimeout` (แม้ 0ms) จะถูกส่งไปเข้า Queue และต้องรอให้ Call Stack ว่างก่อน (ซึ่งต้องรอ `C` เสร็จก่อน)
:::

### 🎯 Challenge 3: Callback Calculator
**หัวข้อ:** 3. Callbacks

**โจทย์:** เขียนฟังก์ชัน `calculate(a, b, callback)` ที่รับตัวเลข 2 ตัว และฟังก์ชันคำนวณ แล้วเรียก callback นั้นพร้อมผลลัพธ์
::: details ✨ ดูเฉลย
```javascript
function calculate(a, b, cb) {
    cb(a, b);
}

calculate(5, 5, (x, y) => console.log(x + y)); // 10
```
:::

### 🎯 Challenge 4: Timer Bomb
**หัวข้อ:** 4. setTimeout

**โจทย์:** เขียนโค้ดที่พิมพ์ "Bomb!" หลังจากผ่านไป 5 วินาที แต่ถ้ามีการเรียก `function defuse()` ให้ยกเลิกการระเบิด
::: details ✨ ดูเฉลย
```javascript
const bombId = setTimeout(() => console.log("Bomb!"), 5000);

function defuse() {
    clearTimeout(bombId);
    console.log("Bomb defused!");
}
```
:::

### 🎯 Challenge 5: Search Throttler
**หัวข้อ:** 5. Practical Patterns

**โจทย์:** การทำ **"Debounce"** มีประโยชน์อย่างไรในระบบ Search?
::: details ✨ ดูเฉลย
ช่วย **ลดจำนวนการเรียก API** ครับ โดยจะรอให้ User หยุดพิมพ์ก่อน (เช่น 500ms) ถึงจะส่ง Request แทนที่จะส่งทุกตัวอักษรที่พิมพ์
:::

### 🎯 Challenge 6: Task Priority
**หัวข้อ:** 6. Microtasks vs Macrotasks

**โจทย์:** ใครได้ทำก่อน? `setTimeout` (Macrotask) หรือ `Promise.then` (Microtask)?
::: details ✨ ดูเฉลย
**Promise.then (Microtask)** ได้ทำก่อนเสมอครับ (VIP Queue!)
:::

---

> **📖 คำศัพท์เทคนิค (Glossary):**
> *   **Synchronous:** การทำงานแบบเรียงลำดับ ทีละบรรทัด ต้องรอให้เสร็จก่อน
> *   **Asynchronous:** การทำงานแบบไม่ Block — เริ่มงานแล้วไปทำอย่างอื่นก่อน
> *   **Callback:** ฟังก์ชันที่ส่งเป็น Argument ให้ฟังก์ชันอื่น เพื่อ "เรียกกลับ" ทีหลัง
> *   **Event Loop:** กลไกที่ตรวจสอบว่า Call Stack ว่างไหม ถ้าว่างก็หยิบงานจาก Queue มาทำ
> *   **Call Stack:** กองงานที่กำลังทำ (LIFO — เข้าหลังออกก่อน)
> *   **Web APIs:** สิ่งที่ Browser จัดการให้ (Timer, Fetch, DOM Events)
> *   **Callback Queue (Macrotask):** คิวรอสำหรับ setTimeout, setInterval
> *   **Microtask Queue:** คิวรอสำหรับ Promise.then (ลำดับสูงกว่า Macrotask)
> *   **Callback Hell:** ปัญหาโค้ดซ้อนลึกจาก Callback หลายชั้น (พีระมิดมรณะ)
> *   **Debounce:** เทคนิครอให้หยุดทำก่อนค่อยเรียก Function
> *   **Single-Threaded:** JavaScript มีแค่ 1 Thread ทำงาน (ไม่ทำหลายอย่างพร้อมกัน)
> *   **`setTimeout()`:** ตั้งเวลาทำงานครั้งเดียวหลังจาก N มิลลิวินาที

---
👉 **[ไปต่อ: 07-2 - Promises (สัญญา)](/07-02-promises)**
