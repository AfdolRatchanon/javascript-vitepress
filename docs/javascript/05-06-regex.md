# 05-06 Regular Expressions (Regex) 🧩

> **"Some people, when confronted with a problem, think 'I know, I'll use regular expressions.' Now they have two problems."**
> — *Jamie Zawinski*

Regular Expression (Regex) คือ "ภาษา" สำหรับกำหนด **รูปแบบ (Pattern)** ของข้อความครับ มันทรงพลังมากในการตรวจสอบข้อมูล (Validation) หรือค้นหาคำที่ซับซ้อน (Search & Replace) แต่ก็อ่านยากสุุดๆ เช่นกัน!

> **💡 Analogy (เปรียบเทียบ):**
> Regex เหมือน **"แม่พิมพ์ขนม"** หรือ **"ตะแกรงร่อนแป้ง"** ครับ:
> - คุณสร้างแม่พิมพ์รูป "ดาว" ขึ้นมา (`/pattern/`)
> - เอาไปปั๊มลงบนแป้ง (String)
> - ถ้าแป้งตรงไหนเข้ากับแม่พิมพ์ได้พอดี (`.test()` หรือ `.match()`) แปลว่า "ผ่าน"!

---

## 📚 MDN Reference
- [Regular Expressions](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Regular_Expressions)
- [RegExp.prototype.test()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/RegExp/test)

---

## 1. Creating Regex 🏗️

สร้างได้ 2 วิธี:
1.  **Regex Literal:** ใช้เครื่องหมาย `/` (นิยมที่สุด)
2.  **Constructor:** ใช้ `new RegExp()` (ใช้เมื่อ Pattern เป็นตัวแปร)

```javascript
// Literal: หาคำว่า "hello" (case-sensitive)
const regex1 = /hello/;

// Constructor
const regex2 = new RegExp("hello");
```

---

## 2. Basic Patterns & Flags 🚩

### Flags (ธงบอกพฤติกรรม)
ใส่ไว้หลัง `/` ตัวสุดท้าย
- `i` (Ignore Case): ไม่สนตัวเล็กตัวใหญ่
- `g` (Global): หา *ทุกคำ* ที่เจอ (ไม่ใช่แค่คำแรก)

```javascript
const str = "Hello hello HELLO";
const pattern = /hello/ig; // ไม่สน case, หาหมดเลย

console.log(str.match(pattern)); // ["Hello", "hello", "HELLO"]
```

### Character Classes (กลุ่มตัวอักษร)
- `.`: แทนตัวอะไรก็ได้ (1 ตัว)
- `\d`: ตัวเลข (0-9)
- `\w`: ตัวอักษร + ตัวเลข + `_` (Word character)
- `\s`: ช่องว่าง (Space, Tab, Newline)
- `[...]`: เลือกตัวใดตัวหนึ่งในวงเล็บ เช่น `[abc]` (a หรือ b หรือ c)
- `[^...]`: ห้ามเป็นตัวในวงเล็บ (Not)

### Quantifiers (จำนวน)
- `+`: มี 1 ตัวขึ้นไป
- `*`: มี 0 หรือมากกว่า
- `?`: มีหรือไม่มีก็ได้ (0 หรือ 1)
- `{n}`: มี n ตัวเป๊ะๆ
- `{n, m}`: มี n ถึง m ตัว

---

## 3. Common Methods 🛠️

### 3.1 `test()` (ตรวจสอบดูว่ามีไหม?)
คืนค่า `true`/`false` (ใช้ใน `if` บ่อยมาก)

```javascript
const emailPattern = /@/;
const email = "test@example.com";

if (emailPattern.test(email)) {
    console.log("Valid Email!");
}
```

### 3.2 `match()` (ดึงค่าออกมา)
คืนค่าเป็น Array ของสิ่งที่เจอ (หรือ `null` ถ้าไม่เจอ)

```javascript
const text = "Order ID: 12345, 67890";
const numberPattern = /\d+/g; // หาเลขทุกกลุ่ม

const numbers = text.match(numberPattern);
console.log(numbers); // ["12345", "67890"]
```

### 3.3 `replace()` (แทนที่)
ใช้ Regex เพื่อแทนที่คำที่ซับซ้อน

```javascript
const rude = "You are bad and bad!";
const polite = rude.replace(/bad/g, "good");

console.log(polite); // "You are good and good!"
```

---

## 4. Real-World Patterns 🌍

### 4.1 Simple Email Validation
```javascript
// อธิบาย: เริ่มต้นด้วยตัวอักษร(@) จบด้วย .ตัวอักษร(2-4ตัว)
const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;

console.log(emailRegex.test("user@domain.com")); // true
console.log(emailRegex.test("user@@domain"));    // false
```

### 4.2 Phone Number (Thailand)
```javascript
// ขึ้นต้นด้วย 0, ตามด้วยเลข 9 ตัว (รวม 10 ตัว)
const phoneRegex = /^0\d{9}$/;

console.log(phoneRegex.test("0812345678")); // true
console.log(phoneRegex.test("191"));        // false
```

> **Warning:** Regex ที่สมบูรณ์แบบ (Perfect Regex) นั้นซับซ้อนและยาวมาก! ในงานจริงถ้า Logic ยากเกินไป อาจใช้ Library อย่าง **Validator.js** หรือ **Zod** ช่วยแทนครับ

---

## 5. Challenges 🏆

### 🎯 Challenge 1: Find The Cat
**หัวข้อ:** Simple Pattern
**โจทย์:** เขียน Regex เพื่อหาคำว่า "cat" (ไม่สนตัวเล็กใหญ่) ในประโยค "I love Cat and cAt"
::: details ✨ ดูเฉลย
```javascript
const regex = /cat/ig;
console.log("I love Cat and cAt".match(regex)); // ["Cat", "cAt"]
```
:::

### 🎯 Challenge 2: Extract Price
**หัวข้อ:** Character Classes
**โจทย์:** ดึง "ตัวเลข" ออกจากข้อความ "Price is $500"
::: details ✨ ดูเฉลย
```javascript
const text = "Price is $500";
const price = text.match(/\d+/);
console.log(price[0]); // "500"
```
:::

### 🎯 Challenge 3: Validate Password
**หัวข้อ:** Validation
**โจทย์:** ตรวจสอบรหัสผ่านว่า:
1. มีความยาวอย่างน้อย 8 ตัว (`.{8,}`)
2. ลงท้ายด้วยตัวเลข (`\d$`)
::: details ✨ ดูเฉลย
```javascript
const passRegex = /.{8,}\d$/;
console.log(passRegex.test("mypassword1")); // true
console.log(passRegex.test("short1"));      // false
```
:::

---

## 📖 Glossary (คำศัพท์เทคนิค)

| คำศัพท์ | ความหมาย |
|:-------|:---------|
| **Pattern** | รูปแบบของ Regex ที่เขียนใน `/.../` |
| **Flag** | ตัวกำหนดพฤติกรรมเพิ่มเติม เช่น `i` (ignore case), `g` (global) |
| **Literal** | การเขียน Regex ตรงๆ ด้วย `/abc/` |
| **Wildcard (`.`)** | ตัวแทนของ "ตัวอักษรอะไรก็ได้" |
| **Escaping (`\`)** | การใส่ Backslash หน้าตัวอักษรพิเศษเพื่อให้มองเป็นตัวอักษรธรรมดา (เช่น `\.`) |
| **Anchor** | ตัวระบุตำแหน่ง: `^` (เริ่มต้น), `$` (สิ้นสุด) |

---

👉 **[ไปต่อ: 5.7 - JSON Fundamentals](/javascript/05-07-json-fundamentals)**
