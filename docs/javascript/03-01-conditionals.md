# 03-1: Conditionals (เงื่อนไข — สมองของโปรแกรม) 🧠

> **"Programs that cannot make decisions are programs that cannot do anything useful."**
> — *Eloquent JavaScript*

มนุษย์ตัดสินใจทุกวัน: "ถ้าฝนตก จะพกร่ม" "ถ้าหิว จะกินข้าว" โปรแกรมก็เช่นกัน — **เงื่อนไข** คือสิ่งที่ทำให้โค้ดของเรา **ฉลาด** และตอบสนองต่อสถานการณ์ต่างๆ ได้

> **💡 Analogy (เปรียบเทียบ):**
> คำสั่งเงื่อนไขเหมือน **"ทางแยก"** บนถนนครับ:
> - **`if`** = "ถ้าเลี้ยวซ้าย..."
> - **`else if`** = "ถ้าไม่ใช่ ลองเลี้ยวขวา..."
> - **`else`** = "ถ้าไม่ใช่ทั้งคู่ ตรงไปเลย"
> - **`switch`** = "วงเวียน (Roundabout)" ที่มีหลายทางออก ซี่งเลือกได้ทางเดียว

## 1. if / else if / else (พื้นฐาน) 🚦

ตาม [MDN](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/if...else):

### A. `if` เดี่ยว — ทำหรือไม่ทำ:

```javascript
const temperature = 35;

if (temperature > 30) {
    console.log("🌡️ อากาศร้อนมาก! เปิดแอร์เลย!");
}
// Output: "🌡️ อากาศร้อนมาก! เปิดแอร์เลย!"

// ⚠️ จำ: เงื่อนไขใน () จะถูกแปลงเป็น Boolean เสมอ!
// if (0)     → false (ไม่ทำ)
// if ("")    → false (ไม่ทำ)
// if (null)  → false (ไม่ทำ)
// if ("hi")  → true (ทำ!)
// if (42)    → true (ทำ!)
```

### B. `if...else` — ทำอย่างใดอย่างหนึ่ง:

```javascript
const age = 16;

if (age >= 18) {
    console.log("✅ คุณมีสิทธิ์ลงคะแนนเสียง");
} else {
    console.log(`❌ คุณต้องรออีก ${18 - age} ปี`);
}
// Output: "❌ คุณต้องรออีก 2 ปี"
```

### C. `if...else if...else` — หลายเงื่อนไข:

```javascript
const score = 85;

if (score >= 80) {
    console.log("🏆 เกรด A — ยอดเยี่ยม!");
} else if (score >= 70) {
    console.log("👍 เกรด B — ดีมาก!");
} else if (score >= 60) {
    console.log("👌 เกรด C — พอใช้");
} else if (score >= 50) {
    console.log("⚠️ เกรด D — ต้องปรับปรุง");
} else {
    console.log("❌ เกรด F — ไม่ผ่าน");
}
// Output: "🏆 เกรด A — ยอดเยี่ยม!"

// ⚠️ สำคัญ: เงื่อนไขถูกตรวจจากบนลงล่าง เจอตัวแรกที่ true จะหยุดเช็คทันที!
```

### Visual: Decision Flow

```
score = 85
     │
     ▼
score >= 80? ──YES──► "Grade A" ✅ (หยุดที่นี่!)
     │
    NO
     │
     ▼
score >= 70? ──YES──► "Grade B" (ไม่ถึง)
     │
    NO
     ▼
... (ไม่ถึง)
```

---

## 2. Ternary Operator (if/else แบบสั้น) ⚡

ตาม [MDN](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Conditional_operator) Ternary ใช้สำหรับ **เงื่อนไขง่ายๆ บรรทัดเดียว**:

```javascript
// Syntax: condition ? valueIfTrue : valueIfFalse

const age = 20;
const status = age >= 18 ? "ผู้ใหญ่" : "เด็ก";
console.log(status); // "ผู้ใหญ่"

// เทียบกับ if/else:
// let status;
// if (age >= 18) {
//     status = "ผู้ใหญ่";
// } else {
//     status = "เด็ก";
// }

// ✅ เหมาะกับ: กำหนดค่าตัวแปร, Template Literal
const greeting = `สวัสดี${age >= 18 ? "คุณ" : "น้อง"}!`;
console.log(greeting); // "สวัสดีคุณ!"

// ❌ ไม่เหมาะกับ: เงื่อนไขซับซ้อน หลายบรรทัด
// ❌ หลีกเลี่ยง Nested Ternary:
// const grade = score >= 80 ? "A" : score >= 60 ? "B" : "F"; // อ่านยากมาก!
```

### 📊 When to Use Ternary vs if/else

| สถานการณ์ | ใช้ Ternary? | ตัวอย่าง |
|:----------|:------------|:---------|
| **กำหนดค่า 1 ตัว (สั้น)** | ✅ ใช้เลย | `const x = a > b ? a : b;` |
| **ใน Template Literal** | ✅ สะดวกมาก | `` `Hello ${isVIP ? "VIP" : "Guest"}` `` |
| **Logic หลายบรรทัด** | ❌ ใช้ if/else | ฟังก์ชันที่มีหลายขั้นตอน |
| **เงื่อนไข 3+ ทาง** | ❌ ใช้ if/else | เกรด A/B/C/D/F |

---

## 3. The switch Statement (สวิตช์เลือกทาง) 🔀

ตาม [MDN](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/switch) `switch` เหมาะเมื่อ **เปรียบเทียบค่าเดียวกับหลายตัวเลือก**:

```javascript
const day = "Monday";

switch (day) {
    case "Monday":
    case "Tuesday":
    case "Wednesday":
    case "Thursday":
    case "Friday":
        console.log("📅 วันทำงาน");
        break;
    case "Saturday":
    case "Sunday":
        console.log("🎉 วันหยุด!");
        break;
    default:
        console.log("❌ ไม่ใช่วัน!");
}
// Output: "📅 วันทำงาน"
```

### ⚠️ อย่าลืม `break`! (Fall-through Trap)

```javascript
const color = "red";

// ❌ ลืม break — โค้ดจะ "ไหล" ลงไปข้อถัดไป!
switch (color) {
    case "red":
        console.log("🔴 Red"); // ✅ ตรง case
    case "green":
        console.log("🟢 Green"); // ❌ ไม่ตรง แต่ถูกรันเพราะไม่มี break!
    case "blue":
        console.log("🔵 Blue"); // ❌ ไม่ตรง แต่ถูกรันเพราะไม่มี break!
}
// Output: "🔴 Red" → "🟢 Green" → "🔵 Blue" ← ไหลหมดเลย!

// ✅ ใส่ break ทุก case!
switch (color) {
    case "red":
        console.log("🔴 Red");
        break; // ← หยุดที่นี่!
    case "green":
        console.log("🟢 Green");
        break;
    default:
        console.log("Unknown color");
}
// Output: "🔴 Red" ✅
```

### 📊 switch vs if/else — เมื่อไหร่ใช้อะไร?

| สถานการณ์ | `if/else` | `switch` |
|:----------|:---------|:---------|
| **เปรียบเทียบช่วง** (>, <, >=) | ✅ เหมาะ | ❌ ไม่เหมาะ |
| **เปรียบเทียบค่าเฉพาะ** (===) | ได้ | ✅ เหมาะมาก |
| **เงื่อนไข 2-3 ทาง** | ✅ เหมาะ | ❌ มากไป |
| **เงื่อนไข 4+ ทาง** (ค่าเฉพาะ) | ❌ ยาวเกินไป | ✅ เหมาะ |
| **Boolean condition** | ✅ เหมาะ | ❌ ไม่เหมาะ |

> **⚠️ หมายเหตุ:** `switch` ใช้ **Strict Equality (`===`)** ในการเปรียบเทียบ!
> ```javascript
> switch ("1") { case 1: console.log("Match!"); } // ไม่ Match! ("1" !== 1)
> ```

---

## 4. Guard Clauses (ดักกรณีพิเศษ — Best Practice!) 🛡️

**Guard Clause** คือ Pattern ที่ดักกรณีพิเศษไว้ข้างบน แล้ว `return` ออกทันที เพื่อลด **Nesting** (ซ้อน if/else หลายชั้น):

```javascript
// ❌ BAD: Nesting ลึก (อ่านยาก!)
function processOrder(order) {
    if (order) {
        if (order.items.length > 0) {
            if (order.paymentMethod) {
                if (order.total > 0) {
                    // ... Process order (ต้อง scroll ยาวมาก)
                    return "Order processed!";
                } else {
                    return "Invalid total";
                }
            } else {
                return "No payment method";
            }
        } else {
            return "No items in order";
        }
    } else {
        return "No order provided";
    }
}

// ✅ GOOD: Guard Clause (อ่านง่าย — ดักเงื่อนไขผิดไว้ข้างบน)
function processOrder(order) {
    if (!order) return "No order provided";
    if (order.items.length === 0) return "No items in order";
    if (!order.paymentMethod) return "No payment method";
    if (order.total <= 0) return "Invalid total";

    // ✅ Happy Path — ทำงานปกติ (ไม่มี Nesting!)
    return "Order processed!";
}
```

> **หลักการ:** ดัก "สิ่งผิดปกติ" ไว้ข้างบน แล้ว "สิ่งปกติ" อยู่ข้างล่าง — โค้ดอ่านง่ายขึ้น 10 เท่า!

---

## 5. Logical Operators in Conditions (Short-Circuit) ⚡

### A. `&&` (AND) — ถ้าซ้ายเป็น Falsy → หยุดทันที Return ซ้าย

```javascript
// Short-circuit: ถ้าเงื่อนไขแรก false → ไม่ต้องเช็คเงื่อนไขถัดไป
const user = null;
const name = user && user.name;
console.log(name); // null (ไม่ Error! เพราะหยุดที่ user ซึ่งเป็น null)

// Modern: Optional Chaining (ทำแบบเดียวกันแต่อ่านง่ายกว่า)
const name2 = user?.name;
console.log(name2); // undefined
```

### B. `||` (OR) — ถ้าซ้ายเป็น Truthy → หยุดทันที Return ซ้าย

```javascript
// ใช้เป็น Default Value:
const username = "" || "Guest";
console.log(username); // "Guest" ← ⚠️ "" เป็น Falsy!

// ⚠️ ปัญหา: ถ้า "" หรือ 0 เป็นค่าที่ถูกต้อง ก็จะถูก Override!
const port = 0 || 3000;
console.log(port); // 3000 ← 0 ถูก Override! (ไม่ต้องการ)
```

### C. `??` (Nullish Coalescing — ES2020) — ดีกว่า `||`!

```javascript
// ?? จะ fallback เฉพาะเมื่อซ้ายเป็น null หรือ undefined เท่านั้น
const username = "" ?? "Guest";
console.log(username); // "" ← ✅ ไม่ Override! ("" ไม่ใช่ null/undefined)

const port = 0 ?? 3000;
console.log(port); // 0 ← ✅ ไม่ Override! (0 ไม่ใช่ null/undefined)

const value = null ?? "Default";
console.log(value); // "Default" ✅

const value2 = undefined ?? "Default";
console.log(value2); // "Default" ✅
```

### 📊 `||` vs `??` Quick Reference

| ค่าซ้าย | `\|\|` result | `??` result |
|:---------|:-------------|:-----------|
| `null` | ✅ ใช้ค่าขวา | ✅ ใช้ค่าขวา |
| `undefined` | ✅ ใช้ค่าขวา | ✅ ใช้ค่าขวา |
| `0` | ✅ ใช้ค่าขวา (!) | ❌ ใช้ `0` |
| `""` | ✅ ใช้ค่าขวา (!) | ❌ ใช้ `""` |
| `false` | ✅ ใช้ค่าขวา (!) | ❌ ใช้ `false` |
| `"hello"` | ❌ ใช้ `"hello"` | ❌ ใช้ `"hello"` |

---

## 6. Challenges 🏆

ทดสอบความเข้าใจกับโจทย์ 5 ข้อ (1 ข้อต่อ 1 หัวข้อ):

### 🎯 Challenge 1: The Bouncer
**หัวข้อ:** 1. if / else if / else

**โจทย์:** เขียนฟังก์ชัน `checkAge(age)`:
- อายุ < 18: "ห้ามเข้า"
- อายุ 18-20: "เข้าได้แต่ห้ามดื่ม"
- อายุ 21+: "จัดไปวัยรุ่น"
::: details ✨ ดูเฉลย
```javascript
function checkAge(age) {
    if (age < 18) return "ห้ามเข้า";
    if (age <= 20) return "เข้าได้แต่ห้ามดื่ม";
    return "จัดไปวัยรุ่น";
}
```
:::

### 🎯 Challenge 2: Short & Sweet
**หัวข้อ:** 2. Ternary Operator

**โจทย์:** แปลงโค้ด `if/else` นี้ให้เป็น **Ternary Operator (บรรทัดเดียว)**:
```javascript
let status;
if (score >= 50) {
    status = "Pass";
} else {
    status = "Fail";
}
```
::: details ✨ ดูเฉลย
```javascript
const status = score >= 50 ? "Pass" : "Fail";
```
:::

### 🎯 Challenge 3: The Switch
**หัวข้อ:** 3. switch Statement

**โจทย์:** เขียน `switch` เพื่อแปลง `grade` ("A", "B", "C") เป็นคำชม:
- "A" → "Excellent"
- "B" → "Good"
- "C" → "Okay"
- อื่นๆ → "Unknown"
::: details ✨ ดูเฉลย
```javascript
switch (grade) {
    case "A": console.log("Excellent"); break;
    case "B": console.log("Good"); break;
    case "C": console.log("Okay"); break;
    default: console.log("Unknown");
}
```
:::

### 🎯 Challenge 4: Guard Your Code
**หัวข้อ:** 4. Guard Clauses

**โจทย์:** Refactor ฟังก์ชันนี้โดยใช้ **Guard Clause** เพื่อลด Nesting:
```javascript
function login(user) {
    if (user) {
        if (user.isVerified) {
            return "Welcome!";
        } else {
            return "Please verify email";
        }
    } else {
        return "No user found";
    }
}
```
::: details ✨ ดูเฉลย
```javascript
function login(user) {
    if (!user) return "No user found";
    if (!user.isVerified) return "Please verify email";
    return "Welcome!";
}
```
:::

### 🎯 Challenge 5: Logic Master
**หัวข้อ:** 5. Logical Operators

**โจทย์:** ใช้ `??` (Nullish Coalescing) เพื่อกำหนดค่า Default ให้ `username` เป็น "Anonymous" ถ้าค่าที่รับมาเป็น `null` หรือ `undefined` (แต่ถ้าเป็น `""` ให้ใช้ค่าว่างได้)
::: details ✨ ดูเฉลย
```javascript
const displayName = inputName ?? "Anonymous";
// ถ้า inputName = "" → displayName = "" (ถูกต้อง)
// ถ้าใช้ || จะได้ "Anonymous" (ผิดโจทย์)
```
:::

---

> **📖 คำศัพท์เทคนิค (Glossary):**
> *   **Conditional Statement:** คำสั่งที่ทำให้โค้ด "ตัดสินใจ" ทำสิ่งต่างๆ ตามเงื่อนไข
> *   **if/else:** คำสั่งเงื่อนไขพื้นฐาน ทำหรือไม่ทำตาม Boolean Expression
> *   **Ternary Operator (`? :`):** if/else แบบย่อ 1 บรรทัด เหมาะกับการกำหนดค่า
> *   **switch:** คำสั่งเปรียบเทียบค่าเดียวกับหลายตัวเลือก (ใช้ ===)
> *   **break:** คำสั่งหยุดไม่ให้ switch ไหลไป case ถัดไป
> *   **default:** กรณีเริ่มต้นใน switch เมื่อไม่ตรงกับ case ใดเลย
> *   **Fall-through:** พฤติกรรมที่ switch ไหลไป case ถัดไปเมื่อไม่มี break
> *   **Guard Clause:** Pattern ดักกรณีพิเศษแล้ว return ออกทันที เพื่อลด Nesting
> *   **Short-Circuit Evaluation:** พฤติกรรมที่ `&&`/`||` หยุดประเมินทันทีเมื่อรู้ผลลัพธ์แล้ว
> *   **Nullish Coalescing (`??`):** Operator ที่ return ค่าขวาเฉพาะเมื่อซ้ายเป็น null/undefined
> *   **Optional Chaining (`?.`):** Operator ที่เข้าถึง Property โดยไม่ Error ถ้า Object เป็น null/undefined
> *   **Nesting:** การซ้อน if/else หลายชั้น ทำให้โค้ดอ่านยาก

---
👉 **[ไปต่อ: 03-2 - Loops (ลูป)](/03-02-loops)**
