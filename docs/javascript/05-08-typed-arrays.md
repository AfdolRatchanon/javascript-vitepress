# 05-08 Typed Arrays & Buffers 💾

> **"JavaScript used to be about text. Now it's about raw bytes, pixels, and audio streams."**
> — *Modern Web Development*

ปกติ JavaScript จัดการตัวเลขแบบ **64-bit Floating Point** (ทศนิยม) เสมอ ซึ่งสะดวกแต่ "กินที่" (8 bytes ต่อตัวเลข 1 ตัว)
แต่พอโลกเว็บต้องการทำกราฟิก (WebGL), เสียง (Web Audio), หรืออ่านไฟล์ (File API) เราต้องการวิธีจัดการข้อมูลระดับ **Binary** (010101) ที่ประหยัดพื้นที่และแม่นยำ

นี่คือที่มาของ **Typed Arrays** และ **Buffers** ครับ!

> **💡 Analogy (เปรียบเทียบ):**
> - **ArrayBuffer:** เหมือน **"ก้อนดินน้ำมันดิบ"** (Raw Memory) ที่จองไว้เฉยๆ ยังเอาไปทำอะไรไม่ได้ อ่านไม่ออก
> - **Typed Array (View):** เหมือน **"แม่พิมพ์"** (Mold) ที่กดลงไปบนดินน้ำมันนั้น เพื่อบอกว่า "ตรงนี้คือเลขจำนวนเต็มนะ", "ตรงนี้คือทศนิยมนะ"
> คุณสามารถใช้แม่พิมพ์หลายแบบ (Views) กดลงบนดินน้ำมันก้อนเดียวกันได้!

---

## 📚 MDN Reference
- [Typed Arrays](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Typed_arrays)
- [ArrayBuffer](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/ArrayBuffer)
- [DataView](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/DataView)

---

## 1. ArrayBuffer (The Raw Data) 🧱

`ArrayBuffer` คือ object ที่ใช้จองพื้นที่ใน Memory (หน่วยเป็น Byte)
แต่มัน **อ่าน/เขียน โดยตรงไม่ได้!** ต้องผ่าน View เท่านั้น

```javascript
// จองพื้นที่ 16 Bytes
const buffer = new ArrayBuffer(16);

console.log(buffer.byteLength); // 16
// console.log(buffer[0]); // undefined! เข้าถึงตรงๆ ไม่ได้
```

---

## 2. Typed Arrays (The Views) 👓

Typed Array คือ Array พิเศษที่ล็อคชนิดข้อมูลไว้ชัดเจน (เช่น `Uint8` = จำนวนเต็มบวก 8 bit)

| Typed Array | Size (Bytes) | Range | เหมาะสำหรับ |
|:------------|:-------------|:------|:------------|
| `Int8Array` | 1 | -128 ถึง 127 | ตัวเลขขนาดเล็ก |
| `Uint8Array` | 1 | 0 ถึง 255 | สี (RGB), ข้อมูลไฟล์ (Byte Stream) |
| `Uint8ClampedArray` | 1 | 0 ถึง 255 | Canvas Image Data (ปัดค่าเกินให้อยู่ในกรอบ) |
| `Int16Array` | 2 | -32,768 ถึง 32,767 | เสียง (Audio Samples) |
| `Float32Array` | 4 | ทศนิยม 7 หลัก | พิกัดกราฟิก (WebGL) |

### การใช้งาน
```javascript
const buffer = new ArrayBuffer(4); // จอง 4 Bytes

// สร้าง View แบบ Uint8 (0-255) บน Buffer นี้
const view8 = new Uint8Array(buffer);

view8[0] = 255;
view8[1] = 10;
view8[2] = 0;
// view8[3] จะเป็น 0 โดยอัตโนมัติ (Default)

console.log(view8); // Uint8Array(4) [255, 10, 0, 0]

// ลองใส่ค่าเกิน?
view8[0] = 300; 
console.log(view8[0]); // 44 (เพราะ 300 % 256 = 44) → Overflow!
```

---

## 3. Multiple Views on Same Data 👀

ความเจ๋งคือเรามองข้อมูลชุดเดียว (Buffer เดียว) ได้หลายแบบ!

```javascript
const buffer = new ArrayBuffer(4);

const uint8 = new Uint8Array(buffer); // มองทีละ 1 byte (มี 4 ช่อง)
const uint32 = new Uint32Array(buffer); // มองทีละ 4 bytes (มี 1 ช่อง)

uint8[0] = 1; // 00000001
uint8[1] = 0; // 00000000
uint8[2] = 0; // 00000000
uint8[3] = 0; // 00000000

console.log(uint8);  // [1, 0, 0, 0]
console.log(uint32); // [1] (เพราะอ่านทั้ง 4 bytes รวมเป็นเลขเดียว)
```

> **Little-Endian vs Big-Endian:**
> คอมพิวเตอร์ส่วนใหญ่ (Intel/AMD/ARM) เป็น **Little-Endian** (เอา Byte น้อยขึ้นก่อน) ทำให้ `[1, 0, 0, 0]` ถูกอ่านค่าเป็น `1` ไม่ใช่ `16777216` (`[0, 0, 0, 1]`)

---

## 4. Typed Array vs Normal Array 🥊

| Feature | Typed Array 💾 | Normal Array 📦 |
|:--------|:---------------|:----------------|
| **Data Type** | ชนิดเดียว (Homogeneous) | ผสมกันได้ (Heterogeneous) |
| **Size** | ความยาวคงที่ (Fixed Length) | ยืดหดได้ (Dynamic) |
| **Performance** | เร็วมาก (ใกล้เคียง Native Code) | ช้ากว่า (เพราะต้องเช็ค Type ตลอด) |
| **Methods** | `map`, `filter`, `reduce`, etc. | มีเหมือนกัน (ยกเว้น `push`/`pop`) |

### When to use? (ใช้อันไหนดี?)
- **งานทั่วไป:** ใช้ **Normal Array** (`[]`) สะดวกสุด
- **งานเฉพาะทาง:** ใช้ **Typed Array** เมื่อยุ่งกับ WebGL, Canvas, File Upload, หรือ WebSocket ที่ส่ง Binary Data

---

## 5. Challenges 🏆

### 🎯 Challenge 1: The Color Maker
**หัวข้อ:** Uint8Array
**โจทย์:** สีในคอมพิวเตอร์ (RGBA) ใช้ 4 bytes (Red, Green, Blue, Alpha) จงสร้าง `Uint8Array` ขนาด 4 ช่อง เพื่อแทนสี "แดงทึบ" (R=255, G=0, B=0, A=255)
::: details ✨ ดูเฉลย
```javascript
const color = new Uint8Array([255, 0, 0, 255]);
console.log(color);
```
:::

### 🎯 Challenge 2: The Overflow
**หัวข้อ:** Data Limit
**โจทย์:** จะเกิดอะไรขึ้นถ้าเราใส่ค่า `128` ลงใน `Int8Array` (รับได้ -128 ถึง 127)? ลองเขียนโค้ดพิสูจน์
::: details ✨ ดูเฉลย
```javascript
const int8 = new Int8Array(1);
int8[0] = 128;
console.log(int8[0]); // -128 (เกิด Wrap around หรือ Overflow ไปฝั่งลบ)
```
:::

### 🎯 Challenge 3: Converter
**หัวข้อ:** Views
**โจทย์:** สร้าง `Uint16Array` (เก็บได้สูงสุด 65535) ที่มีสมาชิก 1 ตัว คือค่า `256` แล้วลองอ่านค่านั้นผ่าน `Uint8Array` ดูว่าจะเห็นเป็นเลขอะไรบ้าง?
*(ใบ้: 256 ในฐานสองคือ `00000001 00000000`)*
::: details ✨ ดูเฉลย
```javascript
const buffer = new ArrayBuffer(2);
const view16 = new Uint16Array(buffer);
view16[0] = 256;

const view8 = new Uint8Array(buffer);
console.log(view8); // [0, 1] (บน Little-Endian system)
// เพราะ 256 แบ่งเป็น 2 bytes คือ 0 (Low byte) และ 1 (High byte)
```
:::

---

## 📖 Glossary (คำศัพท์เทคนิค)

| คำศัพท์ | ความหมาย |
|:-------|:---------|
| **Binary Data** | ข้อมูลระดับเลขฐานสอง (0/1) ที่คอมพิวเตอร์เข้าใจโดยตรง |
| **Buffer** | พื้นที่พักข้อมูลชั่วคราวใน Memory |
| **Overflow** | การใส่ค่าเกินขอบเขตที่ตัวแปรรับได้ จนค่าวนกลับไปเริ่มใหม่ |
| **Endianness** | ลำดับการเรียง Byte (ตัวเลขหลักน้อยขึ้นก่อน หรือหลักมากขึ้นก่อน) |
| **View** | อินเทอร์เฟซสำหรับอ่าน/เขียนข้อมูลใน Buffer ในรูปแบบต่างๆ |

---

👉 **[ไปต่อ: Project 6 - Data Cleanser CLI](/javascript/05-project-data-cleanser)**
