# 02 - Variables & Data Types (ตัวแปรและชนิดข้อมูล)

> **References & Standards:**
> *   [MDN: Grammar and types](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Grammar_and_types)
> *   [MDN: Data structures](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Data_structures)

## 1. Variables (ตัวแปร)
ตัวแปร (Variable) คือ "กล่องเก็บข้อมูล" ที่เราตั้งชื่อแปะไว้ เพื่อจะได้เรียกใช้งานภายหลังได้

### การประกาศตัวแปร (Declaration)
ใน Modern JS (ES6+) เรามี 3 คำสั่งหลัก:

1.  **`let`**: ใช้สำหรับค่าที่ **เปลี่ยนได้** (Reassignable)
2.  **`const`**: ใช้สำหรับค่าที่ **ห้ามเปลี่ยน** (Constant) - *แนะนำให้ใช้เป็นค่าเริ่มต้น*
3.  **`var`**: แบบเก่า (Legacy) - *ไม่แนะนำให้ใช้ ถ้าไม่มีเหตุจำเป็น*

```javascript
let score = 100;
score = 200; // ✅ OK

const pi = 3.14;
pi = 3.15; // ❌ TypeError: Assignment to constant variable.

var old = "Legacy";
```

### 🔬 Deep Dive: `var` vs `let` vs `const`
ความแตกต่างทางเทคนิคที่ลึกซึ้ง (Technical Differences):

| Feature | `var` | `let` | `const` |
| :--- | :--- | :--- | :--- |
| **Scope** | Function Scope | Block Scope `{}` | Block Scope `{}` |
| **Reassign** | ✅ Yes | ✅ Yes | ❌ No |
| **Hoisting** | ✅ Yes (undefined) | ❌ TDZ (Error) | ❌ TDZ (Error) |
| **Global Property** | ✅ Yes (window.x) | ❌ No | ❌ No |

> **ศัพท์เทคนิค:**
> *   **Block Scope:** ตัวแปรจะอยู่แค่ในปีกกา `{ ... }` เท่านั้น (เช่นใน if, for)
> *   **Hoisting:** การที่ตัวแปรถูก "ยก" ขึ้นไปประกาศด้านบนสุดของ Scope โดยอัตโนมัติ
> *   **TDZ (Temporal Dead Zone):** พื้นที่มรณะระหว่างจุดเริ่ม Scope จนถึงบรรทัดประกาศตัวแปร ถ้าเรียกใช้ก่อนจะ Error

## 2. Data Types (ชนิดข้อมูล)
JS เป็นภาษา **Dynamic Types** (ไม่ต้องบอกประเภท) แต่ค่าทุกค่ามีประเภทของมัน แบ่งเป็น 2 กลุ่มใหญ่:

### A. Primitive Types (พื้นฐาน - เก็บค่าใน Stack)
มี 7 ชนิด ข้อมูลจะถูกเก็บค่าโดยตรง (Passed by Value)
1.  **String**: ข้อความ (`"Note"`, `'Note'`, `` `Note` ``)
2.  **Number**: ตัวเลขทั้งจำนวนเต็มและทศนิยม (`10`, `3.14`)
    *   *Note:* ระวัง `NaN` (Not a Number) ที่ถือเป็นชนิด Number!
3.  **BigInt**: เลขจำนวนเต็มขนาดใหญ่มาก (`9007199254740991n`)
4.  **Boolean**: ค่าความจริง (`true`, `false`)
5.  **Undefined**: ตัวแปรที่ประกาศแล้วแต่ยังไม่กำหนดค่า
6.  **Null**: ค่าว่างที่ตั้งใจกำหนดให้ว่าง
7.  **Symbol**: ค่าที่มีเอกลักษณ์เฉพาะตัว (Unique)

### B. Object Types (ซับซ้อน - เก็บ Reference)
เช่น `Object`, `Array`, `Function` ข้อมูลจะถูกเก็บใน **Heap** ตัวแปรจะเก็บแค่ "ที่อยู่" (Reference/Pointer) เท่านั้น

```javascript
// Primitive (Copy Value)
let a = 10;
let b = a;
b = 20;
console.log(a); // 10 (a ไม่เปลี่ยน)

// Object (Copy Reference)
let x = { value: 10 };
let y = x; // y ชี้ไปที่เดียวกับ x
y.value = 20;
console.log(x.value); // 20 (x เปลี่ยนด้วย! 😱)
```

## 3. Exercises: Memory Master

### Level 1: Swap Logic (Easy)
จงเขียนโค้ดสลับค่าของตัวแปร `a` และ `b` โดยห้ามพิมพ์บรรทัด `a = 5` หรือ `b = 10` ตรงๆ
```javascript
let a = 10;
let b = 5;

// Write code here to swap values

console.log(a); // Expected: 5
console.log(b); // Expected: 10
```

### Level 2: Constant Object mutation (Medium)
ทำไมโค้ดนี้ถึง **ไม่ Error**? ทั้งที่เราใช้ `const`?
```javascript
const user = { name: "Alice" };
user.name = "Bob"; // ✅ Works!
console.log(user.name);
```
*   **Task:** อธิบายเหตุผลโดยใช้ความรู้เรื่อง Stack vs Heap

### Level 3: The Hoisting Trap (Hard) 😈
จงทำนายผลลัพธ์ของ Code ต่อไปนี้ **โดยห้ามรันก่อนเทาย**:

```javascript
console.log(a);
var a = 5;
console.log(a);

console.log(b);
let b = 10;
```
*   **Task:** อันไหนจะได้ `undefined`? อันไหนจะ `Error`? เพราะอะไร?

### Level 4: Type Coercion Challenge (Challenge) 🏆
(โจทย์ปราบเซียน) จงทายผลลัพธ์ของนิพจน์ต่อไปนี้:
1.  `"5" + 2`
2.  `"5" - 2`
3.  `[] + []`

*Note: เรื่อง Type Coercion อาจจะยังไม่ได้สอนละเอียด แต่ลองค้นหาดู (Research) หรือทดลองรันดูครับ*

---
👉 **[ดูเฉลย (Solutions)](/solutions/02-sol)**
