# 02-05 Memory Management 🧠

> **"Memory is a terrible thing to waste."**
> — *Anonymous* (Often attributed to the United Negro College Fund slogan)

การจัดการหน่วยความจำ (Memory Management) คือ "หัวใจ" ของการเขียนโปรแกรมที่มีประสิทธิภาพครับ แม้ JavaScript จะจัดการ Memory ให้เราแบบอัตโนมัติ (Automatic Garbage Collection) แต่ถ้าเราไม่เข้าใจกลไกเบื้องหลัง เราอาจสร้างโปรแกรมที่กินแรมมหาศาล (Memory Leak) โดยไม่รู้ตัว โดยเฉพาะเมื่อทำ Backend ด้วย Node.js ที่ต้องรันตลอดเวลา 24/7!

> **💡 Analogy (เปรียบเทียบ):**
> ลองจินตนาการว่า Memory ของคอมพิวเตอร์คือ **"โต๊ะทำงาน"** ครับ:
> - **Stack Memory:** เหมือน **"ชั้นวางเอกสารตั้งโต๊ะ"** (Tray) ที่วางซ้อนๆ กันได้ เป็นระเบียบ หยิบใช้ง่าย แต่มีพื้นที่จำกัด ใช้เก็บข้อมูลเล็กๆ ที่รู้ขนาดแน่นอน (เช่น นามบัตร, ปากกา)
> - **Heap Memory:** เหมือน **"พื้นที่ว่างบนโต๊ะ"** (Space) ขนาดใหญ่ ที่คุณจะโยนแฟ้มหนาๆ หรือกล่องพัสดุขนาดใหญ่ลงไปวางตรงไหนก็ได้ มันยืดหยุ่นกว่า แต่หาของยากกว่า และถ้าไม่เก็บกวาด โต๊ะก็จะรกจนไม่มีที่ทำงานครับ!

---

## 📚 MDN Reference
เนื้อหาในบทนี้อ้างอิงจากมาตรฐาน MDN Web Docs:
- [Memory Management](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Memory_Management)

---

## 1. Stack vs Heap Memory 🏗️

ก่อนจะไปดูโค้ด เราต้องเข้าใจก่อนว่า JavaScript Engine (V8) เก็บข้อมูลไว้ที่ไหนบ้างหลักๆ 2 ที่ครับ:

### 1.1 Stack (สแต็ก)
- **ลักษณะ:** เป็นโครงสร้างข้อมูลแบบ LIFO (Last In, First Out)
- **เก็บอะไร:** 
    - **Primitive Data Types** (String, Number, Boolean, null, undefined, Symbol, BigInt)
    - **Reference Address** (ที่อยู่ของ Object ใน Heap)
- **ความเร็ว:** เร็วมาก เพราะเข้าถึงข้อมูลตามลำดับ
- **การจัดการ:** สร้างและลบอัตโนมัติเมื่อจบ Function Scope

### 1.2 Heap (ฮีป)
- **ลักษณะ:** เป็นพื้นที่หน่วยความจำขนาดใหญ่ที่ไม่มีโครงสร้างตายตัว
- **เก็บอะไร:** **Reference Data Types** (Object, Array, Function)
- **ความเร็ว:** ช้ากว่า Stack เพราะต้องค้นหาที่อยู่
- **การจัดการ:** ต้องรอ Garbage Collector มาเก็บกวาด

```javascript
// 1. Primitives (Stack)
let age = 25;        // เก็บค่า 25 ใน Stack
let name = "John";   // เก็บค่า "John" ใน Stack

// 2. References (Heap)
let user = {         // ตัวแปร 'user' เก็บ "ที่อยู่" (pointer) ใน Stack
    id: 1,           // ตัว Object จริงๆ ถูกสร้างใน Heap
    role: "Admin"
};
```

> ⚠️ **Key Takeaway:** ตัวแปรที่เก็บ Object (`user`) ไม่ได้เก็บข้อมูล Object ไว้กับตัวมันเอง แต่มันเก็บ **"ลายแทง" (Reference/Pointer)** ที่ชี้ไปยังตำแหน่งของ Object ใน Heap ครับ

---

## 2. Pass-by-Value vs Pass-by-Reference 🔄

นี่คือจุดที่ทำให้หลายคนงงและเกิด Bug มากที่สุดครับ! การส่งค่าตัวแปร (Primitive vs Object) มีพฤติกรรมต่างกันโดยสิ้นเชิง

### 2.1 Pass-by-Value (สำหรับ Primitives)
เมื่อเรา Copy ตัวแปร Primitive หรือส่งเข้า Function มันจะ **"ก๊อปปี้ค่าใหม่"** แยกจากตัวเดิม 100%

```javascript
let a = 10;
let b = a;  // ก๊อปปี้ค่า 10 จาก a ไปใส่ b

b = 20;     // เปลี่ยนค่า b เป็น 20

console.log(a); // 10 (a ไม่เปลี่ยน!)
console.log(b); // 20
```
**คำอธิบาย:** การแก้ `b` ไม่กระทบ `a` เพราะมันอยู่คนละที่ใน Stack เหมือนถ่ายเอกสารกระดาษ แล้วเราไปขีดเขียนใบที่ถ่ายมา ใบต้นฉบับก็ยังเหมือนเดิมครับ

### 2.2 Pass-by-Reference (สำหรับ Objects/Arrays)
เมื่อเรา Copy ตัวแปร Object มันจะ **"ก๊อปปี้แค่ลายแทง (Reference)"** ครับ! ทั้งสองตัวแปรจะชี้ไปที่ Object เดียวกันใน Heap

```javascript
let user1 = { name: "Alice" };
let user2 = user1; // ก๊อปปี้ "ลายแทง" (Reference)

user2.name = "Bob"; // แก้ข้อมูลผ่าน user2

console.log(user1.name); // "Bob" (เฮ้ย! user1 เปลี่ยนด้วย?!)
console.log(user2.name); // "Bob"
```

**คำอธิบาย:**
1. `user1` ถือลายแทงที่ชี้ไปบ้านหลังหนึ่งใน Heap
2. `user2 = user1` คือการก๊อปปี้ลายแทงนั้นมา
3. พอ `user2` เดินตามลายแทงไปทาสีบ้านใหม่เป็นสีแดง (`name = "Bob"`)
4. `user1` เดินตามลายแทงเดิมมาดูบ้าน ก็จะเห็นว่าบ้านกลายเป็นสีแดงไปแล้ว!

### 📊 Comparison Table: Value vs Reference

| คุณสมบัติ | Primitives (Strings, Numbers...) | References (Objects, Arrays) |
|:----------|:---------------------------------|:-----------------------------|
| **Memory** | เก็บค่าใน **Stack** | เก็บค่าจริงใน **Heap** (Stack เก็บ Pointer) |
| **Assignment** | Copy ค่าใหม่แยกอิสระ | Copy Address (ชี้ไปที่เดียวกัน) |
| **Comparison (`===`)** | เทียบที่ **ค่า (Value)** | เทียบที่ **ที่อยู่ (Address)** |
| **Side Effect** | ปลอดภัย (Immutable) | ระวังแก้แล้วกระทบตัวอื่น (Mutable) |

```javascript
// การเปรียบเทียบ
let num1 = 5;
let num2 = 5;
console.log(num1 === num2); // true (ค่าเท่ากัน)

let obj1 = { id: 1 };
let obj2 = { id: 1 };
console.log(obj1 === obj2); // false! (เพราะอยู่คนละบ้าน คนละ Address)

let obj3 = obj1;
console.log(obj1 === obj3); // true (ชี้ไปที่เดียวกัน)
```

---

## 3. Garbage Collection (GC) 🧹

ในภาษา C หรือ C++ เราต้องจองแรม (`malloc`) และคืนแรม (`free`) เอง แต่ใน JavaScript มี **Garbage Collector (GC)** คอยทำงานให้เราครับ

### หลักการ: Reachability (การเข้าถึงได้)
GC จะมองว่าข้อมูลไหน "ยังจำเป็น" โดยดูว่า **"มันยังเข้าถึงได้จาก Root หรือไม่?"**
- **Root** คือจุดเริ่มต้น เช่น Global Variables หรือ Function ที่กำลังทำงานอยู่
- ถ้าข้อมูลไหนไม่มีใครเชื่อมโยงถึง (Unreachable) = **ขยะ (Garbage)** GC จะมาเก็บกวาดทิ้งเพื่อคืนแรม

### เครื่องมือของ V8: Mark-and-Sweep Algorithm
1. **Mark:** GC จะเริ่มจาก Root และเดินตาม Reference ไปเรื่อยๆ แล้ว "แปะป้าย" (Mark) ทุก Object ที่เจอว่า "ยังมีชีวิต" (Reachable)
2. **Sweep:** หลังจาก Mark เสร็จ Object ไหนที่ **"ไม่มีป้ายแปะ"** จะถือว่าเป็นขยะ และถูกลบออกจาก Heap ทันที

```javascript
let family = {
    father: { name: "Dad" },
    mother: { name: "Mom" }
};

// ตอนนี้ทั้ง Dad และ Mom ยังเชื่อมโยงกับ 'family' (Reachable)

delete family.father; 
// ตอนนี้ Object name: "Dad" ถูกตัดขาดแล้ว! ไม่มีใครชี้ถึง
// GC จะเห็นและลบทิ้งในรอบถัดไป
```

---

## 4. Common Memory Leaks ⚠️

แม้จะมี GC แต่ถ้าเราเขียนโค้ดไม่ดี GC ก็อาจจะไม่กล้าลบขยะทิ้ง เพราะเข้าใจผิดว่าเรายังใช้อยู่ นี่คือที่มาของ **Memory Leak** (แรมรั่ว) ครับ

### 4.1 Global Variables ที่ถูกลืม
```javascript
function createLeak() {
    leak = "I am a global variable!"; // ลืมใส่ let/const/var
}
// พอจบฟังก์ชัน ตัวแปรนี้ไม่หายไป เพราะมันไปเกาะอยู่ที่ window (ใน browser) หรือ global (ใน node)
```

### 4.2 Forgotten Timer / Interval
```javascript
const bigData = new Array(10000).fill("data");

setInterval(() => {
    // Callback นี้อ้างอิงถึง bigData
    console.log(bigData[0]); 
}, 1000);

// ตราบใดที่ Interval ยังเดิน bigData จะไม่มีวันถูก GC เก็บกวาด!
// ทางแก้: ต้อง clearInterval() เมื่อไม่ใช้
```

### 4.3 Closures (บางกรณี)
ถ้า Function ลูกอ้างอิงตัวแปรแม่มหาศาล แล้ว Function ลูกถูกเก็บไว้นานๆ ตัวแปรแม่ก็จะค้างอยู่ในแรมนานตามไปด้วย

---

## 🌎 Real-World Use Case: Backend Server Crash!

สมมติคุณทำ API ด้วย Node.js และเก็บข้อมูล Session ของ User ทุกคนไว้ในตัวแปร Array (Global Variable) แทนที่จะใช้ Database หรือ Redis:

```javascript
// ❌ Don't do this in production!
const sessions = []; 

app.post('/login', (req, res) => {
    const user = { id: Date.now(), ...req.body, hugeData: new Array(1000) };
    sessions.push(user); // เก็บใส่ Array นี้ตลอดไป ไม่เคยลบ
    res.send("Logged in");
});

// ผลลัพธ์: ยิ่งคน Login เยอะ Array 'sessions' จะโตขึ้นเรื่อยๆ
// จน RAM หมด Server ก็จะ Crash (Out of Memory) ในที่สุด! 💥
```
**Solution:**
- อย่าเก็บ State ใหญ่ๆ ไว้ในตัวแปร Global
- ให้ใช้ External Store เช่น Redis หรือ Database
- ถ้าจำเป็นต้องเก็บ (เช่น Cache) ต้องมีกลไกการลบ (Expiration) หรือจำกัดขนาด (Limit Size)

---

## 5. Challenges 🏆

ทดสอบความเข้าใจเรื่อง Memory กันหน่อยครับ

### 🎯 Challenge 1: Reference Detective
**โจทย์:** วิเคราะห์โค้ดนี้แล้วตอบว่า `product.price` คือเท่าไหร่?

```javascript
let product = { name: "Laptop", price: 1000 };
let cartItem = product;

product.price = 1200;
cartItem.price = 900;

console.log(product.price); // ???
```
::: details ✨ ดูเฉลย
**ตอบ:** `900`
**เหตุผล:** `product` และ `cartItem` ชี้ไปที่ Object ตัวเดียวกันใน Heap ตอนแรกแก้เป็น 1200 ผ่าน `product` แต่บรรทัดต่อมาแก้เป็น 900 ผ่าน `cartItem` ดังนั้นค่าล่าสุดคือ 900 ครับ
:::

### 🎯 Challenge 2: Pure Function vs Mutation
**โจทย์:** เขียนฟังก์ชัน `updateUser(user)` ที่รับ Object user เข้ามา และคืนค่า Object ใหม่ที่มีชื่อเป็นตัวพิมพ์ใหญ่ (Uppercase) **โดยห้ามแก้ไข Object ต้นฉบับ (Immutability)**

```javascript
const john = { name: "john", age: 25 };

function updateUser(u) {
    // เขียนโค้ดตรงนี้... ห้ามแก้ u นะ!
}

const newJohn = updateUser(john);
console.log(john.name);     // ต้องได้ "john"
console.log(newJohn.name);  // ต้องได้ "JOHN"
```
::: details ✨ ดูเฉลย
```javascript
function updateUser(u) {
    // วิธี 1: ใช้ Spread Syntax (ES6) - แนะนำ!
    return { ...u, name: u.name.toUpperCase() };

    // วิธี 2: Object.assign
    // return Object.assign({}, u, { name: u.name.toUpperCase() });
}
// การทำแบบนี้จะสร้าง Object ใหม่ใน Heap ไม่ไปยุ่งกับของเดิมครับ
```
:::

### 🎯 Challenge 3: Find the Leak
**โจทย์:** โค้ดนี้มีโอกาสเกิด Memory Leak ตรงไหน?

```javascript
function startTracking() {
    const hugeData = new Array(1000000).fill("*");
    
    window.addEventListener('resize', () => {
        console.log(hugeData.length);
    });
}
startTracking();
// ผู้ใช้งานเปลี่ยนหน้าไปหน้าอื่น (SPA Route change)
```
::: details ✨ ดูเฉลย
**ตอบ:** Event Listener `resize`
**เหตุผล:** Even Listener ผูกติดกับ `window` ซึ่งเป็น Global Root มันจะถือ Reference ของ Function Callback ไว้ และ Callback นั้นดันไป Closure เอา `hugeData` ไว้ ทำให้ `hugeData` (ล้านตัว) ไม่ถูก GC เก็บกวาด แม้เราจะเปลี่ยนหน้าไปแล้วก็ตาม (ใน SPA)
**ทางแก้:** ต้อง `removeEventListener` เมื่อ Component Unmount หรือเลิกใช้งานครับ
:::

---

## 📖 Glossary (คำศัพท์เทคนิค)

| คำศัพท์ | ความหมาย |
|:-------|:---------|
| **Stack Memory** | หน่วยความจำแบบซ้อนทับ ทำงานเร็ว ใช้เก็บ Primitives และ Pointers |
| **Heap Memory** | หน่วยความจำขนาดใหญ่ ปราศจากโครงสร้าง ใช้เก็บ Objects และ Arrays |
| **Pointer / Reference** | ค่าที่อยู่ใน Stack ซึ่งทำหน้าที่ชี้ตำแหน่งของข้อมูลจริงใน Heap |
| **Pass-by-Value** | การส่งค่าแบบก๊อปปี้ข้อมูลไปใหม่ ตัดขาดจากตัวเดิม (ใช้กับ Primitives) |
| **Pass-by-Reference** | การส่งค่าแบบก๊อปปี้แค่ที่อยู่ ข้อมูลจริงยังแชร์กัน (ใช้กับ Objects) |
| **Garbage Collection (GC)** | กระบวนการอัตโนมัติของ Engine เพื่อคืนหน่วยความจำที่ไม่ได้ใช้แล้ว |
| **Reachability** | หลักการของ GC ที่ดูว่าข้อมูลยัง "เข้าถึงได้" จาก Root หรือไม่ |
| **Memory Leak** | อาการที่โปรแกรมไม่ยอมคืนหน่วยความจำที่เลิกใช้แล้ว ทำให้เครื่องอืดหรือค้าง |
| **Mutable** | ข้อมูลที่สามารถเปลี่ยนแปลงไส้ในได้หลังสร้าง (เช่น Object, Array) |
| **Immutable** | ข้อมูลที่ไม่สามารถเปลี่ยนแปลงได้ ถ้าจะแก้ต้องทำสำเนาใหม่ (เช่น String, Number) |

---

👉 **[ไปต่อ: Module 3 - Control Flow (Conditionals)](/javascript/03-01-conditionals)**
