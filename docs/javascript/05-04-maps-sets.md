# 05-04 Maps & Sets 🗺️

> **"If you need a key that isn't a string, or you need to know the size of your collection efficiently, stop using Objects. Use a Map."**
> — *MDN Web Docs*

ในอดีต (ก่อน ES6) เราใช้ `Object` เก็บข้อมูลแบบ Key-Value และใช้ `Array` เก็บรายการของ แต่พวกมันมีข้อจำกัด:
- `Object` ใช้ Key ได้แค่ String/Symbol เท่านั้น และไม่มีวิธีนับจำนวนสมาชิก (`.length`) ที่ง่าย
- `Array` เก็บค่าซ้ำได้ และการค้นหาข้อมูล (Search) ช้าถ้าข้อมูลเยอะ ($O(n)$)

ES6 เลยส่ง **Map** และ **Set** มาช่วยแก้ปัญหาพวกนี้ครับ!

> **💡 Analogy (เปรียบเทียบ):**
> - **Map:** เหมือน **"ตู้ล็อคเกอร์อัจฉริยะ"** ที่คุณจะเอา *อะไร* ไปแปะหน้าตู้เป็นกุญแจก็ได้ (ไม่จำเป็นต้องเป็นป้ายชื่อ) เช่น เอารองเท้าแปะหน้าตู้เพื่อระบุว่าเป็นตู้เก็บรองเท้า
> - **Set:** เหมือน **"กระเป๋าโดเรมอนที่คัดของซ้ำทิ้ง"** ถ้าคุณโยนแอปเปิ้ลลงไป 3 ลูก มันจะเหลือแอปเปิ้ลแค่ลูกเดียวในกระเป๋า (Unique Collection)

---

## 📚 MDN Reference
- [Map](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Map)
- [Set](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Set)
- [WeakMap](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/WeakMap)

---

## 1. Map (The Better Object) 🗺️

`Map` คือคลังเก็บข้อมูลแบบ Key-Value ที่ "เหนือกว่า" Object ทั่วไปตรงที่:
1. **Key เป็นอะไรก็ได้** (Object, Function, Number) — Object ปกติใช้ได้แค่ String
2. **รักษาลำดับ** (Insertion Order) — Object ไม่การันตีลำดับ
3. **หาขนาดได้ทันที** (`.size`) — Object ต้องวนนับหรือใช้ `Object.keys().length`

### การใช้งานพื้นฐาน

```javascript
const myMap = new Map();

// 1. Set (เพิ่มค่า)
myMap.set("name", "Dolar");       // String Key
myMap.set(1, "Number One");       // Number Key
myMap.set(true, "Boolean Key");   // Boolean Key

const objKey = { id: 1 };
myMap.set(objKey, "Object Value"); // 💥 Object Key! (ทำไม่ได้ใน Object ปกติ)

// 2. Get (ดึงค่า)
console.log(myMap.get("name"));   // "Dolar"
console.log(myMap.get(objKey));   // "Object Value"

// 3. Check & Delete
console.log(myMap.has(1));        // true
myMap.delete(1);
console.log(myMap.size);          // 3
```

### Map vs Object: ใช้อะไรดี?

| Feature | Map 🗺️ | Object 📦 |
|:--------|:-------|:----------|
| **Key Type** | อะไรก็ได้ (Any) | String / Symbol |
| **Order** | ตามลำดับที่ใส่ (Insertion) | ไม่การันตี |
| **Size** | `.size` (เร็วมาก) | ต้องนับเอง (ช้า) |
| **Performance** | เร็วกว่า (ถ้ารับส่งข้อมูลบ่อย) | เร็วกว่า (ถ้าข้อมูลน้อยและคงที่) |
| **JSON** | แปลง JSON ยากกว่า | แปลง JSON ง่าย (`JSON.stringify`) |

> **คำแนะนำ:** ถ้าข้อมูลของคุณเป็น Structure คงที่ (เช่น User Profile) ใช้ **Object** แต่ถ้าเป็น Dictionary ที่มีการเพิ่มลบตลอดเวลา และ Key อาจไม่ใช่ String ให้ใช้ **Map**

---

## 2. Set (Unique Collection) 🦄

`Set` คือ Array ที่ **"ห้ามมีสมาชิกซ้ำ"** (Unique Values Only)

### การใช้งานพื้นฐาน

```javascript
const mySet = new Set();

// 1. Add (เพิ่มค่า)
mySet.add(1);
mySet.add(5);
mySet.add(5); // ❌ ไม่เข้า! (ซ้ำ)
mySet.add("Hello");

console.log(mySet); // Set(3) { 1, 5, "Hello" }

// 2. Check (เช็คว่ามีไหม)
console.log(mySet.has(5)); // true (เร็วมาก $O(1)$)

// 3. Delete & Clear
mySet.delete(1);
mySet.clear(); // ลบเกลี้ยง
```

### 🔥 Use Case: ลบค่าซ้ำออกจาก Array (De-duplicate)
นี่คือท่ายากที่ใช้บ่อยที่สุดของ Set!

```javascript
const numbers = [1, 2, 2, 3, 3, 3, 4];

// วิธีเดิม (ช้าและยาว)
const uniqueOld = numbers.filter((v, i, a) => a.indexOf(v) === i);

// วิธี Set (เร็วและเท่)
const uniqueNew = [...new Set(numbers)]; 

console.log(uniqueNew); // [1, 2, 3, 4]
```

---

## 3. WeakMap & WeakSet (Garbage Collection Friendly) 👻

คู่หู "สายวิญญาณ" ของ Map/Set ความต่างคือ:
1. **Key ต้องเป็น Object เท่านั้น** (Primitives ไม่ได้)
2. **Weak (อ่อนแอ):** ถ้า Object ที่เป็น Key ไม่มีใครใช้แล้ว (Unreachable), **GC จะลบข้อมูลใน WeakMap ทิ้งให้เลย!** (ไม่ทำให้เกิด Memory Leak)
3. **Loop ไม่ได้:** ไม่มี `.forEach` หรือ `.size` (เพราะข้อมูลอาจหายไปเมื่อไหร่ก็ได้)

### Use Case: เก็บ Private Data หรือ Metadata

```javascript
const visitCount = new WeakMap(); // เก็บจำนวนครั้งที่ user เข้าชม

let user = { name: "Alice" };
visitCount.set(user, 1); // ผูกข้อมูลไว้กับ object user

// ... เวลาผ่านไป ...
user = null; // ผู้ใช้ถูกลบออกจากระบบ

// ผลลัพธ์: ข้อมูลใน visitCount ที่ผูกกับ Alice จะถูก GC ลบทิ้งอัตโนมัติ!
// (ถ้าใช้ Map ธรรมดา ข้อมูลจะค้างอยู่ ทำให้ Memory Leak)
```

---

## 4. Iteration (การวนลูป) 🔄

ทั้ง Map และ Set มี Built-in Iterator:

```javascript
const menu = new Map([
    ["Coffee", 50],
    ["Tea", 40]
]);

// 1. for...of
for (const [item, price] of menu) {
    console.log(`${item}: $${price}`);
}

// 2. forEach
menu.forEach((value, key) => {
    console.log(`${key}: ${value}`);
});

// 3. Convert to Array
const arr = [...menu]; // [ ["Coffee", 50], ["Tea", 40] ]
```

---

## 5. Challenges 🏆

### 🎯 Challenge 1: Clean Keywords
**หัวข้อ:** Set
**โจทย์:** คุณมี Array ของ Hashtags ที่ User พิมพ์มา (อาจมีซ้ำ) จงสร้างฟังก์ชัน `cleanTags(tags)` เพื่อคืนค่า Array ที่ไม่มีคำซ้ำ
```javascript
const tags = ["#js", "#react", "#js", "#code"];
```
::: details ✨ ดูเฉลย
```javascript
function cleanTags(tags) {
    return [...new Set(tags)];
}
// ผล: ["#js", "#react", "#code"]
```
:::

### 🎯 Challenge 2: Cache System
**หัวข้อ:** Map
**โจทย์:** สร้างฟังก์ชัน `memoize(fn)` ที่รับฟังก์ชันคำนวณหนักๆ เข้าไป ถ้า Input เดิมเคยคำนวณแล้ว ให้คืนค่าจาก Map (Cache) โดยไม่ต้องคำนวณใหม่
```javascript
// โครงร่าง
function memoize(fn) {
    const cache = new Map();
    return function(n) {
        // ... เติมโค้ดตรงนี้
    };
}
```
::: details ✨ ดูเฉลย
```javascript
function memoize(fn) {
    const cache = new Map();
    return function(n) {
        if (cache.has(n)) {
            console.log("Fetching from cache...");
            return cache.get(n);
        }
        const result = fn(n);
        cache.set(n, result);
        return result;
    };
}
```
:::

### 🎯 Challenge 3: Object Key Limit
**หัวข้อ:** Map vs Object
**โจทย์:** ทำไมโค้ดนี้ถึงทำงานผิดพลาด? และถ้าใช้ Map จะแก้ปัญหานี้ได้อย่างไร?
```javascript
const map = {}; // Object ธรรมดา
const user1 = { id: 1 };
const user2 = { id: 2 };

map[user1] = "Alice";
map[user2] = "Bob";

console.log(map[user1]); // ผลลัพธ์คือ "Bob" (ทำไมไม่ใช่ "Alice"?)
```
::: details ✨ ดูเฉลย
**สาเหตุ:** Object จะแปลง Key เป็น String เสมอ
- `user1.toString()` ได้ `"[object Object]"`
- `user2.toString()` ได้ `"[object Object]"`
- ดังนั้นมันจึงทับกันที่ Key เดียวกัน!

**วิธีแก้ใช้ Map:**
```javascript
const map = new Map();
map.set(user1, "Alice");
map.set(user2, "Bob");
console.log(map.get(user1)); // "Alice" ✅
```
:::

---

## 📖 Glossary (คำศัพท์เทคนิค)

| คำศัพท์ | ความหมาย |
|:-------|:---------|
| **Map** | โครงสร้างข้อมูล Key-Value ที่ Key เป็นชนิดข้อมูลใดก็ได้ และรักษาลำดับ |
| **Set** | โครงสร้างข้อมูลที่เก็บเฉพาะค่าที่ไม่ซ้ำกัน (Unique Values) |
| **WeakMap / WeakSet** | เวอร์ชันลดรูปที่ Key ต้องเป็น Object และถูก Garbage Collect ได้ถ้าระบบต้องการแรมคืน |
| **Collision (Key)** | การที่ Key ซ้ำกันจนข้อมูลทับกัน (เกิดบ่อยใน Object ที่ใช้ Key เป็น Object) |
| **Iterable** | ข้อมูลที่สามารถวนลูปได้ (Map และ Set เป็น Iterable ใช้ for...of ได้) |
| **Hash Table** | เบื้องหลังการทำงานของ Map/Set ที่ทำให้การค้นหาเร็วระดับ $O(1)$ |

---

👉 **[ไปต่อ: 5.5 - Strings, Math & Date](/javascript/05-05-strings-math-dates)**
