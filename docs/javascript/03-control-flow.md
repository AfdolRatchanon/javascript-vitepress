# 03 - Control Flow (การควบคุมการทำงาน)

> **References & Standards:**
> *   [MDN: Control flow and error handling](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Control_flow_and_error_handling)
> *   [MDN: Loops and iteration](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Loops_and_iteration)

โปรแกรมที่มีประโยชน์ คือโปรแกรมที่ "ตัดสินใจ" ได้ (Decision Making) และ "ทําซ้ำ" ได้ (Repetition)

## 1. Conditionals (การตัดสินใจ)

### `if` / `else if` / `else`
โครงสร้างพื้นฐานที่สุดในการตรวจสอบเงื่อนไข

```javascript
const score = 75;

if (score >= 80) {
  console.log("Grade A");
} else if (score >= 70) {
  console.log("Grade B");
} else {
  console.log("Grade C");
}
```

### `switch` vs `if`
ใช้ `switch` เมื่อต้องเช็คค่าที่ "แน่นอน" (Exact Match) หลายๆ กรณี

```javascript
const role = "admin";

switch (role) {
  case "admin":
    console.log("Full Access");
    break; // อย่าลืม break! ไม่งั้นมันจะไหลไป case ถัดไป (Fallthrough)
  case "user":
    console.log("View Only");
    break;
  default:
    console.log("No Access");
}
```

### Ternary Operator (`? :`)
การเขียน `if...else` แบบย่อในบรรทัดเดียว (นิยมมากใน React)

```javascript
const age = 20;
const status = (age >= 18) ? "Adult" : "Minor";
// เงื่อนไข ? (ถ้าจริง) : (ถ้าเท็จ)
```

### 🔬 Deep Dive: Truthy & Falsy
ใน JS ทุกค่าสามารถแปลงเป็น Boolean ได้
*   **Falsy Values (เป็นเท็จ):** `false`, `0`, `""` (Empty String), `null`, `undefined`, `NaN`
*   **Truthy Values (เป็นจริง):** ที่เหลือทั้งหมด (รวมถึง `[]`, `{}`, `"0"`, `"false"`)

## 2. Loops (การทำซ้ำ)

### `for` Loop (Standard)
ใช้เมื่อรู้จำนวนรอบที่แน่นอน

```javascript
for (let i = 0; i < 5; i++) {
  console.log(`Round ${i}`);
}
```

### `while` Loop
ใช้เมื่อไม่รู้จำนวนรอบที่แน่นอน (ทำตราบเท่าที่เงื่อนไขยังเป็นจริง)

```javascript
let fuel = 100;
while (fuel > 0) {
  console.log("Driving...");
  fuel -= 20;
}
```

### `break` & `continue`
*   **break:** หยุด Loop ทันที
*   **continue:** ข้ามรอบนี้ ไปทำรอบถัดไปทันที

## 3. Exercises: Logic Master

### Level 1: Grade Calculator (Easy)
จงเขียนฟังก์ชันตัดเกรดง่ายๆ:
*   คะแนน >= 50: "Pass"
*   คะแนน < 50: "Fail"
*   **Task:** ใช้ **Ternary Operator** ให้เขียนจบในบรรทัดเดียว!

### Level 2: FizzBuzz (Medium - Interview Classic) 🌟
จงพิมพ์เลข 1 ถึง 100
*   ถ้าหาร 3 ลงตัว พิมพ์ "Fizz"
*   ถ้าหาร 5 ลงตัว พิมพ์ "Buzz"
*   ถ้าหารลงตัวทั้ง 3 และ 5 พิมพ์ "FizzBuzz"
*   นอกนั้นพิมพ์ตัวเลขเฉยๆ

### Level 3: Pyramid Builder (Hard) 🏗️
จงใช้ Loop วาดรูปปิรามิดความสูง `n` ชั้น (สมมติ n=3)
```
  *
 ***
*****
```
*   **Hint:** สังเกตจำนวนช่องว่าง (Space) และจำนวนดาว (Star) ในแต่ละชั้น

### Level 4: The Infinite Trap (Challenge) 💀
จงวิเคราะห์โค้ดนี้ (ห้ามรัน!) ว่าจะเกิด Infinite Loop หรือไม่? เพราะอะไร?
```javascript
let i = 0;
while (i < 5) {
  console.log(i);
  if (i === 3) continue;
  i++;
}
```
*   **Task:** อธิบายการทำงานทีละ Step ว่าทำไมมันถึงค้าง (ถ้าค้าง)

---
👉 **[ดูเฉลย (Solutions)](/solutions/03-sol)**
