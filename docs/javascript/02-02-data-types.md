# 02-2: Data Types (ชนิดข้อมูล) 🧬

> **"JavaScript has a confusing and error-prone set of type coercion rules. But once you understand the type system, it becomes predictable."**
> — *Douglas Crockford (JavaScript: The Good Parts)*

ข้อมูลในโลกจริงมีหลายรูปแบบ: ข้อความ ตัวเลข จริง/เท็จ ในโลก JavaScript ก็เช่นกัน ก่อนจะเขียนโปรแกรมได้ดี คุณต้อง **รู้จักชนิดข้อมูลทุกตัว** และ **เข้าใจว่ามันทำงานอย่างไร**

> **💡 Analogy (เปรียบเทียบ):**
> ชนิดข้อมูลเหมือน **"ภาชนะที่ต่างกัน"** ครับ:
> - **String** = ม้วนกระดาษ (เก็บข้อความ)
> - **Number** = เครื่องคิดเลข (เก็บตัวเลข)
> - **Boolean** = สวิตช์ไฟ (เปิด/ปิด — true/false)
> - **undefined** = กล่องว่าง (ยังไม่มีของ)
> - **null** = กล่องที่ตั้งใจเคลียร์ให้ว่าง
> - **Object** = ตู้เซฟ (เก็บได้หลายอย่างพร้อมกัน)

## 1. Two Categories of Data Types (สองจักรวาลของข้อมูล) 🌓

ตาม [MDN](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Grammar_and_types#data_types) JavaScript แบ่งข้อมูลเป็น 2 กลุ่มใหญ่:

### A. Primitive Types (ชนิดดั้งเดิม) — 7 ชนิด

ข้อมูลที่เก็บ **ค่าเดียว** และ **Immutable** (เปลี่ยนค่าไม่ได้ ต้องสร้างใหม่ทั้งตัว):

```javascript
// 1. String — ข้อความ
const name = "Dolar";
const greeting = 'สวัสดีครับ';    // Single quotes ก็ได้
const template = `Hi ${name}!`;   // Template Literal (ES6)

// 2. Number — ตัวเลข (ทั้งจำนวนเต็มและทศนิยม)
const integer = 42;
const float = 3.14;
const negative = -10;
const infinity = Infinity;
const notANumber = NaN;          // "Not a Number" — ผลลัพธ์ของเลขที่ผิด

// 3. BigInt — ตัวเลขจำนวนเต็มขนาดใหญ่มาก (ES2020)
const hugeNumber = 9007199254740992n;  // ลงท้ายด้วย n
const reallyBig = BigInt("99999999999999999999");

// 4. Boolean — ค่าความจริง (true/false)
const isActive = true;
const isDeleted = false;

// 5. undefined — ยังไม่มีค่า (JS กำหนดให้อัตโนมัติ)
let notYetDefined;
console.log(notYetDefined);  // undefined

// 6. null — ตั้งใจให้ว่าง (Programmer กำหนดเอง)
const emptyValue = null;

// 7. Symbol — ค่า Unique ที่ซ้ำไม่ได้ (ES6)
const id1 = Symbol("id");
const id2 = Symbol("id");
console.log(id1 === id2);   // false ← แม้ Description เหมือนกัน ค่าก็ต่าง!
```

### B. Object Types (ชนิดอ้างอิง) — ทุกอย่างที่ไม่ใช่ Primitive

ข้อมูลที่เก็บ **หลายค่า** และ **Mutable** (แก้ไขข้อมูลข้างในได้):

```javascript
// Object (สารบัญ Key-Value)
const user = { name: "Dolar", age: 25 };

// Array (รายการเรียงลำดับ) *Array เป็น Object ชนิดพิเศษ!
const colors = ["Red", "Green", "Blue"];

// Function (ก็เป็น Object!) *Function เป็น First-Class Object
const greet = function() { return "Hi!"; };

// Date, RegExp, Map, Set, etc. — ล้วนเป็น Object ทั้งหมด
const now = new Date();
```

### 📊 All 8 Data Types at a Glance

| # | Type | Category | ตัวอย่าง | `typeof` Result |
|:-:|:-----|:---------|:---------|:----------------|
| 1 | **String** | Primitive | `"Hello"`, `'Hi'`, `` `Hey` `` | `"string"` |
| 2 | **Number** | Primitive | `42`, `3.14`, `NaN`, `Infinity` | `"number"` |
| 3 | **BigInt** | Primitive | `9999n`, `BigInt(999)` | `"bigint"` |
| 4 | **Boolean** | Primitive | `true`, `false` | `"boolean"` |
| 5 | **undefined** | Primitive | `undefined` | `"undefined"` |
| 6 | **null** | Primitive | `null` | ⚠️ `"object"` (Bug!) |
| 7 | **Symbol** | Primitive | `Symbol("id")` | `"symbol"` |
| 8 | **Object** | Object | `{}`, `[]`, `function(){}` | `"object"` / `"function"` |


---

## 2. String Deep Dive (เจาะลึก String) 📝

String คือลำดับของตัวอักษร (Character Sequence) ที่ถูกครอบด้วย Quotes:

### 3 วิธีเขียน String:

```javascript
// 1. Single Quotes — ง่าย สั้น
const name = 'Dolar';

// 2. Double Quotes — เหมือน Single Quotes แต่ใช้ " แทน '
const greeting = "Hello!";

// 3. Template Literals (Backticks) — ⭐ แนะนำ!
const fullGreeting = `Hello, ${name}! You are ${25} years old.`;
//                     └── Expression Interpolation ──┘
console.log(fullGreeting); // "Hello, Dolar! You are 25 years old."

// Template Literal ยัง "ขึ้นบรรทัดใหม่" ได้ โดยไม่ต้องใช้ \n
const multiLine = `Line 1
Line 2
Line 3`;
console.log(multiLine);
// Line 1
// Line 2
// Line 3
```

### Escape Characters (ตัวอักษรพิเศษ):

| Escape | ผลลัพธ์ | ตัวอย่าง |
|:-------|:-------|:---------|
| `\n` | ขึ้นบรรทัดใหม่ | `"Line 1\nLine 2"` |
| `\t` | Tab | `"Name:\tDolar"` |
| `\\` | Backslash | `"C:\\Users\\dol"` |
| `\'` | Single Quote | `'It\'s ok'` |
| `\"` | Double Quote | `"He said \"hi\""` |

### String Methods ที่ใช้บ่อย:

```javascript
const text = "Hello, World!";

console.log(text.length);          // 13
console.log(text.toUpperCase());   // "HELLO, WORLD!"
console.log(text.toLowerCase());   // "hello, world!"
console.log(text.includes("World")); // true
console.log(text.indexOf("World")); // 7
console.log(text.slice(0, 5));     // "Hello"
console.log(text.replace("World", "JS")); // "Hello, JS!"
console.log(text.split(", "));    // ["Hello", "World!"]
console.log(text.trim());         // "Hello, World!" (ตัดเว้นว่างหน้า-หลัง)
```

---

## 3. Number Deep Dive (เจาะลึก Number) 🔢

ตาม [MDN](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Number) Number ใน JS ใช้ **IEEE 754 Double Precision (64-bit)** ทั้งหมด ไม่แยก int/float!

### Number Gotchas (กับดัก!):

```javascript
// กับดัก 1: Floating Point Precision (ทศนิยมไม่แม่นยำ!)
console.log(0.1 + 0.2);        // 0.30000000000000004 ← ไม่ใช่ 0.3!
console.log(0.1 + 0.2 === 0.3); // false! 😱

// วิธีแก้: ใช้ toFixed() หรือ Math.round()
console.log((0.1 + 0.2).toFixed(1)); // "0.3" (เป็น String!)
console.log(Math.round((0.1 + 0.2) * 10) / 10); // 0.3

// กับดัก 2: NaN (Not a Number) — ผลลัพธ์ของการคำนวณที่ไม่ถูกต้อง
console.log("hello" * 2);    // NaN
console.log(parseInt("abc")); // NaN
console.log(NaN === NaN);    // false! ← NaN ไม่เท่ากับอะไรเลย แม้แต่ตัวเอง!
console.log(Number.isNaN(NaN)); // true ← ใช้อันนี้เช็คแทน!

// กับดัก 3: Safe Integer Limit
console.log(Number.MAX_SAFE_INTEGER); // 9007199254740991
console.log(9007199254740991 + 1);    // 9007199254740992 ✅
console.log(9007199254740991 + 2);    // 9007199254740992 ❌ (ค่าซ้ำ! ไม่แม่นยำ)
// ถ้าต้องการเลขใหญ่กว่านี้ ใช้ BigInt: 9007199254740993n

// กับดัก 4: Infinity
console.log(1 / 0);      // Infinity
console.log(-1 / 0);     // -Infinity
console.log(Infinity + 1); // Infinity
```

### Useful Number Methods:

```javascript
console.log(Number("42"));       // 42 (String → Number)
console.log(Number(""));         // 0
console.log(Number(true));       // 1
console.log(Number(false));      // 0
console.log(Number(null));       // 0
console.log(Number(undefined));  // NaN

console.log(parseInt("42px"));   // 42 (ตัดหน่วยออกให้)
console.log(parseFloat("3.14")); // 3.14
console.log(Number.isInteger(42)); // true
console.log(Number.isFinite(Infinity)); // false
```

---

## 4. undefined vs null (สับสนที่สุด!) 🤔

| | `undefined` | `null` |
|:--|:-----------|:-------|
| **ความหมาย** | **ยังไม่ได้กำหนดค่า** | **ตั้งใจกำหนดให้ว่าง** |
| **ใครกำหนด** | JavaScript Engine (อัตโนมัติ) | Programmer (ตั้งใจ) |
| **เปรียบเทียบ** | กล่องที่ยังไม่ได้ใส่ของ | กล่องที่ตั้งใจเคลียร์ของออก |
| **typeof** | `"undefined"` | ⚠️ `"object"` (Bug!) |
| **Loose Equality** | `undefined == null` → `true` | `null == undefined` → `true` |
| **Strict Equality** | `undefined === null` → `false` | `null === undefined` → `false` |

```javascript
// undefined: JS กำหนดให้เอง
let score;
console.log(score);                 // undefined (ยังไม่ได้กำหนดค่า)

function getNothing() {}
console.log(getNothing());          // undefined (ไม่มี return)

const user = { name: "Dolar" };
console.log(user.age);              // undefined (Property ไม่มีอยู่จริง)

// null: Programmer กำหนดเอง
let selectedUser = null;            // "ตอนนี้ยังไม่ได้เลือก User"
selectedUser = { name: "Dolar" };   // "เลือก User แล้ว"
selectedUser = null;                // "ยกเลิกการเลือก"
```


---

## 5. Memory: Stack vs Heap (หน่วยความจำ) 🧠

การเข้าใจว่าข้อมูลถูก **เก็บตรงไหน** ในหน่วยความจำ จะช่วยให้คุณเข้าใจพฤติกรรมของ JavaScript ที่ดูแปลกๆ ได้:

### Primitive → เก็บใน Stack (เร็ว ขนาดคงที่)

```javascript
let a = 10;
let b = a;     // ← คัดลอก "ค่า" ไปเก็บใน b (Copy)

a = 20;
console.log(a); // 20
console.log(b); // 10 ← b ยังเป็น 10! เพราะ b เป็น "สำเนา" ไม่ใช่ตัวเดียวกัน
```

```
Stack Memory:
┌─────────────┐
│  a  │  20   │  ← เปลี่ยนแค่ a
├─────────────┤
│  b  │  10   │  ← b ไม่เปลี่ยน (คนละสำเนา)
└─────────────┘
```

### Object → เก็บ Reference ใน Stack, ข้อมูลจริงใน Heap

```javascript
const obj1 = { name: "Dolar" };
const obj2 = obj1;  // ← คัดลอก "ที่อยู่" (Reference) ไม่ใช่ "ข้อมูล"!

obj1.name = "Somchai";
console.log(obj1.name); // "Somchai"
console.log(obj2.name); // "Somchai" ← เปลี่ยนด้วย! เพราะชี้ไปที่เดียวกัน!
```

```
Stack Memory:              Heap Memory:
┌───────────────────┐     ┌─────────────────────┐
│  obj1  │  @ref001 │────►│ { name: "Somchai" } │
├───────────────────┤     │                     │
│  obj2  │  @ref001 │────►│  (ที่เดียวกัน!)      │
└───────────────────┘     └─────────────────────┘
```

### 📊 Stack vs Heap Comparison

| | **Stack** | **Heap** |
|:--|:---------|:---------|
| **เก็บอะไร** | Primitive values, References | Object data |
| **ขนาด** | คงที่ เล็ก | ยืดหยุ่น ใหญ่ |
| **ความเร็ว** | ⚡ เร็วมาก | 🐢 ช้ากว่า |
| **Garbage Collection** | อัตโนมัติ (Stack unwind) | ต้องมี GC คอยเก็บกวาด |
| **การ Copy** | Copy ค่าจริง (Deep Copy) | Copy Reference (Shallow) |

> **เรื่องนี้สำคัญมาก** และจะเจาะลึกอีกครั้งในบท [05-3: Reference vs Value](/05-03-reference-vs-value) ครับ


---

## 6. Dynamic Typing (ชนิดข้อมูลแบบยืดหยุ่น) 🌀

JavaScript เป็นภาษา **Dynamically Typed** — ตัวแปรไม่ได้ถูกผูกกับชนิดข้อมูลใดตลอดไป:

```javascript
// JS ไม่สนใจว่าตัวแปรเคยเป็นชนิดอะไรมาก่อน
let value = 42;          // Number
value = "hello";         // String (เปลี่ยนชนิดได้!)
value = true;            // Boolean
value = [1, 2, 3];       // Array (Object)
value = null;            // null

// ⚠️ เทียบกับ Java/C++ ที่เป็น Statically Typed:
// int value = 42;
// value = "hello"; // ❌ Compile Error!
```

> **ข้อดี:** เขียนโค้ดได้เร็ว ยืดหยุ่น
> **ข้อเสีย:** Bug แอบแฝงง่าย เพราะ JS ไม่ Error เมื่อชนิดข้อมูลไม่ตรงกับที่คาดหวัง
> **วิธีแก้:** ใช้ TypeScript (Superset ของ JS ที่เพิ่ม Static Typing)

---

## 7. Checking Types (ตรวจสอบชนิดข้อมูล) 🔎

### `typeof` Operator:
```javascript
console.log(typeof "hello");     // "string"
console.log(typeof 42);          // "number"
console.log(typeof true);        // "boolean"
console.log(typeof undefined);   // "undefined"
console.log(typeof null);        // ⚠️ "object" (Historical Bug!)
console.log(typeof {});          // "object"
console.log(typeof []);          // "object" (Array = Object)
console.log(typeof function(){}); // "function"
```

### เมื่อ `typeof` ไม่พอ:
```javascript
// ตรวจสอบ Array
Array.isArray([1, 2, 3]);  // true
Array.isArray({});          // false

// ตรวจสอบ null
const val = null;
val === null;               // true (ใช้ === เปรียบเทียบตรงๆ)

// ตรวจสอบ NaN
Number.isNaN(NaN);          // true
Number.isNaN("hello");      // false (typeof "hello" ≠ NaN)

// ตรวจสอบ Instance
const date = new Date();
date instanceof Date;       // true
```

---

## 8. Challenges 🏆

ทดสอบความเข้าใจเรื่อง Data Types ทั้ง 7 หัวข้อครับ:

### 🎯 Challenge 1: The Two Worlds
**หัวข้อ:** 1. Two Categories

**โจทย์:** ใน JavaScript, ข้อมูลประเภท `Object`, `Array`, `Function` จัดอยู่ใน Category ไหน? (Primitive หรือ Object Type?) และต่างจาก Primitive อย่างไร?
::: details ✨ ดูเฉลย
จัดอยู่ใน **Object Type (Reference Type)** ครับ
ความต่างคือ **Mutable** (แก้ไขไส้ในได้) และเก็บเป็น **Reference** (ที่อยู่)
:::

### 🎯 Challenge 2: Template Magic
**หัวข้อ:** 2. String Deep Dive

**โจทย์:** จงใช้ Template Literal เชื่อมประโยค:
`const item = "Coffee";`
`const price = 50;`
ให้ได้ผลลัพธ์: `"Price of Coffee is 50 baht."`
::: details ✨ ดูเฉลย
```javascript
const item = "Coffee";
const price = 50;
console.log(`Price of ${item} is ${price} baht.`);
```
:::

### 🎯 Challenge 3: Floating Nightmare
**หัวข้อ:** 3. Number Deep Dive

**โจทย์:** ทำไม `0.1 + 0.2 === 0.3` ถึงเป็น `false`?
::: details ✨ ดูเฉลย
เพราะ **Floating Point Precision** (ความคลาดเคลื่อนของทศนิยม) ในระบบคอมพิวเตอร์ (IEEE 754) ครับ ผลลัพธ์จริงๆ คือ `0.30000000000000004`
:::

### 🎯 Challenge 4: Empty Box
**หัวข้อ:** 4. undefined vs null

**โจทย์:** ถ้าคุณต้องการประกาศตัวแปร `selectedProduct` ไว้รอรับค่าจากการเลือกของผู้ใช้ (แต่ตอนนี้ยังไม่ได้เลือก) คุณควรให้ค่าเริ่มต้นเป็นอะไร? (`undefined` หรือ `null`)
::: details ✨ ดูเฉลย
ควรใช้ **`null`** ครับ เพื่อสื่อความหมายว่า "ว่างอย่างตั้งใจ" (Intentionally Empty)
:::

### 🎯 Challenge 5: Memory Leak?
**หัวข้อ:** 5. Memory: Stack vs Heap

**โจทย์:** เมื่อเราทำคำสั่ง `let b = a;` ถ้า `a` เป็น **Number** จะเกิดการ Copy แบบไหน? และถ้า `a` เป็น **Object** จะเกิดการ Copy แบบไหน?
::: details ✨ ดูเฉลย
- ถ้า `a` เป็น Number: Copy **Value** (ค่าจริง) → ตัวใหม่แยกจากตัวเก่า
- ถ้า `a` เป็น Object: Copy **Reference** (ที่อยู่) → ชี้ไปที่ตัวเดียวกัน
:::

### 🎯 Challenge 6: Shapeshifter
**หัวข้อ:** 6. Dynamic Typing

**โจทย์:** JavaScript เป็น Dynamic Typing แปลว่าอะไร?
::: details ✨ ดูเฉลย
แปลว่า **ตัวแปรไม่ได้ผูกติดกับชนิดข้อมูล** ครับ ตัวแปรเดียวสามารถเปลี่ยนจาก String เป็น Number เป็น Object ได้ตลอดเวลา
:::

### 🎯 Challenge 7: The Bug Detector
**หัวข้อ:** 7. Checking Types

**โจทย์:** ถ้าใช้ `typeof null` จะได้ผลลัพธ์เป็นอะไร? และถ้าจะเช็ค Array ควรใช้อะไร?
::: details ✨ ดูเฉลย
- `typeof null` ได้ **`"object"`** (เป็น Bug เก่าแก่)
- เช็ค Array ให้ใช้ **`Array.isArray(ตัวแปร)`** ครับ
:::

---

> **📖 คำศัพท์เทคนิค (Glossary):**
> *   **Primitive:** ชนิดข้อมูลพื้นฐานที่เก็บ "ค่าเดียว" และ Immutable (String, Number, BigInt, Boolean, undefined, null, Symbol)
> *   **Object:** ชนิดข้อมูลที่เก็บ "หลายค่า" เป็น Key-Value Pairs และ Mutable
> *   **Immutable:** เปลี่ยนค่าไม่ได้ (ต้องสร้างใหม่ทั้งตัว)
> *   **Mutable:** เปลี่ยนค่าข้างในได้โดยไม่ต้องสร้างใหม่
> *   **NaN (Not a Number):** ค่าพิเศษที่บอกว่า "ผลลัพธ์ของการคำนวณนี้ไม่ใช่ตัวเลข"
> *   **Infinity:** ค่าที่แสดงถึง "อนันต์" (เช่น 1/0)
> *   **IEEE 754:** มาตรฐานระบบเลขทศนิยมที่ JavaScript ใช้ (64-bit Double Precision)
> *   **typeof:** Operator ที่คืนค่า String บอกชนิดข้อมูล (มี Bug กับ null!)
> *   **Stack:** พื้นที่หน่วยความจำที่เก็บ Primitive และ Reference (เร็ว ขนาดคงที่)
> *   **Heap:** พื้นที่หน่วยความจำที่เก็บ Object จริงๆ (ขนาดยืดหยุ่น)
> *   **Reference:** "ที่อยู่" ของ Object ใน Heap Memory ที่ถูกเก็บไว้ใน Stack
> *   **Dynamic Typing:** ภาษาที่ตัวแปรไม่ถูกผูกกับชนิดข้อมูล (เปลี่ยนชนิดได้)
> *   **Template Literal:** String ที่ใช้ Backtick (`` ` ``) ที่ใส่ Expression ด้วย `${}` ได้
> *   **Garbage Collection (GC):** กระบวนการที่ JS Engine ลบข้อมูลที่ไม่ถูกใช้จาก Heap อัตโนมัติ

---
👉 **[ไปต่อ: 02-3 - Type Conversion (การแปลงชนิดข้อมูล)](/02-03-type-conversion)**
