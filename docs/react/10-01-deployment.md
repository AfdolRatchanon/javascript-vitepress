# 10.1 Deployment

> *"It doesn't count until it's in production."*

## 📦 Building for Production

ก่อนที่เราจะเอาเว็บขึ้น Server เราต้อง **Build** โปรเจกต์ก่อนครับ
เพราะโค้ด React ที่เราเขียน มีทั้ง JSX, ES6+, และไฟล์แยกย่อยมากมาย Browser อ่านไม่รู้เรื่อง!

### คำสั่ง Build
```bash
npm run build
```

### สิ่งที่เกิดขึ้น (`/dist` or `/build`)
Vite (หรือ Webpack) จะทำการ:
1.  **Transpile**: แปลง JSX/TS เป็น JS มาตรฐานที่ทุก Browser รองรับ
2.  **Minify**: ย่อไฟล์ ลบช่องว่าง เปลี่ยนชื่อตัวแปรให้สั้นลง (เพื่อความเร็ว)
3.  **Bundle**: รวมไฟล์หลายๆ ไฟล์เป็นไฟล์เดียว (หรือน้อยที่สุด)


## ☁️ Where to Host?

React App (SPA) เป็นแค่ไฟล์ Static (HTML/CSS/JS) ดังนั้นเอาไปวางที่ไหนก็ได้!
แต่ที่นิยมและฟรีสำหรับนักพัฒนาคือ:

1.  **Vercel** (ผู้สร้าง Next.js) — เหมาะที่สุดสำหรับ React
2.  **Netlify** — ใช้งานง่าย ลากวางไฟล์ได้เลย
3.  **GitHub Pages** — ฟรีและผูกกับ repository โดยตรง


## 🚧 Router Issue on Production

ถ้าใช้ React Router และ deploy ไปแล้วเจอปัญหา **"Refresh หน้าแล้ว 404"**
สาเหตุคือ Server พยายามหาไฟล์ตาม path (เช่น `/about.html`) ซึ่งไม่มีจริง

### วิธีแก้
ต้องบอก Server ให้ **Rewrite** ทุก request กลับมาที่ `index.html` แล้วให้ React Router จัดการต่อเอง
- **Vercel/Netlify**: จัดการให้อัตโนมัติ (Default settings)
- **Apache/Nginx**: ต้องแก้ config (`.htaccess` หรือ `nginx.conf`)


## 🥊 Challenge: Optimize Build

ลองรันคำสั่ง:
```bash
npx vite-bundle-visualizer
```
เพื่อดูว่าไฟล์ไหนใหญ่ที่สุดในโปรเจกต์เรา? (เช่น library ตัวไหนกินที่เยอะสุด)


> 👉 **ไปต่อ: [Project 10: Deploy to Vercel/Netlify](/react/10-project-hosting)**
