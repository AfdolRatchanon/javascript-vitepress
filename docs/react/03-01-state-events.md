# 3.1 State & Events

> *"State is the heart of every React component."*

## 🎬 Intro: The Light Switch (สวิตช์ไฟ) 💡

ในบทก่อนๆ เราเรียนเรื่อง Props ซึ่งเหมือน "คำสั่งที่แก้ไม่ได้" (Read-only)
แต่ถ้าเราอยากให้หน้าเว็บ "เปลี่ยนไป" เมื่อผู้ใช้กดปุ่ม (เช่น กดเปิด/ปิดไฟ) เราต้องใช้ **State** ครับ

- **Props**: ข้อมูลที่รับมาจากข้างนอก (Parent) → เปลี่ยนเองไม่ได้
- **State**: ข้อมูลส่วนตัวภายใน Component → **เปลี่ยนได้** (Mutable) และเมื่อเปลี่ยน React จะ **Render ใหม่** ทันที

---

## 🖱️ Event Handling

ก่อนจะไปเรื่อง State เราต้องดักจับเหตุการณ์ (Event) จากผู้ใช้ก่อน เช่น การคลิกปุ่ม

หลักการ:
1.  ใช้ชื่อ event แบบ camelCase (เช่น `onClick`, `onChange`, `onSubmit`)
2.  ส่ง **Function** เข้าไปทำงาน (ห้ามใส่วงเล็บ `()` ต่อท้ายชื่อฟังก์ชัน ไม่งั้นมันจะรันทันที!)

```jsx
const Button = () => {
  const handleClick = () => {
    alert("Button clicked!");
  };

  // ✅ ถูกต้อง: ส่งชื่อฟังก์ชัน
  return <button onClick={handleClick}>Click me</button>;
  
  // ❌ ผิด: handleClick() จะรันทันทีตอน render
  // return <button onClick={handleClick()}>Click me</button>;
};
```

---

## 🎣 The `useState` Hook

React สมัยใหม่ใช้ **Hooks** ในการจัดการ State โดยตัวที่สำคัญที่สุดคือ `useState`

### Syntax
```javascript
const [state, setState] = useState(initialValue);
```

- `state`: ตัวแปรเก็บค่าปัจจุบัน
- `setState`: ฟังก์ชันสำหรับเปลี่ยนค่า
- `initialValue`: ค่าเริ่มต้น

### ตัวอย่าง: Counter (ตัวนับเลข)

```jsx
import { useState } from 'react'; // 1. Import

function Counter() {
  // 2. ประกาศ State (เริ่มที่ 0)
  const [count, setCount] = useState(0);

  return (
    <div>
      <p>Count: {count}</p>
      
      {/* 3. เรียกใช้ setCount เมื่อคลิก */}
      <button onClick={() => setCount(count + 1)}>
        Increase
      </button>
    </div>
  );
}
```

> **🔥 กฎของ State**: ห้ามแก้ตัวแปร `state` โดยตรง (เช่น `count = 5` ❌) ต้องใช้ `setCount(5)` ✅ เท่านั้น React ถึงจะรู้ตัวและอัปเดตหน้าจอ

---

## 🔄 Update State from Previous Value

ถ้าค่าใหม่ ต้องอิงจากค่าเก่า (เช่น เพิ่มทีละ 1) ควรส่ง **Arrow Function** เข้าไปใน setState ครับ

```javascript
// ✅ Best Practice
setCount(prevCount => prevCount + 1);
```

เหตุผล: ถ้าเราเรียก `setCount(count + 1)` 3 ครั้งติดกัน React อาจจะรวบยอดทำทีเดียว ทำให้ค่าเพิ่มแค่ 1 แต่ถ้าใช้ callback จะเพิ่มทีละสเต็ปถูกต้อง

---

## 📝 Text Input State

การรับค่าจาก Input form เราจะใช้ `onChange` คู่กับ `value` (เรียกว่า Controlled Component)

```jsx
function NameInput() {
  const [name, setName] = useState("");

  const handleChange = (event) => {
    setName(event.target.value); // ดึงค่าจากช่องกรอก
  };

  return (
    <div>
      <input 
        type="text" 
        value={name} 
        onChange={handleChange} 
      />
      <p>Your name is: {name}</p>
    </div>
  );
}
```

---

## 🥊 Challenges

### Level 1: Toggler
สร้างปุ่มที่กดแล้วสลับข้อความระหว่าง "ON" (สีเขียว) กับ "OFF" (สีแดง)

::: details ✨ เฉลย
```jsx
// ลองทำเองก่อนนะ!
const Toggler = () => {
  const [isOn, setIsOn] = useState(false);

  return (
    <button 
      onClick={() => setIsOn(!isOn)}
      style={{ backgroundColor: isOn ? "green" : "red" }}
    >
      {isOn ? "ON" : "OFF"}
    </button>
  );
};
```
:::

### Level 2: Character Counter
สร้าง Textarea ที่เมื่อพิมพ์ข้อความ จะบอกจำนวนตัวอักษรที่พิมพ์ไป (Length)

::: details ✨ เฉลย
```jsx
const CharCounter = () => {
  const [text, setText] = useState("");

  return (
    <div>
      <textarea onChange={(e) => setText(e.target.value)} />
      <p>Count: {text.length}</p>
    </div>
  );
};
```
:::

---

> 👉 **ไปต่อ: [Project 3: Counter & Toggler](/react/03-project-counter-toggler)**
