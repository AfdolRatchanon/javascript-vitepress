# ⚡ Project 11: Optimization Challenge

ในบทนี้ เราจะเห็นผลลัพธ์ของการ Optimize อย่างชัดเจน!
เราจะสร้างแอปที่ **"ตั้งใจทำให้ช้า"** แล้วค่อยๆ แก้ให้เร็วขึ้นด้วย Hook เทพๆ

> **ความรู้ที่ใช้**: `useMemo`, `React.memo`

---

## 🎯 เป้าหมาย (Goal)
สร้าง List รายชื่อ 5,000+ รายการ พร้อมระบบ Search
1.  **Before**: พิมพ์แล้วกระตุก (Laggy) เพราะกรองใหม่ทุกครั้งที่พิมพ์ และ Render ใหม่หมด
2.  **After**: พิมพ์ลื่นไหลขึ้น ด้วยการใช้ `useMemo`

---

## 🚀 ลงมือทำ (Step-by-Step)

### Step 1: Create Dummy Data
สร้างฟังก์ชัน Generate ข้อมูลเยอะๆ (หรือใช้ loop)

```javascript
const generateItems = (count) => {
  const items = [];
  for (let i = 0; i < count; i++) {
    items.push({ id: i, name: `Item ${i}`, value: Math.random() });
  }
  return items;
};

const allItems = generateItems(10000); // 1 หมื่นรายการ!
```

### Step 2: Implement Slow Version
ลองเขียนแบบปกติ (ยังไม่ Optimize)

```jsx
const App = () => {
  const [search, setSearch] = useState("");
  const [dark, setDark] = useState(false);

  // ❌ ช้า! คำนวณใหม่ทุกครั้งที่ render (แม้แต่ตอนเปลี่ยน Theme!)
  const filteredItems = allItems.filter(item => 
    item.name.toLowerCase().includes(search.toLowerCase())
  );

  return (
    <div style={{ background: dark ? '#333' : '#fff', color: dark ? '#fff' : '#000' }}>
      <button onClick={() => setDark(!dark)}>Toggle Theme</button>
      <input value={search} onChange={e => setSearch(e.target.value)} />
      
      <ul>
        {filteredItems.map(item => <li key={item.id}>{item.name}</li>)}
      </ul>
    </div>
  );
};
```

### Step 3: Optimize with `useMemo`
เป้าหมาย:
1.  การ filter ควรทำใหม่เฉพาะเมื่อ `search` เปลี่ยนเท่านั้น (ไม่ใช่ทุกครั้งที่ `dark` เปลี่ยน)

```jsx
// ✅ เร็ว! ถ้ากด toggle theme จะไม่คำนวณ filter ใหม่
const filteredItems = useMemo(() => {
  console.log("Filtering..."); // เช็ค log ดูว่ารันตอนไหนบ้าง
  return allItems.filter(item => 
    item.name.toLowerCase().includes(search.toLowerCase())
  );
}, [search]); // Dependency: เปลี่ยนเฉพาะเมื่อ search เปลี่ยน
```

---

## 📊 Testing & Observation

1.  เปิด Console
2.  ลองกดปุ่ม **Toggle Theme** รัวๆ
    - **Before**: จะเห็น log "Filtering..." ขึ้นรัวๆ (เปลือง CPU)
    - **After**: จะ **ไม่เห็น** log ขึ้นเลย (เพราะ search ไม่เปลี่ยน React เลยข้ามการคำนวณให้)

นี่คือพลังของ `useMemo` ครับ! ช่วยประหยัดการประมวลผลในส่วนที่ไม่จำเป็น

---

> 👉 **ไปต่อ: [Module 12 - Capstone Project](/react/12-01-capstone)** (Final Project!)
