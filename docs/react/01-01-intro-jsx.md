# 1.1 Introduction to React & JSX

> *"React is a declarative, efficient, and flexible JavaScript library for building user interfaces."* — **React Documentation**

ยินดีต้อนรับสู่โลกของ **React.js** ครับ! 🎉
ในบทเรียนแรกนี้ เราจะมาปูพื้นฐานที่สำคัญที่สุดกัน นั่นคือการทำความเข้าใจว่า **"React คิดยังไง?"** (The React Mindset) และทำไมมันถึงพลิกโฉมวงการ Web Development ไปตลอดกาล

## 🐣 Analogy: Lego Master Builder (นักต่อเลโก้)

ลองจินตนาการว่าการสร้างเว็บไซด์ก็เหมือนการสร้าง **"ปราสาท"** 🏰

### 🏗️ แบบเดิม (HTML/jQuery) = "การปั้นดินน้ำมัน"
สมัยก่อน เวลาเราทำเว็บ เรามักจะมองหน้าเว็บเป็น **"หน้ากระดาษแผ่นใหญ่"** (Document) ที่เราต้องคอยปั้นแต่งมันทีละจุด
- อยากเพิ่มประตู? ก็เอาดินน้ำมันไปแปะ
- อยากย้ายหน้าต่าง? ก็ต้องขุดออกแล้วปั้นใหม่
- ถ้าทำปราสาทพัง? อาจจะต้องรื้อทำใหม่ทั้งหลังเพราะโครงสร้างมันผูกกันมั่วไปหมด

**ข้อเสีย**: แก้ไขยาก, ใช้ซ้ำไม่ได้, ยิ่งโตยิ่งพัง (Spaghetti Code)

### 🧱 แบบ React = "การต่อ Lego"
React สอนให้เรามองทุกอย่างเป็น **"ชิ้นส่วนเล็กๆ"** หรือ **"Component"**
- เราสร้างชิ้นส่วน **"ประตู"** ไว้ 1 ชิ้น
- เราสร้างชิ้นส่วน **"หน้าต่าง"** ไว้ 1 ชิ้น
- เราสร้างชิ้นส่วน **"กำแพง"** ไว้ 1 ชิ้น

เวลาจะสร้างปราสาท เราก็แค่เอาชิ้นส่วนพวกนี้มา **"ประกอบกัน"** (Compose)
- ถ้าประตูพัง? ก็แค่ถอดชิ้นประตูออก แล้วเอาอันใหม่ใส่เข้าไป (ไม่กระทบกำแพง)
- ถ้าอยากได้ปราสาท 10 หลัง? ก็แค่ copy แบบแปลนแล้วประกอบใหม่ (Reusability)

นี่คือหัวใจของ React ครับ: **"Build components, not pages."**

## 📚 What is React? (React คืออะไร?)

**React** คือ JavaScript Library ที่สร้างโดยทีมวิศวกรของ **Facebook (Meta)** ในปี 2013 เพื่อแก้ปัญหาการจัดการ UI ที่ซับซ้อนใน Facebook News Feed

### 3 เสาหลักของ React (The Three Pillars)

#### 1. Component-Based Architecture
อย่างที่เล่าไปเรื่อง Lego ครับ React ให้เราแตกหน้าเว็บที่ซับซ้อน ออกเป็นชิ้นส่วนย่อยๆ ที่จัดการตัวเองได้ (Self-contained)
- ตัวอย่าง: `Navbar`, `Sidebar`, `Button`, `UserInfo`, `CommentBox`

#### 2. Declarative (เชิงประกาศ) ⭐️ *สำคัญมาก*
นี่คือจุดที่มือใหม่งงที่สุด!
- **Imperative (แบบเดิม)**: เราต้องสั่งคอมพิวเตอร์ทีละก้าวว่าจะ "ทำอย่างไร" (How)
  - *เดินไปที่ห้องครัว -> หยิบแก้ว -> เปิดตู้เย็น -> รินน้ำ -> เดินกลับมา*
- **Declarative (React)**: เราแค่บอกคอมพิวเตอร์ว่า "อยากได้ผลลัพธ์อะไร" (What)
  - *ฉันอยากได้น้ำ 1 แก้ว (เดี๋ยว React หาวิธีทำเอง)*

ดูตัวอย่างโค้ดเปรียบเทียบ:

```javascript
// ❌ Imperative (Vanilla JS)
// เราต้องสั่ง DOM ทีละบรรทัด
const btn = document.getElementById('btn');
btn.addEventListener('click', () => {
  const box = document.getElementById('box');
  if (box.style.display === 'none') {
    box.style.display = 'block';
    btn.innerText = 'Hide';
  } else {
    box.style.display = 'none';
    btn.innerText = 'Show';
  }
});

// ✅ Declarative (React)
// เราแค่นิยาม "สถานะ" (State) แล้ว React จะวาดหน้าจอให้ตรงกับสถานะเอง
return (
  <div>
    {isVisible && <Box />}
    <button onClick={() => setIsVisible(!isVisible)}>
      {isVisible ? 'Hide' : 'Show'}
    </button>
  </div>
)
```

#### 3. Learn Once, Write Anywhere
React ไม่ได้ผูกติดกับแค่เว็บ (React DOM) แต่สามารถนำ Logic เดียวกันไปเขียน:
- **Mobile Apps**: React Native (iOS/Android)
- **VR**: React 360
- **Desktop**: Electron + React


## 💻 JSX: JavaScript XML

เมื่อเราเขียน React เราจะเจอกับภาษาหน้าตาประหลาดที่เหมือน HTML ผสม JS... มันคือ **JSX** ครับ!

### JSX คืออะไร?
JSX (JavaScript Syntax Extension) เป็น "ส่วนขยาย" ของภาษา JavaScript ที่ทำให้เราเขียนโครงสร้าง UI (HTML) ลงไปในโค้ด JS ได้โดยตรง

> **Note**: Browser อ่าน JSX ไม่รู้เรื่องนะครับ! มันต้องถูกแปลงร่าง (Compile) โดยเครื่องมือที่ชื่อ **Babel** ให้กลายเป็น JavaScript ธรรมดาก่อนเสมอ

### เบื้องหลังความมหัศจรรย์ (Under the Hood)
เมื่อเราเขียน JSX:
```jsx
const element = <h1 className="greeting">Hello, World!</h1>;
```

Babel จะแปลงมันเป็น:
```javascript
const element = React.createElement(
  'h1',                    // Type
  { className: 'greeting' }, // Props
  'Hello, World!'          // Children
);
```

เห็นไหมครับว่าจริงๆ แล้ว JSX ก็คือการเรียกฟังก์ชัน `React.createElement()` รัวๆ นั่นเอง (แบบแรกเขียนง่ายกว่าเยอะใช่ไหมล่ะ?)


## ⚡ 5 กฎเหล็กของ JSX (Must Know Rules)

การเขียน JSX ไม่ใช่ HTML 100% ครับ มีข้อห้ามและข้อปฏิบัติที่ต้องจำให้แม่น:

### 1. ต้องมี Root Element เพียงตัวเดียว
JSX ที่จะ return ออกไป **ต้องถูกห่อหุ้มด้วย Tag พ่อแม่เพียง 1 ตัวเท่านั้น**

❌ **ผิด:** Return 2 tags ลอยๆ
```jsx
return (
  <h1>Header</h1>
  <p>Content</p>
);
```

✅ **ถูก:** ห่อด้วย `div` หรือ `Fragment` (`<>...</>`)
```jsx
return (
  <> 
    <h1>Header</h1>
    <p>Content</p>
  </>
);
```
> *Tip: การใช้ `<>...</>` (Fragment) ดีกว่า `div` เพราะจะไม่สร้าง node ขยะใน HTML จริง*

### 2. ต้องปิด Tag เสมอ (Self-Closing Tags)
ใน HTML บาง tag ไม่ต้องปิด (เช่น `<input>`, `<br>`, `<img>`)
แต่ใน JSX **ต้องปิดเสมอ!**

❌ `<img src="logo.png">`
✅ `<img src="logo.png" />`
✅ `<input type="text" />`
✅ `<br />`

### 3. ใช้ `className` แทน `class`
คำว่า `class` เป็นคำสงวน (Reserved Keyword) ใน JavaScript (เอาไว้สร้าง Class OOP)
React เลยเลี่ยงไปใช้ `className` แทน

```jsx
❌ <div class="container">
```
```jsx
✅ <div className="container">
```
### 4. ใช้ `{ }` เพื่อแทรก JavaScript (Interpolation)
ถ้าเราอยากให้ JSX แสดงค่าจากตัวแปร หรือคำนวณเลข ต้องใส่ปีกกา `{ }`

```jsx
const name = "Somchai";
const age = 20;

return (
  <div>
    <p>Name: {name}</p>           {/* Name: Somchai */}
    <p>Next Year: {age + 1}</p>   {/* Next Year: 21 */}
    <p>Random: {Math.random()}</p>
  </div>
);
```
> *กฎ: ใน `{ }` ต้องเป็น **Expression** (ค่าที่ return ผลลัพธ์) เท่านั้น ห้ามใส่ Statement (เช่น `if`, `for`, `while`) ลงไปตรงๆ*

### 5. Style ต้องเป็น Object
การเขียน Inline Style ใน JSX จะไม่ใช้ string แบบ HTML แต่ใช้ **Object** และชื่อ property ต้องเป็น **camelCase**

```jsx
❌ style="background-color: red; font-size: 20px;"
```
```jsx
✅ style={{ backgroundColor: 'red', fontSize: '20px' }}
```

> *สังเกตปีกกา 2 ชั้น: ชั้นนอกคือ "ฉันจะเขียน JS", ชั้นในคือ "นี่คือ Object"*



## 🏗️ Virtual DOM คืออะไร? (ฉบับเจาะลึก)

คำถามปราบเซียน: **"ทำไม React ถึงเร็ว ทั้งที่มันสร้าง DOM ใหม่ตลอดเวลา?"**
คำตอบคือ: **"มันไม่ได้สร้าง Real DOM ใหม่... มันสร้าง Virtual DOM ต่างหาก!"**

### กลไกการทำงาน (The Reconciliation Process)

1.  **Rendering**: เมื่อข้อมูล (State/Props) เปลี่ยน, React จะเรียก Component Function
2.  **Virtual DOM Creation**: Component จะ return JSX หน้าตาใหม่ ให้ออกมาเป็น Virtual DOM Tree (เป็นแค่ Object ใน Memory เบาๆ)
3.  **Diffing Algorithm**: React จะเอา Virtual DOM **"ใหม่"** ไปเทียบกับ Virtual DOM **"เก่า"**
    - *React ถาม: "มีอะไรเปลี่ยนไปบ้าง?"*
    - *React ตอบ: "อ๋อ แค่ข้อความใน `<span>` บรรทัดนี้เปลี่ยน นอกนั้นเหมือนเดิม"*
4.  **Commit (Update Real DOM)**: React จะยิงคำสั่งไปแก้ Real DOM **เฉพาะจุดที่เปลี่ยนเท่านั้น** (Surgical Update)

### ⚖️ เปรียบเทียบให้เห็นภาพ
สมมติบ้านเราทาสีฟ้าอยู่ แล้วเราอยากเปลี่ยนเป็นสีแดง

- **Traditional (HTML String)**:
  - 💣 ทุบบ้านทิ้งทั้งหลัง!
  - 🏗️ สร้างบ้านหลังใหม่ ทาสีแดง
  - (ช้า และเปลืองทรัพยากรมาก)

- **React (Virtual DOM)**:
  - 📝 วาดแปลนบ้านใหม่ (Virtual) สีแดง
  - 🧐 เทียบกับแปลนเก่า (Virtual) สีฟ้า
  - 💡 เจอว่า "อ๋อ ต่างกันแค่สีผนังนี่นา"
  - 🖌️ เดินไปทาสีทับที่ผนังเดิม (Real Update) จบ!
  - (เร็ว และฉลาดสุดๆ)



## 🌍 Real-World Use Case: A Twitter Card

มาลองดูตัวอย่างการประยุกต์ใช้ JSX ในการสร้าง UI จริงๆ กันครับ เช่น "การ์ดทวีต"

```jsx
const Tweet = () => {
  const user = {
    name: "Elon Musk",
    handle: "@elonmusk",
    avatar: "https://.../elon.jpg",
    verified: true
  };
  
  const content = "React is awesome! 🚀";
  const date = "Oct 25";

  return (
    // Container หลัก
    <div className="tweet-card">
      
      {/* Avatar ส่วนซ้าย */}
      <img src={user.avatar} className="avatar" alt={user.name} />
      
      {/* เนื้อหาส่วนขวา */}
      <div className="content">
        <div className="header">
          <span className="name">{user.name}</span>
          
          {/* Conditional Rendering: โชว์ติ๊กถูกเฉพาะถ้า verified เป็น true */}
          {user.verified && <span className="badge">✅</span>}
          
          <span className="handle">{user.handle}</span>
          <span className="dot">·</span>
          <span className="date">{date}</span>
        </div>
        
        <p className="message">{content}</p>
        
        <div className="actions">
          <button>💬 5K</button>
          <button>RETWEET 10K</button>
          <button>❤️ 50K</button>
        </div>
      </div>
    
    </div>
  );
};
```

**สิ่งที่สังเกตได้:**
1.  โครงสร้าง html ซับซ้อน แต่อ่านง่ายใน JSX
2.  มีการใช้ `{}` แทรกข้อมูล dynamic
3.  มีการใช้ **Conditional Rendering** (`user.verified && ...`) เพื่อโชว์/ซ่อน element ตาม logic



## 🥊 Challenges (แบบฝึกหัดท้าทาย)

ฝึกเขียนเยอะๆ จะได้ชินมือครับ!

### Level 1: Personal Bio (พื้นฐาน)
สร้าง Component ชื่อ `Bio` ที่แสดงข้อมูลของคุณเอง โดยต้องใช้ตัวแปรทั้งหมด (ห้าม hardcode ใน JSX)
- `firstName`, `lastName` (แสดงเป็น H1)
- `age` (แสดงเป็น Paragraph)
- `hobbies` (Array ของงานอดิเรก แสดงผลข้อแรกสุด)

::: details ✨ เฉลย
```jsx
const Bio = () => {
  const firstName = "John";
  const lastName = "Doe";
  const age = 25;
  const hobbies = ["Coding", "Gaming", "Reading"];

  return (
    <div className="bio-card">
      <h1>{firstName} {lastName}</h1>
      <p>Age: {age}</p>
      <p>My favorite hobby is: {hobbies[0]}</p>
    </div>
  );
};
```
:::

### Level 2: Conditional Greeting (Logic)
สร้าง Component ที่รับ `time` (0-23) เป็นตัวแปร
- ถ้า time < 12 ให้แสดง "Good Morning ☀️" (สีเหลือง)
- ถ้า time < 18 ให้แสดง "Good Afternoon 🌤️" (สีส้ม)
- ถ้า time >= 18 ให้แสดง "Good Evening 🌙" (สีน้ำเงิน)
*Hint: ใช้ตัวแปรเก็บ style object*

::: details ✨ เฉลย
```jsx
const Greeting = () => {
  const time = 14; // ลองเปลี่ยนเลขดู
  let message = "";
  let color = "";

  if (time < 12) {
    message = "Good Morning ☀️";
    color = "#FFD700"; // Gold
  } else if (time < 18) {
    message = "Good Afternoon 🌤️";
    color = "#FFA500"; // Orange
  } else {
    message = "Good Evening 🌙";
    color = "#191970"; // MidnightBlue
  }

  return <h1 style={{ color: color }}>{message}</h1>;
};
```
:::

### Level 3: Pokemon List (Mapping)
มี Array รายชื่อ Pokemon `['Pikachu', 'Charmander', 'Squirtle', 'Bulbasaur']`
จงใช้ `.map()` เพื่อสร้าง `<li>` สำหรับแต่ละตัว โดยไม่ต้องเขียนซ้ำๆ

::: details ✨ เฉลย
```jsx
const PokemonList = () => {
  const pokemons = ['Pikachu', 'Charmander', 'Squirtle', 'Bulbasaur'];

  return (
    <ul>
      {/* ใช้ map() แปลง string เป็น jsx */}
      {pokemons.map((name, index) => (
        <li key={index}>
          I choose you, <b>{name}</b>!
        </li>
      ))}
    </ul>
  );
};
```
:::



## 📚 Glossary (คำศัพท์น่ารู้)

| คำศัพท์ | ความหมาย |
|:--------|:---------|
| **JSX** | (JavaScript Syntax Extension) การเขียน HTML ใน JS ต้องผ่านการแปลงโดย Babel |
| **Component** | ชิ้นส่วน UI ที่เป็นอิสระ (Reusable Building Block) |
| **Declarative** | การเขียนโค้ดที่เน้น "ผลลัพธ์ที่อยากได้" (What) แทน "ขั้นตอนการทำ" (How) |
| **Babel** | เครื่องมือ (Transpiler) ที่แปลง JSX และ ES6+ ให้เป็น JS รุ่นเก่าที่ Browser เข้าใจ |
| **Virtual DOM** | แบบจำลอง DOM ใน Memory ที่ React ใช้คำนวณความเปลี่ยนแปลงเพื่อประสิทธิภาพสูงสุด |
| **Reconciliation** | กระบวนการเปรียบเทียบ Diffing และอัปเดต Real DOM ของ React |
| **Root Element** | Element นอกสุดที่ห่อหุ้ม JSX ทั้งหมด (React บังคับให้มีแค่ 1 อัน) |
| **Fragment** | `<>...</>` ตัวห่อหุ้ม JSX แบบล่องหน ไม่สร้าง tag จริงใน HTML |
| **Interpolation** | การแทรกค่า `{expression}` ลงใน JSX |
| **CamelCase** | รูปแบบการตั้งชื่อที่ React ใช้กับ Attribute เช่น `className`, `onClick`, `tabIndex` |

---

> 👉 **ไปต่อ: [Project 1: JSX Art Gallery](/react/01-project-jsx-art)**
