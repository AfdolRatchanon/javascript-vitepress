# 11.2 Security Tools (Middlewares)

> *"Defense in Depth."* — **Layered Security Strategy**

รู้ทฤษฎี (OWASP Top 10) ไปแล้ว คราวนี้มาลงมือปฏิบัติ "ติดเกราะ" ให้ Server เรากันครับ
โชคดีที่ในโลก Node.js มี Middleware ดีๆ ที่ช่วยปิดช่องโหว่พื้นฐานให้เราได้ทันที โดยแทบไม่ต้องเขียนโค้ดเองเลย!

3 ทหารเสือที่เราจะใช้ในวันนี้คือ:
1.  **Helmet** 🪖: จัดการ HTTP Headers
2.  **CORS** 🌐: จัดการสิทธิ์การข้ามโดเมน
3.  **Express-Rate-Limit** 🛑: ป้องกันการกดรัวๆ (Brute Force / DDoS)

---

## 🪖 Helmet: Secure HTTP Headers

Browser สมัยใหม่ฉลาดมากครับ มันจะอ่าน **HTTP Headers** ที่ Server ส่งมาเพื่อตัดสินใจว่าจะอนุญาตให้ทำอะไรได้บ้าง
แต่ค่า Default ของ Express นั้น... โล่งโจ้ง! แถมยังมี Header `X-Powered-By: Express` แปะบอก Hacker อี กว่า "ฉันใช้ Express นะ มาเจาะสิ" 🤦‍♂️

**Helmet** จะช่วยลบ Header ที่ไม่ปลอดภัยออก และเติม Header ป้องกันเข้ามาให้ครับ

### Setup

```bash
npm install helmet
```

```javascript
/* app.js */
const helmet = require('helmet');

// ใส่เป็น Middleware ตัวแรกๆ เลย (ก่อน Route ก่อน Static file)
app.use(helmet());
```

### มันทำอะไรบ้าง? (Behind the scenes)
แค่บรรทัดเดียว `app.use(helmet())` มันจะเปิดใช้งานหลายอย่างพร้อมกัน:
- **Hide `X-Powered-By`**: ลบป้ายบอกยี่ห้อ Server ทิ้ง
- **Strict-Transport-Security (HSTS)**: บังคับให้ Browser เรียกเว็บเราผ่าน HTTPS เท่านั้น (ถ้าเคยเข้าแล้วครั้งนึง)
- **X-Frame-Options**: ป้องกันไม่ให้เว็บอื่นเอาเว็บเราไปฝังใน `<iframe>` (กัน Clickjacking)
- **X-XSS-Protection**: เปิดตัวกรอง XSS ของ Browser (เก่าแล้ว แต่ก็ใส่ไว้ดีกว่าไม่มี)
- **Content-Security-Policy (CSP)**: (Advance) กำหนดว่า Script/Image โหลดจากเว็บไหนได้บ้าง

---

## 🌐 CORS: Cross-Origin Resource Sharing

โดยปกติ Browser จะมีกฎ **Same-Origin Policy**:
- เว็บ `a.com` จะยิง AJAX ไปขอข้อมูลจาก `b.com` **ไม่ได้** (เพื่อความปลอดภัย)
- แต่ในยุค Modern App: Frontend (`localhost:5173`) กับ Backend (`localhost:3000`) มันคนละ Origins กัน!

ถ้าไม่แก้ จะเจอ Error แดงเถือกใน Console: *Access to fetch has been blocked by CORS policy* 😱

### Setup

```bash
npm install cors
```

```javascript
/* app.js */
const cors = require('cors');

// แบบที่ 1: เปิดอ้าซ่า (อนุญาตทุกโดเมน) - เหมาะกับ Public API / Dev Mode
app.use(cors());

// แบบที่ 2: ระบุโดเมน (อนุญาตเฉพาะเพื่อนเรา) - เหมาะกับ Production 🛡️
app.use(cors({
  origin: ['https://my-frontend.com', 'https://admin.my-site.com'],
  methods: ['GET', 'POST', 'PUT', 'DELETE'], // อนุญาตแค่ Method ที่จำเป็น
  credentials: true // อนุญาตให้ส่ง Cookie/Auth Header มาด้วย
}));
```

---

## 🛑 Rate Limiting: Traffic Control

ถ้ามี Hacker (หรือ User มือบอน) เขียน Script วนลูปยิง API Login วินาทีละ 100 ครั้ง...
Database เราพังแน่นอน (หรือโดนเดารหัสผ่านจนถูก)

**Rate Limiting** คือการจำกัดว่า **"IP นี้ ยิงได้ไม่เกิน X ครั้ง ในเวลา Y นาที"**

### Setup

```bash
npm install express-rate-limit
```

```javascript
/* middleware/limiter.js */
const rateLimit = require('express-rate-limit');

const limiter = rateLimit({
  windowMs: 15 * 60 * 1000, // 15 นาที
  max: 100, // จำกัด 100 request ต่อ IP (ต่อ 15 นาที)
  message: 'Too many requests from this IP, please try again later.', // คำเตือนเมื่อเกิน
  standardHeaders: true, // ส่ง Header แจ้ง User (RateLimit-Remaining)
  legacyHeaders: false,
});

// วิธีใช้:
// 1. ใช้กับทั้งแอป (Global)
// app.use(limiter);

// 2. ใช้เฉพาะ Route อันตราย (Login/Register) - แนะนำ!
// app.use('/api/auth', limiter);

module.exports = limiter;
```

---

## 🌍 Real-World Integration

มาประกอบร่าง 3 ทหารเสือเข้ากับ Express App ของเรา:

```javascript
/* app.js */
const express = require('express');
const helmet = require('helmet');
const cors = require('cors');
const rateLimit = require('express-rate-limit');

const app = express();

// 1. Security Headers (สำคัญสุด ใส่ก่อนเลย)
app.use(helmet());

// 2. CORS (ให้ Frontend เราคุยได้)
app.use(cors({ origin: 'http://localhost:5173' }));

// 3. Body Parser (จำกัดขนาด Body กัน Hacker ส่งไฟล์ยักษ์มาถล่ม)
app.use(express.json({ limit: '10kb' })); 

// 4. Rate Limit (เฉพาะ Route สำคัญ)
const loginLimiter = rateLimit({
  windowMs: 10 * 60 * 1000, // 10 นาที
  max: 5, // ผิดได้แค่ 5 ครั้ง
  message: "ใส่รหัสผิดบ่อยเกินไป โปรดรอ 10 นาที"
});
app.use('/api/login', loginLimiter);

// ... Routes ...

app.listen(3000, () => console.log('Secure Server Running 🛡️'));
```

---

## 🥊 Challenges

### Level 1: Strict CORS
จงตั้งค่า CORS ให้อนุญาตเฉพาะ:
- Origin: `https://production-app.com`
- Method: `GET` และ `POST` เท่านั้น (ห้าม DELETE)

::: details ✨ เฉลย
```javascript
app.use(cors({
  origin: 'https://production-app.com',
  methods: ['GET', 'POST']
}));
```
:::

### Level 2: Custom Rate Limit Response
จงตั้งค่า Rate Limit ให้เมื่อ User ยิงเกินลิมิต ให้ส่ง JSON Response กลับไปว่า:
`{ error: "ใจเย็นๆ วัยรุ่น! (Too Many Requests)", retryAfter: "15m" }`

::: details ✨ เฉลย
```javascript
const limiter = rateLimit({
  // ... config อื่นๆ
  handler: (req, res, next, options) => {
    res.status(429).json({
      error: "ใจเย็นๆ วัยรุ่น! (Too Many Requests)",
      retryAfter: "15m"
    });
  }
});
```
:::

---

## 📚 Glossary

| คำศัพท์ | ความหมาย |
|:--------|:---------|
| **HTTP Headers** | ข้อมูลแปะหน้าซองจดหมาย HTTP ที่บอก Browser ว่าควรปฏิบัติตัวอย่างไร |
| **Origin** | Protocol + Domain + Port (เช่น `http` + `localhost` + `3000`) |
| **CORS** | ระบบอนุญาตการข้ามเขต Origin (โดยปกติ Browser จะบล็อกถ้า Server ไม่อนุญาต) |
| **DDoS** | Distributed Denial of Service (การระดมยิงเว็บจนล่ม) |
| **Clickjacking** | การหลอกให้ User คลิกปุ่มอันตรายโดยเอาเว็บไปซ่อนใน iframe โปร่งใส |
| **Man-in-the-Middle** | การดักฟังข้อมูลกลางทาง (ป้องกันได้ด้วย HTTPS/HSTS) |

---

## 🔗 References

- [Helmet.js Docs](https://helmetjs.github.io/)
- [MDN: CORS](https://developer.mozilla.org/en-US/docs/Web/HTTP/CORS) - อ่านเพื่อให้เข้าใจลึกซึ้ง
- [Express Rate Limit](https://www.npmjs.com/package/express-rate-limit)

---

> 👉 **จบ Module 11 แล้วครับ! ยินดีด้วย 🎉 คุณพร้อมที่จะสร้าง Secure Web API แล้ว!**
> ไปลองทำโปรเจกต์ส่งท้ายกันเถอะ:
> **[Project: Secure Blog API](/node/11-project-secure-api) (Coming Soon)**
