# Module 4.1: REST API Concepts & Design Principles 🌐

> **"API is the user interface of the developer."**

ในยุคปัจจุบันที่ Application ไม่ได้มีแค่หน้าเว็บตัวเดียว แต่มีทั้ง Mobile App, Desktop App, IoT, และ 3rd Party Integrations... การสื่อสารระหว่าง Client และ Server จึงเป็นเรื่องที่สำคัญที่สุด และมาตรฐานที่ครองโลกอยู่ตอนนี้ก็คือ **RESTful API** ครับ

บทนี้เราจะไม่ใช่แค่เรียนว่า "GET/POST คืออะไร" แต่เราจะลงลึกถึง **ปรัชญาการออกแบบ (Design Philosophy)**, **สถาปัตยกรรม (Architecture)**, และ **Best Practices** ที่จะทำให้ API ของคุณดูเป็นมืออาชีพและ Scalable ครับ

---

## 🏗️ 1. What is REST? (The Architecture)

**REST** ย่อมาจาก **REpresentational State Transfer**
เป็นรูปแบบสถาปัตยกรรมซอฟต์แวร์ (Software Architectural Style) ที่ถูกนำเสนอโดย Dr. Roy Fielding ในวิทยานิพนธ์ปริญญาเอกของเขาในปี 2000 (ซึ่งเป็นคนเดียวกับที่ร่วมร่างมาตรฐาน HTTP 1.1)

REST ไม่ใช่ "กฎ" (Rule) ที่บังคับตายตัว แต่เป็น "ข้อแนะนำ" (Guideline) หรือ "ข้อจำกัด" (Constraints) 6 ข้อ ที่ถ้าทำตามแล้ว ระบบของเราจะมีประสิทธิภาพ, ยืดหยุ่น และรองรับการขยายตัวได้ดีเยี่ยม

### The 6 Guiding Constraints of REST

1.  **Client-Server Architecture**:
    *   **แยกส่วนกันชัดเจน**: Client (คนขอ) และ Server (คนให้) ต้องไม่ขึ้นต่อกัน
    *   **ข้อดี**: เราสามารถแก้โค้ดฝั่ง Server (เปลี่ยน Database, Refactor Code) ได้โดยไม่ต้องไปแก้โค้ดฝั่ง Client (ตราบใดที่ Interface หรือ URL ยังเหมือนเดิม) และ Client หลายๆ แบบ (Web, iOS, Android) สามารถใช้ Server เดียวกันได้

2.  **Stateless** (สำคัญมาก!):
    *   **ไม่จำ**: Server จะไม่เก็บ "สถานะ" ของการคุยครั้งก่อนหน้าไว้ (No Session/Context stored on Server)
    *   **ครบจบในตัว**: ทุก Request ที่ส่งมา ต้องมีข้อมูลครบถ้วนที่ Server จำเป็นต้องใช้ในการประมวลผล (เช่น ส่ง Token มาทุกครั้งเพื่อบอกว่าเป็นใคร)
    *   **ข้อดี**: Scale ง่ายมาก! ถ้าเรามี Server 100 เครื่อง Request จะวิ่งไปเครื่องไหนก็ได้ เพราะไม่ต้องกังวลว่า "Session อยู่ที่เครื่อง 1 นะ"

3.  **Cacheable**:
    *   ข้อมูลที่ส่งกลับไป ต้องบอกได้ว่า "จำไว้ได้ไหม? นานแค่ไหน?" (Cache-Control)
    *   **ข้อดี**: ลดภาระ Server และ Client ได้ข้อมูลเร็วขึ้น ถ้าข้อมูลนั้นไม่เปลี่ยนบ่อย

4.  **Layered System**:
    *   Client ไม่จำเป็นต้องรู้ว่ากำลังคุยกับ Server ตัวจริง หรือคุยผ่าน Proxy, Load Balancer, Gateway
    *   **ข้อดี**: เราสามารถเพิ่ม Security Layer, Caching Layer หรือแบ่ง Microservices ได้โดย Client ไม่ต้องแก้โค้ด

5.  **Code on Demand** (Optional):
    *   Server สามารถส่ง Executable Code (เช่น Java Applet, Flash, JavaScript) ไปรันที่ Client ได้ (ข้อนี้เดี๋ยวนี้ไม่ค่อยเน้นเท่าไหร่ในบริบทของ API)

6.  **Uniform Interface** (หัวใจสำคัญ):
    *   ต้องมีรูปแบบการคุยที่เป็นมาตรฐานเดียวกันทั้งระบบ (Resource-Based)

---

## 🚥 2. HTTP Methods Deep Dive (The Verbs)

ใน REST เรามองทุกอย่างเป็น **"ทรัพยากร" (Resource)** และเราใช้ **HTTP Methods** เป็น **"กริยา" (Verbs)** เพื่อกระทำกับทรัพยากรนั้นๆ

### 🛠️ Common Methods

| Method | คำอธิบาย | SQL Equivalent | Idempotent? | Safe? |
| :--- | :--- | :--- | :--- | :--- |
| **GET** | ขอข้อมูล (Read) | SELECT | ✅ Yes | ✅ Yes |
| **POST** | สร้างข้อมูลใหม่ (Create) | INSERT | ❌ No | ❌ No |
| **PUT** | แก้ไขข้อมูล **"ทั้งก้อน"** (Replace) | UPDATE | ✅ Yes | ❌ No |
| **PATCH** | แก้ไขข้อมูล **"บางส่วน"** (Modify) | UPDATE | ⚠️ Maybe | ❌ No |
| **DELETE** | ลบข้อมูล (Delete) | DELETE | ✅ Yes | ❌ No |

### 🤓 Technical Terms ที่ต้องรู้

*   **Safe Methods**: คือ Method ที่เรียกแล้ว **Server State ไม่เปลี่ยน** (Database ไม่เปลี่ยน)
    *   ได้แก่: `GET`, `HEAD`, `OPTIONS`
    *   *หมายความว่าเราเรียก GET กี่ล้านครั้ง ข้อมูลใน DB ก็ยังเหมือนเดิม*

*   **Idempotent Methods**: คือ Method ที่ **ทำซ้ำหลายครั้ง ได้ผลลัพธ์เหมือนทำครั้งเดียว**
    *   ได้แก่: `GET`, `PUT`, `DELETE` (และ `HEAD`, `OPTIONS`)
    *   *ตัวอย่าง DELETE*: สั่งลบ ID 1 ครั้งแรก -> หาย, สั่งลบ ID 1 ครั้งที่สอง -> ก็ยังหายเหมือนเดิม (ระบบ state สุดท้ายเหมือนเดิม)
    *   *ตัวอย่าง POST (ไม่ Idempotent)*: สั่งซื้อของ 1 ครั้ง -> ได้ของ 1 ชิ้น, สั่งซื้อของซ้ำอีก 1 ครั้ง -> ได้ของ 2 ชิ้น! (State เปลี่ยนไปเรื่อยๆ)

### ⚔️ PUT vs PATCH: The Eternal Battle

นี่คือคำถามปราบเซียนเวลาสัมภาษณ์งาน!

*   **PUT (Replace)**:
    *   Client ส่งมา: `{ "name": "John", "age": 30 }`
    *   Server ทำ: เอา Object นี้ไป **ทับ** ของเดิมเลย
    *   *ข้อควรระวัง*: ถ้าของเดิมมี field "email" แต่ Client ไม่ส่งมา... "email" จะหายไปเลยนะ! (กลายเป็น null)

*   **PATCH (Update Partial)**:
    *   Client ส่งมา: `{ "age": 31 }`
    *   Server ทำ: หา record นั้น แล้วแก้ **เฉพาะ** field "age" เป็น 31 ส่วน "name" และ "email" ยังอยู่เหมือนเดิม
    *   *ความจริง*: ในทางปฏิบัติ Developer หลายคนขี้เกียจแยก และใช้ PUT แต่เขียน Logic แบบ PATCH (ซึ่งผิดหลัก REST แต่ก็เป็นที่ยอมรับได้ในระดับหนึ่ง เรียกว่า Pragmatic REST)

---

## 🔢 3. HTTP Status Codes (The Response)

Status Code เปรียบเสมือน "ภาษา" ที่ Server ตอบกลับมา Client ที่ดีควรอ่าน Status Code ก่อนอ่าน Body เสมอ

### 🟢 2xx Success (ผ่านฉลุย)
*   **200 OK**: มาตรฐานสำหรับการสำเร็จทั่วไป (ใช้กับ GET, PUT, PATCH)
*   **201 Created**: สร้างสำเร็จแล้วนะ (ใช้กับ POST - ควรส่ง Location Header กลับมาด้วยว่าของใหม่อยู่ไหน)
*   **204 No Content**: ทำสำเร็จแล้ว แต่ไม่มีอะไรจะส่งคืน (นิยมใช้กับ DELETE หรือ PUT ที่ไม่อยากส่ง Data กลับ)

### 🟡 3xx Redirection (ไปทางนู้น)
*   **301 Moved Permanently**: ย้ายบ้านถาวรแล้ว (SEO จะจำ URL ใหม่)
*   **304 Not Modified**: ข้อมูลยังเหมือนเดิม (ใช้คู่กับ Caching - Browser จะเอาของเก่าใน Cache มาแสดงแทน)

### 🟠 4xx Client Error (ความผิดเธอ)
*   **400 Bad Request**: ส่งข้อมูลมาผิดรูปแบบ, ลืมส่ง Required Field, JSON Syntax Error
*   **401 Unauthorized**: "คุณเป็นใคร?" (ยังไม่ได้ Login / Token หมดอายุ)
*   **403 Forbidden**: "คุณคือใครเรารู้แล้ว แต่คุณไม่มีสิทธิ์!" (เช่น User ธรรมดาพยายามลบข้อมูล Admin)
*   **404 Not Found**: หาไม่เจอ (URL ผิด หรือ ID ไม่มีในระบบ)
*   **405 Method Not Allowed**: URL ถูก แต่ห้ามใช้ Method นี้ (เช่น `POST /users/1` ทั้งที่ควรเป็น PUT)
*   **422 Unprocessable Entity**: ข้อมูลถูก Format แต่ผิด Logic (เช่น อีเมลซ้ำ, รหัสผ่านสั้นไป) -> นิยมใช้แจ้ง Validation Error
*   **429 Too Many Requests**: ใจเย็นๆ อย่ารัว! (Rate Limiting)

### 🔴 5xx Server Error (ความผิดฉัน)
*   **500 Internal Server Error**: บั๊กแตก, โค้ดพัง, Uncaught Exception
*   **502 Bad Gateway**: Server ตัวหน้า (Nginx) ติดต่อ Server ตัวหลัง (Node) ไม่ได้
*   **503 Service Unavailable**: Server กำลังปิดปรับปรุง หรือ Load หนักจนรับไม่ไหว

---

## 🛣️ 4. Resource Naming Guide (Best Practices)

การตั้งชื่อ URL (Endpoint) คือศิลปร ะอย่างหนึ่งของ API Designer

### ✅ กฎทองของการตั้งชื่อ
1.  **ใช้คำนาม (Nouns) ไม่ใช่คำกริยา (Verbs)**:
    *   ❌ `GET /getAllUsers`
    *   ❌ `POST /createUser`
    *   ✅ `GET /users`
    *   ✅ `POST /users`

2.  **ใช้ Plural Nouns (พหูพจน์) เสมอ**:
    *   ✅ `/users`, `/products`, `/orders`
    *   (เพื่อให้รู้ว่าเป็น Collection ของ Resource)

3.  **ใช้ Hierarchy เพื่อแสดงความสัมพันธ์**:
    *   ✅ `GET /users/1/orders` (ขอรายการสั่งซื้อ *ของ* user 1)
    *   ✅ `GET /users/1/orders/5` (ขอรายการสั่งซื้อใบที่ 5 *ของ* user 1)

4.  **ใช้ Query Parameters สำหรับ Filtering/Sorting/Pagination**:
    *   การกรองไม่ควรเปลี่ยน URL Path
    *   ✅ `GET /products?category=electronics&sort=price_desc&page=2`

### 🏗️ Advanced Naming Patterns

*   **Nested Resources (ลึกไม่เกิน 2 ชั้น)**:
    *   ถ้าซ้อนลึกเกินไป URL จะยาวและจัดการยาก
    *   ❌ `GET /users/1/orders/99/items/5/reviews`
    *   ✅ ให้ Flatted มันออกมาถ้าทำได้ เช่น `GET /orders/99/items`

*   **Actions (ที่ไม่อยู่ใน CRUD)**:
    *   บางครั้งเราต้องการ "Action" ที่ไม่ใช่การสร้าง/แก้ไขข้อมูลตรงๆ เช่น "Login", "Search", "Calculate"
    *   อนุโลมให้ใช้ Verb ได้ในกรณีนี้ (เรียกว่า Controller Resource)
    *   ✅ `POST /auth/login`
    *   ✅ `GET /search?q=keyword`
    *   ✅ `POST /products/1/activate` (หรือ `PATCH /products/1` body `{ status: 'active' }`)

---

## 📡 5. Advanced REST Topics

### 🔄 Content Negotiation
Server เดียวกัน สามารถส่งข้อมูลได้หลายรูปแบบ (JSON, XML, CSV) ตามความต้องการของ Client
Client จะส่ง Header `Accept` มาบอก

*   Client A: `Accept: application/json` -> Server ตอบ JSON
*   Client B: `Accept: application/xml` -> Server ตอบ XML (ถ้าทำรองรับไว้)

### 🏷️ API Versioning
เมื่อ API มีการเปลี่ยนแปลงโครงสร้าง (Breaking Change) เราไม่ควรแก้ทับของเดิม เพราะจะทำให้ Client เก่าพัง
เราควรทำ Versioning:

1.  **URL Versioning** (นิยมสุด):
    *   `GET /api/v1/products`
    *   `GET /api/v2/products`
2.  **Header Versioning**:
    *   `Accept-Version: v1`
3.  **Media Type Versioning**:
    *   `Accept: application/vnd.mycompany.v1+json`

### 📄 HATEOAS (Hypermedia As The Engine Of Application State)
Constraint ข้อสุดท้ายของ REST ที่คนมักละเลย
คือการที่ API ตอบกลับมาพร้อมกับ "Links" ที่บอกว่า "ทำอะไรต่อได้บ้าง?"

**Example Response:**
```json
{
  "id": 1,
  "name": "IPhone 15",
  "price": 35000,
  "links": [
    { "rel": "self", "method": "GET", "href": "http://api.com/products/1" },
    { "rel": "update", "method": "PUT", "href": "http://api.com/products/1" },
    { "rel": "delete", "method": "DELETE", "href": "http://api.com/products/1" },
    { "rel": "add_to_cart", "method": "POST", "href": "http://api.com/cart" }
  ]
}
```
*ข้อดี*: Client ไม่ต้อง Hardcode URL, Server สามารถเปลี่ยน URL ได้ตามใจตราบใดที่ `rel` ยังเหมือนเดิม

---

## 🆚 Comparison: REST vs The Others

### REST vs GraphQL
*   **REST**:
    *   Fixed Structure: ขอ `/users` ได้ field ทั้งหมดตามที่ Server กำหนด
    *   Over-fetching: ได้ข้อมูลมาเกินความจำเป็น
    *   Under-fetching: ต้องยิงหลาย Request กว่าจะได้ข้อมูลครบ (N+1 Problem)
    *   ❤️ เหมาะกับ: Simple CRUD, Caching ง่าย, Public API
*   **GraphQL**:
    *   Flexible Structure: Client เลือก field เองได้ `query { user { name } }`
    *   Single Endpoint: ยิง `/graphql` ที่เดียว
    *   ❤️ เหมาะกับ: Complex Data Relation, Frontend ที่ต้องการความยืดหยุ่นสูง, Mobile App (ประหยัด Data)

### REST vs gRPC
*   **REST**: Text-based (JSON), Human Readable, browser รองรับทันที
*   **gRPC**: Binary (Protobuf), เร็วกว่ามาก, ใช้ HTTP/2
*   ❤️ เหมาะกับ: Internal Microservices Communication (Service-to-Service) ที่ต้องการความเร็วสูง

---

## ⚡ Challenge: Architecture Quiz 📝

จงวิเคราะห์ Scenario ต่อไปนี้และเลือกว่าควรออกแบบ API อย่างไร

1.  ต้องการดึงข้อมูลสินค้า โดยสามารถกรองตามช่วงราคา และเรียงตามความนิยม
    *   *Option A*: `POST /products/search` Body: `{ minPrice: 100, sort: 'popular' }`
    *   *Option B*: `GET /products?min_price=100&sort=popular`
    *   *Option C*: `GET /products/price/100/sort/popular`

2.  ต้องการลบสินค้าหลายชิ้นพร้อมกัน (Batch Delete)
    *   *Option A*: ยิง `DELETE /products/:id` วน Loop 10 รอบ
    *   *Option B*: `DELETE /products` Body: `[1, 2, 3]`
    *   *Option C*: `POST /products/delete` Body: `[1, 2, 3]`

::: details ✨ เฉลยและคำอธิบาย
1.  **ตอบ Option B**: การค้นหาและกรองข้อมูลไม่ควรเปลี่ยน State ของ Server ดังนั้นใช้ `GET` ถูกต้องที่สุด และใช้ Query Params ในการกรองเพื่อให้ URL Clean และ Cacheable
    *   *Option A ผิดเพราะ*: POST ไม่ควรใช้กับการดึงข้อมูล (ถ้าไม่จำเป็นจริงๆ) และ Cache ไม่ได้
    *   *Option C ผิดเพราะ*: URL Path ควรใช้ระบุ Resource ไม่ใช่เงื่อนไขการกรอง

2.  **ตอบ Option B หรือ C**:
    *   *Option A ผิด*: ไม่ Efficient อย่างแรงที่ต้องสร้าง HTTP Connection 10 รอบ
    *   *Option B*: ตามทฤษฎี DELETE สามารถรับ Body ได้ใน HTTP Spec แต่บาง Server/Client อาจจะไม่รองรับ
    *   *Option C*: ปลอดภัยที่สุดและ Practical ในการใช้งานจริง (Controller Action)
:::

---

## 📚 FAQ & Common Mistakes

**Q: ส่งข้อมูลผ่าน Body ใน GET Request ได้ไหม?**
A: ตาม HTTP Spec แล้ว **ทำได้** แต่ **ไม่ควรทำอย่างยิ่ง!** เพราะ:
1.  Server/Proxy บางตัวจะตัด Body ทิ้งถ้าเป็น GET
2.  Browser/Cache จะไม่เอา Body ไปคำนวณ Cache Key
3.  เครื่องมือ Log ส่วนใหญ่ไม่เก็บ Body ของ GET

**Q: ควรใช้ HTTP 200 หรือ 404 ถ้าค้นหา List แล้วไม่เจอข้อมูล? (`GET /users?name=unknown`)**
A: ควรใช้ **200 OK** พร้อม Body เป็น Array ว่าง `[]`
เพราะคำสั่งค้นหา "ทำงานสำเร็จ" (Server ไม่ได้พัง, URL ไม่ได้ผิด) แค่ผลลัพธ์เป็นเซตว่าง
*ใช้ 404 ก็ต่อเมื่อระบุเจาะจง Resource ID แล้วไม่เจอ เช่น `GET /users/999`*

**Q: Stateless แล้วจะรู้ได้ไงว่า User Login แล้ว?**
A: Stateless หมายถึง Server ไม่เก็บ Session State ใน Memory แต่ Client ต้องส่ง "หลักฐาน" (State) มาในทุก Request
หลักฐานนั้นคือ **Token (JWT)** หรือ **Session ID (Cookie)** นั่นเอง Server แค่เอาหลักฐานไป verify ก็รู้แล้วว่าใคร

---

## 🔗 References for Deep Dive
- [Richardson Maturity Model](https://martinfowler.com/articles/richardsonMaturityModel.html) - ระดับความ "แท้" ของ REST API
- [Microsoft REST API Guidelines](https://github.com/microsoft/api-guidelines) - มาตรฐานการออกแบบของ Microsoft
- [Fielding Dissertation: Chapter 5](https://www.ics.uci.edu/~fielding/pubs/dissertation/rest_arch_style.htm) - ต้นฉบับทฤษฎี REST

> 👉 **บทต่อไป: [Module 4.2 - Handling Requests & Routing](/node/04-02-handling-requests)**
