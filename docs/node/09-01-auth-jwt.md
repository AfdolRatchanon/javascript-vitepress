# 9.1 Authentication & JWT Basics

> *"Security is not an option, is a necessity."*

ในที่สุดก็ถึงเวลาทำระบบที่ทุกคนรอคอย... **ระบบสมาชิก! (Login/Register)** 🔐
หัวข้อนี้สำคัญมากเพราะเกี่ยวข้องกับความปลอดภัยของข้อมูลผู้ใช้โดยตรง ห้ามพลาดแม้แต่บรรทัดเดียว!

---

## 🐣 Analogy: ID Card (บัตรประชาชน) vs Wristband (สายรัดข้อมือ)

การยืนยันตัวตน (Authentication) มี 2 แบบหลักๆ:

1.  **Session-Based (แบบเก่า)**:
    - เหมือนไปติดต่อราชการ คุณยื่นบัตรประชาชน
    - เจ้าหน้าที่ตรวจสอบ แล้วสร้างแฟ้มประวัติ (Session) เก็บไว้ในตู้ที่สำนักงาน
    - คุณได้บัตรคิว (Session ID) กลับมา
    - ครั้งหน้ามาติดต่อ ก็ยื่นบัตรคิว เจ้าหน้าที่ก็ไปค้นแฟ้มเดิมมาดู
    - **ข้อเสีย**: Server ต้องเก็บแฟ้มทุกคน (เปลือง RAM), ขยาย Server ยาก

2.  **Token-Based (JWT - แบบใหม่/นิยม)**:
    - เหมือนไปงาน Concert คุณยื่นตั๋ว
    - เจ้าหน้าที่ให้ **สายรัดข้อมือ (Token)** ที่มีข้อมูลเขียนไว้เลยว่า "Vip Zone, Expire 22:00"
    - ครั้งหน้าจะเข้าโซนไหน ก็แค่โชว์สายเลรัดข้อมือ
    - เจ้าหน้าที่แค่ส่องไฟดู (Verify Signature) ว่าสายรัดของแท้ไหม ไม่ต้องวิ่งไปเปิดแฟ้มที่ส่วนกลาง
    - **ข้อดี**: Server ไม่ต้องจำอะไรเลย (Stateless), ขยาย Server ง่ายมาก

ในคอร์สนี้เราจะใช้แบบที่ 2 คือ **JWT (JSON Web Token)** ครับ

---

## 🔐 Hashing Password (การเข้ารหัสรหัสผ่าน)

กฎเหล็ก: **ห้ามเก็บ Password ของผู้ใช้เป็น Plain Text เด็ดขาด!!** ❌
ถ้า Database หลุด แล้วคุณเก็บ "123456" ไว้ Hacker จะเอาไปล็อกอินเว็บอื่นของเหยื่อได้ทันที

เราต้องแปลงรหัสผ่านให้เป็นภาษาต่างดาวที่อ่านไม่ออก เรียกว่าการ **Hash**
โดยเราจะใช้ไลบรารีชื่อ **bcrypt**

```javascript
const bcrypt = require('bcryptjs');

async function hashPassword(plainPassword) {
  // 1. สร้าง Salt (ค่าสุ่มเพื่อกัน Pattern)
  const salt = await bcrypt.genSalt(10);
  
  // 2. Hash!
  const hashedPassword = await bcrypt.hash(plainPassword, salt);
  
  return hashedPassword; 
  // ผลลัพธ์: $2a$10$X7... (ยาวๆ อ่านไม่ออก และแปลงกลับไม่ได้)
}

async function verifyPassword(inputPassword, storedHash) {
  // เช็คว่ารหัสที่กรอกมา ตรงกับ Hash ที่เก็บไว้ไหม
  const isMatch = await bcrypt.compare(inputPassword, storedHash);
  return isMatch; // true/false
}
```

---

## 🎟️ JWT (JSON Web Token)

JWT คือสตริงยาวๆ ที่มี 3 ส่วนคั่นด้วยจุด (`.`):
1.  **Header**: บอกว่าเป็น JWT และใช้อัลกอริทึมอะไร
2.  **Payload**: ข้อมูลที่เราอยากฝากไว้ (เช่น User ID, Role)
3.  **Signature**: ลายเซ็นดิจิทัล (รับรองว่าข้อมูลไม่ถูกแก้ไข)

### 🛠️ Setup

```bash
npm install jsonwebtoken bcryptjs
```

### 1. สร้าง Token (Login)

```javascript
const jwt = require('jsonwebtoken');

function generateToken(user) {
  const payload = { 
    id: user._id, 
    role: user.role 
  };
  
  const secret = 'MY_SUPER_SECRET_KEY'; // ของจริงต้องเก็บใน .env
  
  // สร้าง Token, หมดอายุใน 1 ชั่วโมง
  const token = jwt.sign(payload, secret, { expiresIn: '1h' });
  
  return token;
}
```

### 2. ตรวจสอบ Token (Middleware)

```javascript
function authenticateToken(req, res, next) {
  // ดึง Token จาก Header: "Authorization: Bearer <token>"
  const authHeader = req.headers['authorization'];
  const token = authHeader && authHeader.split(' ')[1];

  if (!token) return res.status(401).json({ error: "Access Denied" });

  try {
    // ตรวจสอบลายเซ็น
    const verified = jwt.verify(token, 'MY_SUPER_SECRET_KEY');
    req.user = verified; // แปะข้อมูล user ลงใน request เพื่อใช้ต่อ
    next(); // ผ่าน! ไปทำงานต่อได้
  } catch (err) {
    res.status(403).json({ error: "Invalid Token" });
  }
}
```

---

## 🥊 Challenges

### Level 1: Strong Password Validation
เขียนฟังก์ชัน `validatePassword(password)` ที่คืนค่า `true` เฉพาะเมื่อ:
1.  ยาวอย่างน้อย 8 ตัวอักษร
2.  มีตัวพิมพ์ใหญ่ (A-Z) อย่างน้อย 1 ตัว
3.  มีตัวเลข (0-9) อย่างน้อย 1 ตัว

::: details ✨ เฉลย
```javascript
function validatePassword(password) {
  const minLength = 8;
  const hasUpperCase = /[A-Z]/.test(password);
  const hasNumber = /[0-9]/.test(password);
  
  return password.length >= minLength && hasUpperCase && hasNumber;
}
```
:::

### Level 2: Role-Based Access Control (RBAC)
เขียน Middleware `isAdmin` ที่ทำงานต่อจาก `authenticateToken`
โดยให้ผ่านเฉพาะ user ที่มี `role === 'admin'` เท่านั้น

::: details ✨ เฉลย
```javascript
function isAdmin(req, res, next) {
  // req.user มาจาก authenticateToken ที่แปะไว้ให้
  if (req.user && req.user.role === 'admin') {
    next();
  } else {
    res.status(403).json({ error: "Admin access required" });
  }
}
```
:::

---

## 📚 Glossary

| คำศัพท์ | ความหมาย |
|:--------|:---------|
| **Authentication (AuthN)** | การยืนยันตัวตน (คุณคือใคร? -> Login) |
| **Authorization (AuthZ)** | การตรวจสอบสิทธิ์ (คุณทำอะไรได้บ้าง? -> Permission) |
| **Hash** | การแปลงข้อมูลทางเดียว (One-way) ให้เป็นค่าที่ไม่ซ้ำและแปลงกลับไม่ได้ |
| **Salt** | ค่าสุ่มที่เติมเข้าไปก่อน Hash เพื่อป้องกัน Rainbow Table Attack |
| **JWT** | JSON Web Token มาตรฐานการส่งข้อมูลที่ปลอดภัยและยืนยันตัวตนได้ |
| **Bearer Token** | รูปแบบการส่ง Token ใน Header (`Authorization: Bearer <token>`) |
| **Stateless** | การที่ Server ไม่ต้องจำข้อมูล Session ของ User แต่ละคน |

---

> 👉 **ไปต่อ: [Project: Secure Auth System](/node/09-project-auth-system)**
