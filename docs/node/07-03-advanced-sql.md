# 7.3 Advanced SQL

> *"Complex problems often require relationship power."*

หลังจากเรา CRUD พื้นฐานได้แล้ว
ในโลกความเป็นจริง ข้อมูลไม่ได้อยู่โดดๆ แค่ตารางเดียวครับ
ข้อมูลมักจะ **สัมพันธ์กัน** เช่น "นักเรียน" ลงทะเบียน "วิชา" เรียนกับ "คุณครู"

วันนี้เราจะมาปลดล็อคพลังที่แท้จริงของ Relational Database ด้วยการทำ **JOIN**, รักษาความถูกต้องด้วย **Transaction**, และเร่งความเร็วด้วย **Index**

---

## 🔗 The Power of JOINs

การ `JOIN` คือการนำตาราง 2 ตาราง (หรือมากกว่า) มาแปะรวมกัน โดยอาศัย column ที่เชื่อมโยงกัน (Foreign Key)

สมมติเรามี 2 ตาราง:
1.  **Users** (`id`, `name`)
2.  **Orders** (`id`, `user_id`, `amount`) -> `user_id` เชื่อมกับ `Users.id`

### 1. INNER JOIN (ส่วนที่ซ้อนทับกัน)
เอาเฉพาะข้อมูลที่ **มีทั้งคู่** (คนที่มีชื่อใน Users **และ** มีประวัติการสั่งซื้อใน Orders)
ถ้าใครสมัครไว้แต่ไม่เคยซื้อ -> ไม่โชว์

```sql
SELECT users.name, orders.amount 
FROM users
INNER JOIN orders ON users.id = orders.user_id;
```

### 2. LEFT JOIN (เอาทางซ้ายเป็นหลัก)
เอาข้อมูลจากตารางหลัก (LEFT - Users) มา **ทั้งหมด**
ถ้าใครเคยซื้อของ ก็โชว์ยอดเงิน
ถ้าใคร **ไม่เคยซื้อ** -> ยอดเงินจะเป็น `NULL` (แต่ชื่อยังโชว์อยู่นะ)

```sql
-- อยากได้รายชื่อลูกค้าทุกคน พร้อมยอดซื้อ (ถ้ามี)
SELECT users.name, orders.amount 
FROM users
LEFT JOIN orders ON users.id = orders.user_id;
```

> 💡 **Analogy**:
> - **Inner Join**: นัดเพื่อนไปกินข้าว "เอาคนที่ **ว่าง** และ **รับสาย** เท่านั้น" (ต้องครบเงื่อนไข)
> - **Left Join**: เช็คชื่อนักเรียนในห้อง "เรียกชื่อ **ทุกคน**" ใครมาก็ขาน ใครไม่มาก็เงียบ (NULL)

---

## ⚡ Transactions (ACID)

**Transaction** คือกลุ่มของคำสั่ง SQL ที่ต้อง **"สำเร็จทั้งหมด"** หรือ **"ล้มเหลวทั้งหมด"** (All or Nothing)
ห้ามสำเร็จแค่ครึ่งๆ กลางๆ เด็ดขาด!

### Real-World Use Case: การโอนเงิน 💸
นาย A โอนเงินให้นาย B 500 บาท
1.  ตัดเงินนาย A 500 บาท
2.  เพิ่มเงินนาย B 500 บาท

ถ้าทำข้อ 1 สำเร็จ แต่ข้อ 2 ไฟดับ? -> **เงินนาย A หายฟรี!** 😱
เราจึงต้องห่อ 2 คำสั่งนี้ไว้ใน Transaction

### ACID Properties
- **A**tomicity: เป็นอะตอม แยกจากกันไม่ได้ (ทำทั้งหมด หรือไม่ทำเลย)
- **C**onsistency: ข้อมูลต้องถูกต้องตามกฎ (เงินติดลบไม่ได้)
- **I**solation: การทำรายการพร้อมกันต้องไม่ตีกัน
- **D**urability: ถ้าบันทึกแล้ว แม้ไฟดับข้อมูลต้องอยู่

### Syntax (Node.js Implementation)

```javascript
/* สมมติใช้ connection จาก connection.getConnection() (ใช้ pool.query ตรงๆ ไม่ได้ ต้องยืม connection มา) */

try {
  await connection.beginTransaction(); // 🏁 เริ่มจับตาดู

  // 1. ตัดเงิน A
  await connection.query('UPDATE accounts SET balance = balance - 500 WHERE id = ?', ['A']);
  
  // 2. เพิ่มเงิน B
  await connection.query('UPDATE accounts SET balance = balance + 500 WHERE id = ?', ['B']);

  // 🎉 ถ้ามาถึงตรงนี้แปลว่าผ่านหมด
  await connection.commit(); // บันทึกจริง!
  console.log('Transfer Success');

} catch (error) {
  // 💥 ถ้าบรรทัดไหน Error...
  await connection.rollback(); // ย้อนเวลากลับมาจุดเริ่มต้น (เงิน A ไม่หาย)
  console.error('Transfer Failed, Rolled back');
}
```

---

## 🚀 Indexing (เร่งความเร็ว)

ถ้าตารางเรามีข้อมูล 1 ล้านบรรทัด การค้นหาชื่อ "Somchai" อาจต้องไล่หาตั้งแต่บรรทัดแรกยันบรรทัดสุดท้าย (Full Table Scan) 🐢
**Index** คือสารบัญ (เหมือนสารบัญท้ายเล่มหนังสือ) ช่วยให้กระโดดไปหาหน้าที่มีคำว่า "Somchai" ได้เลย ⚡

### การสร้าง Index

```sql
-- สร้าง Index ให้ column 'email'
CREATE INDEX idx_email ON users(email);
```

### ข้อดี vs ข้อเสีย
- ✅ **ข้อดี**: `SELECT` เร็วขึ้นมหาศาล (จาก 5 วินาที เหลือ 0.01 วินาที)
- ❌ **ข้อเสีย**: `INSERT`, `UPDATE`, `DELETE` ช้าลงนิดหน่อย (เพราะต้องไปแก้สารบัญด้วย) และเปลืองพื้นที่ Harddisk

> **Rule of Thumb**: สร้าง Index เฉพาะ column ที่เอามา `WHERE`, `JOIN` หรือ `ORDER BY` บ่อยๆ

---

## 📊 Aggregation (รวมกลุ่มข้อมูล)

Database เก่งเรื่องคณิตศาสตร์ครับ อย่าดึงข้อมูลมานับเองใน Node.js ถ้าไม่จำเป็น

| Function | หน้าที่ |
|:---------|:--------|
| `COUNT(*)` | นับจำนวนแถว |
| `SUM(col)` | หาผลรวม |
| `AVG(col)` | หาค่าเฉลี่ย |
| `MAX(col)` | หาค่ามากสุด |
| `MIN(col)` | หาค่าน้อยสุด |

### GROUP BY
มักใช้คู่กับ Aggregate Function เพื่อแบ่งกลุ่มคำนวณ

```sql
-- นับจำนวนสินค้า แบ่งตามหมวดหมู่ (Category)
SELECT category, COUNT(*) as total_items
FROM products
GROUP BY category;
/*
Result:
| category    | total_items |
| Electronics | 50          |
| Clothing    | 120         |
*/
```

### HAVING
ใช้กรองข้อมูล **หลัง** จาก GROUP BY แล้ว (WHERE ใช้กรองก่อน GROUP BY)

```sql
-- หาหมวดหมู่ที่มีสินค้ามากกว่า 100 ชิ้น
SELECT category, COUNT(*) as total_items
FROM products
GROUP BY category
HAVING total_items > 100;
```

---

##  Boxing Challenges

### Level 1: Report ยอดขาย (JOIN + SUM + GROUP BY)
มีตาราง `orders` (`id`, `user_id`, `total`) และ `users` (`id`, `name`)
จงหาว่า **User แต่ละคน มียอดซื้อรวม (`SUM`) เท่าไหร่**
- เรียงลำดับจากซื้อมากไปน้อย
- แสดงชื่อ User และ ยอดรวม

::: details ✨ เฉลย
```sql
SELECT users.name, SUM(orders.total) as grand_total
FROM users
JOIN orders ON users.id = orders.user_id
GROUP BY users.id, users.name
ORDER BY grand_total DESC;
```
:::

### Level 2: Transaction Challenge (Logic)
สถานการณ์: ลูกค้าสั่งซื้อสินค้า (Create Order) และต้องตัดสต็อก (Cut Stock)
ถ้าสต็อกไม่พอ ต้อง Error และห้ามสร้าง Order
จงเขียน Pseudo Code หรือ Node.js Code จำลอง Transaction นี้

::: details ✨ เฉลย
```javascript
await conn.beginTransaction();
try {
  // 1. เช็คสต็อก (Lock row for update เพื่อกันคนอื่นแย่งซื้อ)
  const [rows] = await conn.query('SELECT stock FROM products WHERE id = ? FOR UPDATE', [prodId]);
  if (rows[0].stock < qty) throw new Error('Stock not enough');

  // 2. ถ้าพอ -> ตัดสต็อก
  await conn.query('UPDATE products SET stock = stock - ? WHERE id = ?', [qty, prodId]);

  // 3. สร้าง Order
  await conn.query('INSERT INTO orders ...');

  await conn.commit();
} catch (err) {
  await conn.rollback();
  throw err;
}
```
:::

---

## 📚 Glossary

| คำศัพท์ | ความหมาย |
|:--------|:---------|
| **JOIN** | การรวมข้อมูลจากหลายตารางเข้าด้วยกันโดยใช้ Key เชื่อมโยง |
| **Transaction** | หน่วยงานทำงานที่ประกอบด้วยหลายคำสั่ง ต้องสำเร็จครบทุกอันถึงจะบันทึก |
| **Commit** | การยืนยันว่า Transaction สำเร็จและบันทึกลง Database ถาวร |
| **Rollback** | การยกเลิก Transaction ย้อนข้อมูลกลับไปสถานะก่อนเริ่มทำ ถ้าเกิดข้อผิดพลาด |
| **ACID** | คุณสมบัติ 4 ข้อที่ทำให้ Transaction เชื่อถือได้ (Atomicity, Consistency, Isolation, Durability) |
| **Index** | โครงสร้างข้อมูลที่ช่วยให้ค้นหาข้อมูลได้เร็วขึ้น (เหมือนสารบัญ) |
| **Full Table Scan** | การที่ DB ต้องไล่อ่านข้อมูลทุกบรรทัดเพื่อหาของ (ช้ามาก) |
| **Aggregate Function** | ฟังก์ชันคำนวณผลสรุปจากข้อมูลหลายแถว (COUNT, SUM, AVG) |

---

## 🔗 References

- [MySQL Constraints & Indexes](https://dev.mysql.com/doc/refman/8.0/en/constraints-and-indexes.html)
- [W3Schools SQL JOINs](https://www.w3schools.com/sql/sql_join.asp) - ภาพ Venn Diagram เข้าใจง่าย
- [Visualizing SQL Joins](https://joins.spathon.com/) - เว็บช่วยเห็นภาพการ JOIN

---

> 👉 **ไปต่อ: [Module 8: MongoDB & NoSQL](/node/08-01-nosql-mongodb)**
