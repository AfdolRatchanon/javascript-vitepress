# 06-1: REST API Concepts & CRUD (หัวใจของ Backend) 📡

> **"REST is not a protocol, it's an architectural style."**
> — *Roy Fielding (ผู้คิดค้น REST)*

ในโลกของ Backend หน้าที่หลักของเราคือการสร้าง **API (Application Programming Interface)** เพื่อให้ Frontend (Mobile/Web) มาดึงข้อมูลไปแสดงผล
และมาตรฐานที่นิยมที่สุดในโลกคือ **RESTful API**

---

## 1. What is REST? (REST คืออะไร?) 🏗️

**REST (Representational State Transfer)** คือมาตรฐานการออกแบบ API ที่ใช้ HTTP Protocol อย่างเต็มประสิทธิภาพ
ไม่ใช่แค่การส่งข้อมูลไปมา แต่คือการ **"จัดการทรัพยากร (Resources)"**

### Analogy (เปรียบเทียบ) 💡
จินตนาการว่า Server คือ **ร้านอาหารตามสั่ง**:
*   **Resource:** เมนูอาหาร (กะเพรา, ไข่ดาว)
*   **HTTP Method:** Verb ที่เราสั่งพ่อครัว (ขอกิน, ขอเพิ่ม, ขอยกเลิก)
*   **Endpoint (URL):** โต๊ะหรือตำแหน่งที่จะเสิร์ฟ

---

## 2. HTTP Methods (กริยาแห่งโลก API) 🗣️

ถ้าเราอยากกระทำอะไรกับข้อมูล เราต้องใช้ **HTTP Method** ให้ถูกประเภท:

| Method | CRUD | หน้าที่ | เปรียบเทียบ SQL |
| :--- | :--- | :--- | :--- |
| **GET** | Read | **ขอข้อมูล** (ห้ามแก้ไขข้อมูลบน Server) | `SELECT` |
| **POST** | Create | **สร้างข้อมูลใหม่** (ส่ง Data ไปใน Body) | `INSERT` |
| **PUT** | Update | **แก้ไขข้อมูลทั้งก้อน** (ทับของเดิม) | `UPDATE` (All Fields) |
| **PATCH** | Update | **แก้ไขบางส่วน** (แก้เฉพาะฟิลด์ที่ส่งไป) | `UPDATE` (Partial) |
| **DELETE** | Delete | **ลบข้อมูล** | `DELETE` |

> **⚠️ กฎเหล็ก:** ห้ามใช้ `GET` ในการลบหรือสร้างข้อมูลเด็ดขาด! เพราะ Browser อาจ Cache ผลลัพธ์ไว้ ทำให้การลบไม่เกิดขึ้นจริง

---

## 3. Resource Naming (การตั้งชื่อ URL) 🏷️

ใน REST เรามองทุกอย่างเป็น **Noun (คำนาม)** ห้ามใช้ Verb ใน URL!
เราใช้ HTTP Method เป็น Verb แทน

### ✅ Good Design (แบบที่ถูกต้อง)
*   `GET /users` (ขอดู User ทั้งหมด)
*   `GET /users/123` (ขอดู User ID 123)
*   `POST /users` (สร้าง User ใหม่)
*   `DELETE /users/123` (ลบ User ID 123)

### ❌ Bad Design (แบบที่ผิด - ห้ามทำ!)
*   `GET /getUsers` (ซ้ำซ้อน)
*   `POST /createUser` (ใช้ POST บอกอยู่แล้ว)
*   `GET /deleteUser?id=123` (ใช้ GET ลบข้อมูล อันตรายมาก!)

---

## 4. HTTP Status Codes (รหัสลับบอกสถานะ) 🚦

Server ตอบกลับมาเป็นตัวเลข 3 หลัก เพื่อบอกว่า "รอด" หรือ "ร่วง":

### 2xx: Success (สำเร็จ) ✅
*   **200 OK:** สำเร็จทั่วไป (GET, PUT, PATCH)
*   **201 Created:** สร้างเสร็จแล้ว (ใช้กับ Response ของ POST)
*   **204 No Content:** สำเร็จแต่ไม่มีของคืนให้ (มักใช้กับ DELETE)

### 4xx: Client Error (ลูกค้าผิด) ❌
*   **400 Bad Request:** ส่งข้อมูลมาผิด Format / Validation ไม่ผ่าน
*   **401 Unauthorized:** ยังไม่ได้ Login (ไม่รู้จักคุณ)
*   **403 Forbidden:** Login แล้วแต่ไม่มีสิทธิ์ (ห้ามเข้า)
*   **404 Not Found:** หาไม่เจอ (URL ผิด หรือ ID ไม่มีจริง)

### 5xx: Server Error (ร้านค้าผิด) 🔥
*   **500 Internal Server Error:** โค้ดพัง! (Bug ใน Code ของเราเอง)

---

## 5. JSON Structure (ภาษาที่ API ใช้คุยกัน) 📋

REST API ยุคใหม่ 99.99% ใช้ **JSON (JavaScript Object Notation)** ในการรับส่งข้อมูล

### Request Body (สิ่งที่ Client ส่งมา)
```json
// POST /products
{
  "name": "iPhone 15",
  "price": 32900,
  "inStock": true
}
```

### Response Body (สิ่งที่ Server ตอบกลับ)
ควรมีโครงสร้างมาตรฐาน (Standard Response Envelope):

#### Success Response
```json
{
  "status": "success",
  "data": {
    "id": 1,
    "name": "iPhone 15",
    "createdAt": "2024-01-01T10:00:00Z"
  }
}
```

#### Error Response
```json
{
  "status": "error",
  "message": "Product not found",
  "code": "PRODUCT_NOT_FOUND" 
}
```

---

## 6. Workshop: ออกแบบ Todo API ในกระดาษ 📝

ก่อนเขียนโค้ดจริง ลองออกแบบ API Endpoint สำหรับ **Todo List** กัน:

1.  **ดูรายการทั้งหมด:** `GET /todos`
2.  **ดูงานเดียว:** `GET /todos/:id`
3.  **สร้างงานใหม่:** `POST /todos`
4.  **แก้ไขงาน (ว่าเสร็จแล้ว):** `PATCH /todos/:id`
5.  **ลบงาน:** `DELETE /todos/:id`

---

> **📖 คำศัพท์เทคนิค (Glossary):**
> *   **REST (Representational State Transfer):** สถาปัตยกรรมการออกแบบ API
> *   **Endpoint:** จุดปลายทาง (URL) ที่ API เปิดให้เรียกใช้งาน
> *   **HTTP Method:** คำสั่งมาตรฐานของ HTTP (GET, POST, PUT, DELETE)
> *   **Status Code:** รหัสตัวเลขบอกผลลัพธ์การร้องขอ (200, 404, 500)
> *   **Idempotency:** คุณสมบัติที่การทำคำสั่งเดิมซ้ำๆ จะได้ผลลัพธ์เท่าเดิมเสมอ (เช่น DELETE, PUT)

## 7. Challenges 🏆

### 🎯 Challenge 1: Method Matching
จับคู่ HTTP Method กับหน้าที่ของมันให้ถูกต้อง:
1. `CREATE`
2. `READ`
3. `UPDATE`
4. `DELETE`

::: details ✨ ดูเฉลย
1. `CREATE` -> **POST**
2. `READ` -> **GET**
3. `UPDATE` -> **PUT / PATCH**
4. `DELETE` -> **DELETE**
:::

### 🎯 Challenge 2: Status Code Quiz
สถานการณ์ต่อไปนี้ควรตอบ Status Code อะไร?
1. User กรอกอีเมลผิดฟอร์แมต (ไม่มี @)
2. User Login สำเร็จ
3. Database ล่ม เชื่อมต่อไม่ได้
4. User จะลบสินค้าที่ไม่มีอยู่จริง

::: details ✨ ดูเฉลย
1. **400 Bad Request**
2. **200 OK** (หรือ 201 ถ้ามองว่าสร้าง Session)
3. **500 Internal Server Error**
4. **404 Not Found**
:::

---
