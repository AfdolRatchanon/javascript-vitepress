# 02-2: npm Packages (ใช้ Library จาก npm) 📚

> **"Good developers write code. Great developers use modules."**
> — *Programming Proverb*

เราไม่ต้องเขียนทุกอย่างเอง! npm มี Package **มากกว่า 2 ล้านตัว** ที่คนอื่นเขียนไว้ให้แล้ว — แค่ `npm install` แล้วใช้ได้เลย! บทนี้จะสอนวิธี **หา, ติดตั้ง, และใช้งาน** npm Packages ยอดนิยม

> **💡 Analogy (เปรียบเทียบ):**
> npm Packages เหมือน **"ส่วนผสมอาหาร" จากซุปเปอร์มาร์เก็ต** 🛒:
> - ทำซอสมะเขือเทศเอง? ได้ แต่ **ใช้เวลานาน**
> - ซื้อซอสสำเร็จรูป? **เร็วกว่า** แล้วไปโฟกัสทำอาหาร!
> - npm คือ **ซุปเปอร์มาร์เก็ต** — เลือกวัตถุดิบที่เราต้องการ

---

## 1. หา Package อย่างไร? 🔍

### เว็บไซต์ npmjs.com

ไปที่ [npmjs.com](https://www.npmjs.com/) แล้วค้นหา — ก่อนเลือก Package ให้ดู:

| ดูอะไร | ทำไม | ตัวอย่าง |
|:-------|:-----|:--------|
| **Weekly Downloads** | ดูว่าคนใช้เยอะไหม | Express: 30M+/week ✅ |
| **Last Publish** | ดูว่ายังดูแลอยู่ไหม | 2 months ago ✅, 5 years ago ⚠️ |
| **GitHub Stars** | ดูความนิยม | 60k+ stars ✅ |
| **Dependencies** | ดูว่าต้องติดตั้งอะไรเพิ่ม | 0 deps ✅, 50 deps ⚠️ |
| **License** | ดูว่าใช้ได้เสรีไหม | MIT ✅, GPL ⚠️ |

> 💡 **หลักการ:** เลือก Package ที่ **คนใช้เยอะ, ยังดูแลอยู่, มี Docs ดี** — อย่าเลือกตัวที่ไม่อัปเดตหลายปี!

---

## 2. Package ยอดนิยมสำหรับ Backend 🌟

| Package | ใช้ทำอะไร | Install | เรียนใน Module |
|:--------|:---------|:--------|:-------------|
| **express** | Web Framework | `npm i express` | 5 |
| **mongoose** | MongoDB ODM | `npm i mongoose` | 8 |
| **mysql2** | MySQL Driver | `npm i mysql2` | 7 |
| **jsonwebtoken** | JWT Auth | `npm i jsonwebtoken` | 9 |
| **bcrypt** | Hash Password | `npm i bcrypt` | 9 |
| **dotenv** | Environment Variables | `npm i dotenv` | 4-5 |
| **cors** | Cross-Origin Requests | `npm i cors` | 11 |
| **helmet** | Security Headers | `npm i helmet` | 11 |
| **multer** | File Upload | `npm i multer` | 10 |
| **morgan** | HTTP Logger | `npm i morgan` | 5 |
| **nodemon** | Auto-restart Dev Server | `npm i -D nodemon` | ทุก Module |
| **chalk** | สีใน Terminal | `npm i chalk` | 1-2 |

---

## 3. ตัวอย่าง: ใช้ dotenv (Environment Variables) 🔐

**dotenv** ช่วยเก็บค่าลับ (API Key, Password) ไว้ในไฟล์ `.env` แทนที่จะเขียนตรงในโค้ด — **ปลอดภัยกว่ามาก!**

### ติดตั้ง

```bash
npm install dotenv
```

### สร้าง .env

```bash
# .env — ไฟล์เก็บค่าลับ (ห้าม Commit!)
PORT=3000
DB_HOST=localhost
DB_USER=root
DB_PASS=my_secret_password
API_KEY=sk-1234567890abcdef
```

### ใช้งาน

```javascript
// app.js
require("dotenv").config(); // โหลด .env เข้า process.env

console.log("Port:", process.env.PORT);       // "3000"
console.log("DB Host:", process.env.DB_HOST); // "localhost"
console.log("API Key:", process.env.API_KEY); // "sk-1234567890abcdef"

// ⚠️ ค่าจาก .env เป็น String เสมอ!
const port = Number(process.env.PORT) || 3000;
```

> ⚠️ **สำคัญ!** เพิ่ม `.env` ใน `.gitignore`:
> ```
> # .gitignore
> node_modules/
> .env
> ```
> ไม่งั้น API Key / Password จะหลุดขึ้น GitHub! 😱

---

## 4. ตัวอย่าง: ใช้ dayjs (จัดการวันที่) 📅

**dayjs** ช่วยจัดการวันที่/เวลาง่ายกว่า `new Date()` — ขนาดเล็กมาก (2KB!)

```bash
npm install dayjs
```

```javascript
const dayjs = require("dayjs");

// วันนี้
console.log(dayjs().format("DD/MM/YYYY")); // "16/02/2026"
console.log(dayjs().format("HH:mm:ss"));   // "03:47:59"

// เปรียบเทียบวัน
const birthday = dayjs("1998-05-15");
const today = dayjs();
console.log("อายุ:", today.diff(birthday, "year"), "ปี"); // "อายุ: 27 ปี"

// บวก/ลบวัน
console.log(dayjs().add(7, "day").format("DD/MM/YYYY"));    // อีก 7 วัน
console.log(dayjs().subtract(1, "month").format("DD/MM/YYYY")); // เดือนก่อน

// เปรียบเทียบ
console.log(dayjs().isAfter(birthday));  // true
console.log(dayjs().isBefore(birthday)); // false
```

---

## 5. ตัวอย่าง: ใช้ uuid (สร้าง ID ไม่ซ้ำ) 🆔

```bash
npm install uuid
```

```javascript
const { v4: uuidv4 } = require("uuid");

// สร้าง Unique ID
const id1 = uuidv4(); // "9b1deb4d-3b7d-4bad-9bdd-2b0d7b3dcb6d"
const id2 = uuidv4(); // "110ec58a-a0f2-4ac4-8393-c866d813b8d1"
const id3 = uuidv4(); // ไม่เคยซ้ำ!

console.log(id1);
console.log(id2);
console.log(id3);

// ✅ Use Case: สร้าง ID สำหรับ User, Order, Product
const users = [];
users.push({ id: uuidv4(), name: "Dolar", email: "dolar@test.com" });
users.push({ id: uuidv4(), name: "Somchai", email: "somchai@test.com" });
console.table(users);
```

> 💡 **UUID** (Universally Unique Identifier) มีโอกาสซ้ำ **น้อยมาก** — ต่ำกว่า 1 ในล้านล้านล้าน!

---

## 6. จัดการ Package 🔧

### ลบ Package

```bash
npm uninstall dayjs          # ลบจาก dependencies
npm uninstall -D nodemon     # ลบจาก devDependencies
```

### ดู Package ที่ติดตั้ง

```bash
npm list                     # ดูทั้งหมด (ลึก)
npm list --depth=0           # ดูแค่ Level บนสุด
npm outdated                 # ดูว่าตัวไหนเก่า
```

### อัปเดต Package

```bash
npm update                   # อัปเดตทั้งหมด (ตาม SemVer)
npm install express@latest   # อัปเดตตัวเดียวเป็นล่าสุด
```

---

## 7. Challenges 🏆

### 🎯 Challenge 1: การเลือก Package (หัวข้อ 1)
ตอบคำถาม: ก่อนเลือกใช้ Package จาก npm ควรดูอะไรบ้าง 3 ข้อ?

::: details ✨ ดูเฉลย
1. **Popularity** (Weekly Downloads)
2. **Quality** (Issues, Maintenance)
3. **Modified Date** (อัปเดตล่าสุดเมื่อไหร่)
:::

### 🎯 Challenge 2: Package เหมาะกับงาน (หัวข้อ 2)
จับคู่ Package ให้ตรงกับงานที่จะทำ:
1. ต้องการเชื่อมต่อ Database (MongoDB)
2. ต้องการจัดการเวลา/วันที่
3. ต้องการสร้าง Web Server

::: details ✨ ดูเฉลย
1. **Database** → `mongoose`
2. **Date/Time** → `dayjs` (หรือ `moment`)
3. **Web Server** → `express`
:::

### 🎯 Challenge 3: dotenv (หัวข้อ 3)
สร้างไฟล์ `.env` เก็บค่า `PORT=8080` และไฟล์ `config.js` ที่อ่านค่านั้นมาแสดงผล:

::: details ✨ ดูเฉลย
สร้างไฟล์ `.env`:
```env
PORT=8080
```

สร้างไฟล์ `config.js`:
```javascript
require("dotenv").config();
console.log("Server Port:", process.env.PORT);
```

ทดสอบด้วยคำสั่ง:
```bash
node config.js
```
:::

### 🎯 Challenge 4: dayjs (หัวข้อ 4)
สร้างไฟล์ `time.js` ใช้ `dayjs` แสดงวันที่ **"อีก 90 วันข้างหน้า"** ในรูปแบบ `DD/MM/YYYY`:

::: details ✨ ดูเฉลย
สร้างไฟล์ `time.js`:
```javascript
const dayjs = require("dayjs");
const future = dayjs().add(90, "day");

console.log("อีก 90 วันคือ:", future.format("DD/MM/YYYY"));
```

ทดสอบด้วยคำสั่ง:
```bash
node time.js
```
:::

### 🎯 Challenge 5: uuid (หัวข้อ 5)
สร้างไฟล์ `id-gen.js` ที่สร้าง UUID มา 3 ตัว แล้วแสดงผล:

::: details ✨ ดูเฉลย
สร้างไฟล์ `id-gen.js`:
```javascript
const { v4: uuidv4 } = require("uuid");

console.log("ID 1:", uuidv4());
console.log("ID 2:", uuidv4());
console.log("ID 3:", uuidv4());
```

ทดสอบด้วยคำสั่ง:
```bash
node id-gen.js
```
:::

### 🎯 Challenge 6: จัดการ Package (หัวข้อ 6)
บอกคำสั่ง npm สำหรับ:
1. ดู Package ทั้งหมดที่ติดตั้งในโปรเจกต์
2. ลบ Package ชื่อ `uuid` ออกจากโปรเจกต์
3. เช็คว่ามี Package ไหนต้องอัปเดตบ้าง

::: details ✨ ดูเฉลย
1. **ดู Package:** `npm list` (หรือ `npm list --depth=0` ดูแค่ชั้นแรก)
2. **ลบ Package:** `npm uninstall uuid`
3. **เช็คอัปเดต:** `npm outdated`
:::

---

> **📖 คำศัพท์เทคนิค (Glossary):**
> *   **npm Package:** Library/Module ที่เผยแพร่บน npm Registry
> *   **npm Registry:** ที่เก็บ Package ทั้งหมด (npmjs.com)
> *   **dotenv:** Package สำหรับโหลด Environment Variables จาก `.env`
> *   **Environment Variables:** ค่าที่ตั้งนอกโค้ด (API Key, Password)
> *   **UUID:** Universally Unique Identifier — ID ที่ไม่ซ้ำ
> *   **dayjs:** Package สำหรับจัดการวันที่/เวลา (เหมือน moment.js แต่เบากว่า)
> *   **`npm list`:** ดู Package ที่ติดตั้งทั้งหมด
> *   **`npm outdated`:** ดูว่า Package ไหนมีเวอร์ชันใหม่
> *   **`npm uninstall`:** ลบ Package
> *   **`.env`:** ไฟล์เก็บค่าลับ — ห้าม Commit ขึ้น Git!
> *   **`process.env`:** Object ที่เก็บ Environment Variables ทั้งหมด

---
👉 **[ไปทำโปรเจกต์: Project — Utility Package](/node/02-project-utility-package)**
