# 7.1 SQL Fundamentals

> *"Data is the new oil."* — **Clive Humby**

ยินดีต้อนรับสู่โลกของ **Database** ครับ! 🗄️
ถ้าเปรียบเทียบ Application เป็นร่างกาย, Database ก็คือ "สมอง" ที่ทำหน้าที่จดจำเรื่องราวที่เกิดขึ้น
ไม่ว่าจะเป็นประวัติการสั่งซื้อ, รายชื่อลูกค้า, หรือคะแนนสอบ ล้วนต้องถูกเก็บรักษาไว้อย่างดีและเป็นระเบียบ

ใน Module นี้เราจะมาเรียนรู้การใช้งาน **Relational Database (SQL)** ที่นิยมที่สุดในโลกตัวหนึ่ง นั่นคือ **MySQL** ครับ

---

## 🐣 Analogy: Spreadsheet (Excel)

ถ้าคุณเคยใช้ Excel หรือ Google Sheets คุณก็เข้าใจ Database ไปครึ่งนึงแล้วครับ! ลองจินตนาการดูนะครับ:

- **Database** = ไฟล์ Excel 1 ไฟล์ (Workbook) เช่น `School.xlsx`
- **Table** = แผ่นงาน (Sheet) 1 แผ่น เช่น Sheet "Students", Sheet "Teachers"
- **Column** = หัวตาราง (Field) เช่น Name, Age, Email (ต้องกำหนดประเภทข้อมูลล่วงหน้า ว่าช่องนี้ใส่ตัวเลข หรือตัวหนังสือ)
- **Row** = ข้อมูลแต่ละบรรทัด (Record) หมายถึงข้อมูลของนักเรียน 1 คน
- **SQL** = ภาษาที่เราใช้สั่ง Excel ว่า "ขอดูรายชื่อนักเรียนที่เกรดเฉลี่ยเกิน 3.00 หน่อย"

### ทำไมไม่ใช้ Excel ไปเลยล่ะ? (Excel vs Database)

| Feature | Excel (Spreadsheet) | Database (SQL) |
|:--------|:--------------------|:---------------|
| **ปริมาณข้อมูล** | รับได้หลักหมื่น/แสนบรรทัดก็เริ่มอืด | รับได้ **หลักล้าน/ร้อยล้าน** บรรทัดสบายๆ (Scalability) |
| **ความสัมพันธ์** | เชื่อมโยงข้าม Sheet ยุ่งยาก (VLOOKUP) | เชื่อมโยงข้อมูลซับซ้อนได้ง่ายและเร็วมาก (Relations) |
| **ความถูกต้อง** | ช่องอายุ อาจเผลอพิมพ์ "ยี่สิบ" (Text) ลงไปได้ | บังคับ Type เข้มงวด (Strictness) ผิด Type ไม่รับ |
| **ผู้ใช้งาน** | แก้ไขพร้อมกันได้ไม่กี่คน | รองรับคนใช้งานพร้อมกันเป็นพัน/หมื่นคน (Concurrency) |

> 💡 **สรุป**: Excel เหมาะกับงานวิเคราะห์ข้อมูลส่วนตัว/ทีมเล็ก แต่ Database เหมาะกับ **Application** ที่ต้องรองรับผู้ใช้งานจำนวนมากครับ

---

## 🆚 Comparison: SQL vs NoSQL

ก่อนจะไปลึกซึ้ง เรามาดูความแตกต่างของ 2 ค่ายยักษ์ใหญ่แห่งโลก Database กันก่อนครับ เพราะเดี๋ยวเราจะได้เรียนทั้งคู่ (MySQL และ MongoDB)

### SQL (Relational Database)
- **ตัวอย่าง**: MySQL, PostgreSQL, SQL Server
- **โครงสร้าง**: เป็นตาราง (Table) มีแถวและคอลัมน์ที่แน่นอน
- **Schema**: **Strict** (เข้มงวด) ต้องสร้างตารางและกำหนด Column ก่อน ถึงจะใส่ข้อมูลได้
- **ความสัมพันธ์**: เชื่อมโยงกันด้วย Key (Foreign Key) ใช้ `JOIN` ในการดึงข้อมูลเกี่ยวข้องกัน
- **เหมาะสำหรับ**: ระบบบัญชี, ระบบสต็อกสินค้า, ธุรกรรมการเงิน, อะไรที่ต้องการความเป๊ะเวอร์ (ACID)

### NoSQL (Non-Relational Database)
- **ตัวอย่าง**: MongoDB, Firebase, Redis
- **โครงสร้าง**: ยืดหยุ่น หลากหลาย (เช่น เก็บเป็น JSON Document, Key-Value)
- **Schema**: **Dynamic** (ยืดหยุ่น) ไม่ต้องสร้างโครงสร้างก่อน อยากเพิ่ม field อะไรก็เพิ่มทีหลังได้
- **ความสัมพันธ์**: มักเก็บข้อมูลซ้อนกัน (Embedded) หรือแยกกันแล้วใช้ App ดึง 2 รอบ
- **เหมาะสำหรับ**: Social Media Feed, Chat Log, ข้อมูลจาก Sensor (IoT), ระบบที่สเกลเร็วๆ

> 💡 **เลือกใช้อะไรดี?**:
> - ถ้าข้อมูลของคุณมีโครงสร้างแน่นอนและซับซ้อน (Relational) -> **เลือก SQL**
> - ถ้าข้อมูลของคุณไม่แน่นอน เปลี่ยนแปลงบ่อย หรือต้องการความเร็วในการอ่าน/เขียนสูง -> **เลือก NoSQL**

---

## 📚 Introduction to SQL

**SQL (Structured Query Language)** คือภาษามาตรฐานที่เราใช้ "คุย" กับ Database ครับ
ไม่ว่าคุณจะใช้ MySQL, PostgreSQL, หรือ Oracle คุณก็ใช้ SQL เหมือนกัน (ต่างกันแค่นิดหน่อยในฟีเจอร์เฉพาะตัว - dialect)

### มาตรฐานภาษา SQL (Sub-languages)

SQL แบ่งคำสั่งออกเป็นกลุ่มๆ ตามหน้าที่ครับ:

1.  **DDL (Data Definition Language)**: คำสั่งกำหนดโครงสร้าง (สร้าง/ลบ/แก้ไข ตาราง)
    - `CREATE`, `DROP`, `ALTER`, `TRUNCATE`
2.  **DML (Data Manipulation Language)**: คำสั่งจัดการข้อมูล (เพิ่ม/ลบ/แก้ไข ข้อมูล)
    - `INSERT`, `UPDATE`, `DELETE`, `SELECT` (บางตำราแยก SELECT เป็น DQL)
3.  **DCL (Data Control Language)**: คำสั่งกำหนดสิทธิ์
    - `GRANT`, `REVOKE`
4.  **TCL (Transaction Control Language)**: คำสั่งจัดการ Transaction
    - `COMMIT`, `ROLLBACK`

ในบทนี้เราจะเน้นที่ **DDL** และ **DML** เป็นหลักครับ เพราะเป็นสิ่งที่ Developer ใช้ทุกวัน

---

## 🏗️ Data Definition Language (DDL)

ก่อนจะเก็บข้อมูล เราต้องสร้าง "บ้าน" ให้มันอยู่ก่อน นั่นคือการสร้าง **Table** ครับ

### 1. Data Types ที่ควรรู้ (MySQL)

เวลาสร้าง Column เราต้องบอก Database ว่าจะเก็บข้อมูลประเภทไหน:

| Type | รายละเอียด | ตัวอย่าง |
|:-----|:-----------|:---------|
| `INT` / `INTEGER` | จำนวนเต็ม (มีหลายขนาด: TINYINT, SMALLINT, BIGINT) | `age INT`, `stock_qty INT` |
| `DECIMAL(M, D)` | ตัวเลขทศนิยมที่แม่นยำ (เหมาะกับการเงิน) M=จำนวนหลักรวม, D=ทศนิยม | `price DECIMAL(10, 2)` (เช่น 12345.67) |
| `FLOAT` / `DOUBLE` | ตัวเลขทศนิยมแบบประมาณค่า (วิทยาศาสตร์) | `temperature FLOAT` |
| `VARCHAR(n)` | ข้อความความยาวแปรผัน (เก็บเท่าที่ใช้จริง) n=ยาวสูงสุด | `username VARCHAR(50)` |
| `TEXT` | ข้อความยาวมากๆ (บทความ, รีวิว) | `content TEXT` |
| `BOOLEAN` | ค่าจริง/เท็จ (ใน MySQL คือ TINYINT(1)) | `is_active BOOLEAN` |
| `DATE` | วันที่ (YYYY-MM-DD) | `birthdate DATE` |
| `DATETIME` | วันที่และเวลา (YYYY-MM-DD HH:MM:SS) | `created_at DATETIME` |
| `TIMESTAMP` | คล้าย DATETIME แต่เก็บเป็น Unix Timestamp (แปลง Timezone ได้) | `updated_at TIMESTAMP` |

### 2. Constraints (ข้อกำหนด)

เพื่อรักษา **Integrity** (ความสมบูรณ์ถูกต้อง) ของข้อมูล เราควรกำหนด Constraint ครับ:

- `PRIMARY KEY`: กุญแจหลัก ห้ามซ้ำและห้ามว่าง (ระบุตัวตนของแถวนั้นๆ)
- `AUTO_INCREMENT`: ให้ Database รันเลขให้เองอัตโนมัติ (1, 2, 3...)
- `NOT NULL`: ห้ามเป็นค่าว่าง
- `UNIQUE`: ห้ามซ้ำกับคนอื่น (เช่น Email, Username)
- `DEFAULT`: ค่าเริ่มต้นถ้าไม่ได้ระบุมา

### 3. CREATE TABLE Syntax

ลองมาดูตัวอย่างการสร้างตาราง `users` กันครับ:

```sql
CREATE TABLE users (
    id INT AUTO_INCREMENT PRIMARY KEY, -- ไอดี รันเลขเอง เป็นคีย์หลัก
    username VARCHAR(50) NOT NULL UNIQUE, -- ชื่อห้ามว่าง ห้ามซ้ำ
    email VARCHAR(100) NOT NULL UNIQUE, -- อีเมลห้ามว่าง ห้ามซ้ำ
    age INT CHECK (age >= 18), -- (Optional) เช็คว่าอายุต้อง 18+ (MySQL 8.0.16+)
    is_active BOOLEAN DEFAULT TRUE, -- เป็นสมาชิก active โดย Default
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP -- เวลาสร้าง ใส่ให้เอง
);
```

### 4. DROP TABLE (ลบตาราง)

ถ้าสร้างผิด หรืออยากลบตารางทิ้ง (ข้อมูลหายหมดนะ!):

```sql
DROP TABLE users;
-- หรือ
DROP TABLE IF EXISTS users; -- (ปลอดภัยกว่า ไม่ error ถ้าไม่มีตาราง)
```

### 5. ALTER TABLE (แก้ไขโครงสร้าง)

ถ้าอยากเพิ่ม Column ทีหลัง:

```sql
-- เพิ่ม column phone
ALTER TABLE users ADD phone VARCHAR(15);

-- ลบ column age
ALTER TABLE users DROP COLUMN age;

-- เปลี่ยนชนิดข้อมูล
ALTER TABLE users MODIFY username VARCHAR(100);
```

---

## 💻 Data Manipulation Language (DML)

เมื่อมีตารางแล้ว เรามาลองจัดการข้อมูลกันครับ นี่คือส่วนที่เราจะใช้เขียนใน Node.js บ่อยที่สุด (CRUD Operation)

### 1. INSERT (Create)

เพิ่มข้อมูลใหม่ลงในตาราง

```sql
-- Pattern 1: ระบุ Column ครบ (แนะนำ)
INSERT INTO users (username, email, age) 
VALUES ('somchai', 'som@chai.com', 25);

-- Pattern 2: ใส่หลายคนพร้อมกัน (Batch Insert)
INSERT INTO users (username, email, age) VALUES 
('john', 'john@doe.com', 30),
('jane', 'jane@doe.com', 28),
('bob', 'bob@builder.com', 40);
```

> ⚠️ **ข้อควรระวัง**: ถ้าใส่ข้อมูลที่ขัดกับ Constraint (เช่น email ซ้ำ, หรือเป็น NULL ในช่อง NOT NULL) Database จะแจ้ง Error ทันที

### 2. SELECT (Read)

ดึงข้อมูลออกมาดู นี่คือคำสั่งที่มีลูกเล่นเยอะที่สุด

```sql
-- 1. ดูข้อมูลทั้งหมด (ทุก Column, ทุก Row)
-- ⚠️ ไม่แนะนำให้ใช้ใน Production ถ้าข้อมูลเยอะ เพราะจะกิน Ram มหาศาล
SELECT * FROM users;

-- 2. เลือกเฉพาะ Column ที่ต้องการ (Projection)
-- ✅ ดีต่อ Performance มากกว่า
SELECT username, email FROM users;

-- 3. กรองข้อมูลด้วย WHERE (Selection)
SELECT * FROM users WHERE age > 25;
SELECT * FROM users WHERE is_active = TRUE;
SELECT * FROM users WHERE username = 'john';

-- 4. เรียงลำดับ (ORDER BY)
SELECT * FROM users ORDER BY age ASC; -- น้อยไปมาก (Default)
SELECT * FROM users ORDER BY created_at DESC; -- มากไปน้อย (ล่าสุดขึ้นก่อน)

-- 5. จำกัดจำนวน (LIMIT) และข้าม (OFFSET) - ใช้ทำ Pagination
SELECT * FROM users LIMIT 10; -- เอาแค่ 10 คนแรก
SELECT * FROM users LIMIT 5 OFFSET 10; -- ข้าม 10 คนแรก แล้วเอา 5 คนถัดไป (หน้า 2)
```

### 3. UPDATE (Update)

แก้ไขข้อมูลเดิม

```sql
-- แก้ไข email และ age ของคนที่มี id = 1
UPDATE users 
SET email = 'new_som@test.com', age = 26 
WHERE id = 1;
```

> 🚨 **CRITICAL WARNING**:
> ถ้าคุณลืมใส่ `WHERE`...
> ```sql
> UPDATE users SET age = 20; -- หายนะ! ทุกคนในระบบอายุ 20 หมดเลย!
> ```
> ก่อนรัน UPDATE/DELETE ควรลอง SELECT ดูก่อนให้แน่ใจว่าเงื่อนไข WHERE ถูกต้อง

### 4. DELETE (Delete)

ลบข้อมูลออกจากตาราง

```sql
-- ลบ user ที่ id = 5
DELETE FROM users WHERE id = 5;

-- ลบ user ที่ inactive ทั้งหมด
DELETE FROM users WHERE is_active = FALSE;
```

> 🚨 **WARNING**: เช่นเดียวกับ UPDATE ถ้าลืม `WHERE` = ข้อมูลหายเกลี้ยง! (Empty Table)

---

## 🌍 Real-World Use Case: E-Commerce Products

ลองมาดูตัวอย่างการออกแบบตารางสำหรับระบบร้านค้าออนไลน์จริงๆ กันครับ

**โจทย์**: เราต้องการเก็บข้อมูล "สินค้า" (Products)
- ต้องมีชื่อ, รายละเอียด, ราคา
- ต้องมีสถานะ (ขายอยู่, หมด, เลิกขาย)
- ต้องเก็บจำนวนสต็อก
- ต้องรู้ว่าสร้างเมื่อไหร่ แก้ไขล่าสุดเมื่อไหร่

**SQL Schema Design:**

```sql
CREATE TABLE products (
    product_id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    description TEXT, -- อาจจะยาวมาก ใช้ TEXT
    price DECIMAL(10, 2) NOT NULL CHECK (price >= 0), -- ราคาห้ามติดลบ
    stock_qty INT DEFAULT 0 CHECK (stock_qty >= 0), -- สต็อกห้ามติดลบ
    status ENUM('active', 'out_of_stock', 'discontinued') DEFAULT 'active', -- ล็อคค่าที่เป็นไปได้
    image_url VARCHAR(2048), -- URL รูปภาพ
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP -- อัปเดตเวลาอัตโนมัติตอนแก้ไข
);
```

**ตัวอย่างการใช้งาน:**

1.  **สินค้าเข้าใหม่**:
    ```sql
    INSERT INTO products (name, description, price, stock_qty)
    VALUES ('Gaming Mouse', 'RGB Wireless Mouse', 1590.00, 50);
    ```

2.  **ลูกค้าสั่งซื้อ (ตัดสต็อก)**:
    ```sql
    UPDATE products 
    SET stock_qty = stock_qty - 1 
    WHERE product_id = 1 AND stock_qty > 0;
    ```

3.  **เปลี่ยนราคาสิ้นเดือน**:
    ```sql
    UPDATE products 
    SET price = 1290.00 
    WHERE product_id = 1;
    ```

---

## 🥊 Challenges

มาทดสอบความเข้าใจกันหน่อยครับ (อยากให้ลองเขียน SQL จริงๆ ใน MySQL Workbench หรือใช้ [SQL Fiddle](http://sqlfiddle.com/) ก็ได้ครับ)

### Level 1: สร้างตารางพนักงาน (Employees)
จงเขียนคำสั่งสร้างตาราง `employees` โดยมีข้อกำหนดดังนี้:
1.  `id`: เป็น Primary Key
2.  `firstname`, `lastname`: ห้ามว่าง
3.  `department`: แผนก (ข้อความ)
4.  `salary`: เงินเดือน (ทศนิยม)
5.  `hire_date`: วันที่จ้างงาน

::: details ✨ เฉลย
```sql
CREATE TABLE employees (
    id INT AUTO_INCREMENT PRIMARY KEY,
    firstname VARCHAR(100) NOT NULL,
    lastname VARCHAR(100) NOT NULL,
    department VARCHAR(100),
    salary DECIMAL(10, 2),
    hire_date DATE
);
```
:::

### Level 2: ขึ้นเงินเดือน!
จงเขียนคำสั่ง UPDATE เพื่อขึ้นเงินเดือน (`salary`) ให้พนักงานทุกคนในแผนก 'IT' เพิ่มขึ้น 10%
(ใบ้: ใช้การคูณ 1.10)

::: details ✨ เฉลย
```sql
UPDATE employees
SET salary = salary * 1.10
WHERE department = 'IT';
```
:::

### Level 3: ล้างบางคนขี้เกียจ (Soft Delete vs Hard Delete)
ในโลกความจริง เรามักไม่ลบข้อมูลทิ้งจริงๆ (Hard Delete) แต่จะแค่แปะป้ายว่า "ลบแล้ว" (Soft Delete)
1. จงเพิ่ม column `is_deleted` (BOOLEAN) ให้ตาราง products (ค่าเริ่มต้น false)
2. จงเขียนคำสั่งเพื่อ "ลบ" สินค้า id = 5 (แบบ Soft Delete)

::: details ✨ เฉลย
```sql
-- 1. เพิ่ม Column
ALTER TABLE products ADD is_deleted BOOLEAN DEFAULT FALSE;

-- 2. Soft Delete
UPDATE products SET is_deleted = TRUE WHERE product_id = 5;

-- เวลาดึงข้อมูลต้องกรองด้วย
SELECT * FROM products WHERE is_deleted = FALSE;
```
:::

---

## 📚 Glossary

| คำศัพท์ | ความหมาย |
|:--------|:---------|
| **RDBMS** | Relational Database Management System ระบบจัดการฐานข้อมูลเชิงสัมพันธ์ (เช่น MySQL, PostgreSQL) |
| **Table** | ตารางเก็บข้อมูล ประกอบด้วย Row และ Column (เทียบเท่า Sheet ใน Excel) |
| **Column (Field)** | คอลัมน์ในตาราง กำหนด Attribute ของข้อมูล (เช่น ชื่อ, อายุ) |
| **Row (Record)** | แถวในตาราง แทนข้อมูล 1 รายการ (เช่น ลูกค้า 1 คน) |
| **Primary Key (PK)** | คอลัมน์ที่ระบุตัวตนของแถว ห้ามซ้ำและห้ามว่าง (มักเป็น ID) |
| **Foreign Key (FK)** | คอลัมน์ที่อ้างอิงไปยัง PK ของตารางอื่น (ใช้เชื่อมความสัมพันธ์) |
| **Query** | คำสั่งที่ส่งไปขอข้อมูลหรือสั่งงาน Database |
| **Schema** | โครงสร้างของ Database (ตารางมีกี่คอลัมน์ เก็บข้อมูลประเภทอะไร) |
| **DDL** | Data Definition Language คำสั่งกำหนดโครงสร้าง (CREATE, DROP, ALTER) |
| **DML** | Data Manipulation Language คำสั่งจัดการข้อมูล (SELECT, INSERT, UPDATE, DELETE) |
| **Soft Delete** | การลบข้อมูลโดยการเปลี่ยนสถานะ Flag (เช่น `is_deleted=true`) แทนการลบจริง |

---

## 🔗 References

- [MySQL Official Documentation](https://dev.mysql.com/doc/refman/8.0/en/) - แหล่งอ้างอิงหลักที่ละเอียดที่สุด
- [W3Schools SQL Tutorial](https://www.w3schools.com/sql/) - แหล่งเรียนรู้ SQL พื้นฐานที่ดีมาก
- [SQL Zoo](https://sqlzoo.net/) - เว็บฝึกเขียน Query แบบ Interactive

---

> 👉 **ไปต่อ: [Node.js + MySQL Integration](/node/07-02-node-mysql)**
