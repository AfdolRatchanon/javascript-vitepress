# 8.1 NoSQL & MongoDB Basics

> *"Not only SQL."*

หลังจากเราได้รู้จัก SQL ที่เป็นระเบียบเป๊ะๆ ไปใน Module ที่แล้ว
วันนี้เรามาเปิดโลกใหม่กับ **NoSQL Database** ที่ฮิตที่สุดในวงการ JavaScript / Node.js นั่นคือ **MongoDB** ครับ 🍃

ทำไมมันถึงฮิต? เพราะมันเก็บข้อมูลเป็น **JSON-like** format ซึ่งหน้าตาเหมือน Object ใน JavaScript เป๊ะๆ! ทำให้ Dev สาย JS อย่างเรารู้สึกเหมือนอยู่บ้านเลยครับ

---

## 🐣 Analogy: File Cabinet (ตู้เก็บเอกสาร)

เพื่อให้เห็นภาพชัดเจน ลองเปรียบเทียบวิธีเก็บข้อมูลนะครับ:

- **SQL (MySQL)**: เหมือน **ตาราง Excel**
    - ทุกแถวต้องมีคอลัมน์เหมือนกันเป๊ะๆ (ชื่อ, อายุ, ที่อยู่)
    - ถ้าแถวไหนไม่มี "ที่อยู่" ก็ต้องเว้นว่างไว้ แต่คอลัมน์ยังอยู่
    - ถ้าจะเพิ่ม field ใหม่ (เช่น เบอร์โทร) ต้องแก้หัวตารางทั้งหมด (Alter Table)

- **NoSQL (MongoDB)**: เหมือน **ตู้เก็บแฟ้มเอกสาร** (Collection)
    - ในตู้ 1 ตู้ เราจะโยนแฟ้ม (Document) ลงไป
    - แฟ้ม A (สมชาย): มีกระดาษ 1 แผ่น เขียนชื่อ, อายุ
    - แฟ้ม B (สมหญิง): มีกระดาษ 1 แผ่น + **รูปถ่าย 10 ใบ** + **โน้ตย่อ**
    - **ความยืดหยุ่น (Flexibility)**: แฟ้มแต่ละแฟ้มไม่จำเป็นต้องมีหน้าตาเหมือนกัน! ใครอยากใส่อะไรเพิ่มก็ใส่ได้เลย

> 💡 **Use Case**: เหมาะมากกับข้อมูลที่โครงสร้างไม่แน่นอน หรือมีการเปลี่ยนแปลงบ่อยๆ เช่น Log, Sensor Data, Social Media Feed (บางโพสต์มีรูป, บางโพสต์มีวิดีโอ)

---

## 🆚 SQL vs NoSQL Cheat Sheet

ตารางเปรียบเทียบคำศัพท์ที่เราต้องปรับตัวครับ:

| SQL (MySQL) | NoSQL (MongoDB) | คำอธิบาย |
|:---|:---|:---|
| **Table** | **Collection** | กลุ่มของข้อมูล |
| **Row** | **Document** | ข้อมูล 1 ชิ้น เก็บเป็น JSON (BSON) |
| **Column** | **Field** | ชื่อตัวแปรใน JSON (เช่น `name`, `age`) |
| **Primary Key** | **ObjectId** (`_id`) | MongoDB สร้าง `_id` ให้เองอัตโนมัติ (ขนาดยาวกว่า ID ปกติ) |
| **JOIN** | `$lookup` / Populate | MongoDB ทำ Join ได้ แต่ไม่เก่งเท่า SQL |
| **Transaction** | รองรับ (ACID) | เริ่มรองรับในเวอร์ชันใหม่ๆ (แต่ SQL ชัวร์กว่า) |
| **Scaling** | Vertical (เพิ่มสเปกเครื่อง) | Horizontal (เพิ่มจำนวนเครื่อง - Sharding) ง่ายกว่ามาก |

---

## 🛠️ MongoDB Atlas Setup

ในคอร์สนี้เราจะใช้ **MongoDB Atlas** ซึ่งเป็น Cloud Database (Database-as-a-Service)
ข้อดีคือ **ฟรี** และไม่ต้องติดตั้ง MongoDB Server ลงในเครื่องให้หนักครับ

1.  **Sign Up**: ไปที่ [mongodb.com/cloud/atlas](https://www.mongodb.com/cloud/atlas) สมัครสมาชิก
2.  **Create Cluster**: เลือกแผน **Shared (Free)** -> เลือก Region ที่ใกล้ไทย (เช่น Singapore)
3.  **Create User**: ตั้ง Username/Password สำหรับ Database (จำไว้ให้ดีนะ!)
4.  **Network Access**: ไปที่เมนู Network Access -> Add IP Address -> เลือก **Allow Access from Anywhere** (0.0.0.0/0) เพื่อให้เครื่องเราต่อได้
5.  **Get Connection String**: กดปุ่ม Connect -> Drivers -> Node.js -> Copy URL มา
    - หน้าตาประมาณ: `mongodb+srv://<username>:<password>@cluster0.xyz.mongodb.net/`

---

## 💻 MongoDB Shell (mongosh) & CRUD

แม้เราจะเขียนผ่าน Node.js เป็นหลัก แต่การรู้คำสั่ง Native ของ MongoDB ไว้มีประโยชน์มากในการ Debug ครับ
เราสามารถโหลดโปรแกรม **MongoDB Compass** (GUI เหมือน MySQL Workbench) มาใช้คลิกๆ ได้ หรือใช้ **mongosh** (CLI) ก็ได้

โครงสร้างข้อมูลใน MongoDB (BSON) หน้าตาแบบนี้:
```json
{
  "_id": ObjectId("507f1f77bcf86cd799439011"),
  "name": "Somchai",
  "age": 25,
  "skills": ["JS", "Node", "React"],
  "address": {
    "city": "Bangkok",
    "zip": "10110"
  }
}
```

### 1. Create (insertOne, insertMany)

```javascript
// Database: my_database --> Collection: users

// เพิ่มข้อมูล 1 คน
db.users.insertOne({
  name: "Mario",
  role: "Plumber",
  level: 1
})

// เพิ่มทีละหลายๆ คน
db.users.insertMany([
  { name: "Luigi", role: "Plumber", level: 1 },
  { name: "Peach", role: "Princess", level: 99 }
])
```

### 2. Read (find)

```javascript
// SELECT * FROM users (หาทั้งหมด)
db.users.find()

// SELECT * FROM users WHERE role = 'Plumber'
db.users.find({ role: "Plumber" })

// SELECT * FROM users WHERE level > 10 ($gt = Greater Than)
db.users.find({ level: { $gt: 10 } })

// หาแบบเจาะจงใน Array (หาคนที่มี skill 'JS')
db.users.find({ skills: "JS" })

// หาแบบเจาะจงใน Embedded Object (Address City = Bangkok)
db.users.find({ "address.city": "Bangkok" })
```

> **Query Operators ที่พบบ่อย:**
> - `$eq` (Equal), `$ne` (Not Equal)
> - `$gt` (Greater Than), `$lt` (Less Than)
> - `$in` (อยู่ใน list), `$nin` (ไม่อยู่ใน list)
> - `$or` (หรือ), `$and` (และ)

### 3. Update (updateOne, updateMany)

MongoDB ใช้ Operator `$set` เพื่อแก้ไขค่า (ถ้าไม่ใช้ `$set` จะเป็นการแทนที่ทั้งก้อน!)

```javascript
// แก้ไข Mario ให้ level เป็น 2
db.users.updateOne(
  { name: "Mario" },     // เงื่อนไข (Filter)
  { $set: { level: 2 } } // สิ่งที่แก้ (必须มี $set)
)

// เพิ่ม skill ให้ Luigi ($push เพิ่มของเข้า Array)
db.users.updateOne(
  { name: "Luigi" },
  { $push: { skills: "Jump" } }
)
```

### 4. Delete (deleteOne, deleteMany)

```javascript
// ลบ Luigi ออกจากระบบ
db.users.deleteOne({ name: "Luigi" })
```

---

## 🥊 Challenges

### Level 1: Query Expert
สมมติมี Collection `products`
จงเขียนคำสั่ง `find` เพื่อหาสินค้าที่:
1.  ราคา (`price`) น้อยกว่า 1000
2.  **หรือ** (`$or`)
3.  มี category เป็น "Freebies"

::: details ✨ เฉลย
```javascript
db.products.find({
  $or: [
    { price: { $lt: 1000 } },
    { category: "Freebies" }
  ]
})
```
:::

### Level 2: The Salary Raise
จงเขียนคำสั่ง `updateMany` เพื่อ "ขึ้นเงินเดือน" (`salary`) ให้พนักงานทุกคนในแผนก "IT" เพิ่มขึ้น 10%
(ใบ้: ใช้ Operator `$mul` เพื่อคูณ)

::: details ✨ เฉลย
```javascript
db.employees.updateMany(
  { department: "IT" },
  { $mul: { salary: 1.10 } }
)
```
:::

---

## 📚 Glossary

| คำศัพท์ | ความหมาย |
|:--------|:---------|
| **Collection** | กลุ่มของข้อมูลใน MongoDB (เทียบเท่า Table ใน SQL) |
| **Document** | ข้อมูล 1 ชิ้นใน Collection เก็บเป็น JSON Object (เทียบเท่า Row) |
| **BSON** | Binary JSON - format ที่ MongoDB ใช้เก็บข้อมูลจริง (รองรับ type พิเศษเช่น Date, ObjectId) |
| **ObjectId** | ID ที่ MongoDB สร้างให้ (เช่น `507f1f77bcf86cd799439011`) มีความ Unique ทั่วโลก |
| **Atlas** | บริการ Cloud MongoDB ที่เราไม่ต้องดูแล Server เอง |
| **Schema-less** | คุณสมบัติที่ไม่บังคับโครงสร้างข้อมูล (Document แต่ละตัวหน้าตาต่างกันได้) |
| **Embedded Document** | การเก็บ Object ซ้อน Object ใน Document เดียว (ลดการทำ Join) |

---

## 🔗 References

- [MongoDB Atlas Registration](https://www.mongodb.com/cloud/atlas/register)
- [MongoDB Query Selectors](https://www.mongodb.com/docs/manual/reference/operator/query/) - คู่มือ Operator ทั้งหมด ($gt, $in, etc.)
- [MongoDB Compass Download](https://www.mongodb.com/try/download/compass) - GUI แนะนำสำหรับมือใหม่

---

> 👉 **ไปต่อ: [Mongoose ODM](/node/08-02-mongoose-odm)**
