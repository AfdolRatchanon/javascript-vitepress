# 06-2: API Design Best Practices (ออกแบบให้โปรเขาทำกันแบบนี้) 🌟

> **"Always code as if the guy who ends up maintaining your code will be a violent psychopath who knows where you live."**
> — *John Woods*

การเขียน API ให้ "ทำงานได้" นั้นง่าย แต่การเขียนให้ "ใช้งานง่าย และ ดูแลรักษาได้" นั้นยากกว่า
บทนี้จะพาไปดู **กฎทองของการออกแบบ REST API** ที่มืออาชีพทั่วโลกใช้กัน

---

## 1. API Versioning (อย่าทิ้งลูกค้าเก่า) ⏳

วันหนึ่งเราอยากแก้ชื่อตัวแปรจาก `username` เป็น `userId` ถ้าเราแก้เลย แอปเก่าๆ ที่ลูกค้าใช้อยู่จะ **พังทันที!** (Breaking Change)
วิธีแก้คือทำ **Versioning**:

### URL Versioning (นิยมสุด) ✅
ใส่ `v1`, `v2` ไว้ใน URL เลย ชัดเจนที่สุด
*   `GET /api/v1/products` (ของเก่า)
*   `GET /api/v2/products` (ของใหม่)

ใครพร้อมก็ย้ายมา v2 ใครไม่พร้อมก็ใช้ v1 ต่อไป

---

## 2. Filtering, Sorting, Pagination (จัดการข้อมูลมหาศาล) 🔍

ถ้า Database มีสินค้า 1 ล้านชิ้น การ `GET /products` แล้วส่งคืนทั้งหมด = **Server ระเบิด!** 💥
เราจึงต้องมีกลไกคัดกรองข้อมูล:

### 2.1 Filtering (กรองข้อมูล)
ใช้ **Query Parameters** (`?key=value`)
*   `GET /products?category=electronics`
*   `GET /products?price_lt=1000` (ราคาต่ำกว่า 1000)

### 2.2 Sorting (เรียงลำดับ)
มักใช้ parameter ชื่อ `sort` หรือ `orderBy`
*   `GET /products?sort=price` (น้อยไปมาก)
*   `GET /products?sort=-price` (มากไปน้อย - ใช้เครื่องหมายลบ)

### 2.3 Pagination (แบ่งหน้า)
สำคัญที่สุด! ต้องระบุ `limit` (จำนวนต่อหน้า) และ `page` (หน้าไหน)
*   `GET /products?page=2&limit=20`

**Response Structure for Pagination:**
อย่าส่งแค่ Array เปล่าๆ ต้องบอก Meta Data ด้วย:
```json
{
  "data": [...],
  "meta": {
    "totalItems": 100,
    "totalPages": 5,
    "currentPage": 2,
    "itemsPerPage": 20
  }
}
```

---

## 3. Logical Nesting (ความสัมพันธ์ของข้อมูล) 🔗

ถ้าอยากดู "Review ของ Product ID 1" ควรออกแบบ URL อย่างไร?

### ✅ Good: Nested Resource
*   `GET /products/1/reviews`
(สื่อความหมายว่า Reviews เป็นของ Product 1)

### ❌ Bad: Flat URL
*   `GET /reviews?productId=1`
(ไม่ผิด แต่สื่อความสัมพันธ์ได้ไม่ดีเท่าแบบแรก)

> **Tip:** Nest อย่าลึกเกิน 2-3 ชั้น! เช่น `/users/1/posts/5/comments/10` อันนี้เริ่มเยอะไป (Deep Nesting Hell)

---

## 4. Standard Error Response (ผิดให้เป็นระเบียบ) ⚠️

Frontend จะเขียนโค้ดจัดการ Error ยากมาก ถ้า Server ตอบมาไม่เหมือนกันสักครั้ง
ควรกำหนดโครงสร้าง Standard Error ไว้เสมอ:

```json
/* ❌ Bad: บางทีส่ง string, บางทีส่ง object */
"Server Error"
{ "err": "Not found" }

/* ✅ Good: โครงสร้างชัดเจนเสมอ */
{
  "status": "error",
  "error": {
    "code": "USER_NOT_FOUND",
    "message": "User with ID 123 does not exist",
    "details": "Checking table 'users' failed"
  }
}
```

---

## 5. Use Proper HTTP Headers 📨

*   **`Content-Type: application/json`**: บอก Client ว่าฉันส่ง JSON นะ
*   **`Cache-Control`**: บอก Browser ว่า Cache ข้อมูลนี้ได้ไหม (ช่วยลด Load Server)
*   **`X-Request-ID`**: แนบ ID แปลกๆ มาในทุก Request เพื่อใช้ตามรอย (Trace) เวลาเกิดปัญหา

---

## 6. Security Checklist 🛡️

1.  **HTTPS Everywhere:** ห้ามใช้ HTTP เด็ดขาดใน Production
2.  **Rate Limiting:** ป้องกันคนยิง API รัวๆ ด้วย `express-rate-limit`
3.  **Authentication:** ใช้ JWT ใน Header `Authorization: Bearer <token>`
4.  **Input Validation:** อย่าเชื่อข้อมูลที่ User ส่งมา (ใช้ Joi/Zod ตรวจเสมอ)

---

> **📖 คำศัพท์เทคนิค (Glossary):**
> *   **Versioning:** การจัดการรุ่นของซอฟต์แวร์/API เพื่อรองรับการเปลี่ยนแปลงโดยไม่กระทบผู้ใช้เดิม
> *   **Pagination:** การแบ่งข้อมูลเป็นหน้าๆ เพื่อลดปริมาณการส่งข้อมูลทีละมากๆ
> *   **Query Parameter:** ค่าที่ส่งไปกับ URL หลังเครื่องหมาย `?` เพื่อกรองหรือปรับแต่งผลลัพธ์
> *   **Rate Limiting:** การจำกัดจำนวนครั้งที่ User สามารถเรียกใช้งาน API ได้ในช่วงเวลาหนึ่ง
> *   **Bearer Token:** รูปแบบการส่ง Token ยืนยันตัวตนผ่าน HTTP Header

## 7. Challenges 🏆

### 🎯 Challenge 1: Design URL (หัวข้อ 3)
จงออกแบบ URL สำหรับ:
1. "ขอดูคำสั่งซื้อ (Orders) ทั้งหมดของลูกค้า ID 99"
2. "ขอยกเลิกคำสั่งซื้อ ID 555 ของลูกค้า ID 99"

::: details ✨ ดูเฉลย
1. `GET /users/99/orders`
2. `DELETE /users/99/orders/555` (หรือแค่ `DELETE /orders/555` ถ้า Order ID นั้น Unique ทั่วทั้งระบบอยู่แล้ว)
:::

### 🎯 Challenge 2: Pagination Math (หัวข้อ 2.3)
ถ้ามีสินค้า 105 ชิ้น, User ขอ `limit=10`
ถามว่า:
1. จะมีทั้งหมดกี่หน้า (`totalPages`)?
2. ถ้า User ขอหน้าที่ 11 จะได้ข้อมูลไหม?

::: details ✨ ดูเฉลย
1. **11 หน้า** (105 / 10 = 10.5 ปัดขึ้นเป็น 11)
2. **ได้ (5 ชิ้นสุดท้าย)** หน้า 11 จะแสดงสินค้าลำดับที่ 101-105
:::

---

👉 **[ไปต่อ: 6 Project - Memory API (สร้าง API จริงแบบ In-Memory)](/node/06-project-memory-api)**
