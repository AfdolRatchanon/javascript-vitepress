# 9.2 JWT Authentication

> *"Stateless but Secure."*

ในยุค Web สมัยก่อน เราใช้ **Session/Cookie** ในการจำว่า User ล็อกอินหรือยัง
แต่ในยุค Modern App (React, Mobile App, Microservices) **Session** เริ่มไม่ตอบโจทย์เพราะ:
1.  **Scalability**: ถ้ามี Server หลายเครื่อง ต้องแชร์ Session กัน (วุ่นวาย)
2.  **Cross-Domain**: Frontend อยู่อีกโดเมน Backend อยู่อีกโดเมน Cookie ส่งข้ามยาก

พระเอกของเราคือ **JSON Web Token (JWT)** ครับ 🎫
มันคือ "บัตรผ่านทาง" ที่ User พกติดตัวไว้ ยื่นให้ Server ดูทุกครั้งที่ขอข้อมูล

---

## 🐣 Analogy: Wristband at a Concert (สายรัดข้อมือคอนเสิร์ต)

- **Login** = เดินไปที่จุดลงทะเบียน (Authentication)
    - คุณยื่นบัตรประชาชน (Username/Password)
    - เจ้าหน้าที่ตรวจสอบ -> ถ้าถูก -> **ใส่สายรัดข้อมือให้ (JWT)**

- **Access** = เดินเข้าโซน VIP (Authorization)
    - คุณโชว์สายรัดข้อมือให้พนักงานดู
    - พนักงานแค่มอง (Verify Signature) -> **รู้ทันทีว่าบัตรจริงหรือปลอม** โดยไม่ต้องวิ่งกลับไปถามจุดลงทะเบียน (Stateless)
    - ถ้าสายรัดข้อมือระบุว่า "VIP" -> เข้าได้
    - ถ้าสายรัดข้อมือขาด/หมดอายุ -> เข้าไม่ได้

---

## 🧩 Structure of JWT

JWT เป็น string ยาวๆ ที่มีจุด `.` คั่น 3 ส่วน: `aaaaa.bbbbb.ccccc`

1.  **Header**: บอกว่าเป็น JWT และใช้อัลกอริทึมอะไร (เช่น HS256)
2.  **Payload**: "ไส้ใน" ที่เราอยากเก็บ (เช่น User ID, Role, Expire) **ส่วนนี้ใครๆ ก็อ่านได้ ห้ามเก็บ Password!**
3.  **Signature**: ลายเซ็นดิจิทัล (เอา Header + Payload + **Secret Key** มาผสมกัน) **ส่วนนี้คือความลับ** ที่ทำให้ปลอมแปลงไม่ได้

> 💡 **Secret Key**: กุญแจลับที่มีแค่ฝั่ง Server เท่านั้นที่รู้ (เก็บใน `.env`) ถ้าหลุด -> จบเห่!

---

## 🛠️ Implementation with `jsonwebtoken`

```bash
npm install jsonwebtoken
```

### 1. Generating Token (Login)

```javascript
/* controller/authController.js */
const jwt = require('jsonwebtoken');

const login = async (req, res) => {
  // ... (ตรวจสอบ password ตามบทที่แล้ว) ...
  
  // ถ้า login ผ่าน -> สร้าง Token
  const payload = {
    id: user._id,
    role: user.role, // ใส่ role ไปด้วยจะได้เช็คสิทธิ์ง่ายๆ
    username: user.username
  };

  // Sign Token (ใส่ลายเซ็น)
  const token = jwt.sign(
    payload,
    process.env.JWT_SECRET, // กุญแจลับ (ห้ามบอกใคร)
    { expiresIn: '1h' }     // อายุ 1 ชั่วโมง (หมดอายุแล้วต้อง login ใหม่)
  );

  res.json({ token }); // ส่งให้ Frontend เก็บไว้
};
```

### 2. Verifying Token (Middleware)

เราจะสร้าง "ยาม" (Middleware) มาเฝ้าหน้า Route สำคัญ
ใครไม่มี JWT หรือ JWT ปลอม -> ห้ามเข้า! 🚫

```javascript
/* middleware/auth.js */
const jwt = require('jsonwebtoken');

const verifyToken = (req, res, next) => {
  // 1. รับ Token จาก Header (Authorization: Bearer <token>)
  const authHeader = req.headers['authorization'];
  if (!authHeader) return res.status(401).json({ msg: 'No token provided' });

  // ตัดคำว่า "Bearer " ออก
  const token = authHeader.split(' ')[1];

  try {
    // 2. ตรวจสอบลายเซ็น (Verify)
    const decoded = jwt.verify(token, process.env.JWT_SECRET);
    
    // 3. ถ้าผ่าน -> แปะข้อมูล User ใส่ request ไว้ใช้ต่อ
    req.user = decoded; 
    
    next(); // ปล่อยผ่านไป
  } catch (err) {
    // 4. ถ้าไม่ผ่าน (หมดอายุ, แก้ไข, ปลอม)
    res.status(403).json({ msg: 'Invalid token' });
  }
};

module.exports = verifyToken;
```

### 3. Protecting Routes

เอา Middleware ไปแปะหน้า Route

```javascript
/* routes/users.js */
const verifyToken = require('../middleware/auth');

// ใครเข้าหน้านี้ต้องมี Token
app.get('/profile', verifyToken, (req, res) => {
  // เข้าถึง req.user ได้เพราะ middleware แปะไว้ให้
  res.json({ 
    message: `Welcome user ${req.user.username}`,
    yourRole: req.user.role
  });
});
```

---

## ⏳ Concept: Refresh Token

**ปัญหา**: Access Token มีอายุน้อย (เช่น 15 นาที) เพื่อความปลอดภัย... แต่ไม่อยากให้ User ล็อกอินใหม่บ่อยๆ
**ทางแก้**: ใช้ระบบ **Refresh Token**

1.  **Login**: ได้รับ 2 tokens
    - `accessToken` (อายุสั้น 15 นาที): ใช้ยื่นเข้างาน
    - `refreshToken` (อายุยาว 7 วัน): เก็บไว้ในเซฟ, ใช้แลก accessToken ใบใหม่
2.  **Access**: ใช้ accessToken จนหมดอายุ -> Server ตอบ 401
3.  **Renew**: Frontend ส่ง refreshToken ไปขอใบใหม่ -> ได้ accessToken ใหม่
4.  **Logout**: ลบ refreshToken ทิ้งจาก Database (ทำให้แลกใหม่ไม่ได้แล้ว)

> *ในคอร์สเบื้องต้นเราจะใช้แค่ Access Token อายุปานกลาง (เช่น 1 วัน) เพื่อความง่ายครับ*

---

## 🥊 Challenges

### Level 1: Role-Based Access Control (RBAC)
จงเขียน Middleware ชื่อ `isAdmin` (ให้ทำงานต่อจาก `verifyToken`)
ที่จะตรวจสอบว่า `req.user.role === 'admin'` หรือไม่
- ถ้าใช่ -> `next()`
- ถ้าไม่ใช่ -> `res.status(403).send('Admin only!')`

::: details ✨ เฉลย
```javascript
const isAdmin = (req, res, next) => {
  // req.user มาจาก verifyToken ที่ทำงานก่อนหน้า
  if (req.user && req.user.role === 'admin') {
    next();
  } else {
    res.status(403).json({ msg: 'Require Admin privilege' });
  }
};

// Usage:
// app.delete('/users/:id', verifyToken, isAdmin, deleteUser);
```
:::

### Level 2: Token Decoding (Frontend Side)
(ข้อนี้ทดสอบความรู้) ถ้าเราได้ JWT มา เราสามารถอ่านข้อมูลข้างใน (Payload) โดยไม่รู้ Secret Key ได้หรือไม่?
และทำไมเราถึง **ห้ามเก็บ Password ใน Payload**?

::: details ✨ เฉลย
**ได้ครับ!**
Payload เป็นแค่ Base64Encode ใครๆ ก็ถอดมาดูได้ (ลองเอา Token ไปวางในเว็บ jwt.io ดูสิ)
ดังนั้น **ข้อมูลความลับ** (Password, บัตรเครดิต) **ห้าม** ใส่ใน Payload เด็ดขาด
Secret Key มีไว้เพื่อกันการ "แก้ไข" ข้อมูล ไม่ได้กันการ "อ่าน" ข้อมูลครับ
:::

---

## 📚 Glossary

| คำศัพท์ | ความหมาย |
|:--------|:---------|
| **JWT** | JSON Web Token มาตรฐาน Token สำหรับส่งข้อมูลอย่างปลอดภัย |
| **Stateless** | ระบบที่ไม่ต้องจำอะไรที่ Server (JWT เก็บข้อมูลในตัวเอง Server แค่เช็กลายเซ็น) |
| **Payload** | ข้อมูลที่เราฝากไว้ใน Token (เช่น ID, Name, Role) |
| **Signature** | ส่วนท้ายของ Token ที่เข้ารหัสไว้ตรวจสอบความถูกต้อง (ป้องกันการปลอมแปลง) |
| **Bearer Token** | รูปแบบมาตรฐานของการส่ง Token ใน HTTP Header (`Authorization: Bearer ...`) |
| **Middleware** | ฟังก์ชันที่ทำงานคั่นกลางก่อนจะไปถึง Route หลัก (ใช้ดักตรวจ Token) |

---

## 🔗 References

- [jwt.io](https://jwt.io/) - เว็บสำหรับ Debug และเรียนรู้โครงสร้าง JWT (ดีมาก!)
- [npm jsonwebtoken](https://www.npmjs.com/package/jsonwebtoken) - เอกสารการใช้ไลบรารี
- [RFC 7519](https://tools.ietf.org/html/rfc7519) - มาตรฐาน JWT อย่างเป็นทางการ

---

> 👉 **ไปต่อ: [Module 10: File Upload](/node/10-01-file-upload)**
