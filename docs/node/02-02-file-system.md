# Module 2.2: File System 📂

> **"Everything in Linux is a file."**
> — *UNIX Philosophy*

ใน Node.js, Module **`fs` (File System)** คือเครื่องมือหลักสำหรับจัดการไฟล์และโฟลเดอร์ ไม่ว่าจะอ่าน config, บันทึก log, หรือจัดการรูปภาพ

> **💡 Analogy (เปรียบเทียบ):**
> ถ้าตัวแปร (`const`) คือ **ram** (ความจำสั้น) 🧠
> **File System** คือ **harddisk** (ความจำยาว) 📓:
> - เก็บข้อมูลถาวร ไม่หายเมื่อปิดโปรแกรม
> - เหมือนเรามีเลขา (Node.js) ที่สั่งให้เดินไปหยิบแฟ้ม (Read), เขียนเอกสารใหม่ (Write), หรือทำลายเอกสาร (Delete) ได้


## 1. Sync vs Async (เลือกแบบไหน?) ⚡

Module `fs` มี 2 รูปแบบหลัก:

1.  **Synchronous (Blocking):** `readFileSync` — ทำเสร็จก่อนค่อยไปต่อ (ง่ายแต่บล็อกโปรแกรม)
2.  **Asynchronous (Non-Blocking):** `readFile` — สั่งไว้แล้วไปทำอย่างอื่นก่อน (เร็วกว่า)

> ⚠️ **ในคอร์สนี้เราใช้ `fs/promises`** (Async แบบ Modern) เป็นหลัก เพราะเขียนง่ายด้วย `await` และไม่บล็อกการทำงานของ Server

```javascript
// ✅ Import แบบ Modern
const fs = require("fs").promises;
```


## 2. อ่านและเขียนไฟล์ (Read & Write) 📖✍️

### 2.1 อ่านไฟล์ (`readFile`)
ต้องระบุ `encoding` ด้วย (`utf-8`) ไม่งั้นจะได้ **Buffer** (ตัวเลข)!

```javascript
try {
    const data = await fs.readFile("message.txt", "utf-8");
    console.log("📄 เนื้อหา:", data);
} catch (err) {
    console.error("❌ หาไฟล์ไม่เจอ:", err.message);
}
```

### 2.2 เขียนไฟล์ (`writeFile`)
*   ถ้าไฟล์ยังไม่มี → สร้างใหม่
*   ถ้ามีแล้ว → **เขียนทับ (Overwrite) ทั้งหมด!**

```javascript
const content = "Updated: " + new Date().toISOString();
await fs.writeFile("log.txt", content); // ทับของเดิม!
```

### 2.3 ต่อท้ายไฟล์ (`appendFile`)
ใช้สำหรับ Logs หรือข้อมูลที่ต้องการเก็บประวัติ

```javascript
const logEntry = `\nLogin at: ${new Date().toISOString()}`;
await fs.appendFile("history.log", logEntry); // ต่อท้าย
```


## 3. จัดการโฟลเดอร์ (Directories) 📁

งานจริงไม่ได้มีแค่ไฟล์ แต่มีโฟลเดอร์ด้วย!

### 3.1 สร้างโฟลเดอร์ (`mkdir`)
ใช้ `recursive: true` เพื่อสร้างโฟลเดอร์ซ้อนกันได้ในคำสั่งเดียว (เช่น `a/b/c`)

```javascript
try {
    // สร้าง folder ชื่อ 'uploads' และ 'images' ข้างใน
    await fs.mkdir("uploads/images", { recursive: true });
    console.log("✅ สร้างโฟลเดอร์สำเร็จ");
} catch (err) {
    console.error("❌ สร้างไม่สำเร็จ:", err);
}
```

### 3.2 อ่านรายชื่อไฟล์ในโฟลเดอร์ (`readdir`)
เหมือนคำสั่ง `ls` หรือ `dir`

```javascript
const files = await fs.readdir("uploads");
console.log("📂 ไฟล์ในโฟลเดอร์:", files); 
// Output: ['image1.jpg', 'data.json']
```

### 3.3 ลบโฟลเดอร์ (`rm`)
*   `rmdir` (เก่า): ลบได้เฉพาะโฟลเดอร์เปล่า
*   `rm` (ใหม่): ลบได้หมดแม้มีของข้างใน (เหมือน `rm -rf`)

```javascript
// ลบทั้งโฟลเดอร์ 'temp' และทุกอย่างข้างใน
await fs.rm("temp", { recursive: true, force: true });
console.log("🗑️ ลบเกลี้ยง!");
```


## 4. จัดการไฟล์ขั้นสูง (Metadata & Move) 🛠️

### 4.1 ตรวจสอบข้อมูลไฟล์ (`stat`)
เช็คขนาดไฟล์, วันที่สร้าง, หรือว่าเป็นไฟล์/โฟลเดอร์?

```javascript
const stats = await fs.stat("video.mp4");

console.log("📦 ขนาดไฟล์:", stats.size, "bytes");
console.log("📅 สร้างเมื่อ:", stats.birthtime);
console.log("เป็นไฟล์โปรแกรม?:", stats.isFile());
console.log("เป็นโฟลเดอร์?:", stats.isDirectory());
```

### 4.2 เปลี่ยนชื่อ / ย้ายไฟล์ (`rename`)
ใช้ `rename` ได้ทั้งเปลี่ยนชื่อ และย้ายที่อยู่ (Move)

```javascript
// 1. เปลี่ยนชื่อ
await fs.rename("old.txt", "new.txt");

// 2. ย้ายไฟล์ (Move)
await fs.rename("new.txt", "uploads/new.txt");
```

### 4.3 Copy ไฟล์ (`copyFile`)

```javascript
// Copy 'source.txt' ไปเป็น 'dest.txt'
await fs.copyFile("source.txt", "dest.txt");
```


## 5. ตรวจสอบว่ามีไฟล์ไหม? (`access`) 🔍

ก่อนอ่าน/ลบ ควรเช็คก่อนว่ามีไฟล์ไหม เพื่อกัน Error

```javascript
try {
    await fs.access("config.json");
    console.log("✅ มีไฟล์อยู่จริง");
} catch {
    console.log("❌ ไม่มีไฟล์นี้");
}
```


## 6. Workshop: ระบบจัดการ Logs อัตโนมัติ 🤖

มาสร้าง Script ที่:
1. สร้างโฟลเดอร์ `logs`
2. เขียนไฟล์ `logs/system.log`
3. เช็คขนาดไฟล์
4. ถ้าไฟล์ใหญ่เกินไป ให้เปลี่ยนชื่อเป็น `.bak` (Backup)

```javascript
const fs = require("fs").promises;

async function manageLogs() {
    const LOG_DIR = "logs";
    const LOG_FILE = `${LOG_DIR}/system.log`;

    try {
        // 1. สร้างโฟลเดอร์ (ถ้ายังไม่มี)
        await fs.mkdir(LOG_DIR, { recursive: true });

        // 2. เขียน Log จำลอง (เขียนซ้ำๆ ให้ไฟล์ใหญ่)
        const logData = "Log Entry: User Login...\n".repeat(1000);
        await fs.appendFile(LOG_FILE, logData);

        // 3. เช็คขนาด
        const stats = await fs.stat(LOG_FILE);
        console.log(`📦 ขนาดไฟล์ปัจจุบัน: ${stats.size} bytes`);

        // 4. ถ้าใหญ่เกิน 10KB ให้ Rotate (เปลี่ยนชื่อเก็บไว้)
        if (stats.size > 10240) { // 10KB
            const backupName = `${LOG_DIR}/system-${Date.now()}.bak`;
            await fs.rename(LOG_FILE, backupName);
            console.log(`♻️ ไฟล์ใหญ่เกิน! Backup ไปที่: ${backupName}`);
        }

    } catch (err) {
        console.error("❌ Error:", err);
    }
}

manageLogs();
```


## 7. Challenges 🏆

### 🎯 Challenge 1: Folder Structure Creator (ระดับกลาง)
เขียนฟังก์ชัน `createProjectStructure(projectName)` ที่รับชื่อโปรเจกต์ แล้วสร้างโครงสร้างโฟลเดอร์ดังนี้:
*   `projectName/`
    *   `src/`
    *   `public/`
    *   `README.md` (ข้างในเขียนว่า `# Project Name`)

::: details ✨ ดูเฉลย
```javascript
const fs = require("fs").promises;

async function createProjectStructure(projectName) {
    try {
        // สร้างโฟลเดอร์ซ้อน
        await fs.mkdir(`${projectName}/src`, { recursive: true });
        await fs.mkdir(`${projectName}/public`, { recursive: true });
        
        // สร้างไฟล์ README
        await fs.writeFile(`${projectName}/README.md`, `# ${projectName}`);
        
        console.log(`✅ Project "${projectName}" ready!`);
    } catch (err) {
        console.error("Failed:", err);
    }
}

createProjectStructure("my-app");
```
:::

### 🎯 Challenge 2: File Cleaner (ระดับยาก)
เขียนโปรแกรมที่:
1. อ่านไฟล์ทั้งหมดในโฟลเดอร์ปัจจุบัน (`readdir`)
2. ลบไฟล์ทั้งหมดที่ลงท้ายด้วย `.tmp` (ไฟล์ขยะ)
3. บอกจำนวนไฟล์ที่ลบไป

::: details ✨ ดูเฉลย
```javascript
const fs = require("fs").promises;
const path = require("path");

async function cleanTempFiles() {
    try {
        const files = await fs.readdir(".");
        let count = 0;

        for (const file of files) {
            if (path.extname(file) === ".tmp") {
                await fs.unlink(file);
                console.log(`🗑️ Deleted: ${file}`);
                count++;
            }
        }
        console.log(`✅ ลบไปทั้งหมด ${count} ไฟล์`);
    } catch (err) {
        console.error(err);
    }
}
cleanTempFiles();
```
:::

### 🎯 Challenge 3: Disk Usage Info (ระดับกลาง)
เขียนโปรแกรมที่รับชื่อโฟลเดอร์ แล้วคำนวณ **ขนาดรวม (Total Size)** ของทุกไฟล์ในนั้น (ไม่ต้องรวม Subfolder ก็ได้) โดยใช้ `readdir` + `stat`

::: details ✨ ดูเฉลย
```javascript
const fs = require("fs").promises;

async function calcSize(dir) {
    let totalSize = 0;
    const files = await fs.readdir(dir);

    for (const file of files) {
        // ต้องต่อ Path ให้ครบด้วย!
        const stats = await fs.stat(`${dir}/${file}`);
        if (stats.isFile()) {
            totalSize += stats.size;
        }
    }
    
    console.log(`📦 Folder "${dir}" size: ${totalSize} bytes`);
}

calcSize("."); // เช็ค folder ปัจจุบัน
```
:::


> **📖 คำศัพท์เทคนิค (Glossary):**
> *   **File System (fs):** Module จัดการไฟล์ของ Node.js
> *   **Blocking (Sync):** การทำงานที่หยุดรอจนกว่าจะเสร็จ (เช่น `readFileSync`)
> *   **Non-Blocking (Async):** การทำงานที่ไม่หยุดรอ (เช่น `readFile`)
> *   **`mkdir -p` (Recursive):** การสร้างโฟลเดอร์ซ้อนกันหลายชั้นในรวดเดียว
> *   **Metadata:** ข้อมูลเกี่ยวกับไฟล์ (เช่น ขนาด, วันที่สร้าง, เจ้าของ) หาได้จาก `fs.stat`
> *   **Directory (dir):** โฟลเดอร์
> *   **Buffer:** รูปแบบข้อมูลดิบ (Binary) เวลาอ่านไฟล์โดยไม่ระบุ encoding


👉 **[ไปต่อ: 2.3 - Buffers & Streams](/node/02-03-buffers-streams)**
