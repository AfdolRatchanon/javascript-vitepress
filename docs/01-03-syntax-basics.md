# 01-3: Syntax Basics & Statements (กฎเหล็กและโครงสร้าง) 🏗️

> **"Code is read much more often than it is written."**
> — *Guido van Rossum (Python Creator)*

ก่อนจะเริ่มเขียนอะไรจริงจัง คุณต้อง **เข้าใจกฎพื้นฐาน** ของภาษาก่อนครับ เหมือนก่อนจะเล่นหมากรุกได้ ต้องรู้กติกาก่อน

> **💡 Analogy (เปรียบเทียบ):**
> Syntax ของ JavaScript เหมือน **"ไวยากรณ์ภาษาอังกฤษ"** ครับ:
> - **Statement** = ประโยค (มี `.` ปิดท้าย → มี `;` ปิดท้าย)
> - **Expression** = วลี/คำ (มีความหมาย สามารถรวมเป็นประโยคได้)
> - **Block `{}`** = ย่อหน้า (จัดกลุ่มประโยคเข้าด้วยกัน)
> - **Comment** = หมายเหตุข้างๆ (คนอ่านเห็น แต่คอมพิวเตอร์ข้ามไป)

## 1. Comments (ความเห็นในโค้ด) 💬

Comments คือ **"โน้ตช่วยจำ"** ที่ JavaScript จะ **ข้ามไป** ไม่ประมวลผล:

```javascript
// ==========================================
// Single-line Comment: ใช้ // นำหน้า
// ==========================================

// ตัวแปรเก็บชื่อผู้ใช้
const userName = "Dolar";

// TODO: เพิ่มระบบ Login ในอนาคต

// ==========================================
// Multi-line Comment: ใช้ /* ... */
// ==========================================

/*
  ฟังก์ชันนี้คำนวณภาษี
  - รับค่า: ราคาสินค้า (Number)
  - return: ราคาหลังภาษี (Number)
  - อัตราภาษี: 7%
*/
function calculateTax(price) {
    return price * 1.07;
}

// ==========================================
// JSDoc Comment: ใช้ /** ... */ (มาตรฐานอุตสาหกรรม!)
// ==========================================

/**
 * คำนวณพื้นที่สี่เหลี่ยม
 * @param {number} width - ความกว้าง
 * @param {number} height - ความสูง
 * @returns {number} พื้นที่
 */
function getArea(width, height) {
    return width * height;
}
```

### 📊 Comment Types Comparison

| Type | Syntax | ใช้เมื่อ | ตัวอย่าง |
|:-----|:-------|:-------|:---------|
| **Single-line** | `// text` | อธิบายบรรทัดเดียว | `// คำนวณภาษี` |
| **Multi-line** | `/* text */` | อธิบายหลายบรรทัด | `/* ฟังก์ชันนี้... */` |
| **JSDoc** | `/** text */` | สร้าง Documentation | `/** @param {number} */` |

### ⚠️ Comment Best Practices:

```javascript
// ❌ BAD: Comment ที่บอกสิ่งที่โค้ดบอกอยู่แล้ว (ซ้ำซ้อน!)
const age = 25; // กำหนดอายุเป็น 25

// ✅ GOOD: Comment ที่บอก "ทำไม" ไม่ใช่ "อะไร"
const age = 25; // อายุขั้นต่ำสำหรับสมัครงานตำแหน่งนี้

// ❌ BAD: Comment เก่าที่ไม่ตรงกับโค้ดปัจจุบัน
// คำนวณภาษี 5%
const tax = price * 0.07; // ← ภาษี 7% แต่ Comment บอก 5%!

// ✅ GOOD: ใช้ Comment เพื่อ Disable โค้ดชั่วคราว (Debugging)
// console.log("Debug:", someValue);
```

### 🧠 Challenge: Secret Note
จงเขียน console.log 2 อัน แต่ให้แสดงผลลัพธ์แค่อันเดียว (ซ่อนอีกอัน):
```javascript
console.log("You can see me");
console.log("I'm invisible!");
```

::: details ✨ ดูเฉลย
```javascript
console.log("You can see me");
// console.log("I'm invisible!");  ← Comment ไว้!
```
:::

---

## 2. Blocks (บล็อกโค้ด) 📦

**Block** คือกลุ่มของ Statements ที่ถูกครอบด้วย `{ }` (Curly Braces):

```javascript
// Block = กรอบที่จัดกลุ่มโค้ดเข้าด้วยกัน
{
    const message = "Inside block";
    console.log(message);  // ✅ ใช้ได้ — อยู่ข้างใน Block
}
// console.log(message);  // ❌ ReferenceError! — message ถูกขังไว้ใน Block

// Block ใช้กับ: if, for, while, function
if (true) {
    // Block ของ if
    console.log("Block 1");
}

for (let i = 0; i < 3; i++) {
    // Block ของ for
    console.log(i);
}

function greet() {
    // Block ของ function
    console.log("Hi!");
}
```

### Scope กับ Block (ตัวอย่าง):

```javascript
const x = "outside";   // Global Scope

{
    const x = "inside"; // Block Scope (คนละตัวกับ x ข้างนอก!)
    console.log(x);     // "inside"
}

console.log(x);         // "outside" (ไม่ถูกเปลี่ยน!)
```

### 🧠 Challenge: Block Boundary
โค้ดนี้จะทำงานได้ไหม? เพราะอะไร?
```javascript
{
    let target = "Bullseye";
}
console.log(target);
```

::: details ✨ ดูเฉลย
**❌ Error ครับ!** (`ReferenceError: target is not defined`)
เพราะ `let` ถูกขังอยู่ใน **Block Scope** `{ }` — คนข้างนอกเข้าถึงไม่ได้
ถ้าจะให้ใช้ข้างนอก ต้องประกาศข้างนอก Block:
```javascript
let target;
{
    target = "Bullseye";
}
console.log(target); // "Bullseye" ✅
```
:::

---

## 3. Case Sensitivity (ตัวเล็ก/ใหญ่ ต่างกัน!) 🔡

JavaScript เป็นภาษา **Case-Sensitive** — ตัวพิมพ์เล็กกับตัวพิมพ์ใหญ่ **ถือว่าเป็นคนละตัว**:

```javascript
// คนละตัวแปรทั้งหมด!
let myName = "Dolar";
let MyName = "Somchai";
let MYNAME = "Somsak";
let myname = "Noi";

console.log(myName);  // "Dolar"
console.log(MyName);  // "Somchai"

// สิ่งที่มือใหม่พิมพ์ผิดบ่อยที่สุด:
// Console.log()  → ❌ ต้อง console.log()
// Console.Log()  → ❌ ต้อง console.log()
// CONSOLE.LOG()  → ❌ ต้อง console.log()
```

### 🧠 Challenge: Spot the Error
```javascript
let Score = 100;
console.log(score);  // (1) จะเกิดอะไรขึ้น?
```

::: details ✨ ดูเฉลย
**❌ ReferenceError!** เพราะ `Score` (ตัว S ใหญ่) กับ `score` (ตัว s เล็ก) เป็น **คนละตัวแปร**!
แก้ไข: `console.log(Score);` ✅
:::

---

## 4. Expression vs Statement (ศัพท์สำคัญ!) 📖

มือใหม่มักสับสน 2 คำนี้ แต่ถ้าเข้าใจแล้วชีวิตจะง่ายขึ้นมาก:

### A. Expression (นิพจน์) — "สิ่งที่ให้ค่าออกมา"
คือส่วนที่ **"ประมวลผลแล้วได้ค่าออกมา"**:

```javascript
// ทุกอันนี้เป็น Expression (เพราะมี "ค่า" ออกมา)
5 + 3           // → 8
"Hello"         // → "Hello"
true            // → true
Math.random()   // → 0.7234...
x > 10          // → true or false
```

### B. Statement (คำสั่ง) — "บอกให้ทำอะไรบางอย่าง"
คือ **"ประโยค"** ที่สั่งให้โปรแกรมทำบางอย่าง:

```javascript
// ทุกอันนี้เป็น Statement (เพราะเป็น "คำสั่ง")
let x = 5;                    // Declaration Statement
if (x > 3) { /* ... */ }      // Conditional Statement
for (let i = 0; i < 5; i++) {} // Loop Statement
function greet() {}            // Function Declaration
```

### 📊 Expression vs Statement

| | **Expression** | **Statement** |
|:--|:-------------|:-------------|
| **คืออะไร** | สิ่งที่ "ให้ค่า" ออกมา | คำสั่งที่ "ทำบางอย่าง" |
| **เปรียบเทียบ** | คำ/วลี ("สวย", "สีแดง") | ประโยค ("ดอกไม้สวย.") |
| **ตัวอย่าง** | `5 + 3`, `"hello"`, `x > 10` | `let x = 5;`, `if (...) {}` |
| **ใส่ใน `console.log()` ได้?** | ✅ ได้ | ❌ ไม่ได้ |

```javascript
// Expression สามารถเป็นส่วนหนึ่งของ Statement:
let total = 5 + 3;
//          └─────┘ ← Expression (ค่า = 8)
// └─────────────┘ ← Declaration Statement (สั่งสร้างตัวแปร)
```

### 🧠 Challenge: Expression or Statement?
จงบอกว่าโค้ดแต่ละบรรทัดเป็น **Expression** หรือ **Statement**:
```javascript
42                         // (1)?
let name = "Dolar";        // (2)?
3 + 4 * 2                  // (3)?
if (true) { }              // (4)?
```

::: details ✨ ดูเฉลย
1. **Expression** — ค่าตัวเลข `42`
2. **Statement** — คำสั่ง `let` (Declaration Statement)
3. **Expression** — การคำนวณที่ให้ค่า `11`
4. **Statement** — คำสั่ง `if` (Conditional Statement)
:::

---

## 5. The ASI Bug (กับดักอัฒภาค) 🪲

ASI (Automatic Semicolon Insertion) บางทีก็ **เติม `;` ผิดที่!**

```javascript
// 🪲 The Classic Bug:
function getUser() {
    return
    {
        name: "Dolar"
    }
}
console.log(getUser()); // undefined! 😱

// สิ่งที่ JS Engine เห็น:
function getUser() {
    return;          // ← ASI เติม ; ตรงนี้! หยุดที่ return!
    {                // ← บรรทัดนี้ไม่ถูกรันเลย!
        name: "Dolar"
    }
}

// ✅ แก้ไข: ใส่ { ไว้บรรทัดเดียวกับ return
function getUser() {
    return {
        name: "Dolar"
    };
}
```

### 🧠 Challenge: Will It Run?
```javascript
score = 100
console.log(score)
```

::: details ✨ ดูเฉลย
**รันผ่านครับ!** ASI เติม `;` ให้ทั้ง 2 บรรทัดอัตโนมัติ:
```javascript
score = 100;
console.log(score);
```
**แต่ในโลกจริง** ควรเติม `;` เองทุกบรรทัด และใช้ `let` หรือ `const` แทนครับ:
```javascript
let score = 100;
console.log(score);
```
:::

---

## 6. Code Formatting Best Practices (การจัดรูปแบบโค้ด) ✨

```javascript
// ❌ BAD: ยัดรวมกัน อ่านไม่รู้เรื่อง
if(x>5){console.log("big");let y=x*2;console.log(y);}

// ✅ GOOD: จัดรูปแบบสวยงาม อ่านง่าย
if (x > 5) {
    console.log("big");
    let y = x * 2;
    console.log(y);
}
```

### กฎทอง 4 ข้อ:

| # | กฎ | ตัวอย่าง |
|:-:|:---|:---------|
| 1 | **Indent ด้วย 2 หรือ 4 Spaces** | ` ` ` ` `console.log()` |
| 2 | **เว้นวรรคหลัง Keyword** | `if (x)` ไม่ใช่ `if(x)` |
| 3 | **เว้นวรรครอบ Operator** | `x + y` ไม่ใช่ `x+y` |
| 4 | **ปิด `{` ไว้บรรทัดเดียวกัน** | `if (x) {` ไม่ใช่ `if (x)\n{` |

> **เครื่องมือช่วย:** ติดตั้ง **Prettier** (VS Code Extension) แล้วกด Save — จัดโค้ดสวยอัตโนมัติ!

---

> **📖 คำศัพท์เทคนิค (Glossary):**
> *   **Syntax:** กฎไวยากรณ์ของภาษาโปรแกรม ที่กำหนดว่าเขียนโค้ดยังไงถึงจะถูกต้อง
> *   **Statement:** คำสั่งที่บอกให้โปรแกรมทำบางอย่าง (เช่น `let x = 5;`)
> *   **Expression:** ส่วนที่ประมวลผลแล้วได้ค่าออกมา (เช่น `5 + 3`)
> *   **Block:** กลุ่มของ Statements ที่ถูกครอบด้วย `{ }` (Curly Braces)
> *   **Comment:** ข้อความที่ JS ข้ามไปไม่ประมวลผล (`//` หรือ `/* */`)
> *   **JSDoc:** มาตรฐานการเขียน Comment สำหรับสร้าง Documentation (`/** */`)
> *   **Case-Sensitive:** คุณสมบัติที่ตัวพิมพ์เล็ก/ใหญ่ถือว่าเป็นคนละตัว
> *   **Semicolon:** อัฒภาค (`;`) ที่ใช้ปิดท้ายคำสั่ง
> *   **ASI:** Automatic Semicolon Insertion — ระบบเติม `;` ให้อัตโนมัติ (อาจทำให้เกิด Bug!)
> *   **Parser:** โปรแกรมที่อ่านและตีความโค้ดของเรา
> *   **Indentation:** การเยื้องบรรทัด (ย่อหน้า) ด้วย Spaces/Tabs เพื่อให้อ่านง่าย
> *   **Prettier:** เครื่องมือจัดรูปแบบโค้ดอัตโนมัติ (Code Formatter)

---
👉 **[พักสมองแล้วไปวาดรูปเล่น: Project 1 - The Console Artist](/01-project-artist)**
