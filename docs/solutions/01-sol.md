# Solutions: Chapter 01 (Deep Dive)

## Level 1: Syntax Check
**Code ที่ผิด:**
```javascript
Console.log("I'm learning JS"); // Error 1: Console (C ตัวใหญ่) -> console
console.log("It's fun!" // Error 2: Missing closing parenthesis )
```

**Code ที่ถูก:**
```javascript
console.log("I'm learning JS");
console.log("It's fun!");
```

## Level 2: Predict & Verify
**ผลลัพธ์ (Output):**
```
A
C
```
**คำอธิบาย (Explanation):**
บรรทัด `// console.log("B");` ถูกมองว่าเป็น **Comment** (เพราะมี `//` นำหน้า) ทำให้ JS Engine ข้ามการทำงานบรรทัดนั้นไป เลยพิมพ์แค่ A และ C ตามลำดับ

## Level 3: Crash the Stack (Lab) 💥
**Error Message:**
```
RangeError: Maximum call stack size exceeded
```
(ข้อความอาจต่างกันเล็กน้อยตาม Browser/Node version แต่ใจความสำคัญคือ "Stack เต็ม")

**คำอธิบาย (Explanation):**
ฟังก์ชัน `bomb` เรียกตัวเองซ้ำๆ ไม่รู้จบ ทำให้มีการสร้าง **Stack Frame** ซ้อนทับกันไปเรื่อยๆ จนเต็มหน่วยความจำที่จองไว้ จึงเกิด Error ครับ (เป็นเหตุผลที่เราต้องระวังเรื่อง Recursion)

## Level 4: Researcher (Challenge) 🏆
เราสามารถดึงข้อมูล Environment ได้จาก object `process` (ใน Node.js)

```javascript
// solution-challenge.js
console.log(process.version);
```
**ผลลัพธ์:** จะได้เลขเวอร์ชัน เช่น `v18.16.0`

> **Note:** ถ้าคุณรันบน Browser (เช่น Chrome Console) จะไม่มี `process` ให้ใช้ `navigator.userAgent` หรือ `navigator.appVersion` แทน

---
[กลับไปบทเรียน (Back to Lesson)](/01-introduction)
