# Solutions: Chapter 02 (Memory Master)

## Level 1: Swap Logic
มี 2 วิธีหลักในการสลับค่าตัวแปร:

### Method 1: Temporary Variable (Classic)
วิธีนี้เข้าใจง่ายที่สุด คือหาตัวแปรชั่วคราว (`temp`) มาพักค่าไว้ก่อน
```javascript
let temp = a;
a = b;
b = temp;
```

### Method 2: Array Destructuring (Modern ES6+)
วิธีนี้เป็น Syntax ใหม่ สั้นและเท่กว่ามาก!
```javascript
[a, b] = [b, a];
```

## Level 2: Constant Object Mutation
**เหตุผล (Reasoning):**
คำสั่ง `const` ป้องกันการเปลี่ยน **Reference** (ที่อยู่) เท่านั้น ไม่ได้ป้องกันการเปลี่ยน **Value** (ค่าข้างใน)

1.  `user` เก็บ **Address** (เช่น `0x001`) ชี้ไปที่ Object ใน Heap
2.  การแก้ `user.name = "Bob"` คือการแก้ค่าใน Heap (ที่อยู่ `0x001`)
3.  ตัวแปร `user` ยังคงชี้ไปที่ `0x001` เหมือนเดิม ไม่ได้เปลี่ยนไปชี้ที่อื่น
4.  ดังนั้น `const` จึงไม่ฟ้อง Error ครับ

> **Note:** ถ้าเราเขียน `user = { name: "Bob" }` จะ Error ทันที เพราะพยายามเปลี่ยน Address ใหม่ให้ตัวแปร `user`

## Level 3: The Hoisting Trap 😈
```javascript
console.log(a); // undefined
var a = 5;
console.log(a); // 5

console.log(b); // ReferenceError: Cannot access 'b' before initialization
let b = 10;
```
**คำอธิบาย:**
*   `var a` ถูก Hoist ขึ้นไปประกาศด้านบน (แต่ยังไม่กำหนดค่า) เลยได้ `undefined`
*   `let b` ถูก Hoist เหมือนกัน แต่ติดอยู่ใน **TDZ (Temporal Dead Zone)** ห้ามเรียกใช้จนกว่าจะถึงบรรทัดประกาศ จึงเกิด Error

## Level 4: Type Coercion Challenge 🏆
1.  `"5" + 2` = `"52"` (String Concatenation: ถ้ามี String ตัวนึง `+` จะกลายเป็นการต่อคำ)
2.  `"5" - 2` = `3` (Math Operation: `-` ใช้กับ String ไม่ได้ JS เลยพยายามแปลง "5" เป็นเลข 5)
3.  `[] + []` = `""` (String Empty: Array ว่างบวกกันจะได้ String ว่าง)

---
[กลับไปบทเรียน (Back to Lesson)](/02-variables)
