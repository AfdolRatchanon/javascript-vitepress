# Solutions: Chapter 03 (Control Flow)

## Level 1: Grade Calculator (Easy)
วิธีใช้ Ternary Operator `(condition) ? true : false`

```javascript
const score = 49;
const result = (score >= 50) ? "Pass" : "Fail";
console.log(result); // Fail
```

## Level 2: FizzBuzz (Medium)
เคล็ดลับคือต้องเช็คเงื่อนไขที่ "เฉพาะเจาะจงที่สุด" ก่อน (หารลงตัวทั้ง 3 และ 5)

```javascript
for (let i = 1; i <= 100; i++) {
  if (i % 3 === 0 && i % 5 === 0) {
    console.log("FizzBuzz");
  } else if (i % 3 === 0) {
    console.log("Fizz");
  } else if (i % 5 === 0) {
    console.log("Buzz");
  } else {
    console.log(i);
  }
}
```

## Level 3: Pyramid Builder (Hard) 🏗️
ต้องใช้ Nested Loop (Loop ซ้อน Loop)

```javascript
const n = 3;

for (let i = 1; i <= n; i++) {
  let row = "";

  // 1. ช่องว่าง (Space) ลดลงเรื่อยๆ: n - i
  for (let j = 1; j <= n - i; j++) {
    row += " ";
  }

  // 2. ดาว (Star) เพิ่มขึ้นเรื่อยๆ: 2*i - 1
  for (let k = 1; k <= 2 * i - 1; k++) {
    row += "*";
  }

  console.log(row);
}
```
**Output:**
```
  *
 ***
*****
```

## Level 4: The Infinite Trap (Challenge) 💀
**คำตอบ:** เกิด Infinite Loop! (ค้างตลอดไป)

**ทำไมถึงค้าง?**
1.  เมื่อ `i` เป็น 3, เงื่อนไข `if (i === 3)` เป็นจริง
2.  คำสั่ง `continue` ทำงาน -> กระโดดไปเริ่มรอบใหม่ทันที **โดยข้ามคำสั่ง `i++` ด้านล่าง**
3.  พอกลับมารอบใหม่ `i` ก็ยังเป็น 3 เหมือนเดิม -> เข้า if -> continue -> วนลูปนรกแบบนี้ไปเรื่อยๆ

**วิธีแก้:** ย้าย `i++` ไปไว้ก่อน continue หรือใช้ `for` loop แทน

---
[กลับไปบทเรียน (Back to Lesson)](/03-control-flow)
